---
phase: 26-fundaci-n-e2e-playwright-vanilla
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - docs/10-tests.md
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - QA-E2E-05
  - QA-CI-04
must_haves:
  truths:
    - "docs/10-tests.md documenta npm run test:e2e, playwright:install y contraste AUTH_DISABLED vs login UI"
    - "CI job e2e-smoke ejecuta smoke vanilla en PR/push a main con env seguro"
    - "Job CI no define AUTH_DISABLED"
    - "Operador puede seguir doc para reproducir smoke local sin tmux"
  artifacts:
    - path: docs/10-tests.md
      provides: "Sección Smoke E2E Playwright"
      contains: "test:e2e"
    - path: .github/workflows/ci.yml
      provides: "Job e2e-smoke vanilla"
      contains: "e2e-smoke"
  key_links:
    - from: docs/10-tests.md
      to: package.json
      via: "npm run test:e2e"
      pattern: "test:e2e"
    - from: .github/workflows/ci.yml
      to: e2e/playwright.config.js
      via: "npm run test:e2e"
      pattern: "npm run test:e2e"
    - from: .github/workflows/ci.yml
      to: docs/17-autenticacion.md
      via: "contraste AUTH_DISABLED"
      pattern: "AUTH_DISABLED"
---

<objective>
Documentar smoke E2E en `docs/10-tests.md` y añadir job CI `e2e-smoke` vanilla con entorno seguro (QA-E2E-05, QA-CI-04).

Purpose: Requisito trazable en docs + feedback automático en PR antes de multi-dashboard (fase 27).
Output: Sección E2E en doc 10; workflow CI extendido.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-fundaci-n-e2e-playwright-vanilla/26-01-SUMMARY.md
@.planning/phases/26-fundaci-n-e2e-playwright-vanilla/26-CONTEXT.md
@.planning/phases/26-fundaci-n-e2e-playwright-vanilla/RESEARCH.md
@docs/10-tests.md
@.github/workflows/ci.yml
@docs/17-autenticacion.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/10-tests.md — sección Smoke E2E (Playwright)</name>
  <files>docs/10-tests.md</files>
  <action>
    Añadir sección **«Smoke E2E (Playwright)»** después de la tabla «Scripts útiles» (≈ L47) o antes de «CI en GitHub Actions», en español, tono didáctico existente (D-15, QA-E2E-05):

    1. **Qué es:** smoke browser del dashboard vanilla — gate → login → tabla → logout; Playwright levanta API + static server (no tmux).

    2. **Setup primera vez:**
       ```bash
       # Desde la raíz
       npm install
       npm run playwright:install
       cd api && npm ci && cd ..
       ```

    3. **Ejecutar:**
       ```bash
       npm run test:e2e
       npm run test:e2e:ui    # depuración interactiva
       ```

    4. **Tabla scripts** — ampliar tabla existente con filas:
       | `npm run test:e2e` | Smoke auth vanilla (Playwright) |
       | `npm run test:e2e:ui` | Modo UI Playwright |
       | `npm run playwright:install` | Instala Chromium (una vez) |

    5. **Contraste didáctico** (tabla breve):
       | Contexto | `AUTH_DISABLED` | Auth |
       |----------|-----------------|------|
       | Tests API (`npm run test:sqlite`) | Sí (CRUD) | Bloque auth prueba login real |
       | Smoke E2E (`npm run test:e2e`) | **No** | Solo login UI |

    6. **Enlaces:** [`17-autenticacion.md`](./17-autenticacion.md); nota que matriz CI triple (sqlite + postgres + e2e) se documenta en fases 28–29.

    7. **Troubleshooting mínimo:** puertos ocupados (`lsof -i :3100 -i :5173`); borrar `api/data/e2e.users.db` si estado raro; en CI `CI=true` fuerza servidores frescos.

    **No** escribir matriz CI completa de tres jobs — eso es fase 29 (DOCS-01).
  </action>
  <verify>
    <automated>grep -q "test:e2e" docs/10-tests.md && grep -q "playwright:install" docs/10-tests.md && grep -q "Smoke E2E" docs/10-tests.md && grep -q "AUTH_DISABLED" docs/10-tests.md && grep -q "17-autenticacion" docs/10-tests.md</automated>
  </verify>
  <acceptance_criteria>
    - Doc explica setup, ejecución y contraste AUTH_DISABLED vs E2E
    - Script test:e2e documentado desde raíz
  </acceptance_criteria>
  <done>QA-E2E-05 completo</done>
</task>

<task type="auto">
  <name>Task 2: CI job e2e-smoke — vanilla con env seguro (QA-CI-04)</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Añadir job **`e2e-smoke`** paralelo a `test-sqlite` (D-09, RESEARCH Pattern 5):

    ```yaml
      e2e-smoke:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v4
            with:
              node-version: '22'
              cache: npm
              cache-dependency-path: |
                package-lock.json
                api/package-lock.json
          - run: npm ci
          - run: npm ci
            working-directory: api
          - run: npx playwright install --with-deps chromium
          - run: npm run test:e2e
            env:
              CI: true
              E2E_OPERATOR_EMAIL: admin@lab.local
              E2E_OPERATOR_PASSWORD: changeme
              LOGIN_RATE_LIMIT_MAX: '1000'
              E2E_JWT_SECRET: e2e-ci-secret-not-for-production
    ```

    **Reglas:**
    - **NO** definir `AUTH_DISABLED` en job `env` ni en steps
    - Job independiente de `test-sqlite` (no `needs:` — paralelo)
    - Actualizar párrafo «CI en GitHub Actions» en `docs/10-tests.md` (mismo task o sub-bullet) mencionando que PRs ejecutan `test-sqlite` + `e2e-smoke`

    **Verificación negativa:** `grep AUTH_DISABLED .github/workflows/ci.yml` dentro del bloque e2e-smoke debe fallar (o solo aparecer en comentarios, no en env del job e2e).

    Nota en comentario YAML: fase 27 extiende a React/Vue; fase 28 añade `test-postgres`.
  </action>
  <verify>
    <automated>grep -q 'e2e-smoke:' .github/workflows/ci.yml && grep -q 'npm run test:e2e' .github/workflows/ci.yml && grep -q 'LOGIN_RATE_LIMIT_MAX' .github/workflows/ci.yml && awk '/e2e-smoke:/{flag=1} flag&&/^  [a-z]/ && !/e2e-smoke/{flag=0} flag' .github/workflows/ci.yml | grep -q 'E2E_OPERATOR_EMAIL' && ! awk '/e2e-smoke:/{flag=1} flag&&/^  [a-z]/ && !/e2e-smoke/{flag=0} flag' .github/workflows/ci.yml | grep -q 'AUTH_DISABLED'</automated>
  </verify>
  <acceptance_criteria>
    - Job e2e-smoke en CI con credenciales lab y rate limit elevado
    - Sin AUTH_DISABLED en env del job E2E
    - Doc 10-tests refleja el nuevo job
  </acceptance_criteria>
  <done>QA-CI-04 completo en CI + documentación alineada</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → API E2E | Env CI con credenciales lab, no prod |
| Docs → operador | Instrucciones sin secretos reales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06 | Elevation | ci.yml e2e job | mitigate | Ausencia explícita de AUTH_DISABLED; grep en verify |
| T-26-07 | Information disclosure | CI env vars | accept | Valores documentados lab (`admin@lab.local`) |
| T-26-08 | Denial of service | CI login retries | mitigate | LOGIN_RATE_LIMIT_MAX=1000 en job env |
</threat_model>

<verification>
- Grep docs/10-tests.md y ci.yml según tasks
- Workflow YAML válido (indentación jobs bajo `jobs:`)
</verification>

<success_criteria>
- QA-E2E-05 y QA-CI-04 satisfechos
- Alumno puede leer doc 10 y ejecutar smoke; CI repite el mismo comando
</success_criteria>

<output>
After completion, create `.planning/phases/26-fundaci-n-e2e-playwright-vanilla/26-02-SUMMARY.md`
</output>
