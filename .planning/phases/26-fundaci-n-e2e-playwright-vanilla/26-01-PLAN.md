---
phase: 26-fundaci-n-e2e-playwright-vanilla
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .nvmrc
  - .gitignore
  - e2e/playwright.config.js
  - e2e/tests/auth-smoke.vanilla.spec.js
autonomous: true
requirements:
  - QA-E2E-01
  - QA-E2E-02
  - QA-CI-04
must_haves:
  truths:
    - "npm run test:e2e desde raíz arranca API :3100 y vanilla :5173 vía webServer y el smoke pasa"
    - "Smoke: gate visible → login operador → tabla con John Doe → logout → gate visible"
    - "E2E no define AUTH_DISABLED; sesión solo por formulario login"
    - "webServer.env usa DB_FILE aislado, JWT_SECRET no vacío y LOGIN_RATE_LIMIT_MAX elevado"
    - "Artefactos Playwright ignorados en git"
  artifacts:
    - path: e2e/playwright.config.js
      provides: "Orquestación dual webServer + env seguro E2E"
      contains: "webServer"
    - path: e2e/tests/auth-smoke.vanilla.spec.js
      provides: "Smoke auth vanilla"
      contains: "Iniciar sesión"
    - path: package.json
      provides: "Scripts test:e2e y devDep Playwright"
      contains: "test:e2e"
    - path: .gitignore
      provides: "Ignora test-results y playwright-report"
      contains: "test-results"
  key_links:
    - from: e2e/tests/auth-smoke.vanilla.spec.js
      to: "http://localhost:5173"
      via: "page.goto con baseURL"
      pattern: "getByRole.*Iniciar sesión"
    - from: e2e/playwright.config.js
      to: api/
      via: "webServer npm start + health :3100/health"
      pattern: "localhost:3100/health"
    - from: e2e/playwright.config.js
      to: dashboard/
      via: "python3 -m http.server 5173"
      pattern: "http.server 5173"
---

<objective>
Scaffold Playwright en la raíz: config dual webServer, smoke auth vanilla y toolchain npm (QA-E2E-01, QA-E2E-02, QA-CI-04 vía webServer.env).

Purpose: Base ejecutable sin levantar servidores a mano; contrato didáctico login UI real.
Output: `e2e/`, root `package.json` + lock, `.gitignore`, `.nvmrc`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-fundaci-n-e2e-playwright-vanilla/26-CONTEXT.md
@.planning/phases/26-fundaci-n-e2e-playwright-vanilla/RESEARCH.md
@.planning/phases/26-fundaci-n-e2e-playwright-vanilla/PATTERNS.md
@package.json
@api/package.json
@api/test-auth-helpers.js
@api/auth.js
@dashboard/index.html
@dashboard/app.js
@.gitignore

<interfaces>
Credenciales operador (alinear spec + webServer):
```javascript
// api/test-auth-helpers.js
const ADMIN_EMAIL = 'admin@lab.local';
const ADMIN_PASSWORD = 'changeme';
```

Selectores DOM verificados (`dashboard/index.html`):
- Gate: `getByRole('heading', { name: 'Iniciar sesión' })`
- Email: `getByLabel('Email')` → `#login-email`
- Contraseña: `getByLabel('Contraseña')` → `#login-password`
- Entrar: `getByRole('button', { name: 'Entrar' })`
- Logout: `getByRole('button', { name: 'Cerrar sesión' })` → `#logout-button`
- Seed users: `John Doe` / `john@example.com` (`api/seed.js`)

Rate limit env (`api/auth.js`):
- `LOGIN_RATE_LIMIT_MAX` leído al cargar módulo — debe estar en `webServer.env` antes de `npm start`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toolchain raíz — Playwright devDep, scripts y .gitignore</name>
  <files>package.json, package-lock.json, .nvmrc, .gitignore</files>
  <action>
    1. **Instalar** `@playwright/test@^1.60.0` como devDependency en `package.json` raíz (D-01). Ejecutar `npm install` en raíz para generar `package-lock.json`.

    2. **Añadir scripts** (D-01, RESEARCH Pattern 3):
       - `"test:e2e": "playwright test --config=e2e/playwright.config.js"`
       - `"test:e2e:ui": "playwright test --config=e2e/playwright.config.js --ui"`
       - `"test:e2e:headed": "playwright test --config=e2e/playwright.config.js --headed"`
       - `"playwright:install": "playwright install chromium"`

    3. **Crear `.nvmrc`** con contenido `22` (D-08 discreción — alinea CI Node 22).

    4. **Ampliar `.gitignore`** (ROADMAP criterio 5):
       ```
       # Playwright E2E
       test-results/
       playwright-report/
       blob-report/
       playwright/.auth/
       ```
       No duplicar `api/data/e2e.users.db` — ya cubierto por `api/data/*.db`.

    5. **Post-install:** ejecutar `npm run playwright:install` (Chromium local).

    **No** añadir Playwright a `api/package.json`. **No** commitear artefactos de test.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.devDependencies['@playwright/test']) process.exit(1); if(!p.scripts['test:e2e']) process.exit(1);" && test -f package-lock.json && grep -q 'test-results/' .gitignore && grep -q 'playwright-report/' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - Root package.json expone test:e2e y devDep @playwright/test
    - package-lock.json generado; .nvmrc = 22
    - Artefactos Playwright en .gitignore
  </acceptance_criteria>
  <done>QA-E2E-01 parcial — toolchain raíz listo</done>
</task>

<task type="auto">
  <name>Task 2: e2e/playwright.config.js — dual webServer y env seguro</name>
  <files>e2e/playwright.config.js</files>
  <action>
    Crear `e2e/playwright.config.js` CommonJS (`require`) per D-02, D-03.

    **Estructura obligatoria:**
    - `testDir`: path a `e2e/tests` (D-08)
    - `use.baseURL`: `'http://localhost:5173'` (obligatorio con webServer array)
    - `use`: `trace: 'on-first-retry'`, `screenshot: 'only-on-failure'`, `devices['Desktop Chrome']`
    - `retries`: CI ? 1 : 0; `workers`: CI ? 1 : undefined
    - `reporter`: CI ? `[['github'], ['list']]` : `'list'`
    - `forbidOnly`: `!!process.env.CI`

    **webServer[0] api** (D-03, D-04, D-05, D-06, D-07):
    - `command`: `'npm start'`, `cwd`: `api/`
    - `url`: `'http://localhost:3100/health'`, `timeout`: 60_000
    - `reuseExistingServer`: `!process.env.CI`
    - `env`:
      - `PORT`: `'3100'`
      - `JWT_SECRET`: `process.env.E2E_JWT_SECRET || 'e2e-local-secret-not-for-production'`
      - `DB_FILE`: `'data/e2e.users.db'`
      - `ADMIN_EMAIL`: `process.env.E2E_OPERATOR_EMAIL || 'admin@lab.local'`
      - `ADMIN_PASSWORD`: `process.env.E2E_OPERATOR_PASSWORD || 'changeme'`
      - `LOGIN_RATE_LIMIT_MAX`: `process.env.LOGIN_RATE_LIMIT_MAX || '1000'`
      - `CORS_ORIGINS`: `'http://localhost:5173,http://localhost:5174,http://localhost:5175'`
      - **NO** incluir `AUTH_DISABLED` ni `DATABASE_URL`

    **webServer[1] vanilla**:
    - `command`: `'python3 -m http.server 5173'`, `cwd`: `dashboard/`
    - `url`: `'http://localhost:5173'`, `timeout`: 30_000
    - `reuseExistingServer`: `!process.env.CI`

    Usar `path.join(__dirname, '..')` para rutas al repo root.
  </action>
  <verify>
    <automated>node --check e2e/playwright.config.js && grep -q 'webServer' e2e/playwright.config.js && grep -q 'e2e.users.db' e2e/playwright.config.js && grep -q 'LOGIN_RATE_LIMIT_MAX' e2e/playwright.config.js && ! grep -q 'AUTH_DISABLED' e2e/playwright.config.js</automated>
  </verify>
  <acceptance_criteria>
    - Config válida JS con dos webServer y baseURL explícito
    - Env E2E seguro: credenciales operador, rate limit elevado, sin AUTH_DISABLED
  </acceptance_criteria>
  <done>QA-E2E-01 + QA-CI-04 (webServer.env) cumplidos en config</done>
</task>

<task type="auto">
  <name>Task 3: Smoke spec auth vanilla — gate → login → tabla → logout</name>
  <files>e2e/tests/auth-smoke.vanilla.spec.js</files>
  <action>
    Crear `e2e/tests/auth-smoke.vanilla.spec.js` (D-08, QA-E2E-02):

    ```javascript
    const { test, expect } = require('@playwright/test');

    const email = process.env.E2E_OPERATOR_EMAIL || 'admin@lab.local';
    const password = process.env.E2E_OPERATOR_PASSWORD || 'changeme';

    test.describe('Smoke auth — dashboard vanilla', () => {
      test('gate → login → tabla con datos → logout → gate', async ({ page }) => {
        await page.goto('/');

        // 1. Gate visible, logout oculto
        await expect(page.getByRole('heading', { name: 'Iniciar sesión' })).toBeVisible();
        await expect(page.getByRole('button', { name: 'Cerrar sesión' })).toBeHidden();

        // 2. Login operador (UI real — cookie httpOnly vía POST /auth/login)
        await page.getByLabel('Email').fill(email);
        await page.getByLabel('Contraseña').fill(password);
        await page.getByRole('button', { name: 'Entrar' }).click();

        // 3. Dashboard + fila semilla users
        await expect(page.getByRole('button', { name: 'Cerrar sesión' })).toBeVisible();
        await expect(page.locator('#dashboard-panel')).toBeVisible();
        await expect(page.getByRole('table')).toBeVisible();
        await expect(page.getByText('John Doe')).toBeVisible();

        // 4. Logout → gate de nuevo
        await page.getByRole('button', { name: 'Cerrar sesión' }).click();
        await expect(page.getByRole('heading', { name: 'Iniciar sesión' })).toBeVisible();
        await expect(page.getByRole('button', { name: 'Cerrar sesión' })).toBeHidden();
      });
    });
    ```

    **Evitar:** `storageState`, `request.post` sin UI, `document.cookie` para `edf_session` (httpOnly).

    **Verificar end-to-end:** desde raíz, con `cd api && npm ci` si falta `node_modules`, ejecutar `npm run test:e2e`. Debe pasar en verde.
  </action>
  <verify>
    <automated>test -f e2e/tests/auth-smoke.vanilla.spec.js && node --check e2e/tests/auth-smoke.vanilla.spec.js && grep -q "Iniciar sesión" e2e/tests/auth-smoke.vanilla.spec.js && grep -q "John Doe" e2e/tests/auth-smoke.vanilla.spec.js && (cd api && test -d node_modules || npm ci) && npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - Un spec lineal cubre flujo completo auth vanilla
    - npm run test:e2e pasa localmente sin servidores manuales
  </acceptance_criteria>
  <done>QA-E2E-02 completo; smoke verde local</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API :3100 | Credenciales operador en formulario login; cookie httpOnly |
| Playwright config → API process | Env vars inyectadas en webServer spawn |
| Repo → git | Artefactos trace/report no deben commitearse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Spoofing | `playwright/.auth/` | mitigate | `.gitignore`; no storageState en smoke |
| T-26-02 | Elevation | E2E env | mitigate | Grep: sin `AUTH_DISABLED` en config/spec |
| T-26-03 | Information disclosure | JWT/creds | mitigate | Defaults lab only; `E2E_JWT_SECRET` override en CI |
| T-26-04 | Denial of service | Login rate limit | mitigate | `LOGIN_RATE_LIMIT_MAX=1000` en webServer.env |
| T-26-05 | Tampering | `e2e.users.db` | accept | BD aislada; no datos prod |
</threat_model>

<verification>
- `node --check` en config y spec
- Grep negativo: `AUTH_DISABLED` ausente en `e2e/`
- `npm run test:e2e` verde desde raíz
</verification>

<success_criteria>
- QA-E2E-01, QA-E2E-02 y QA-CI-04 (env webServer) satisfechos
- Operador puede repetir smoke con un solo comando
</success_criteria>

<output>
After completion, create `.planning/phases/26-fundaci-n-e2e-playwright-vanilla/26-01-SUMMARY.md`
</output>
