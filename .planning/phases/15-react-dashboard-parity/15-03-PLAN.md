---
phase: 15-react-dashboard-parity
plan: 03
type: execute
wave: 2
depends_on:
  - 15-02
files_modified:
  - dashboard-react/README.md
  - README.md
  - .planning/phases/15-react-dashboard-parity/15-UAT.md
autonomous: true
requirements:
  - FRWK-07
  - FRWK-08
must_haves:
  truths:
    - "D-16: CORS verified from :5174 without api/ code change"
    - "D-14: README documents npm scripts in dashboard-react/"
    - "Vanilla :5173 still works after verification"
  artifacts:
    - path: .planning/phases/15-react-dashboard-parity/15-UAT.md
      provides: "Manual verification checklist"
      contains: "5174"
    - path: dashboard-react/README.md
      provides: "Learner setup guide"
      contains: "VITE_API_BASE_URL"
  key_links:
    - from: README.md
      to: dashboard-react/README.md
      via: "advanced optional pointer"
      pattern: "dashboard-react"
---

<objective>
Verify CORS/ports end-to-end, document UAT checklist, and add README pointers (vanilla remains primary).

Purpose: FRWK-07, FRWK-08 completion; phase 15 sign-off.
Output: 15-UAT.md, updated READMEs, verification notes for planner/executor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-react-dashboard-parity/15-CONTEXT.md
@.planning/phases/15-react-dashboard-parity/15-RESEARCH.md
@docs/05-cors-explicado.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run and document UAT (FRWK-07, D-16)</name>
  <read_first>dashboard/app.js behavior, 15-CONTEXT.md D-16</read_first>
  <files>.planning/phases/15-react-dashboard-parity/15-UAT.md</files>
  <action>
    Create `15-UAT.md` with executable checklist:

    **Setup:** API `PORT=3100 npm start` in api/; React `npm run dev` in dashboard-react/

    **Scenarios (pass/fail table):**
    1. Initial load — health, API info, users visible on :5174
    2. Create user via form
    3. Edit user — form mode, PUT success message
    4. Delete user — confirm, DELETE success
    5. Duplicate email — 409, global feedback + inline email error
    6. API stopped — offline/error panel on React app
    7. CORS — no console CORS errors on :5174
    8. Vanilla regression — `http://localhost:5173` still works (python server + vanilla)

  </action>
  <verify>
    <automated>test -f .planning/phases/15-react-dashboard-parity/15-UAT.md && grep -q "5174" .planning/phases/15-react-dashboard-parity/15-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - UAT doc has at least 8 scenarios
    - Notes whether api/index.js was changed (expect: no)
  </acceptance_criteria>
  <done>UAT checklist written</done>
</task>

<task type="auto">
  <name>Task 2: Execute UAT and record results</name>
  <read_first>15-UAT.md</read_first>
  <files>.planning/phases/15-react-dashboard-parity/15-UAT.md</files>
  <action>
    Run the UAT scenarios with API + React dev server. Update `15-UAT.md` with Pass/Fail column filled and date.

    If CORS fails (unexpected), document in NOTEBOOK.md as blocker only — do not expand scope to whitelist unless user approves (D-16 default: no api change).

    If vanilla :5173 fails only due to port conflict, document EADDRINUSE remediation (link NOTEBOOK).
  </action>
  <verify>
    <automated>grep -q "Pass" .planning/phases/15-react-dashboard-parity/15-UAT.md || grep -q "PASS" .planning/phases/15-react-dashboard-parity/15-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - All critical scenarios pass or failures documented with cause
  </acceptance_criteria>
  <done>FRWK-07 verified</done>
</task>

<task type="auto">
  <name>Task 3: Update README files (FRWK-08)</name>
  <read_first>README.md Compose section style</read_first>
  <files>dashboard-react/README.md, README.md</files>
  <action>
    **dashboard-react/README.md** — complete guide:
    - Prerequisites: Node, API on 3100
    - `cp .env.example .env` optional
    - `npm install`, `npm run dev` → http://localhost:5174
    - Port conflict notes (5173 vanilla vs 5174 react)
    - Link to vanilla `dashboard/` as primary path

    **README.md** (root) — add short "Opcional: Dashboard React" subsection under learning paths:
    - Mark as avanzado/opcional
    - Link `dashboard-react/README.md`
    - Do NOT demote vanilla instructions

    No Mission/doc 16 yet (phase 17).
  </action>
  <verify>
    <automated>grep -q "dashboard-react" README.md && grep -q "VITE_API_BASE_URL" dashboard-react/README.md</automated>
  </verify>
  <acceptance_criteria>
    - Root README mentions React path as optional
    - dashboard-react README is self-contained for learners
  </acceptance_criteria>
  <done>FRWK-08 documentation complete</done>
</task>

</tasks>

<verification>
- UAT executed and recorded
- grep checks for README content
</verification>

<success_criteria>
- FRWK-07 and FRWK-08 complete
- Phase 15 ready for 15-VERIFICATION.md after execute
</success_criteria>

<output>
After completion, create `.planning/phases/15-react-dashboard-parity/15-03-SUMMARY.md`
</output>
