---
phase: 15-react-dashboard-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - dashboard-react/src/App.jsx
  - dashboard-react/src/components/ConnectionStatus.jsx
  - dashboard-react/src/components/HealthCard.jsx
  - dashboard-react/src/components/ApiInfoCard.jsx
  - dashboard-react/src/components/UsersTable.jsx
  - dashboard-react/src/components/UserForm.jsx
autonomous: true
requirements:
  - FRWK-02
  - FRWK-03
  - FRWK-06
must_haves:
  truths:
    - "D-04: Component split mirrors vanilla sections"
    - "D-06: Lifted state in App with props to children"
    - "D-08: Reload dashboard data after successful mutations"
    - "D-09: Edit flow matches vanilla (Edit/Cancel)"
    - "D-11: 409 shows mutation-feedback AND inline email error"
    - "FRWK-06: Same HTTP methods and JSON bodies as vanilla"
  artifacts:
    - path: dashboard-react/src/components/UserForm.jsx
      provides: "Create/edit user form"
      contains: "POST"
    - path: dashboard-react/src/components/UsersTable.jsx
      provides: "CRUD actions"
      contains: "DELETE"
  key_links:
    - from: dashboard-react/src/App.jsx
      to: dashboard/app.js
      via: "handleUserFormSubmit parity"
      pattern: "loadDashboardData"
---

<objective>
Extract components and implement full CRUD parity with vanilla, including dual 409 feedback.

Purpose: FRWK-02, FRWK-03, FRWK-06.
Output: Feature-complete React dashboard (except docs/UAT in 15-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-react-dashboard-parity/15-CONTEXT.md
@.planning/phases/15-react-dashboard-parity/15-RESEARCH.md
@dashboard/app.js
@dashboard/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract components (D-04, D-06)</name>
  <read_first>dashboard/index.html structure, 15-PATTERNS.md</read_first>
  <files>dashboard-react/src/components/*.jsx, dashboard-react/src/App.jsx</files>
  <action>
    Create components under `src/components/`:

    - `ConnectionStatus.jsx` — status dot + connection text
    - `HealthCard.jsx` — status, timestamp
    - `ApiInfoCard.jsx` — message, version, endpoints list
    - `UsersTable.jsx` — table with Editar/Eliminar per row
    - `UserForm.jsx` — name, email, submit, cancel edit, form mode message, mutation feedback area, email inline error

    Refactor `App.jsx` to lift state and pass props/callbacks. Keep Tailwind layout approximating vanilla sections (D-02).
    Reuse Spanish strings from `dashboard/index.html` (D-03).
  </action>
  <verify>
    <automated>cd dashboard-react && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Five component files exist
    - App.jsx orchestrates without duplicating table/form markup inline
  </acceptance_criteria>
  <done>Component architecture complete</done>
</task>

<task type="auto">
  <name>Task 2: CRUD + edit mode (FRWK-03, D-08, D-09)</name>
  <read_first>dashboard/app.js handleUserFormSubmit, startEditingUser, deleteUser</read_first>
  <files>dashboard-react/src/App.jsx, dashboard-react/src/components/UserForm.jsx, dashboard-react/src/components/UsersTable.jsx</files>
  <action>
    Implement:

    - POST `/users` create, PUT `/users/:id` update, DELETE `/users/:id` with confirm
    - `editingUserId` state; Edit fills form; Cancel resets (vanilla parity)
    - Success feedback strings: `POST /users -> usuario creado`, `PUT /users/:id -> usuario actualizado`, `DELETE /users/:id -> usuario eliminado`
    - After success: `resetUserForm` + `loadDashboardData()` (D-08)
    - Empty users: "No hay usuarios disponibles."

    Use `fetchJson` from `src/api.js` only.
  </action>
  <verify>
    <automated>cd dashboard-react && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All three mutation types implemented
    - Reload after successful mutation
  </acceptance_criteria>
  <done>FRWK-03 CRUD complete</done>
</task>

<task type="auto">
  <name>Task 3: Error handling — 409 dual feedback (D-11)</name>
  <read_first>api/index.js 409 responses, src/api.js err.status</read_first>
  <files>dashboard-react/src/App.jsx, dashboard-react/src/components/UserForm.jsx</files>
  <action>
    On mutation catch:

    - Show global mutation-feedback with error message (vanilla `showMutationFeedback` style)
    - When `error.status === 409`, also set inline email field error (red text under input)

    Clear inline email error on successful submit or `resetUserForm`.

    Global load failure: error-box style panel, offline connection state (D-10).
  </action>
  <verify>
    <automated>cd dashboard-react && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Code branches on status 409 for inline email hint
    - Duplicate email testable manually with same email twice
  </acceptance_criteria>
  <done>D-11 error UX complete</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Spoofing | API URL env | accept | Lab default localhost only |
</threat_model>

<verification>
Manual UAT (full checklist in 15-03):
- Create user, edit, delete
- POST duplicate email → 409 with both feedbacks
- Stop API → offline error state
</verification>

<success_criteria>
- FRWK-02, FRWK-03, FRWK-06 satisfied in code
</success_criteria>

<output>
After completion, create `.planning/phases/15-react-dashboard-parity/15-02-SUMMARY.md`
</output>
