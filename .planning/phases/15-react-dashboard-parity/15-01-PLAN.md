---
phase: 15-react-dashboard-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard-react/package.json
  - dashboard-react/package-lock.json
  - dashboard-react/vite.config.js
  - dashboard-react/index.html
  - dashboard-react/.env.example
  - dashboard-react/.gitignore
  - dashboard-react/src/main.jsx
  - dashboard-react/src/index.css
  - dashboard-react/src/api.js
  - dashboard-react/src/App.jsx
  - dashboard-react/README.md
autonomous: true
requirements:
  - FRWK-01
  - FRWK-08
must_haves:
  truths:
    - "D-01: dashboard-react/ exists with Vite + React + Tailwind"
    - "D-05: src/api.js exports fetchJson and API_BASE_URL from VITE_* env"
    - "D-12: Dev server on port 5174"
    - "D-15: .env.example documents VITE_API_BASE_URL"
    - "D-09: No changes to api/ or dashboard/ vanilla"
  artifacts:
    - path: dashboard-react/package.json
      provides: "React app package"
      contains: "vite"
    - path: dashboard-react/src/api.js
      provides: "HTTP client"
      contains: "fetchJson"
  key_links:
    - from: dashboard-react/src/api.js
      to: dashboard/app.js
      via: "behavioral parity"
      pattern: "localhost:3100"
---

<objective>
Scaffold `dashboard-react/` with Vite, React 18, Tailwind v4, `src/api.js`, and an `App.jsx` shell that loads health, API info, and users on mount.

Purpose: FRWK-01, FRWK-08 (partial — README in 15-03).
Output: Runnable dev server on :5174 with initial data load (read-only UI acceptable before 15-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-react-dashboard-parity/15-CONTEXT.md
@.planning/phases/15-react-dashboard-parity/15-RESEARCH.md
@.planning/phases/15-react-dashboard-parity/15-PATTERNS.md
@dashboard/app.js
@dashboard/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vite + React + Tailwind scaffold (FRWK-01)</name>
  <read_first>15-RESEARCH.md, 15-CONTEXT.md D-01 D-12 D-13</read_first>
  <files>dashboard-react/*</files>
  <action>
    Create `dashboard-react/` at repo root (sibling to `dashboard/`).

    - Use Vite React template (React 18 LTS)
    - Add Tailwind v4 with `@tailwindcss/vite` plugin in `vite.config.js`
    - `server.port: 5174`, `strictPort: true`
    - `src/index.css`: `@import "tailwindcss";`
    - `index.html` lang="es", title referencing React dashboard lab
    - `.gitignore` for node_modules, dist, .env
    - `.env.example`: `VITE_API_BASE_URL=http://localhost:3100`
    - `dashboard-react/README.md`: how to `npm install`, `npm run dev`, required API on :3100

    Do NOT add React Router, Redux, or axios.
  </action>
  <verify>
    <automated>test -f dashboard-react/package.json && test -f dashboard-react/vite.config.js && grep -q "5174" dashboard-react/vite.config.js</automated>
  </verify>
  <acceptance_criteria>
    - `cd dashboard-react && npm run build` succeeds
    - Dev server configured for port 5174
  </acceptance_criteria>
  <done>FRWK-01 scaffold complete</done>
</task>

<task type="auto">
  <name>Task 2: Implement src/api.js (FRWK-08)</name>
  <read_first>dashboard/app.js fetchJson, 15-RESEARCH.md api.js section</read_first>
  <files>dashboard-react/src/api.js</files>
  <action>
    Implement `src/api.js`:

    - Export `API_BASE_URL` from `import.meta.env.VITE_API_BASE_URL` with fallback `http://localhost:3100`
    - Export `fetchJson(path, options)` using native `fetch`
    - On `!response.ok`, parse JSON `{ error }` when present; attach `err.status = response.status` for callers (409 handling in plan 15-02)
    - Spanish error messages matching vanilla tone
  </action>
  <verify>
    <automated>node --check dashboard-react/src/api.js 2>/dev/null || (cd dashboard-react && npm run build)</automated>
  </verify>
  <acceptance_criteria>
    - api.js exports API_BASE_URL and fetchJson
    - No axios or other HTTP libraries
  </acceptance_criteria>
  <done>HTTP layer ready</done>
</task>

<task type="auto">
  <name>Task 3: App.jsx initial load shell (FRWK-02 partial)</name>
  <read_first>dashboard/app.js loadDashboardData, renderHealth, renderApiInfo, renderUsers</read_first>
  <files>dashboard-react/src/App.jsx</files>
  <action>
    Implement `App.jsx` with useState (D-07):

    - On mount: `loadDashboardData()` calling `Promise.all` on `/health`, `/`, `/users`
    - Loading state disables reload button / shows checking connection
    - On success: render placeholder sections for connection status, health, API message/version/endpoints list, users table (can be inline JSX in App for now — components split in 15-02)
    - On failure: show error panel (offline), clear data (D-10 default)
    - Display `API_BASE_URL` in toolbar (like vanilla `#api-base-url`)
    - Reuse Spanish copy from `dashboard/index.html` for headings

    CRUD form/table interactions may be stubs; full parity is plan 15-02.
  </action>
  <verify>
    <automated>cd dashboard-react && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - With API running, build passes and code structure supports triple fetch on load
  </acceptance_criteria>
  <done>FRWK-02 load path implemented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| New folder only | No api/ changes in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | VITE_* env | accept | Lab-only; document .env.example |
</threat_model>

<verification>
- `cd dashboard-react && npm run build`
- Manual: API up → `npm run dev` → page shows health status (after 15-02 may show full UI)
</verification>

<success_criteria>
- FRWK-01 and FRWK-08 scaffold satisfied
- FRWK-02 load logic present (CRUD completed in 15-02)
</success_criteria>

<output>
After completion, create `.planning/phases/15-react-dashboard-parity/15-01-SUMMARY.md`
</output>
