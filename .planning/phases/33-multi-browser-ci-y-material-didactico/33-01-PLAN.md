---
phase: 33-multi-browser-ci-y-material-didactico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/playwright.config.js
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - QA-ADV-04
user_setup: []
must_haves:
  truths:
    - "playwright.config.js define proyectos chromium y firefox por dashboard"
    - "npm run test:e2e sigue ejecutando solo chromium (6 tests)"
    - "npm run test:e2e:ci ejecuta chromium + firefox (12 tests)"
    - "CI e2e-smoke instala firefox y ejecuta test:e2e:ci"
    - "Proyecto vanilla-webkit existe y está documentado, no en CI"
  artifacts:
    - path: e2e/playwright.config.js
      provides: Proyectos multi-browser
      contains: firefox
    - path: package.json
      provides: Scripts test:e2e:ci
      contains: test:e2e:ci
    - path: .github/workflows/ci.yml
      provides: CI multi-browser
      contains: firefox
  key_links:
    - from: e2e-smoke job
      to: test:e2e:ci
      via: npm run
      pattern: test:e2e:ci
    - from: playwright.config.js
      to: auth-smoke + crud specs
      via: testMatch per browser project
      pattern: vanilla-firefox
---

<objective>
Añadir soporte Firefox en Playwright y cablearlo en CI `e2e-smoke`, manteniendo `test:e2e` local en Chromium (6 tests).

Purpose: QA-ADV-04 — CI valida más de un motor.
Output: proyectos multi-browser, scripts `test:e2e:ci`, CI actualizado.
</objective>

<context>
@.planning/phases/33-multi-browser-ci-y-material-didactico/33-RESEARCH.md
@e2e/playwright.config.js
@.github/workflows/ci.yml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor proyectos multi-browser en playwright.config.js</name>
  <read_first>
    e2e/playwright.config.js
    .planning/phases/33-multi-browser-ci-y-material-didactico/33-RESEARCH.md
  </read_first>
  <files>e2e/playwright.config.js</files>
  <action>
Refactorizar `projects` usando un array de dashboards y browsers:

Dashboards (mantener testMatch y baseURL actuales):
- vanilla → `:5173`, `/(auth-smoke|crud)\.vanilla\.spec\.js/`
- react → `:5174`, `/(auth-smoke|crud)\.react\.spec\.js/`
- vue → `:5175`, `/(auth-smoke|crud)\.vue\.spec\.js/`

Browsers CI/local:
- `chromium` → `devices['Desktop Chrome']`
- `firefox` → `devices['Desktop Firefox']`

Generar 6 proyectos: `{dashboard}-chromium`, `{dashboard}-firefox`.

Añadir **opcional** séptimo proyecto `vanilla-webkit` con `devices['Desktop Safari']`, mismo testMatch vanilla — solo para uso local documentado.

Quitar `...devices['Desktop Chrome']` del bloque `use` global (cada proyecto define device en `use`).

Mantener `webServer`, `workers`, `retries`, `apiEnv` sin cambios funcionales.
  </action>
  <acceptance_criteria>
    - `playwright.config.js` contiene `vanilla-firefox` y `react-chromium`
    - `playwright.config.js` contiene `vanilla-webkit`
    - Proyectos legacy `name: 'vanilla'` sin sufijo browser **no** existen (reemplazados por `-chromium`)
    - `webServer` sigue siendo array único de 4 servidores
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Scripts package.json</name>
  <read_first>
    package.json
    e2e/playwright.config.js
  </read_first>
  <files>package.json</files>
  <action>
Actualizar scripts:

- `test:e2e` — ejecutar solo proyectos chromium:
  `playwright test --config=e2e/playwright.config.js --project=vanilla-chromium --project=react-chromium --project=vue-chromium`

- `test:e2e:ci` — chromium + firefox (12 tests):
  `playwright test --config=e2e/playwright.config.js --project=vanilla-chromium --project=react-chromium --project=vue-chromium --project=vanilla-firefox --project=react-firefox --project=vue-firefox`

- `test:e2e:firefox` — solo firefox (6 tests) para prueba local opt-in

- `playwright:install:ci` — `playwright install chromium firefox`

- `test:e2e:ui` y `test:e2e:headed` — añadir los tres `--project=*-chromium` para coherencia

No cambiar `test:e2e:pg` (sigue config PG separada; chromium-only aceptable).
  </action>
  <acceptance_criteria>
    - `package.json` contiene `test:e2e:ci` y `playwright:install:ci`
    - `test:e2e` limita a 3 proyectos chromium
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: CI e2e-smoke multi-browser</name>
  <read_first>
    .github/workflows/ci.yml
    package.json
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
En job `e2e-smoke`:
- Reemplazar `npx playwright install --with-deps chromium` por `npx playwright install --with-deps chromium firefox`
- Reemplazar `npm run test:e2e` por `npm run test:e2e:ci`

**No** modificar job `e2e-postgres` (sigue `test:e2e:pg` + chromium).
  </action>
  <acceptance_criteria>
    - `ci.yml` job `e2e-smoke` ejecuta `test:e2e:ci`
    - `ci.yml` e2e-smoke install incluye `firefox`
    - Job `e2e-postgres` sin cambios en comando test
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Verificación multi-browser</name>
  <read_first>
    package.json
  </read_first>
  <files></files>
  <action>
Ejecutar:
1. `npm run playwright:install:ci` (o install firefox si falta)
2. `npm run test:e2e` — 6 passed
3. `npm run test:e2e:ci` — 12 passed (objetivo; si firefox flake, documentar en SUMMARY)

Registrar en `33-01-SUMMARY.md`.
  </action>
  <acceptance_criteria>
    - `npm run test:e2e` exit 0 con 6 tests
    - `npm run test:e2e:ci` exit 0 con 12 tests o SUMMARY documenta bloqueo firefox local
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
node --check e2e/playwright.config.js
npm run test:e2e
npm run test:e2e:ci
grep test:e2e:ci .github/workflows/ci.yml package.json
```
</verification>

<success_criteria>
1. CI e2e-smoke corre Chromium + Firefox.
2. Flujo local `test:e2e` sigue siendo 6 tests Chromium.
3. WebKit disponible como proyecto vanilla-webkit (sin CI).
</success_criteria>
