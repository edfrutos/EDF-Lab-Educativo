---
phase: 40-oauth-social-login-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/auth.js
  - api/index.js
  - api/db-sqlite.js
  - api/db-pg.js
  - api/index.test.js
  - api/index.pg.test.js
  - api/test-auth-helpers.js
autonomous: true
requirements:
  - AUTH-ADV-01
user_setup:
  - "OAuth provider real opcional; debe existir ruta testeable en local"
must_haves:
  truths:
    - "Existe contrato OAuth backend para start/callback"
    - "Flujo OAuth emite sesión compatible con rutas protegidas"
    - "Login clásico y refresh no se rompen"
  artifacts:
    - path: api/index.js
      provides: "Rutas OAuth"
      contains: "/auth/oauth"
    - path: api/test-auth-helpers.js
      provides: "Cobertura de auth extendida con OAuth"
      contains: "oauth"
  key_links:
    - from: api/index.js
      to: api/auth.js
      via: "handlers OAuth"
      pattern: "oauth"
---

<objective>
Implementar la base técnica de AUTH-ADV-01 con endpoints OAuth/social (inicio + callback) y emisión de sesión compatible con el flujo vigente.

Purpose: abrir autenticación social de forma incremental y didáctica, sin romper contrato actual.
Output: contrato OAuth backend + tests API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-oauth-social-login-foundation/40-CONTEXT.md
@.planning/phases/40-oauth-social-login-foundation/40-RESEARCH.md
@api/index.js
@api/auth.js
@api/test-auth-helpers.js
@api/db-sqlite.js
@api/db-pg.js

<interfaces>
Contrato sugerido:

```http
GET  /auth/oauth/start
GET  /auth/oauth/callback
```

Criterios mínimos:
- manejo de `state` anti-CSRF
- emisión de `edf_session` + `edf_refresh` en callback exitoso
- errores claros en callback inválido
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar rutas OAuth backend foundation</name>
  <read_first>
    api/auth.js
    api/index.js
    api/db-sqlite.js
    api/db-pg.js
  </read_first>
  <files>api/auth.js, api/index.js, api/db-sqlite.js, api/db-pg.js</files>
  <action>
Añadir foundation OAuth con start/callback:
- generar/validar state
- resolver identidad operador (mock/provider adapter)
- emitir cookies de sesión/refresh usando flujo ya existente
- mantener `/auth/login`, `/auth/refresh`, `/auth/logout`, `/auth/password` sin regresión
  </action>
  <verify>
    <automated>node --check api/index.js && node --check api/auth.js</automated>
  </verify>
  <acceptance_criteria>
    - rutas OAuth disponibles
    - callback exitoso crea sesión compatible
    - rutas auth existentes se mantienen estables
  </acceptance_criteria>
  <done>Base OAuth backend implementada</done>
</task>

<task type="auto">
  <name>Task 2: Cobertura de tests auth para OAuth</name>
  <read_first>
    api/test-auth-helpers.js
    api/index.test.js
    api/index.pg.test.js
  </read_first>
  <files>api/test-auth-helpers.js, api/index.test.js, api/index.pg.test.js</files>
  <action>
Extender pruebas de auth con casos OAuth mínimos:
- start devuelve contrato esperado
- callback con state inválido falla
- callback exitoso emite sesión usable para `/users`
  </action>
  <verify>
    <automated>cd api && npm run test:sqlite</automated>
  </verify>
  <acceptance_criteria>
    - suite SQLite auth verde con casos OAuth
    - sin regresión en casos login/refresh/password previos
  </acceptance_criteria>
  <done>OAuth cubierto por tests API</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OAuth callback | Validación estricta de state para evitar CSRF |
| Session issuance | Reusar política segura de cookies actuales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | S | Callback sin state válido | mitigate | Rechazar callback con state ausente/inválido |
| T-40-02 | E | Sesión OAuth inconsistente | mitigate | Emitir mismas cookies/session path que login clásico |
</threat_model>

<verification>
- `node --check api/index.js`
- `node --check api/auth.js`
- `cd api && npm run test:sqlite`
- `cd api && npm run test:pg` (opcional)
</verification>

<success_criteria>
- AUTH-ADV-01 foundation implementada en backend
- compatibilidad total con flujo auth actual
</success_criteria>

<output>
After completion, create `.planning/phases/40-oauth-social-login-foundation/40-01-SUMMARY.md`
</output>
