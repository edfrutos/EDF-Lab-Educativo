---
phase: 32-e2e-contra-api-postgres
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/scripts/prepare-test-db.js
  - e2e/playwright.config.pg.js
  - package.json
  - e2e/helpers/crud-flow.js
autonomous: true
requirements:
  - QA-ADV-03
  - QA-CI-05
user_setup: []
must_haves:
  truths:
    - "Existe playwright.config.pg.js que arranca API con DATABASE_URL y sin DB_FILE"
    - "prepare-test-db.js crea edf_lab_e2e de forma idempotente"
    - "npm run test:e2e:pg ejecuta auth smoke (mínimo) contra Postgres"
    - "E2E_DATABASE_URL por defecto apunta a edf_lab_e2e, no edf_lab ni edf_lab_test"
  artifacts:
    - path: e2e/playwright.config.pg.js
      provides: Perfil Playwright Postgres
      contains: DATABASE_URL
    - path: api/scripts/prepare-test-db.js
      provides: Bootstrap BD edf_lab_e2e
      contains: edf_lab_e2e
  key_links:
    - from: playwright.config.pg.js
      to: api/index.js
      via: webServer env DATABASE_URL
      pattern: DATABASE_URL
    - from: prepare-test-db.js
      to: edf_lab_e2e
      via: CREATE DATABASE
      pattern: edf_lab_e2e
---

<objective>
Añadir perfil Playwright Postgres y bootstrap de base `edf_lab_e2e` sin alterar el flujo SQLite existente (`test:e2e`).

Purpose: QA-ADV-03 núcleo — API E2E puede usar Postgres aislado.
Output: `playwright.config.pg.js`, script prepare ampliado, `npm run test:e2e:pg`.
</objective>

<context>
@.planning/phases/32-e2e-contra-api-postgres/32-RESEARCH.md
@e2e/playwright.config.js
@api/scripts/prepare-test-db.js
@api/db.js
@docs/10-tests.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap edf_lab_e2e en prepare-test-db.js</name>
  <read_first>
    api/scripts/prepare-test-db.js
    .planning/REQUIREMENTS.md (QA-CI-05)
    .planning/phases/32-e2e-contra-api-postgres/32-RESEARCH.md
  </read_first>
  <files>api/scripts/prepare-test-db.js</files>
  <action>
Refactorizar mínimamente para crear **dos** bases idempotentes con el mismo `ADMIN_URL`:
1. `edf_lab_test` (comportamiento actual — no romper API tests)
2. `edf_lab_e2e` (nueva — dedicada E2E Postgres)

Extraer helper interno `createDatabaseIfNotExists(name)` que ejecute `CREATE DATABASE ${name} OWNER edf_lab` y trate `42P04` como éxito.

Mensajes de consola en español, uno por base creada o ya existente.

No cambiar `PG_ADMIN_URL` ni credenciales por defecto.
  </action>
  <acceptance_criteria>
    - `prepare-test-db.js` contiene la cadena literal `edf_lab_e2e`
    - `prepare-test-db.js` sigue creando `edf_lab_test`
    - `node api/scripts/prepare-test-db.js` sale 0 cuando Postgres está arriba (o 1 con mensaje ECONNREFUSED si no)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: playwright.config.pg.js</name>
  <read_first>
    e2e/playwright.config.js
    .planning/phases/32-e2e-contra-api-postgres/32-RESEARCH.md
    api/db.js
  </read_first>
  <files>e2e/playwright.config.pg.js</files>
  <action>
Crear `e2e/playwright.config.pg.js` copiando la estructura de `playwright.config.js` con estos cambios en `apiEnv`:

- Añadir `DATABASE_URL` desde `process.env.E2E_DATABASE_URL` con default `postgresql://edf_lab:edf_lab_dev@localhost:5432/edf_lab_e2e`
- **Eliminar** la clave `DB_FILE` del objeto env (no debe estar presente)
- Mantener `PORT`, `JWT_SECRET`, `ADMIN_EMAIL`, `ADMIN_PASSWORD`, `LOGIN_RATE_LIMIT_MAX`, `CORS_ORIGINS` igual que config SQLite
- Mantener los mismos 4 `webServer`, 3 `projects` (vanilla/react/vue) y mismos `testMatch` que config actual

Añadir comentario de cabecera: perfil Postgres E2E — requiere Postgres y `npm run test:db:prepare` en local.
  </action>
  <acceptance_criteria>
    - Archivo `e2e/playwright.config.pg.js` existe
    - Contiene `DATABASE_URL` y no contiene `DB_FILE`
    - Default connection string incluye `/edf_lab_e2e`
    - `testMatch` incluye `auth-smoke` y `crud` para vanilla, react y vue (misma regex que config SQLite)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Script raíz test:e2e:pg y comentario crud-flow</name>
  <read_first>
    package.json
    e2e/helpers/crud-flow.js
  </read_first>
  <files>package.json, e2e/helpers/crud-flow.js</files>
  <action>
En `package.json` raíz añadir script:
`"test:e2e:pg": "playwright test --config=e2e/playwright.config.pg.js"`

En `crud-flow.js`, actualizar el comentario del email único para mencionar que aplica tanto a SQLite (`e2e.users.db`) como a Postgres E2E (`edf_lab_e2e`), sin cambiar lógica.
  </action>
  <acceptance_criteria>
    - `package.json` contiene `"test:e2e:pg"`
    - `crud-flow.js` menciona Postgres o `edf_lab_e2e` en comentario de unicidad
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Verificación local perfil PG (si Postgres disponible)</name>
  <read_first>
    e2e/playwright.config.pg.js
    README.md
  </read_first>
  <files></files>
  <action>
Si Postgres responde en localhost:5432:
1. `npm run test:db:prepare`
2. `npm run test:e2e:pg`

Si Postgres no está disponible en el entorno de ejecución, documentar en SUMMARY que la verificación queda para plan 02/CI; no fallar el plan por entorno sin PG.

Registrar en SUMMARY: número de tests passed y si CRUD pasó o solo smoke.
  </action>
  <acceptance_criteria>
    - Con Postgres local: `npm run test:e2e:pg` sale 0 con al menos 3 tests passed (auth smoke ×3)
    - Sin Postgres: tareas 1–3 completadas; SUMMARY indica verificación diferida
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
node --check e2e/playwright.config.pg.js
grep -q edf_lab_e2e api/scripts/prepare-test-db.js
grep -q test:e2e:pg package.json
# Con Postgres:
npm run test:db:prepare && npm run test:e2e:pg
```
</verification>

<success_criteria>
1. Perfil PG arranca API sin SQLite.
2. BD `edf_lab_e2e` creada por prepare script.
3. `test:e2e` SQLite sin regresión (no modificado en este plan).
</success_criteria>
