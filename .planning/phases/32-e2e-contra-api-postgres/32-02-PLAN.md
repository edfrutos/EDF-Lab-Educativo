---
phase: 32-e2e-contra-api-postgres
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - .github/workflows/ci.yml
  - docs/10-tests.md
  - README.md
autonomous: true
requirements:
  - QA-ADV-03
  - QA-CI-05
user_setup: []
must_haves:
  truths:
    - "CI job e2e-postgres ejecuta test:e2e:pg con servicio Postgres y BD edf_lab_e2e"
    - "Job e2e-postgres nunca usa DATABASE_URL con /edf_lab ni /edf_lab_test"
    - "docs/10-tests.md documenta flujo local Postgres E2E y diferencia de las tres BDs"
    - "README menciona job e2e-postgres o test:e2e:pg"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: Job e2e-postgres
      contains: e2e-postgres
    - path: docs/10-tests.md
      provides: Documentación Postgres E2E
      contains: edf_lab_e2e
  key_links:
    - from: e2e-postgres job
      to: playwright.config.pg.js
      via: npm run test:e2e:pg
      pattern: test:e2e:pg
    - from: ci.yml services.postgres
      to: edf_lab_e2e
      via: POSTGRES_DB
      pattern: POSTGRES_DB: edf_lab_e2e
---

<objective>
Cablear CI Postgres E2E y documentar el flujo didáctico (local + GHA) sin tocar el job `e2e-smoke` SQLite.

Purpose: QA-CI-05 + cierre QA-ADV-03 en CI.
Output: job `e2e-postgres`, docs actualizadas, verificación 6 tests PG en CI.
</objective>

<context>
@.planning/phases/32-e2e-contra-api-postgres/32-RESEARCH.md
@.planning/phases/32-e2e-contra-api-postgres/32-01-SUMMARY.md
@.github/workflows/ci.yml
@docs/10-tests.md
@e2e/playwright.config.pg.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Job e2e-postgres en ci.yml</name>
  <read_first>
    .github/workflows/ci.yml (jobs test-postgres y e2e-smoke)
    e2e/playwright.config.pg.js
    .planning/phases/32-e2e-contra-api-postgres/32-RESEARCH.md
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
Añadir job `e2e-postgres` en paralelo con los existentes (sin `needs:`):

- `runs-on: ubuntu-latest`
- `services.postgres`: imagen `postgres:16`, env `POSTGRES_USER: edf_lab`, `POSTGRES_PASSWORD: edf_lab_dev`, `POSTGRES_DB: edf_lab_e2e`, puerto 5432
- `options` health: `pg_isready -U edf_lab -d edf_lab_e2e` con interval 5s (mismo estilo que `test-postgres`)

Steps (espejo de `e2e-smoke`):
- checkout, setup-node 22, cache lockfiles raíz + api + dashboard-react + dashboard-vue
- `npm ci` en raíz, api, dashboard-react, dashboard-vue
- `npx playwright install --with-deps chromium`
- `npm run test:e2e:pg` con env:
  - `CI: true`
  - `E2E_DATABASE_URL: postgresql://edf_lab:edf_lab_dev@localhost:5432/edf_lab_e2e`
  - `E2E_OPERATOR_EMAIL`, `E2E_OPERATOR_PASSWORD`, `LOGIN_RATE_LIMIT_MAX`, `E2E_JWT_SECRET` (mismos valores que `e2e-smoke`)

**No** modificar job `e2e-smoke` (sigue SQLite).

**No** ejecutar `test:db:prepare` en este job (POSTGRES_DB crea la base).
  </action>
  <acceptance_criteria>
    - `ci.yml` contiene job id `e2e-postgres`
    - `POSTGRES_DB: edf_lab_e2e` en el servicio del job
    - Step ejecuta `npm run test:e2e:pg`
    - `E2E_DATABASE_URL` en el step apunta a `/edf_lab_e2e`
    - Job `e2e-smoke` sin cambios en comando `test:e2e`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Documentar Postgres E2E en docs/10-tests.md</name>
  <read_first>
    docs/10-tests.md
    e2e/playwright.config.js
    e2e/playwright.config.pg.js
    .planning/REQUIREMENTS.md
  </read_first>
  <files>docs/10-tests.md</files>
  <action>
Añadir sección **E2E contra Postgres** (después de CRUD E2E multi-dashboard si existe) con:

1. Tabla de tres bases: `edf_lab` (dev Compose), `edf_lab_test` (API tests), `edf_lab_e2e` (E2E Postgres) — nunca mezclar
2. Comandos local: arrancar Postgres → `npm run test:db:prepare` → `npm run test:e2e:pg`
3. Variables: `E2E_DATABASE_URL` override opcional
4. CI: cuatro jobs (`test-sqlite`, `test-postgres`, `e2e-smoke`, `e2e-postgres`) — actualizar tabla de jobs si existe
5. Nota: `npm run test:e2e` sigue siendo SQLite; Postgres es opt-in vía `test:e2e:pg`

Si CRUD en PG no pasó en plan 01 localmente pero pasa en CI, documentar expectativa de 6 tests; si se limita a smoke, documentar explícitamente el alcance acordado en SUMMARY.
  </action>
  <acceptance_criteria>
    - `docs/10-tests.md` contiene `edf_lab_e2e`
    - `docs/10-tests.md` contiene `test:e2e:pg`
    - `docs/10-tests.md` advierte no usar `edf_lab` para E2E
    - Sección menciona job `e2e-postgres`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: README — mención breve</name>
  <read_first>
    README.md (párrafo CI / tests)
  </read_first>
  <files>README.md</files>
  <action>
Actualizar el párrafo que lista jobs CI de GitHub Actions: añadir `e2e-postgres` (Playwright contra API Postgres / `edf_lab_e2e`). Una línea o frase, coherente con el estilo actual del README.
  </action>
  <acceptance_criteria>
    - `README.md` menciona `e2e-postgres` o `test:e2e:pg`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Verificación final fase 32</name>
  <read_first>
    package.json
    e2e/playwright.config.pg.js
    .github/workflows/ci.yml
  </read_first>
  <files></files>
  <action>
Ejecutar:
1. `npm run test:e2e` — debe seguir pasando (SQLite, 6 tests) si entorno lo permite
2. Con Postgres: `npm run test:e2e:pg` — objetivo 6 passed; mínimo 3 auth smoke

Validar sintaxis: `node --check e2e/playwright.config.pg.js`

Registrar resultados en `32-02-SUMMARY.md`.
  </action>
  <acceptance_criteria>
    - `node --check e2e/playwright.config.pg.js` exit 0
    - `npm run test:e2e` exit 0 (regresión SQLite)
    - `npm run test:e2e:pg` exit 0 con ≥3 tests o SUMMARY documenta bloqueo de entorno
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
node --check e2e/playwright.config.pg.js
grep e2e-postgres .github/workflows/ci.yml
grep edf_lab_e2e .github/workflows/ci.yml docs/10-tests.md
npm run test:e2e
# Con Postgres:
npm run test:e2e:pg
```
</verification>

<success_criteria>
1. CI valida E2E Postgres en BD aislada `edf_lab_e2e`.
2. Documentación clara para alumno (local vs CI, tres bases).
3. Flujo SQLite E2E intacto.
</success_criteria>
