---
phase: 22-react-dashboard-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard-react/src/api.js
  - dashboard-react/src/components/LoginGate.jsx
autonomous: true
requirements:
  - FRWK-AUTH-01
  - FRWK-AUTH-02
must_haves:
  truths:
    - "D-03: login() and logout() exported from api.js using fetchJson with credentials"
    - "D-00e: fetchJson retains credentials include on all requests"
    - "D-01: LoginGate.jsx provides email/password form for unauthenticated learners"
    - "D-05 D-06 D-07: LoginGate uses Tailwind card layout with lab credentials hint"
    - "D-13: LoginGate shows inline error prop; not global loadError"
  artifacts:
    - path: dashboard-react/src/api.js
      provides: "login and logout helpers"
      contains: "export async function login"
    - path: dashboard-react/src/components/LoginGate.jsx
      provides: "login form UI"
      contains: "Iniciar sesión"
  key_links:
    - from: dashboard-react/src/api.js
      to: api POST /auth/login
      via: "login()"
      pattern: "/auth/login"
    - from: dashboard-react/src/components/LoginGate.jsx
      to: dashboard-react/src/App.jsx
      via: "onSubmit callback"
      pattern: "onSubmit"
---

<objective>
Add `login()`/`logout()` to React `api.js` and create `LoginGate.jsx` with Tailwind card form (FRWK-AUTH-01 partial, FRWK-AUTH-02).

Purpose: HTTP + UI building blocks for auth; App.jsx wiring in plan 22-02.
Output: `dashboard-react/src/api.js`, `dashboard-react/src/components/LoginGate.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-react-dashboard-auth/22-CONTEXT.md
@.planning/phases/22-react-dashboard-auth/22-RESEARCH.md
@.planning/phases/22-react-dashboard-auth/22-PATTERNS.md
@.planning/phases/19-vanilla-dashboard-login/19-CONTEXT.md
@dashboard-react/src/api.js
@dashboard/app.js
@api/auth.js
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| XSS steals session | High | No localStorage/JWT in JS — httpOnly cookie only (Phase 18) |
| Password in console | Medium | Do not console.log credentials or login body |
| Credentials omitted | High | login/logout must call fetchJson (inherits credentials include) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: api.js — login() and logout()</name>
  <read_first>22-CONTEXT.md (D-03, D-00e), dashboard-react/src/api.js, api/auth.js</read_first>
  <files>dashboard-react/src/api.js</files>
  <action>
    1. Confirm `fetchJson` defaults `credentials: 'include'` — keep as-is if present.
    2. Add and export:
       ```javascript
       export async function login(email, password) {
         return fetchJson('/auth/login', {
           method: 'POST',
           headers: { 'Content-Type': 'application/json' },
           body: JSON.stringify({ email, password }),
         });
       }

       export async function logout() {
         return fetchJson('/auth/logout', { method: 'POST' });
       }
       ```
    3. Do not add axios or new dependencies.
  </action>
  <verify>
    <automated>node --check dashboard-react/src/api.js 2>/dev/null || true; grep -q "export async function login" dashboard-react/src/api.js && grep -q "export async function logout" dashboard-react/src/api.js && grep -q "credentials: 'include'" dashboard-react/src/api.js</automated>
  </verify>
  <acceptance_criteria>
    - login and logout exported from api.js
    - Both use fetchJson (credentials inherited)
    - No new npm dependencies
  </acceptance_criteria>
  <done>API auth helpers ready for App.jsx</done>
</task>

<task type="auto">
  <name>Task 2: LoginGate.jsx — card form, hint, inline error</name>
  <read_first>22-CONTEXT.md (D-01, D-05–D-07, D-13), dashboard/index.html login-gate section, dashboard-react/src/App.jsx (Tailwind tokens)</read_first>
  <files>dashboard-react/src/components/LoginGate.jsx</files>
  <action>
    Create `LoginGate.jsx` as default export functional component.

    Props:
    - `onSubmit(event)` — parent handles preventDefault + async login (or pass email/password via controlled form in parent — planner choice: recommend controlled inputs in LoginGate with `onLogin(email, password)` callback)
    - `error` — string for inline alert (empty = hide)
    - `isSubmitting` — disable submit button

    UI (Spanish, Tailwind slate/indigo):
    - Outer: centered `max-w-md mx-auto` card `rounded-xl border border-slate-200 bg-white p-6 shadow-sm`
    - `h2` «Iniciar sesión»
    - Short intro: variant React en puerto 5174
    - `form` with `label` + `input type="email"` (required) and `input type="password"` (required)
    - Submit button «Entrar» (disabled when isSubmitting)
    - `p` with `role="alert"` `aria-live="polite"` for `error` when non-empty (rose text)
    - Muted hint: «Credenciales del lab: admin@lab.local / changeme» (D-06)

    Do not import App state — presentational + local form state OK.
  </action>
  <verify>
    <automated>test -f dashboard-react/src/components/LoginGate.jsx && grep -q 'Iniciar sesión' dashboard-react/src/components/LoginGate.jsx && grep -q 'admin@lab.local' dashboard-react/src/components/LoginGate.jsx</automated>
  </verify>
  <acceptance_criteria>
    - LoginGate.jsx exists with email/password form
    - Lab credentials hint visible
    - Inline error region for 403/network messages
    - Tailwind card styling consistent with App.jsx
  </acceptance_criteria>
  <done>LoginGate component ready</done>
</task>

</tasks>

<verification>
- `grep` checks on api.js and LoginGate.jsx
- App integration verified in plan 22-02
</verification>

<success_criteria>
- FRWK-AUTH-02 satisfied at HTTP layer
- LoginGate satisfies FRWK-AUTH-01 UI shell (gating wired in 22-02)
</success_criteria>
