---
phase: 22-react-dashboard-auth
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - dashboard-react/src/App.jsx
  - dashboard-react/README.md
autonomous: true
requirements:
  - FRWK-AUTH-01
  - FRWK-AUTH-03
  - FRWK-AUTH-04
must_haves:
  truths:
    - "D-09: Full dashboard shell hidden until isAuthenticated true"
    - "D-10: isBootstrapping shows Comprobando sesión during probe"
    - "D-04 D-11: bootstrapAuth health then GET /users probe then gate or loadDashboardData"
    - "D-08: Logout button in toolbar beside Recargar datos calls logout()"
    - "D-14 D-15: 401 on load or mutation returns to gate with Spanish API message"
    - "D-17: Successful logout clears loginError and form state"
    - "Network tab shows Cookie on :3100 after login on :5174"
  artifacts:
    - path: dashboard-react/src/App.jsx
      provides: "auth bootstrap and conditional shell"
      contains: "isAuthenticated"
    - path: dashboard-react/README.md
      provides: "auth instructions for React dashboard"
      contains: "Iniciar sesión"
  key_links:
    - from: dashboard-react/src/App.jsx
      to: dashboard-react/src/components/LoginGate.jsx
      via: "conditional render when !isAuthenticated"
      pattern: "LoginGate"
    - from: dashboard-react/src/App.jsx
      to: api logout
      via: "handleLogout"
      pattern: "logout()"
---

<objective>
Wire auth bootstrap, shell gating, logout, and 401 UX in App.jsx; update README (FRWK-AUTH-01, FRWK-AUTH-03, FRWK-AUTH-04).

Purpose: Complete React auth parity with vanilla Phase 19 on port 5174.
Output: `dashboard-react/src/App.jsx`, `dashboard-react/README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-react-dashboard-auth/22-CONTEXT.md
@.planning/phases/22-react-dashboard-auth/22-RESEARCH.md
@.planning/phases/22-react-dashboard-auth/22-PATTERNS.md
@dashboard-react/src/App.jsx
@dashboard-react/src/components/LoginGate.jsx
@dashboard-react/src/api.js
@dashboard/app.js
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Dashboard flash before auth | Medium | isBootstrapping + !isAuthenticated render only LoginGate |
| 401 shown as connection error | Medium | Branch error.status === 401 before setLoadError |
| Session left in UI after logout | Medium | clearDashboardData + isAuthenticated false on logout |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: App.jsx — state, bootstrapAuth, conditional render</name>
  <read_first>22-CONTEXT.md (D-02, D-04, D-09, D-10), dashboard/app.js bootstrapAuth, dashboard-react/src/App.jsx</read_first>
  <files>dashboard-react/src/App.jsx</files>
  <action>
    1. Add state: `isAuthenticated` (false), `isBootstrapping` (true), `loginError` ('').
    2. Import `LoginGate`, `login`, `logout` from api.js.
    3. Implement `bootstrapAuth`:
       - setIsBootstrapping(true); clear loginError
       - try fetchJson('/health') — on fail: clearDashboardData, setIsOnline false, setLoadError (rose panel), setIsAuthenticated false, return
       - on health ok: setIsOnline true, clear loadError
       - try fetchJson('/users') — 200: setIsAuthenticated(true), await loadDashboardData()
       - catch: if status 401 → setIsAuthenticated(false), setLoginError(message or fallback «Inicia sesión para ver y gestionar usuarios»)
       - other errors on probe: setIsAuthenticated(false), setLoginError as appropriate
       - finally setIsBootstrapping(false)
    4. Replace mount useEffect to call bootstrapAuth() instead of loadDashboardData().
    5. Render logic:
       - if isBootstrapping → minimal full-page «Comprobando sesión…» (Tailwind text center)
       - else if !isAuthenticated → LoginGate only (+ rose loadError panel if health failed during bootstrap)
       - else → existing dashboard JSX (hero, toolbar, CRUD, pedagogy)
    6. Implement handleLogin: call login(email, password); on success setIsAuthenticated(true), clear loginError, loadDashboardData(); on catch status 403 setLoginError(detail); other errors setLoginError
    7. Pass props to LoginGate: onLogin/handleLogin, error=loginError, isSubmitting
  </action>
  <verify>
    <automated>grep -q 'isAuthenticated' dashboard-react/src/App.jsx && grep -q 'bootstrapAuth' dashboard-react/src/App.jsx && grep -q 'LoginGate' dashboard-react/src/App.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Mount calls bootstrapAuth not bare loadDashboardData
    - Unauthenticated users see only LoginGate (no CRUD flash)
    - Bootstrap loading state visible during probe
  </acceptance_criteria>
  <done>Auth bootstrap and gating wired</done>
</task>

<task type="auto">
  <name>Task 2: App.jsx — 401 handling, logout, toolbar</name>
  <read_first>22-CONTEXT.md (D-08, D-12, D-14–D-17), dashboard/app.js 401 branches</read_first>
  <files>dashboard-react/src/App.jsx</files>
  <action>
    1. Update `loadDashboardData` catch: if error.status === 401 → setIsAuthenticated(false), clearDashboardData(), setLoginError(error.message or fallback), return (do not set loadError rose panel for 401).
    2. Update `handleMutationError`: if error.status === 401 → same gate flip as load; else existing 409/feedback logic.
    3. Add `handleLogout`: await logout(); clearDashboardData(); setIsAuthenticated(false); clear loginError; reset form-related state if needed.
    4. In toolbar section (beside Recargar datos), add button «Cerrar sesión» calling handleLogout (secondary/muted Tailwind style).
    5. Recargar datos still calls loadDashboardData only (post-auth path D-12).
  </action>
  <verify>
    <automated>grep -q 'Cerrar sesión' dashboard-react/src/App.jsx && grep -q 'handleLogout\|logout()' dashboard-react/src/App.jsx && grep -q '401' dashboard-react/src/App.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Logout button in toolbar
    - 401 paths set isAuthenticated false and show LoginGate
    - loadError rose panel reserved for connectivity not auth
  </acceptance_criteria>
  <done>401 and logout complete</done>
</task>

<task type="auto">
  <name>Task 3: README — auth on React dashboard</name>
  <read_first>dashboard-react/README.md, 22-CONTEXT.md, docs/17-autenticacion.md intro</read_first>
  <files>dashboard-react/README.md</files>
  <action>
    Update README:
    1. Replace «no incluye login» / AUTH_DISABLED-as-primary with: React dashboard **includes login** on :5174; uses httpOnly cookie via `credentials: 'include'`.
    2. Note lab credentials admin@lab.local / changeme (same as vanilla).
    3. Keep AUTH_DISABLED mention only as **test-only** API escape hatch, not learner path.
    4. Link `docs/17-autenticacion.md` for full auth narrative.
    5. Add manual verify pointer to phase 22 UAT (this plan Task 4).
  </action>
  <verify>
    <automated>grep -q 'Iniciar sesión\|login\|credenciales' dashboard-react/README.md && ! grep -q 'no incluye login' dashboard-react/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README documents React login flow
    - AUTH_DISABLED not presented as primary workaround
  </acceptance_criteria>
  <done>README reflects auth parity</done>
</task>

<task type="checkpoint" human="true">
  <name>Task 4: Manual UAT — React auth on :5174</name>
  <read_first>22-RESEARCH.md Manual UAT, api/.env.example credentials</read_first>
  <action>
    API on PORT=3100 **without** AUTH_DISABLED; React on 5174:
    1. First visit — login gate only, no users table
    2. Wrong password — inline error, no rose connection panel
    3. Login admin@lab.local / changeme — dashboard loads
    4. DevTools Network: request to localhost:3100/users includes Cookie header
    5. Cerrar sesión — gate returns
    6. Delete edf_session cookie, click Recargar — gate with Spanish 401 message
    7. Optional: create user, delete cookie, attempt POST — returns to gate
  </action>
  <acceptance_criteria>
    - All UAT steps pass manually
    - FRWK-AUTH-01 through FRWK-AUTH-04 observable in browser
  </acceptance_criteria>
  <done>Phase 22 auth verified manually</done>
</task>

</tasks>

<verification>
- Grep checks on App.jsx and README
- Manual UAT Task 4
</verification>

<success_criteria>
- React dashboard auth parity with vanilla Phase 19 on port 5174
- All FRWK-AUTH-01 … FRWK-AUTH-04 requirements satisfied
</success_criteria>
