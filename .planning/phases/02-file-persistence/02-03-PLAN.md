---
phase: 02-file-persistence
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - docs/08-memoria-vs-persistencia.md
  - missions/05-restart-y-persistencia.md
  - missions/06-corrupcion-y-restauracion.md
autonomous: true
requirements:
  - PERS-04
  - PERS-05

must_haves:
  truths:
    - "Existe un documento que explica la diferencia entre memoria y persistencia con ejemplos ejecutables"
    - "Existe una misión que el alumno puede seguir para observar que los usuarios sobreviven un restart"
    - "Existe una misión que el alumno puede seguir para observar la recuperación automática de un archivo corrupto"
    - "Las misiones siguen el formato exacto de missions/04-romper-y-arreglar-cors.md"
    - "Los comandos y rutas en los documentos son correctos y ejecutables"
  artifacts:
    - path: "docs/08-memoria-vs-persistencia.md"
      provides: "Explicación conceptual con diagrama antes/después y ejemplos curl"
      contains: "## Antes: estado en memoria"
    - path: "missions/05-restart-y-persistencia.md"
      provides: "Misión de restart con pasos ejecutables"
      contains: "## Objetivo"
    - path: "missions/06-corrupcion-y-restauracion.md"
      provides: "Misión de corrupción y recuperación con pasos ejecutables"
      contains: "## Objetivo"
  key_links:
    - from: "docs/08-memoria-vs-persistencia.md"
      to: "missions/05-restart-y-persistencia.md"
      via: "referencia directa al número de misión"
      pattern: "Misión 05"
    - from: "docs/08-memoria-vs-persistencia.md"
      to: "missions/06-corrupcion-y-restauracion.md"
      via: "referencia directa al número de misión"
      pattern: "Misión 06"
---

<objective>
Crear la documentación conceptual y las dos misiones ejecutables de la fase de persistencia.

Purpose: Los planes 01 y 02 implementan la funcionalidad; este plan hace el cambio visible y enseñable. Sin documentación, el alumno ve que los datos persisten pero no sabe por qué ni cómo experimentarlo intencionadamente.

Output:
- docs/08-memoria-vs-persistencia.md — explicación conceptual con diagrama antes/después
- missions/05-restart-y-persistencia.md — misión para observar persistencia tras restart
- missions/06-corrupcion-y-restauracion.md — misión para observar recuperación automática
</objective>

<execution_context>
@/Users/edefrutos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edefrutos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/edefrutos/Desktop/EDF-Lab-Educativo/.planning/PROJECT.md
@/Users/edefrutos/Desktop/EDF-Lab-Educativo/.planning/ROADMAP.md
@/Users/edefrutos/Desktop/EDF-Lab-Educativo/.planning/STATE.md
@/Users/edefrutos/Desktop/EDF-Lab-Educativo/.planning/phases/02-file-persistence/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Crear docs/08-memoria-vs-persistencia.md</name>
  <files>docs/08-memoria-vs-persistencia.md</files>

  <read_first>
    - docs/03-api-express.md — leer completo para replicar el formato de heading, estilo, longitud de párrafos y bloques de código
    - missions/04-romper-y-arreglar-cors.md — ver cómo se referencia desde docs
    - .planning/phases/02-file-persistence/02-PATTERNS.md — sección "docs/08-memoria-vs-persistencia.md — nuevo (doc)" con contenido mínimo requerido
    - api/index.js — verificar los mensajes de consola reales ([info], [warn]) para citarlos correctamente
  </read_first>

  <action>
Crear el archivo docs/08-memoria-vs-persistencia.md con la siguiente estructura y contenido. Seguir el formato de docs/03-api-express.md: H1 concepto, H2 secciones temáticas, H3 elementos individuales, bloques de código ejecutables. Sin frontmatter YAML.

```markdown
# Memoria vs Persistencia

Cuando la API arranca, carga los usuarios desde `api/data/users.json`. Cada vez que creas, editas o borras un usuario, el archivo se actualiza inmediatamente. Si la API se reinicia, los datos siguen ahí porque están en disco, no solo en memoria.

Este capítulo explica la diferencia entre los dos comportamientos.

---

## Antes: estado en memoria

En la versión original, los usuarios vivían únicamente dentro del proceso de Node.js:

```
Arranque de la API
  │
  ▼
let users = [
  { id: 1, name: 'John Doe', ... },
  { id: 2, name: 'Jane Smith', ... }
]
  │
  ▼
La API responde peticiones usando este array en RAM

Si la API se para (Ctrl+C o reinicio):
  └── El array desaparece. Los datos creados durante la sesión se pierden.
```

Esto es útil para aprender, pero no para guardar trabajo real.

---

## Después: estado en disco

Ahora la API lee y escribe `api/data/users.json`:

```
Arranque de la API
  │
  ▼
loadUsers() lee api/data/users.json
  ├── Archivo existe y es JSON válido
  │     └── users ← data.users  |  nextUserId ← data.nextId
  │
  ├── Archivo no existe (primera vez o borrado)
  │     └── Crea el archivo con los datos semilla
  │         [info] data/users.json no encontrado — creando con semilla
  │
  └── Archivo existe pero JSON inválido (corrupto)
        └── Sobrescribe con la semilla y continúa
            [warn] data/users.json corrupto — restaurando semilla
  │
  ▼
app.listen(3100)  ← El servidor solo acepta conexiones después de cargar

Mutación (POST / PUT / DELETE)
  │
  ▼
El handler actualiza el array en memoria
  │
  ▼
saveUsers() escribe api/data/users.json  ← inmediatamente, antes de responder
  ├── Éxito → responde 201/200 al cliente
  └── Error de escritura → revierte la mutación en memoria → responde 500
```

### `api/data/users.json`

El archivo es legible en cualquier momento. Mientras la API corre, puedes abrirlo con un editor o con `cat` y ver el estado actual:

```json
{
  "users": [
    { "id": 1, "name": "John Doe", "email": "john@example.com" },
    { "id": 2, "name": "Jane Smith", "email": "jane@example.com" }
  ],
  "nextId": 3
}
```

El campo `nextId` guarda el siguiente ID a usar. Sin él, los IDs se repetirían tras un restart.

---

## Comparación directa

| Situación | Sin persistencia | Con persistencia |
|-----------|-----------------|-----------------|
| Crear usuario, reiniciar API | El usuario desaparece | El usuario sigue ahí |
| Abrir `api/data/users.json` | No existe | Muestra el estado actual |
| Consola al arrancar | (silencio) | `[info]` o carga normal |
| Archivo corrupto al arrancar | Error fatal / datos vacíos | Restaura semilla + `[warn]` |

---

## Ejemplos ejecutables

### Ver el archivo mientras la API corre

```bash
# Terminal 1 — arrancar la API
cd api && PORT=3100 npm start

# Terminal 2 — crear un usuario
curl -s -X POST http://localhost:3100/users \
  -H 'Content-Type: application/json' \
  -d '{"name":"Alumno Test","email":"alumno@example.com"}'

# Ver el archivo actualizado
cat api/data/users.json
```

### Comprobar persistencia tras restart

```bash
# Crear un usuario (si no lo tienes ya del paso anterior)
curl -s -X POST http://localhost:3100/users \
  -H 'Content-Type: application/json' \
  -d '{"name":"Alumno Persistente","email":"p@example.com"}'

# Parar la API (Ctrl+C en Terminal 1) y arrancar de nuevo
PORT=3100 npm start

# El usuario sigue ahí
curl -s http://localhost:3100/users
```

Sigue los pasos detallados en la **Misión 05: Restart y Persistencia**.

### Observar la recuperación ante corrupción

```bash
# Parar la API, corromper el archivo y arrancar de nuevo
echo "esto no es json válido" > api/data/users.json
PORT=3100 npm start
# En consola: [warn] data/users.json corrupto — restaurando semilla
curl -s http://localhost:3100/users
# Resultado: los dos usuarios semilla
```

Sigue los pasos detallados en la **Misión 06: Corrupción y Restauración**.

---

## Resumen

La persistencia en este laboratorio usa solo módulos incorporados de Node.js (`fs/promises` y `path`). No hay base de datos ni dependencias adicionales. El objetivo es que veas exactamente dónde y cuándo el estado en memoria se serializa a disco.

Cuando estés listo para persistencia más robusta (datos relacionales, consultas, concurrencia), el siguiente paso natural es SQLite o PostgreSQL — pero eso es para después de que este flujo sea familiar.
```

Reemplazar los bloques de código dentro del markdown con el formateo correcto de triple backtick. El contenido de arriba usa backtick escapados para este plan; al crear el archivo real se usan backticks normales.

Verificar que el archivo resultante NO tiene frontmatter YAML, que los comandos usan el puerto 3100 y las rutas `api/data/users.json`, y que las referencias a Misión 05 y Misión 06 están presentes.
  </action>

  <verify>
    <automated>grep -c "## Antes: estado en memoria" /Users/edefrutos/Desktop/EDF-Lab-Educativo/docs/08-memoria-vs-persistencia.md && grep -c "Misión 05" /Users/edefrutos/Desktop/EDF-Lab-Educativo/docs/08-memoria-vs-persistencia.md && grep -c "Misión 06" /Users/edefrutos/Desktop/EDF-Lab-Educativo/docs/08-memoria-vs-persistencia.md && echo "doc OK"</automated>
  </verify>

  <acceptance_criteria>
    - docs/08-memoria-vs-persistencia.md existe
    - Contiene `## Antes: estado en memoria`
    - Contiene `## Después: estado en disco`
    - Contiene `## Comparación directa`
    - Contiene `## Ejemplos ejecutables`
    - Contiene la cadena `Misión 05`
    - Contiene la cadena `Misión 06`
    - Contiene `api/data/users.json` (ruta correcta, no `data/users.json` sin prefijo)
    - Contiene `PORT=3100` en los ejemplos de bash
    - Contiene `nextId` con explicación de su propósito
    - NO contiene frontmatter YAML (el archivo NO empieza con `---`)
  </acceptance_criteria>

  <done>docs/08-memoria-vs-persistencia.md existe con las secciones requeridas, diagrama antes/después, comparación directa, ejemplos ejecutables y referencias a las dos misiones.</done>
</task>

<task type="auto">
  <name>Tarea 2: Crear missions/05-restart-y-persistencia.md y missions/06-corrupcion-y-restauracion.md</name>
  <files>missions/05-restart-y-persistencia.md, missions/06-corrupcion-y-restauracion.md</files>

  <read_first>
    - missions/04-romper-y-arreglar-cors.md — leer completo para replicar el formato EXACTO (H1, H2 Objetivo, H2 Pasos, H2 Resultado esperado, H2 Reto extra)
    - .planning/phases/02-file-persistence/02-PATTERNS.md — secciones de misión 05 y misión 06 con contenido mínimo requerido
    - api/index.js — verificar los mensajes de consola reales para citarlos exactamente ([info] vs [warn])
  </read_first>

  <action>
Crear los dos archivos de misión siguiendo el formato EXACTO de missions/04-romper-y-arreglar-cors.md: H1 título → H2 Objetivo → H2 Pasos (lista numerada) → H2 Resultado esperado → H2 Reto extra. Sin frontmatter YAML, sin badges, sin tabla de contenidos. Tono imperativo en los pasos.

---

### missions/05-restart-y-persistencia.md

```markdown
# Misión 05: restart y persistencia

## Objetivo

Comprobar que los usuarios creados sobreviven un reinicio de la API, observando que el estado se lee desde `api/data/users.json` al arrancar.

## Pasos

1. Arranca la API si no está en marcha:
   ```bash
   cd api && PORT=3100 npm start
   ```

2. Crea un usuario nuevo:
   ```bash
   curl -s -X POST http://localhost:3100/users \
     -H 'Content-Type: application/json' \
     -d '{"name":"Alumno Persistente","email":"persistente@example.com"}'
   ```

3. Abre el archivo y comprueba que el usuario aparece:
   ```bash
   cat api/data/users.json
   ```

4. Para la API con `Ctrl+C`.

5. Vuelve a arrancar la API:
   ```bash
   PORT=3100 npm start
   ```

6. Pide la lista de usuarios:
   ```bash
   curl -s http://localhost:3100/users
   ```

## Resultado esperado

El usuario `Alumno Persistente` sigue presente en la respuesta aunque la API se ha reiniciado. El archivo `api/data/users.json` conserva el estado entre sesiones.

## Reto extra

Crea tres usuarios distintos, reinicia la API y comprueba que los tres aparecen en `GET /users`. Después borra uno con `DELETE /users/:id` y reinicia de nuevo — comprueba que solo quedan dos.
```

---

### missions/06-corrupcion-y-restauracion.md

```markdown
# Misión 06: corrupción y restauración

## Objetivo

Observar cómo la API detecta un archivo `api/data/users.json` corrupto al arrancar, lo restaura automáticamente con los datos semilla y avisa en consola.

## Pasos

1. Para la API si está en marcha (`Ctrl+C`).

2. Corrompe el archivo con texto inválido:
   ```bash
   echo "esto no es JSON válido" > api/data/users.json
   ```

3. Arranca la API y observa la consola:
   ```bash
   cd api && PORT=3100 npm start
   ```

4. Busca el aviso en la salida de consola:
   ```
   [warn] data/users.json corrupto — restaurando semilla
   ```

5. Pide la lista de usuarios:
   ```bash
   curl -s http://localhost:3100/users
   ```

## Resultado esperado

La API arranca sin errores fatales. La consola muestra `[warn] data/users.json corrupto — restaurando semilla`. `GET /users` devuelve los dos usuarios semilla (John Doe y Jane Smith). El archivo `api/data/users.json` ha sido sobreescrito con la semilla válida.

## Reto extra

Borra el archivo por completo en lugar de corromperlo:
```bash
rm api/data/users.json
```
Arranca la API y observa el mensaje diferente en consola:
```
[info] data/users.json no encontrado — creando con semilla
```
¿Qué diferencia ves entre el aviso `[warn]` y el aviso `[info]`? ¿Por qué son mensajes distintos si el resultado es el mismo?
```

---

Crear ambos archivos con exactamente ese contenido. Los bloques de código dentro del markdown usan backticks triples normales (no escapados).
  </action>

  <verify>
    <automated>grep -c "## Objetivo" /Users/edefrutos/Desktop/EDF-Lab-Educativo/missions/05-restart-y-persistencia.md && grep -c "## Objetivo" /Users/edefrutos/Desktop/EDF-Lab-Educativo/missions/06-corrupcion-y-restauracion.md && grep -c "Reto extra" /Users/edefrutos/Desktop/EDF-Lab-Educativo/missions/05-restart-y-persistencia.md && grep -c "Reto extra" /Users/edefrutos/Desktop/EDF-Lab-Educativo/missions/06-corrupcion-y-restauracion.md && echo "misiones OK"</automated>
  </verify>

  <acceptance_criteria>
    - missions/05-restart-y-persistencia.md existe
    - missions/05-restart-y-persistencia.md contiene `## Objetivo`
    - missions/05-restart-y-persistencia.md contiene `## Pasos`
    - missions/05-restart-y-persistencia.md contiene `## Resultado esperado`
    - missions/05-restart-y-persistencia.md contiene `## Reto extra`
    - missions/05-restart-y-persistencia.md contiene `PORT=3100 npm start`
    - missions/05-restart-y-persistencia.md contiene `api/data/users.json`
    - missions/06-corrupcion-y-restauracion.md existe
    - missions/06-corrupcion-y-restauracion.md contiene `## Objetivo`
    - missions/06-corrupcion-y-restauracion.md contiene `## Pasos`
    - missions/06-corrupcion-y-restauracion.md contiene `## Resultado esperado`
    - missions/06-corrupcion-y-restauracion.md contiene `## Reto extra`
    - missions/06-corrupcion-y-restauracion.md contiene `[warn] data/users.json corrupto — restaurando semilla` (texto exacto del mensaje de consola)
    - missions/06-corrupcion-y-restauracion.md contiene `[info] data/users.json no encontrado — creando con semilla` (texto exacto del mensaje de consola, en el reto extra)
    - Ninguno de los dos archivos empieza con `---` (sin frontmatter)
  </acceptance_criteria>

  <done>missions/05-restart-y-persistencia.md y missions/06-corrupcion-y-restauracion.md existen con formato correcto, pasos ejecutables con rutas y puertos reales, y mensajes de consola citados exactamente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archivos markdown → Lector humano | Documentación estática; no hay superficie de ataque de ejecución de código |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | Comandos en docs/missions — ruta o puerto incorrecto | mitigate | Acceptance criteria verifican que los comandos usan `api/data/users.json` y `PORT=3100` correctamente. Un puerto o ruta equivocado haría que el alumno no pudiera completar la misión. |
| T-02-03-02 | Information Disclosure | Comandos de misión 06 que borran o sobreescriben archivos | accept | Los comandos operan sobre `api/data/users.json` — archivo de datos de demo. El alumno ejecuta los comandos conscientemente en su entorno local. La misión es intencionalmente destructiva como ejercicio de aprendizaje (análogo a la misión de CORS). |
| T-02-03-03 | Denial of Service | Misión 06 paso 2 — `echo > api/data/users.json` ejecutado sin parar la API | accept | El paso 1 instruye explícitamente parar la API antes. Si el alumno lo hace con la API en marcha, la próxima escritura de saveUsers() sobrescribirá el archivo corrupto con estado válido — comportamiento seguro. |
</threat_model>

<verification>
Verificación manual completa tras ejecutar las tareas:

```bash
# 1. Verificar existencia y estructura de los tres archivos
ls -la docs/08-memoria-vs-persistencia.md missions/05-restart-y-persistencia.md missions/06-corrupcion-y-restauracion.md

# 2. Comprobar secciones obligatorias en la doc
grep "## Antes\|## Después\|## Comparación\|## Ejemplos\|Misión 05\|Misión 06" docs/08-memoria-vs-persistencia.md

# 3. Comprobar formato de misiones
grep "## Objetivo\|## Pasos\|## Resultado esperado\|## Reto extra" missions/05-restart-y-persistencia.md
grep "## Objetivo\|## Pasos\|## Resultado esperado\|## Reto extra" missions/06-corrupcion-y-restauracion.md

# 4. Comprobar mensajes de consola exactos en misión 06
grep "\[warn\]\|\[info\]" missions/06-corrupcion-y-restauracion.md

# 5. Comprobar que los mensajes en las misiones coinciden con los del código
grep "\[warn\]\|\[info\]\|\[error\]" api/index.js
# Los mensajes [warn] y [info] deben ser idénticos entre index.js y la misión 06
```
</verification>

<success_criteria>
- docs/08-memoria-vs-persistencia.md existe con diagrama antes/después, comparación en tabla y ejemplos curl ejecutables
- missions/05-restart-y-persistencia.md tiene los 4 H2 requeridos y comandos con puerto 3100
- missions/06-corrupcion-y-restauracion.md tiene los 4 H2 requeridos y cita los mensajes exactos de consola
- Los mensajes [warn]/[info] en la misión 06 son idénticos a los del código en api/index.js
- Ningún archivo tiene frontmatter YAML
</success_criteria>

<output>
Tras completar este plan, crear el archivo:
.planning/phases/02-file-persistence/02-03-SUMMARY.md

Incluir:
- Los tres archivos creados con sus rutas absolutas
- Resultado de grep de secciones obligatorias
- Confirmación de que los mensajes de consola en misión 06 coinciden con index.js
- Cualquier desviación del plan o ajuste de contenido realizado
</output>
