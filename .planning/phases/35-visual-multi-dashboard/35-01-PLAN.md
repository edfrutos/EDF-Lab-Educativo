---
phase: 35-visual-multi-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard-react/src/App.jsx
  - dashboard-react/src/components/LoginGate.jsx
  - dashboard-react/src/components/HealthCard.jsx
  - dashboard-vue/src/App.vue
  - dashboard-vue/src/components/LoginGate.vue
  - dashboard-vue/src/components/HealthCard.vue
  - e2e/tests/visual.react.spec.js
  - e2e/tests/visual.vue.spec.js
autonomous: true
requirements:
  - QA-VIS-02
user_setup: []
must_haves:
  truths:
    - "React y Vue exponen #dashboard-panel, #login-gate y #health-timestamp para visual-flow"
    - "visual.react.spec.js y visual.vue.spec.js reutilizan prepareVisualState sin duplicar lógica"
    - "Cada spec captura dashboard-post-login.png sobre #dashboard-panel"
  artifacts:
    - path: e2e/tests/visual.react.spec.js
      provides: "Spec visual React"
      contains: "dashboard-post-login"
    - path: e2e/tests/visual.vue.spec.js
      provides: "Spec visual Vue"
      contains: "dashboard-post-login"
    - path: dashboard-react/src/App.jsx
      provides: "id dashboard-panel en rama autenticada"
      contains: "dashboard-panel"
    - path: dashboard-vue/src/App.vue
      provides: "id dashboard-panel en rama autenticada"
      contains: "dashboard-panel"
  key_links:
    - from: e2e/tests/visual.react.spec.js
      to: e2e/helpers/visual-flow.js
      via: "require('../helpers/visual-flow.js')"
      pattern: "prepareVisualState"
    - from: e2e/helpers/visual-flow.js
      to: dashboard-react/src/App.jsx
      via: "#dashboard-panel, #login-gate, #health-timestamp"
      pattern: "#dashboard-panel"
---

<objective>
Añadir IDs visuales mínimos en React/Vue y crear specs `visual.react` / `visual.vue` que reutilizan `visual-flow.js` sin modificarlo.

Purpose: Cumplir el núcleo de QA-VIS-02 — mismo contrato de snapshot que vanilla en :5174 y :5175.
Output: 6 archivos UI + 2 specs listos para cablear en Playwright (plan 35-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-visual-multi-dashboard/35-CONTEXT.md
@.planning/phases/35-visual-multi-dashboard/35-RESEARCH.md
@e2e/tests/visual.vanilla.spec.js
@e2e/helpers/visual-flow.js
@dashboard/index.html

<interfaces>
IDs a añadir (D-01, D-03):

```jsx
// LoginGate root
<section id="login-gate" ...>

// App authenticated branch
<main id="dashboard-panel" className="...">

// HealthCard timestamp
<strong id="health-timestamp" ...>{healthTimestamp}</strong>
```

Specs (espejar vanilla):

```javascript
test.describe('Visual — dashboard React', () => {
  test('dashboard post-login snapshot estable', async ({ page }) => {
    await prepareVisualState(page, { email, password });
    await expect(page.locator('#dashboard-panel')).toHaveScreenshot(
      'dashboard-post-login.png',
      getVisualScreenshotOptions(page)
    );
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IDs visuales en dashboard React</name>
  <read_first>
    dashboard-react/src/components/LoginGate.jsx
    dashboard-react/src/components/HealthCard.jsx
    dashboard-react/src/App.jsx
    dashboard/index.html
    .planning/phases/35-visual-multi-dashboard/35-CONTEXT.md
  </read_first>
  <files>dashboard-react/src/components/LoginGate.jsx, dashboard-react/src/components/HealthCard.jsx, dashboard-react/src/App.jsx</files>
  <action>
**LoginGate.jsx:** Añadir `id="login-gate"` al `<section>` raíz (mantener `aria-labelledby`).

**HealthCard.jsx:** Añadir `id="health-timestamp"` al `<strong>` que muestra `{healthTimestamp}`.

**App.jsx:** En la rama autenticada (`return` con toolbar + grid), añadir `id="dashboard-panel"` al `<main>` que envuelve el contenido post-login (el que empieza tras `if (!isAuthenticated)`). No añadir el id al `<main>` de bootstrapping ni al de login.

Solo atributos `id` — sin cambiar layout, clases ni copy. No modificar `visual-flow.js`.
  </action>
  <verify>
    <automated>node --check dashboard-react/src/App.jsx 2>/dev/null || npx --prefix dashboard-react vite build --emptyOutDir false 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - LoginGate tiene id login-gate
    - HealthCard tiene id health-timestamp en el strong del timestamp
    - App rama autenticada tiene id dashboard-panel en main
  </acceptance_criteria>
  <done>React expone los tres selectores que visual-flow espera</done>
</task>

<task type="auto">
  <name>Task 2: IDs visuales en dashboard Vue</name>
  <read_first>
    dashboard-vue/src/components/LoginGate.vue
    dashboard-vue/src/components/HealthCard.vue
    dashboard-vue/src/App.vue
  </read_first>
  <files>dashboard-vue/src/components/LoginGate.vue, dashboard-vue/src/components/HealthCard.vue, dashboard-vue/src/App.vue</files>
  <action>
Mismos tres IDs que React (paridad D-01):

- `LoginGate.vue` → `id="login-gate"` en `<section>` raíz
- `HealthCard.vue` → `id="health-timestamp"` en `<strong>` del timestamp
- `App.vue` → `id="dashboard-panel"` en `<main v-else>` (rama autenticada)

Sin cambios de estilo ni texto. No tocar vanilla ni API.
  </action>
  <verify>
    <automated>node --check dashboard-vue/src/App.vue 2>/dev/null || npx --prefix dashboard-vue vite build --emptyOutDir false 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Misma tríada de IDs que React y vanilla
  </acceptance_criteria>
  <done>Vue expone los tres selectores que visual-flow espera</done>
</task>

<task type="auto">
  <name>Task 3: Specs visual.react y visual.vue</name>
  <read_first>
    e2e/tests/visual.vanilla.spec.js
    e2e/helpers/visual-flow.js
  </read_first>
  <files>e2e/tests/visual.react.spec.js, e2e/tests/visual.vue.spec.js</files>
  <action>
Crear dos specs paralelos a `visual.vanilla.spec.js`:

**`visual.react.spec.js`**
- `describe`: `Visual — dashboard React`
- Mismo test title en español
- Importa `prepareVisualState`, `getVisualScreenshotOptions`
- Credenciales `E2E_OPERATOR_*` con defaults admin@lab.local / changeme
- `toHaveScreenshot('dashboard-post-login.png')` sobre `#dashboard-panel`

**`visual.vue.spec.js`**
- `describe`: `Visual — dashboard Vue`
- Idéntica estructura

No modificar `visual-flow.js` ni `visual.vanilla.spec.js`. Verificación local en este plan: `node --check` en ambos specs; ejecución Playwright completa en plan 35-02 tras añadir proyectos.
  </action>
  <verify>
    <automated>node --check e2e/tests/visual.react.spec.js && node --check e2e/tests/visual.vue.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - Dos specs creados importando visual-flow
    - Nombre snapshot dashboard-post-login.png en ambos
    - describe/test en español
  </acceptance_criteria>
  <done>Specs React/Vue listos para proyectos Playwright visual</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI ID changes | Solo atributos id; no alteran auth ni datos |
| visual-flow → React/Vue | Mismos selectores que vanilla; masks en zonas dinámicas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | I | ID collision | accept | IDs únicos por página; sin shadow DOM |
| T-35-02 | D | visual-flow en React | mitigate | IDs alineados antes de ejecutar specs |
</threat_model>

<verification>
- `node --check` en specs nuevos
- Tras plan 35-02: specs ejecutables con proyectos react/vue-chromium-visual
</verification>

<success_criteria>
- QA-VIS-02: specs equivalentes en React y Vue con helper compartido
- IDs mínimos en ambos frameworks
</success_criteria>

<output>
After completion, create `.planning/phases/35-visual-multi-dashboard/35-01-SUMMARY.md`
</output>
