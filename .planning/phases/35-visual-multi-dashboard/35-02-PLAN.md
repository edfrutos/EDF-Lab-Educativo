---
phase: 35-visual-multi-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - "35-01"
files_modified:
  - e2e/playwright.config.js
  - package.json
  - docs/10-tests.md
  - e2e/__snapshots__/visual.react.spec.js/dashboard-post-login-react-chromium-visual-darwin.png
  - e2e/__snapshots__/visual.vue.spec.js/dashboard-post-login-vue-chromium-visual-darwin.png
autonomous: true
requirements:
  - QA-VIS-02
user_setup: []
must_haves:
  truths:
    - "npm run test:visual ejecuta vanilla + React + Vue (3 tests) y pasa"
    - "Proyectos react-chromium-visual y vue-chromium-visual aislados de test:e2e"
    - "Baselines React y Vue commiteadas bajo e2e/__snapshots__/"
    - "docs/10-tests.md documenta visual en los tres dashboards"
  artifacts:
    - path: e2e/playwright.config.js
      provides: "Proyectos react/vue-chromium-visual"
      contains: "react-chromium-visual"
    - path: package.json
      provides: "test:visual con tres proyectos"
      contains: "react-chromium-visual"
    - path: docs/10-tests.md
      provides: "Sección visual multi-dashboard"
      contains: "5174"
  key_links:
    - from: package.json
      to: e2e/playwright.config.js
      via: "--project=vanilla-chromium-visual --project=react-chromium-visual --project=vue-chromium-visual"
      pattern: "vue-chromium-visual"
    - from: e2e/playwright.config.js
      to: e2e/tests/visual.react.spec.js
      via: "testMatch visual.react"
      pattern: "visual\\.react"
---

<objective>
Cablear proyectos Playwright visual para React/Vue, ampliar `test:visual` a tres dashboards, generar baselines y verificar la suite visual completa.

Purpose: Cerrar fase 35 con comando local único y traza didáctica multi-framework.
Output: Config + script + docs + 2 baselines nuevas + 3 tests verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-visual-multi-dashboard/35-01-SUMMARY.md
@.planning/phases/35-visual-multi-dashboard/35-CONTEXT.md
@e2e/playwright.config.js
@package.json
@docs/10-tests.md

<interfaces>
Añadir proyectos (tras vanilla-chromium-visual):

```javascript
{
  name: 'react-chromium-visual',
  testMatch: /visual\.react\.spec\.js/,
  use: { ...devices['Desktop Chrome'], baseURL: 'http://localhost:5174', viewport: { width: 1280, height: 720 } }
},
{
  name: 'vue-chromium-visual',
  testMatch: /visual\.vue\.spec\.js/,
  use: { ...devices['Desktop Chrome'], baseURL: 'http://localhost:5175', viewport: { width: 1280, height: 720 } }
}
```

Script:

```json
"test:visual": "playwright test --config=e2e/playwright.config.js --project=vanilla-chromium-visual --project=react-chromium-visual --project=vue-chromium-visual"
```

Mantener `test:e2e` sin proyectos visual (D-10).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proyectos react-chromium-visual y vue-chromium-visual</name>
  <read_first>
    e2e/playwright.config.js
    e2e/tests/visual.react.spec.js
    e2e/tests/visual.vue.spec.js
  </read_first>
  <files>e2e/playwright.config.js</files>
  <action>
En `e2e/playwright.config.js`, añadir dos proyectos después de `vanilla-chromium-visual`:

- `react-chromium-visual` — `testMatch: /visual\.react\.spec\.js/`, `baseURL: http://localhost:5174`, viewport 1280×720
- `vue-chromium-visual` — `testMatch: /visual\.vue\.spec\.js/`, `baseURL: http://localhost:5175`, viewport 1280×720

Comentario breve en español: suite visual multi-dashboard (fase 35).

**No modificar** proyectos E2E funcionales ni `vanilla-chromium-visual`.
  </action>
  <verify>
    <automated>node --check e2e/playwright.config.js</automated>
  </verify>
  <acceptance_criteria>
    - Ambos proyectos visual existen con testMatch correcto
    - Viewport 1280×720 en cada uno
  </acceptance_criteria>
  <done>Playwright puede ejecutar specs visual de los tres dashboards</done>
</task>

<task type="auto">
  <name>Task 2: Ampliar npm run test:visual</name>
  <read_first>
    package.json
  </read_first>
  <files>package.json</files>
  <action>
Actualizar `scripts.test:visual` para incluir los tres proyectos:

```json
"test:visual": "playwright test --config=e2e/playwright.config.js --project=vanilla-chromium-visual --project=react-chromium-visual --project=vue-chromium-visual"
```

No cambiar `test:e2e`, `test:e2e:ci`, ni otros scripts E2E.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const s=p.scripts['test:visual']||''; if(!s.includes('react-chromium-visual')||!s.includes('vue-chromium-visual')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - test:visual referencia los tres proyectos visual
  </acceptance_criteria>
  <done>Comando único ejecuta regresión visual en 3 dashboards</done>
</task>

<task type="auto">
  <name>Task 3: Docs, baselines y verificación</name>
  <read_first>
    docs/10-tests.md
    .planning/phases/35-visual-multi-dashboard/35-CONTEXT.md
  </read_first>
  <files>docs/10-tests.md</files>
  <action>
**docs/10-tests.md:**

- Renombrar/ampliar sección a `### Regresión visual (tres dashboards, fase 35)` o equivalente
- Documentar que `test:visual` corre vanilla (:5173), React (:5174), Vue (:5175) — **3 tests**
- Mencionar baselines separadas por spec (no comparar pixel-a-pixel entre frameworks)
- Actualizar fila tabla scripts: `test:visual` → tres dashboards Chromium
- Mantener masks, threshold, `--update-snapshots`

**Baselines:** Ejecutar `npm run test:visual -- --update-snapshots` si faltan PNG de react/vue. Commitear baselines generadas.

**Verificación:**
- `npm run test:visual` → **3 passed**
- `npm run test:e2e` → **6 passed** (regresión)
  </action>
  <verify>
    <automated>npm run test:visual</automated>
  </verify>
  <acceptance_criteria>
    - docs menciona 5174 y 5175
    - Baselines react y vue existen en e2e/__snapshots__/
    - npm run test:visual exit 0 con 3 tests
  </acceptance_criteria>
  <done>Fase 35 verificada con suite visual triple verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test:visual vs test:e2e | Visual projects excluded from functional E2E CI |
| Separate baselines | No cross-framework pixel diff in phase 35 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-03 | D | test:e2e regression | mitigate | Verificar 6 passed tras ampliar test:visual |
| T-35-04 | I | Baseline drift | accept | Documentar --update-snapshots por dashboard |
</threat_model>

<verification>
- `npm run test:visual` → 3 passed
- `npm run test:e2e` → 6 passed
</verification>

<success_criteria>
- QA-VIS-02 completo: React y Vue con snapshots equivalentes en estado
- Comando test:visual unificado para tres dashboards
</success_criteria>

<output>
After completion, create `.planning/phases/35-visual-multi-dashboard/35-02-SUMMARY.md`
</output>
