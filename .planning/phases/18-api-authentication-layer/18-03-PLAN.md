---
phase: 18-api-authentication-layer
plan: 03
type: execute
wave: 3
depends_on:
  - 18-02
files_modified:
  - api/index.auth.test.js
  - api/index.test.js
  - api/package.json
  - api/openapi.yaml
  - api/README.md
  - .planning/phases/18-api-authentication-layer/18-UAT.md
autonomous: true
requirements:
  - AUTH-07
must_haves:
  truths:
    - "D-21: index.auth.test.js exists and runs in test:sqlite"
    - "D-23: Login ok/fail, 401 without token, 200 with Bearer covered"
    - "D-26: openapi.yaml has /auth/login path (draft)"
    - "18-UAT.md manual checklist exists"
    - "index.test.js deletes AUTH_ENABLED for regression"
  artifacts:
    - path: api/index.auth.test.js
      provides: "Auth-enabled API tests"
      contains: "POST /auth/login"
    - path: .planning/phases/18-api-authentication-layer/18-UAT.md
      provides: "Phase 18 manual UAT"
      contains: "Authorization"
  key_links:
    - from: api/package.json
      to: api/index.auth.test.js
      via: "test:sqlite script"
      pattern: "index.auth.test.js"
---

<objective>
Add automated auth tests, harden regression suite, draft OpenAPI for login, document manual UAT, and brief api/README auth section.

Purpose: AUTH-07, D-26, D-19.
Output: `index.auth.test.js`, updated scripts, OpenAPI draft, `18-UAT.md`, README note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-api-authentication-layer/18-CONTEXT.md
@.planning/phases/18-api-authentication-layer/18-PATTERNS.md
@api/index.test.js
@api/openapi.yaml
@api/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard index.test.js against stray AUTH_ENABLED</name>
  <read_first>api/index.test.js setup block, 18-PATTERNS.md</read_first>
  <files>api/index.test.js</files>
  <action>
    In api/index.test.js setup (before require('./index.js')), add:

    ```javascript
    delete process.env.AUTH_ENABLED;
    ```

    Ensures 16/16 tests pass even if developer shell has auth enabled.
    Run `npm run test:sqlite` — must still pass (auth file may not exist yet; if only index.test.js, pass).
  </action>
  <verify>
    <automated>cd api && node --test index.test.js --test-force-exit 2>&1 | grep -E '# pass|# fail'</automated>
  </verify>
  <acceptance_criteria>
    - delete AUTH_ENABLED present in index.test.js
    - 16 tests pass
  </acceptance_criteria>
  <done>Regression guard in place</done>
</task>

<task type="auto">
  <name>Task 2: Create index.auth.test.js and update test:sqlite</name>
  <read_first>18-CONTEXT.md D-21–D-23, api/index.test.js patterns</read_first>
  <files>api/index.auth.test.js, api/package.json</files>
  <action>
    Create `api/index.auth.test.js` as a **standalone** test file (separate node process):

    Setup BEFORE require:
    ```javascript
    process.env.DB_FILE = TEST_DB;
    delete process.env.DATABASE_URL;
    process.env.AUTH_ENABLED = 'true';
    process.env.AUTH_USER = 'testadmin';
    process.env.AUTH_PASSWORD = 'testpass';
    process.env.JWT_SECRET = 'test-jwt-secret-for-auth-suite-32ch';
    ```

    beforeEach: unlink TEST_DB, app.initDb()
    afterEach: cleanup

    Test cases (minimum):
    1. `POST /auth/login` — 200 + token with valid credentials
    2. `POST /auth/login` — 401 wrong password
    3. `POST /auth/login` — 400 missing username
    4. `GET /users` without Authorization — 401
    5. `GET /users` with valid Bearer from login — 200 + array
    6. `GET /health` without token — 200 (public route)

    Helper: function loginAgent() returns token via supertest POST.

    Update api/package.json:
    ```json
    "test:sqlite": "node --test index.test.js --test-force-exit && node --test index.auth.test.js --test-force-exit"
    ```

    Optional: add `test:auth` script for auth file only.
  </action>
  <verify>
    <automated>cd api && npm run test:sqlite 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - All auth tests pass
    - Full test:sqlite passes (16 + auth tests)
  </acceptance_criteria>
  <done>AUTH-07 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: OpenAPI draft, 18-UAT.md, api/README auth note</name>
  <read_first>api/openapi.yaml structure, docs/10-tests.md tone, 18-CONTEXT.md D-19, D-26</read_first>
  <files>api/openapi.yaml, api/README.md, .planning/phases/18-api-authentication-layer/18-UAT.md</files>
  <action>
    1. Add to `api/openapi.yaml`:
       - Path `/auth/login` with POST, request body username/password, responses 200/400/401/404
       - Do NOT add full securitySchemes on /users yet (Phase 20 DEPLOY-06) — comment in yaml: `# securitySchemes: Phase 20`

    2. Create `.planning/phases/18-api-authentication-layer/18-UAT.md` checklist:
       - Auth off: npm run test:sqlite 16+ pass; curl /users 200
       - Auth on: fail-fast without JWT_SECRET; login curl; /users 401; /users with Bearer 200; /health 200 without token
       - CORS smoke: OPTIONS or browser note for Authorization header (prep Phase 19)
       - Sign-off block

    3. Add short **Autenticación (opcional)** section to api/README.md:
       - AUTH_ENABLED, export vars, link .env.example
       - curl login + Bearer example
       - Note: dashboard login in Phase 19
  </action>
  <verify>
    <automated>grep -q '/auth/login' api/openapi.yaml && test -f .planning/phases/18-api-authentication-layer/18-UAT.md && grep -qi AUTH_ENABLED api/README.md</automated>
  </verify>
  <acceptance_criteria>
    - OpenAPI documents login endpoint
    - UAT checklist complete
    - README mentions auth env vars
  </acceptance_criteria>
  <done>Phase 18 documentation artifacts ready</done>
</task>

</tasks>

<verification>
```bash
cd api && npm run test:sqlite
node --check index.auth.test.js
```
</verification>

<success_criteria>
- Combined test suite green
- Manual UAT document ready for human sign-off
</success_criteria>

<output>
After completion, create `.planning/phases/18-api-authentication-layer/18-03-SUMMARY.md` and `18-VERIFICATION.md` mapping AUTH-01–07 to evidence.
</output>
