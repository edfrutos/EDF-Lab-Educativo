---
phase: 19-vanilla-dashboard-login
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/index.html
  - dashboard/styles.css
  - dashboard/app.js
autonomous: true
requirements:
  - AUTH-10
  - AUTH-11
must_haves:
  truths:
    - "D-01: Learner sees login gate until valid session; dashboard panel hidden until then"
    - "D-02: Login form email+password POST /auth/login with JSON and credentials include"
    - "D-04: Logout POST /auth/logout with credentials then gate and cleared user data"
    - "D-00a D-13: All API fetch calls use credentials include so edf_session cookie is sent"
    - "D-05 D-06 D-07: Bootstrap health first then session probe GET /users then login or full load"
    - "D-09 D-00b: 403 Credenciales inválidas inline under login form not error-box"
    - "D-10 D-11 D-00b D-15: 401 shows gate with API error or fallback; callers branch error.status 401"
    - "D-12: Network failures on login use login-error; connection card reflects health not auth"
    - "D-00c: Optional hint references admin@lab.local changeme from lab defaults"
    - "Network tab shows Cookie header on :3100 requests after successful login"
  artifacts:
    - path: dashboard/app.js
      provides: "fetchJson with credentials and error.status"
      contains: "credentials: 'include'"
    - path: dashboard/index.html
      provides: "login gate and logout control"
      contains: "login-gate"
  key_links:
    - from: dashboard/app.js
      to: api POST /auth/login
      via: "fetchJson with credentials"
      pattern: "/auth/login"
  notes:
    - "D-00d: CORS credentials true on API — no dashboard change; verify ports 5173 in CORS_ORIGINS"
---

<objective>
Implement vanilla dashboard login gate, session bootstrap, credentialed fetchJson, and Spanish 401/403 UX (AUTH-10, AUTH-11).

Purpose: Connect `dashboard/` to Phase 18 httpOnly cookie auth without changing API auth logic.
Output: Updated `dashboard/index.html`, `dashboard/styles.css`, `dashboard/app.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-vanilla-dashboard-login/19-CONTEXT.md
@.planning/phases/19-vanilla-dashboard-login/19-RESEARCH.md
@.planning/phases/19-vanilla-dashboard-login/19-UI-SPEC.md
@.planning/phases/19-vanilla-dashboard-login/19-PATTERNS.md
@dashboard/app.js
@dashboard/index.html
@dashboard/styles.css
@dashboard-react/src/api.js
@api/auth.js
</context>

<threat_model>
| Threat | Severity | Mitigation in this plan |
|--------|----------|-------------------------|
| XSS steals session token | High | No localStorage/JWT in JS — cookie is httpOnly (Phase 18); do not add token to JS |
| CSRF on state-changing API | Medium | API uses SameSite=lax cookie; dashboard only calls same lab origins |
| Credentials sent to wrong origin | Medium | Keep `API_BASE_URL` hardcoded to localhost:3100; do not derive from user input |
| Password logged to console | Medium | Do not `console.log` password or login body in handlers |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: HTML — login gate, dashboard panel wrapper, logout button</name>
  <read_first>19-CONTEXT.md (D-01, D-03), 19-UI-SPEC.md, dashboard/index.html</read_first>
  <files>dashboard/index.html</files>
  <action>
    1. After the `hero` section, add `#login-gate` section (class `login-gate card` or similar) containing:
       - `h2` «Iniciar sesión»
       - `form#login-form` with labeled `input#login-email` (type email, required) and `input#login-password` (type password, required), submit button «Entrar»
       - `p#login-error` with `role="alert"`, `aria-live="polite"`, initially empty/hidden
       - Optional muted hint: lab default credentials reference (admin@lab.local / changeme)
    2. Wrap everything from `.toolbar` through the users CRUD section (inclusive) in `div.dashboard-panel` with `hidden` attribute by default.
    3. In `.toolbar`, add `button#logout-button` type="button" with text «Cerrar sesión» placed before «Recargar datos».
    4. Do not remove existing `error-box` — still for connectivity/CORS only.
  </action>
  <verify>
    <automated>grep -q 'login-gate' dashboard/index.html && grep -q 'logout-button' dashboard/index.html && grep -q 'dashboard-panel' dashboard/index.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains id login-gate, login-form, login-error, logout-button
    - dashboard-panel wraps toolbar and data sections and is hidden by default
  </acceptance_criteria>
  <done>Login gate markup and panel wrapper in place</done>
</task>

<task type="auto">
  <name>Task 2: CSS — login gate layout</name>
  <read_first>dashboard/styles.css, 19-UI-SPEC.md</read_first>
  <files>dashboard/styles.css</files>
  <action>
    Add minimal rules:
    - `.login-gate` — centered, max-width 420px, margin below hero, uses existing card/surface tokens
    - `#login-error` — visible when non-empty; color `var(--danger)`; margin-top spacing
    - `.dashboard-panel[hidden]` — ensure hidden panels do not receive focus (browser default OK)
    Reuse existing `button` primary styles for submit; style logout as secondary (outline or muted) consistent with toolbar.
  </action>
  <verify>
    <automated>grep -q '.login-gate' dashboard/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - styles.css defines .login-gate
    - No regression to existing .app-shell layout
  </acceptance_criteria>
  <done>Login gate styled</done>
</task>

<task type="auto">
  <name>Task 3: fetchJson — credentials and error.status</name>
  <read_first>dashboard/app.js (fetchJson), dashboard-react/src/api.js, 19-RESEARCH.md, 19-CONTEXT.md (D-13, D-14)</read_first>
  <files>dashboard/app.js</files>
  <action>
    Replace `fetchJson` implementation:
    - Merge options: default `credentials: 'include'` (caller override still possible via spread order: `{ credentials: 'include', ...options }` then allow options.credentials to win if explicitly set)
    - On `!response.ok`: try `response.json()` for `body.error`; build `Error` with Spanish prefix `La petición a ${url} ha fallado: ${detail}`; set `error.status = response.status`
    - Return `response.json()` on success unchanged
    Do not change render helpers yet.
  </action>
  <verify>
    <automated>node --check dashboard/app.js && grep -q "credentials: 'include'" dashboard/app.js && grep -q 'error.status' dashboard/app.js</automated>
  </verify>
  <acceptance_criteria>
    - node --check dashboard/app.js exits 0
    - fetchJson includes credentials include
    - Thrown errors have status property when response not ok
  </acceptance_criteria>
  <done>fetchJson aligned with React reference</done>
</task>

<task type="auto">
  <name>Task 4: Auth bootstrap, gate toggles, login/logout handlers</name>
  <read_first>dashboard/app.js (full), 19-CONTEXT.md (D-05..D-11), 19-RESEARCH.md bootstrap diagram, api/auth.js loginHandler</read_first>
  <files>dashboard/app.js</files>
  <action>
    1. Extend `elements` with: loginGate, loginForm, loginEmailInput, loginPasswordInput, loginError, dashboardPanel, logoutButton.
    2. Implement `showLoginGate()` — show #login-gate, hide .dashboard-panel, clearLoginError optional on logout success.
    3. Implement `showDashboardPanel()` — hide gate, show panel.
    4. Implement `showLoginError(msg)` / `clearLoginError()` — write to #login-error only (never showError for 403).
    5. `bootstrapAuth()`:
       - setLoadingState as needed
       - try fetchJson('/health') — on fail: clearDashboardData, setOfflineState, showError (error-box), return
       - on health ok: renderHealth, setOnlineState
       - try fetchJson('/users') — 200: showDashboardPanel + loadDashboardData(); catch if status 401: showLoginGate(); other errors: showLoginGate + showLoginError(message)
    6. `handleLoginSubmit`: preventDefault; POST /auth/login with JSON body { email, password } via fetchJson; on success clearLoginError, showDashboardPanel, loadDashboardData(); on catch status 403 showLoginError(body message); other errors showLoginError
    7. `handleLogoutClick`: POST /auth/logout via fetchJson; clearDashboardData; showLoginGate; reset login form fields and errors
    8. Update `loadDashboardData` catch: if error.status === 401 → showLoginGate + showLoginError(error.message or fallback «Inicia sesión para ver y gestionar usuarios»); do not use showError for 401
    9. Update CRUD handlers (handleUserFormSubmit, etc.) — on 401 same as loadDashboardData (gate + message)
    10. Wire listeners: loginForm submit, logoutButton click; replace final `loadDashboardData()` with `bootstrapAuth()`
    11. reloadButton still calls loadDashboardData (post-login path per D-08)
  </action>
  <verify>
    <automated>node --check dashboard/app.js && grep -q bootstrapAuth dashboard/app.js && grep -q showLoginGate dashboard/app.js</automated>
  </verify>
  <acceptance_criteria>
    - Bottom of app.js calls bootstrapAuth not bare loadDashboardData
    - 401 paths call showLoginGate not only showError
    - Login uses POST /auth/login with credentials
    - Logout uses POST /auth/logout then returns to gate
  </acceptance_criteria>
  <done>Auth flow complete in app.js</done>
</task>

<task type="checkpoint" human="true">
  <name>Task 5: Manual UAT — cookie visible in Network tab</name>
  <read_first>19-RESEARCH.md Manual UAT section, README.md ports</read_first>
  <action>
    With API on PORT=3100 and dashboard on 5173:
    1. Open http://localhost:5173 — login gate visible, users table hidden
    2. Wrong password — inline error, error-box stays hidden
    3. Login admin@lab.local / changeme — dashboard loads
    4. DevTools Network: request to localhost:3100/users includes Cookie header
    5. Logout — gate returns; refresh — still gate
    6. Mid-session: delete edf_session cookie, click Recargar — returns to gate with Spanish message
  </action>
  <acceptance_criteria>
    - All six UAT steps pass manually
    - Cookie visible on credentialed API requests after login
  </acceptance_criteria>
  <done>AUTH-10 and AUTH-11 verified manually</done>
</task>

</tasks>

<verification>
- `node --check dashboard/app.js`
- Manual UAT checklist in Task 5
</verification>

<success_criteria>
- All must_haves truths observable in browser with API + dashboard running
- No localStorage session storage introduced
</success_criteria>
