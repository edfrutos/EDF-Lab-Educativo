---
phase: 19-vanilla-dashboard-login
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - api/README.md
autonomous: true
requirements:
  - AUTH-12
must_haves:
  truths:
    - "D-16: api/README.md Clientes frontend subsection documents credentials include for vanilla React Vue"
    - "D-00d: Doc mentions CORS_ORIGINS includes dashboard ports with credentials"
    - "Reader can find the pattern without opening framework source first"
  artifacts:
    - path: api/README.md
      provides: "Clientes frontend subsection"
      contains: "credentials"
  key_links:
    - from: api/README.md
      to: dashboard/app.js
      via: "references vanilla dashboard"
      pattern: "credentials"
---

<objective>
Document credentialed fetch requirement for all lab frontends (AUTH-12).

Purpose: Learners running React/Vue comparison apps know cookies require `credentials: 'include'`.
Output: Updated `api/README.md` only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-vanilla-dashboard-login/19-CONTEXT.md (D-16)
@.planning/phases/19-vanilla-dashboard-login/19-RESEARCH.md
@api/README.md
@dashboard-react/src/api.js
@dashboard-vue/src/api.js
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Doc suggests storing JWT in localStorage | High | Explicitly state httpOnly cookie only; cite credentials include, not Authorization header for lab v1.5 |
| Misleading CORS guidance | Medium | Note dashboard origins must be listed in CORS_ORIGINS (already in auth section) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add «Clientes frontend» subsection to api/README.md</name>
  <read_first>api/README.md (Autenticación section), 19-CONTEXT.md D-16, dashboard-react/src/api.js</read_first>
  <files>api/README.md</files>
  <action>
    After the existing «Autenticación (fase 18)» content (or as subsection within it), add heading **«Clientes frontend (credentials)»** with:
    1. Explain: session is httpOnly cookie `edf_session`; browser sends it only when `fetch(..., { credentials: 'include' })` (or axios `withCredentials: true`).
    2. List lab apps and paths:
       - Vanilla: `dashboard/app.js` — all calls via `fetchJson` with default credentials
       - React: `dashboard-react/src/api.js` — must pass credentials on fetch (note if already present or needs one-line fix in doc only; do not change React code in this plan unless missing)
       - Vue: `dashboard-vue` equivalent path
    3. One minimal fetch example for vanilla and mention React/Vue ports 5174/5175 in CORS_ORIGINS.
    4. State full auth narrative ships in Phase 21 `docs/17-authentication.md`.
    Keep Spanish, same tone as existing README.
  </action>
  <verify>
    <automated>grep -q 'Clientes frontend' api/README.md && grep -q "credentials" api/README.md && grep -q 'dashboard/app.js' api/README.md</automated>
  </verify>
  <acceptance_criteria>
    - api/README.md contains heading or subsection Clientes frontend
    - Documents credentials include for vanilla and mentions React/Vue
    - Does not document localStorage JWT pattern
  </acceptance_criteria>
  <done>AUTH-12 documentation complete</done>
</task>

</tasks>

<verification>
- `grep -q 'Clientes frontend' api/README.md`
- `grep -q 'credentials' api/README.md`
</verification>

<success_criteria>
- AUTH-12 satisfied per REQUIREMENTS.md
- Cross-reference accurate for implemented vanilla dashboard from plan 19-01
</success_criteria>
