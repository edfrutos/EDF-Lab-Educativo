---
phase: 07-migration-test-confidence
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - api/db.js
  - api/index.js
  - api/openapi.yaml
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - "D-10: createUser/updateUser catch SQLITE_CONSTRAINT_UNIQUE and routes return 409 Conflict with Ya existe un usuario con ese email."
    - "POST /users with duplicate email returns 409 with Ya existe un usuario con ese email."
    - "PUT /users/:id changing email to another user's email returns 409"
    - "PUT /users/:id keeping same email returns 200"
    - "OpenAPI documents 409 on POST and PUT /users"
  artifacts:
    - path: api/db.js
      provides: "DuplicateEmailError thrown on UNIQUE violation"
      exports: ["DuplicateEmailError"]
    - path: api/openapi.yaml
      provides: "409 Conflict response examples"
      contains: "409"
  key_links:
    - from: api/index.js
      to: api/db.js
      via: "catch DuplicateEmailError → 409"
      pattern: "DuplicateEmailError|409"
    - from: api/db.js
      to: api/schema.sql
      via: "UNIQUE constraint triggers error"
      pattern: "SQLITE_CONSTRAINT_UNIQUE"
---

<objective>
Implement duplicate-email detection via SQLite UNIQUE constraint and map to HTTP 409 in routes and OpenAPI.

Purpose: D-09..D-12 — teach schema-level constraints with observable API contract change.
Output: DuplicateEmailError in db.js, 409 handlers in index.js, OpenAPI updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-migration-test-confidence/07-CONTEXT.md (D-09..D-12)
@.planning/phases/07-migration-test-confidence/07-RESEARCH.md (Patterns 2, 4)
@api/db.js
@api/index.js
@api/openapi.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Throw DuplicateEmailError from createUser and updateUser</name>
  <read_first>api/db.js, 07-RESEARCH.md Pattern 2, api/schema.sql UNIQUE column</read_first>
  <action>
    In `api/db.js`:

    1. Define and export `DuplicateEmailError` class with message exactly: `Ya existe un usuario con ese email.`
    2. Add helper `isUniqueConstraintError(err)` checking `err.code === 'SQLITE_CONSTRAINT_UNIQUE'` or message match.
    3. Wrap `createUser()` insert in try/catch — rethrow as DuplicateEmailError on unique violation.
    4. Wrap `updateUser()` update similarly — SQLite allows same-row email unchanged; collision with another row throws UNIQUE.

    Export DuplicateEmailError in module.exports.
  </action>
  <acceptance_criteria>
    - module.exports includes DuplicateEmailError
    - createUser duplicate email throws DuplicateEmailError not generic Error
    - updateUser to existing other user's email throws DuplicateEmailError
  </acceptance_criteria>
  <verify>
    <automated>node --check api/db.js</automated>
  </verify>
  <done>db.js surfaces DuplicateEmailError on UNIQUE violations</done>
</task>

<task type="auto">
  <name>Task 2: Map DuplicateEmailError to 409 in index.js routes</name>
  <read_first>api/index.js POST and PUT handlers, api/db.js DuplicateEmailError export</read_first>
  <action>
    In `api/index.js`:

    1. Destructure `DuplicateEmailError` from require('./db').
    2. In POST /users catch block: if `err instanceof DuplicateEmailError` or `err.name === 'DuplicateEmailError'`, return `res.status(409).json({ error: err.message })`.
    3. Same for PUT /users/:id catch block.
    4. Keep existing 500 path for other errors.

    Do not change GET/DELETE handlers or validation messages.
  </action>
  <acceptance_criteria>
    - POST duplicate john@example.com returns status 409
    - Response body.error equals exactly: Ya existe un usuario con ese email.
    - PUT /users/1 with jane@example.com returns 409
    - PUT /users/1 with john@example.com returns 200
  </acceptance_criteria>
  <verify>
    <automated>node --check api/index.js</automated>
  </verify>
  <done>Routes return 409 for duplicate email conflicts</done>
</task>

<task type="auto">
  <name>Task 3: Update openapi.yaml with 409 responses</name>
  <read_first>api/openapi.yaml POST /users and PUT /users/{id} sections, 07-CONTEXT.md D-12</read_first>
  <action>
    In `api/openapi.yaml`:

    1. Add `"409"` response to POST /users with description duplicate email and example error string exactly: `Ya existe un usuario con ese email.`
    2. Add `"409"` response to PUT /users/{id} with same error example.
    3. Update POST /users description — replace "persiste en data/users.json" with SQLite/database wording (MIG-03 alignment).

    Do not change response schemas for 200/201/400/404.
  </action>
  <acceptance_criteria>
    - grep finds 409 under /users post and put sections
    - Example error string matches D-10 literal
    - POST description no longer claims JSON file as runtime store
  </acceptance_criteria>
  <verify>
    <automated>grep -c "409:" api/openapi.yaml | grep -qv "^0$"</automated>
  </verify>
  <done>OpenAPI documents 409 duplicate email</done>
</task>

</tasks>

<verification>
Manual smoke after API start:
```bash
curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:3100/users -H "Content-Type: application/json" -d '{"name":"Dup","email":"john@example.com"}'
# expect 409
```
</verification>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Email enumeration via 409 | low | Acceptable for educational lab; no auth |
| Error message inconsistency | low | Literal string locked in CONTEXT D-10 |
</threat_model>
