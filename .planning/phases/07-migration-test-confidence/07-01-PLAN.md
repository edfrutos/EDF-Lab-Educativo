---
phase: 07-migration-test-confidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/schema.sql
  - api/db.js
autonomous: true
requirements:
  - MIG-01
  - MIG-03
must_haves:
  truths:
    - "Empty users table on startup imports rows from api/data/users.json preserving ids"
    - "Console logs Migrados N usuarios desde users.json on successful JSON migration"
    - "Missing or empty users.json falls back to hardcoded John/Jane seed"
    - "Corrupt users.json restores hardcoded seed with warn log and continues startup"
    - "Migration never runs when users table already has rows"
    - "users.json is seed/migration source only — not read on every CRUD request"
    - "D-02: JSON path fixed at api/data/users.json via path.join — no SEED_FILE env var"
  artifacts:
    - path: api/db.js
      provides: "populateIfEmpty, migrateFromJson logic, initDb({ skipSeed })"
      exports: ["initDb", "getAllUsers", "getUserById", "createUser", "updateUser", "deleteUser"]
    - path: api/schema.sql
      provides: "email UNIQUE constraint"
      contains: "UNIQUE"
  key_links:
    - from: api/db.js
      to: api/data/users.json
      via: "readFileSync on empty table only"
      pattern: "users\\.json"
    - from: api/db.js
      to: api/schema.sql
      via: "db.exec(schema) before populate"
      pattern: "populateIfEmpty|seedIfEmpty"
---

<objective>
Implement JSON→SQLite auto-migration in db.js and add UNIQUE on email in schema.sql.

Purpose: MIG-01/MIG-03 — learners see users.json imported on first run without a separate script.
Output: Refactored `initDb()` with migration-first population and `skipSeed` option for tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-migration-test-confidence/07-CONTEXT.md (D-01..D-08)
@.planning/phases/07-migration-test-confidence/07-RESEARCH.md (Patterns 1, 3)
@.planning/phases/07-migration-test-confidence/07-PATTERNS.md
@api/db.js
@api/data/users.json
@docs/08-memoria-vs-persistencia.md (corrupt JSON recovery precedent)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UNIQUE constraint to schema.sql</name>
  <read_first>07-CONTEXT.md (D-09), api/schema.sql, 07-RESEARCH.md Schema change section</read_first>
  <action>
    Update `api/schema.sql` email column to: `email TEXT NOT NULL UNIQUE`.

    Keep header comment and other columns unchanged. Do not add migration SQL to schema file.
  </action>
  <acceptance_criteria>
    - grep finds `UNIQUE` on email line in api/schema.sql
    - CREATE TABLE IF NOT EXISTS users still present with id AUTOINCREMENT
  </acceptance_criteria>
  <verify>
    <automated>grep -q "email.*UNIQUE" api/schema.sql</automated>
  </verify>
  <done>schema.sql updated with UNIQUE on email</done>
</task>

<task type="auto">
  <name>Task 2: Refactor seedIfEmpty into populateIfEmpty with JSON migration</name>
  <read_first>api/db.js (full file), api/data/users.json, 07-CONTEXT.md (D-01..D-08), 07-RESEARCH.md Pattern 1</read_first>
  <action>
    In `api/db.js`:

    1. Add constant `USERS_JSON_PATH = path.join(__dirname, 'data', 'users.json')`.
    2. Add `DEFAULT_SEED` array matching users.json content (id 1 John, id 2 Jane).
    3. Add helper `tryParseUsersJson()` returning:
       - `[]` if file missing (ENOENT)
       - parsed `data.users` array if valid JSON with users array
       - `null` if JSON parse fails or structure invalid (corrupt)
    4. Replace `seedIfEmpty()` with `populateIfEmpty()`:
       - Return immediately if COUNT(*) &gt; 0 (D-04)
       - Call tryParseUsersJson()
       - If null (corrupt): console.warn corrupt message, use DEFAULT_SEED (D-07)
       - If empty array or missing file: use DEFAULT_SEED (D-06)
       - If users from JSON: console.log `Migrados ${n} usuarios desde users.json` (D-03)
       - INSERT with explicit `(id, name, email)` preserving JSON ids (D-05)
       - Ignore nextId field (D-08)
    5. Change `initDb(options = {})` signature:
       - After schema exec, call `populateIfEmpty()` only when `!options.skipSeed` (D-13)
    6. Export `DuplicateEmailError` class (stub for plan 07-02 — can add empty class now or in 07-02)

    Do NOT read users.json outside populateIfEmpty (MIG-03).
  </action>
  <acceptance_criteria>
    - initDb() accepts optional `{ skipSeed: true }`
    - Empty DB + existing users.json yields 2 rows with ids 1 and 2 from JSON
    - Console logs migration message when importing from JSON
    - Existing npm test still passes (12/12) after change
  </acceptance_criteria>
  <verify>
    <automated>cd api && npm test 2>&1 | grep -E "pass 12|tests 12"</automated>
  </verify>
  <done>db.js migrates from JSON on empty table; skipSeed option available</done>
</task>

</tasks>

<verification>
1. Delete api/data/users.test.db if exists; run `cd api && DB_FILE=./data/users.test.db node -e "require('./index.js').initDb().then(()=>process.exit(0))"` — verify 2 users imported
2. `npm test` — 12/12 pass
</verification>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Migration overwrites learner data | medium | COUNT(*) guard before populate |
| Corrupt JSON crashes API | low | Fallback seed + warn, continue startup |
| SQL injection via JSON | low | Prepared statements with bound params |
</threat_model>
