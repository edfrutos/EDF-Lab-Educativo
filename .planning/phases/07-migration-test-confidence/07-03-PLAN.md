---
phase: 07-migration-test-confidence
plan: 03
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - api/index.test.js
  - docs/10-tests.md
  - api/README.md
autonomous: false
requirements:
  - MIG-02
  - TEST-01
  - TEST-02
  - TEST-03
must_haves:
  truths:
    - "D-16: Test suite grows to ~15-16 tests — all 12 existing cases still pass plus new edge cases"
    - "npm test passes all existing tests plus new edge-case tests (~15-16 total)"
    - "Tests use isolated users.test.db per run with DB_FILE set before require"
    - "Empty database test returns GET /users → []"
    - "Duplicate email tests cover POST and PUT collision"
    - "docs/10-tests.md documents DB_FILE not DATA_FILE"
    - "Brief JSON vs SQLite comparison note exists for learners (MIG-02)"
  artifacts:
    - path: api/index.test.js
      provides: "Expanded SQLite test suite"
      pattern: "skipSeed|409"
    - path: docs/10-tests.md
      provides: "Updated test harness documentation"
      contains: "DB_FILE"
  key_links:
    - from: api/index.test.js
      to: api/db.js
      via: "initDb({ skipSeed: true })"
      pattern: "skipSeed"
    - from: docs/10-tests.md
      to: api/index.test.js
      via: "documents actual harness"
      pattern: "users\\.test\\.db"
---

<objective>
Expand test suite for SQLite edge cases and update learner documentation for DB_FILE harness and JSON vs SQLite note.

Purpose: TEST-01..03, MIG-02 — automated confidence + aligned docs.
Output: ~15-16 passing tests, updated docs/10-tests.md, brief MIG-02 note in api/README.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-migration-test-confidence/07-CONTEXT.md (D-13..D-16, MIG-02 discretion)
@.planning/phases/07-migration-test-confidence/07-PATTERNS.md
@api/index.test.js
@docs/10-tests.md
@api/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add edge-case tests to index.test.js</name>
  <read_first>api/index.test.js (full), api/db.js initDb signature, 07-CONTEXT.md D-13 D-14</read_first>
  <action>
    Add new describe blocks to `api/index.test.js`:

    1. **Base de datos vacía** — in test, `await unlink(TEST_DB)` then `await app.initDb({ skipSeed: true })`; GET /users → 200, body length 0.

    2. **Email duplicado — POST** — after beforeEach seed, POST user with email `john@example.com` → status 409, error message exact match.

    3. **Email duplicado — PUT** — PUT /users/1 with email `jane@example.com` → 409.

    4. **PUT mismo email** — PUT /users/1 with email `john@example.com` and updated name → 200 (D-11 exclude self).

    Keep existing beforeEach using `initDb()` without skipSeed for standard tests.
    Preserve DB_FILE-before-require pattern and AAA comments.
    Update test count comments if present.
  </action>
  <acceptance_criteria>
    - npm test reports at least 15 passing tests
    - npm test reports 0 failures
    - Empty DB test uses initDb({ skipSeed: true })
    - POST and PUT duplicate tests assert status 409
  </acceptance_criteria>
  <verify>
    <automated>cd api && npm test</automated>
  </verify>
  <done>Test suite expanded with empty DB and duplicate email cases</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/10-tests.md for SQLite harness</name>
  <read_first>docs/10-tests.md, api/index.test.js setup section, 07-CONTEXT.md D-15</read_first>
  <action>
    Rewrite stale sections in `docs/10-tests.md`:

    1. Replace all `DATA_FILE` references with `DB_FILE`.
    2. Replace `users.test.json` with `users.test.db`.
    3. Replace `loadUsers()` / `writeFile` fixture pattern with `unlink(TEST_DB)` + `app.initDb()` pattern from current index.test.js.
    4. Update example test count from 12 to current count (~16).
    5. Add brief **JSON vs SQLite** subsection (MIG-02): when JSON file persistence is enough (small data, single process) vs when SQLite helps (constraints, queries, concurrent access) — 4-6 sentences max; defer deep dive to Phase 8.

    Do not rewrite unrelated sections.
  </action>
  <acceptance_criteria>
    - docs/10-tests.md contains DB_FILE and users.test.db
    - docs/10-tests.md does not reference DATA_FILE or loadUsers
    - JSON vs SQLite comparison paragraph present
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "DATA_FILE" docs/10-tests.md && grep -q "DB_FILE" docs/10-tests.md</automated>
  </verify>
  <done>Test documentation aligned with SQLite harness</done>
</task>

<task type="auto">
  <name>Task 3: Add migration note to api/README.md</name>
  <read_first>api/README.md persistence section if any, 07-CONTEXT.md MIG-01</read_first>
  <action>
    Add short section to `api/README.md` explaining:
    - On first start with empty DB, users import from `data/users.json`
    - Console message when migration runs
    - `users.json` is seed source, runtime store is `users.db`
    - Duplicate emails return 409

    Keep section under 15 lines. Match existing README tone (Spanish).
  </action>
  <acceptance_criteria>
    - api/README.md mentions users.json migration and users.db runtime store
    - api/README.md mentions 409 duplicate email
  </acceptance_criteria>
  <verify>
    <automated>grep -q "users.json" api/README.md && grep -q "409" api/README.md</automated>
  </verify>
  <done>README documents migration and duplicate email behavior</done>
</task>

<task type="checkpoint">
  <name>Task 4: Human verification gate</name>
  <read_first>07-CONTEXT.md success criteria, dashboard/app.js (no changes expected)</read_first>
  <action>
    Operator verifies:
    1. Delete api/data/users.db, restart API — see migration log, dashboard shows John/Jane
    2. Create user with duplicate email in dashboard or curl — observe error
    3. npm test — all green

    Confirm dashboard CRUD still works unchanged.
  </action>
  <acceptance_criteria>
    - Migration log visible on fresh DB start
    - Dashboard CRUD functional
    - npm test 0 failures
  </acceptance_criteria>
  <done>UAT gate passed for Phase 7</done>
</task>

</tasks>

<verification>
```bash
cd api && npm test
grep -L "DATA_FILE" docs/10-tests.md
node --check api/index.test.js
```
</verification>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Stale docs mislead learners | medium | grep gate on DATA_FILE removal |
| Test order dependency | low | beforeEach isolation per test |
</threat_model>
