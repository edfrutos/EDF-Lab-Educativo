---
phase: 12-postgresql-persistence-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/db.js
  - api/db-sqlite.js
  - api/package.json
  - api/package-lock.json
autonomous: true
requirements:
  - PGSQL-05
must_haves:
  truths:
    - "D-01: SQLite logic extracted to db-sqlite.js"
    - "D-13: npm start without DATABASE_URL uses SQLite unchanged"
    - "D-15: SQLite code and bind mount not removed"
    - "D-18: No dashboard, ORM, auth, or Phase 14 docs"
    - "api/db.js is a thin router; SQLite logic lives in db-sqlite.js"
    - "Existing index.test.js passes without changes (still uses DB_FILE + SQLite)"
  artifacts:
    - path: api/db-sqlite.js
      provides: "SQLite persistence extracted from original db.js"
      contains: "DatabaseSync"
    - path: api/db.js
      provides: "Backend router by DATABASE_URL"
      contains: "db-sqlite"
  key_links:
    - from: api/index.js
      to: api/db.js
      via: "require('./db')"
      pattern: "initDb"
---

<objective>
Extract SQLite into `db-sqlite.js` and convert `db.js` into a DATABASE_URL router. Add `pg` dependency without implementing Postgres yet.

Purpose: Safe refactor preserving PGSQL-05 before adding db-pg.js (Plan 12-02).
Output: `api/db-sqlite.js`, refactored `api/db.js`, updated `api/package.json`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-postgresql-persistence-layer/12-CONTEXT.md
@.planning/phases/12-postgresql-persistence-layer/12-RESEARCH.md
@.planning/phases/12-postgresql-persistence-layer/12-PATTERNS.md
@api/db.js
@api/index.js
@api/index.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract db-sqlite.js from db.js</name>
  <read_first>api/db.js (full file), 12-CONTEXT.md (D-01, D-05, D-15), 12-RESEARCH.md (Finding 1)</read_first>
  <files>api/db-sqlite.js</files>
  <action>
    Create `api/db-sqlite.js` by moving the **entire current** `api/db.js` implementation into the new file unchanged in behavior:

    - Keep `DuplicateEmailError`, `initDb`, all CRUD functions
    - Keep `DB_FILE`, `populateIfEmpty`, `tryParseUsersJson`, `DEFAULT_SEED`
    - Keep `node:sqlite` / `DatabaseSync` usage
    - Export same module.exports object as current db.js

    Do NOT add Postgres code. Do NOT remove seed logic (SQLite still needs it).
  </action>
  <verify>
    <automated>node --check api/db-sqlite.js</automated>
  </verify>
  <acceptance_criteria>
    - api/db-sqlite.js exists and passes node --check
    - File contains DatabaseSync, populateIfEmpty, and DuplicateEmailError
    - No require('pg') in db-sqlite.js
  </acceptance_criteria>
  <done>SQLite module extracted as standalone file</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite db.js as router and add pg dependency</name>
  <read_first>api/db.js, api/db-sqlite.js, 12-CONTEXT.md (D-01, D-02, D-13), api/package.json</read_first>
  <files>api/db.js, api/package.json, api/package-lock.json</files>
  <action>
    Replace `api/db.js` with a re-export of `db-sqlite.js` only (Plan 12-02 adds the DATABASE_URL branch):

    ```javascript
    'use strict';
    module.exports = require('./db-sqlite');
    ```

    1. Add `"pg": "^8.13.1"` (or latest 8.x) to api/package.json dependencies
    2. Run `npm install` in api/ to update package-lock.json

    Do NOT create db-pg.js in this plan. Do NOT change api/index.js.
  </action>
  <verify>
    <automated>cd api && node --check db.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 (16 tests pass on SQLite)
    - api/db.js re-exports db-sqlite module (under 30 lines)
    - api/package.json lists pg in dependencies
    - node --check db.js passes
  </acceptance_criteria>
  <done>SQLite default path verified; pg dependency installed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host npm start → SQLite file | Unchanged from v1.1; local file DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information disclosure | users.db on disk | accept | Fictional lab data; unchanged risk profile |
</threat_model>

<verification>
- `cd api && npm test` — all tests pass
- `cd api && npm start` + curl GET /users — SQLite responds
</verification>

<success_criteria>
- PGSQL-05: host npm start without DATABASE_URL unchanged
- Refactor complete; ready for db-pg.js in Plan 12-02
</success_criteria>

<output>
After completion, create `.planning/phases/12-postgresql-persistence-layer/12-01-SUMMARY.md`
</output>
