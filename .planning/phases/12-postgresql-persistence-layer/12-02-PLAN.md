---
phase: 12-postgresql-persistence-layer
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - api/db.js
  - api/db-pg.js
  - api/schema.pg.sql
autonomous: true
requirements:
  - PGSQL-01
  - PGSQL-02
  - PGSQL-03
  - PGSQL-04
must_haves:
  truths:
    - "D-01: db.js routes DATABASE_URL to db-pg, else db-sqlite"
    - "D-03: db-pg uses pg Pool with $1 $2 parameterized queries"
    - "D-04: schema.pg.sql uses SERIAL PRIMARY KEY matching SQLite columns"
    - "D-06: Postgres err.code 23505 maps to DuplicateEmailError"
    - "D-16: No JSON seed in Postgres initDb (deferred Phase 13)"
    - "D-18: No dashboard, ORM, auth, or doc changes"
    - "schema.pg.sql defines users table with id SERIAL, name TEXT NOT NULL, email TEXT UNIQUE"
    - "Duplicate email on Postgres returns 409 via DuplicateEmailError"
    - "CRUD JSON responses match SQLite shape { id, name, email }"
  artifacts:
    - path: api/schema.pg.sql
      provides: "PostgreSQL DDL for users table"
      contains: "SERIAL PRIMARY KEY"
    - path: api/db-pg.js
      provides: "pg Pool CRUD implementation"
      contains: "Pool"
    - path: api/db.js
      provides: "Routes DATABASE_URL to db-pg"
      contains: "DATABASE_URL"
  key_links:
    - from: api/db-pg.js
      to: api/schema.pg.sql
      via: "initDb reads and executes schema"
      pattern: "schema.pg.sql"
---

<objective>
Implement PostgreSQL persistence: schema.pg.sql, db-pg.js with pg Pool, and wire db.js router.

Purpose: PGSQL-01 through PGSQL-04 — Postgres CRUD with same HTTP contract.
Output: `api/schema.pg.sql`, `api/db-pg.js`, updated `api/db.js`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-postgresql-persistence-layer/12-CONTEXT.md
@.planning/phases/12-postgresql-persistence-layer/12-RESEARCH.md
@.planning/phases/12-postgresql-persistence-layer/12-PATTERNS.md
@api/db-sqlite.js
@api/schema.sql
@api/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create schema.pg.sql</name>
  <read_first>api/schema.sql, 12-CONTEXT.md (D-04, D-05), 12-RESEARCH.md (Finding 3)</read_first>
  <files>api/schema.pg.sql</files>
  <action>
    Create `api/schema.pg.sql` with Spanish header comment (match schema.sql style):

    ```sql
    CREATE TABLE IF NOT EXISTS users (
      id    SERIAL PRIMARY KEY,
      name  TEXT NOT NULL,
      email TEXT NOT NULL UNIQUE
    );
    ```

    Do NOT modify api/schema.sql.
  </action>
  <verify>
    <automated>grep -q "SERIAL PRIMARY KEY" api/schema.pg.sql && grep -q "UNIQUE" api/schema.pg.sql</automated>
  </verify>
  <acceptance_criteria>
    - schema.pg.sql exists with users table
    - Columns: id (SERIAL), name (TEXT NOT NULL), email (TEXT NOT NULL UNIQUE)
    - schema.sql unchanged
  </acceptance_criteria>
  <done>Postgres schema file ready for initDb</done>
</task>

<task type="auto">
  <name>Task 2: Implement db-pg.js with pg Pool</name>
  <read_first>api/db-sqlite.js (CRUD signatures), 12-RESEARCH.md (Findings 2, 4, 5), 12-CONTEXT.md (D-03, D-06, D-16)</read_first>
  <files>api/db-pg.js</files>
  <action>
    Create `api/db-pg.js`:

    1. **DuplicateEmailError** — same class as db-sqlite.js (copy or shared; copy is fine for lab clarity)
    2. **Pool** — `new Pool({ connectionString: process.env.DATABASE_URL })`; module-level `let pool = null`
    3. **isUniqueViolation(err)** — `err.code === '23505'`
    4. **initDb(options)**:
       - If pool exists, `await pool.end()` and reset
       - Create new Pool from DATABASE_URL
       - Read `schema.pg.sql`, execute via `pool.query(schema)`
       - **Do NOT seed** — D-16 defers populateIfEmpty to Phase 13 (skipSeed ignored or no-op is OK)
       - Log `[db] Using PostgreSQL` on first init
    5. **getAllUsers** — `SELECT id, name, email FROM users ORDER BY name`
    6. **getUserById(id)** — parameterized `$1`
    7. **createUser(name, email)** — `INSERT ... RETURNING id, name, email`; catch 23505 → DuplicateEmailError
    8. **updateUser(id, name, email)** — `UPDATE ... WHERE id = $3`; rowCount 0 → null; 23505 → DuplicateEmailError
    9. **deleteUser(id)** — fetch user first, DELETE, return deleted row or null

    All queries use `$1`, `$2` placeholders only (PGSQL-02).
  </action>
  <verify>
    <automated>node --check api/db-pg.js</automated>
  </verify>
  <acceptance_criteria>
    - db-pg.js exports initDb, getAllUsers, getUserById, createUser, updateUser, deleteUser, DuplicateEmailError
    - No SQL string concatenation with user input
    - initDb does not read users.json or seed data
    - node --check passes
  </acceptance_criteria>
  <done>Postgres CRUD module complete</done>
</task>

<task type="auto">
  <name>Task 3: Wire db.js router to db-pg when DATABASE_URL set</name>
  <read_first>api/db.js, api/db-pg.js, 12-CONTEXT.md (D-01, D-02, D-13)</read_first>
  <files>api/db.js</files>
  <action>
    Update `api/db.js` to select backend:

    ```javascript
    'use strict';
    const backend = process.env.DATABASE_URL
      ? require('./db-pg')
      : require('./db-sqlite');
    module.exports = {
      DuplicateEmailError: backend.DuplicateEmailError,
      initDb: backend.initDb,
      getAllUsers: backend.getAllUsers,
      getUserById: backend.getUserById,
      createUser: backend.createUser,
      updateUser: backend.updateUser,
      deleteUser: backend.deleteUser,
    };
    ```

    Optional: log which backend at first require time (not in db.js if db-pg/db-sqlite already log on initDb).

    Run `cd api && npm test` — must still pass (no DATABASE_URL in test env).
  </action>
  <verify>
    <automated>cd api && npm test</automated>
  </verify>
  <acceptance_criteria>
    - db.js contains DATABASE_URL conditional
    - npm test exits 0
    - index.js requires unchanged
  </acceptance_criteria>
  <done>Dual adapter wired; SQLite tests green</done>
</task>

<task type="auto">
  <name>Task 4: Manual Postgres smoke test (local or compose prep)</name>
  <read_first>12-RESEARCH.md (Verification Commands), 12-CONTEXT.md (D-17)</read_first>
  <files>api/db-pg.js</files>
  <action>
    If Docker Postgres available (compose from Plan 12-03 may not exist yet), run quick smoke:

    1. Start postgres container OR use compose after 12-03
    2. `DATABASE_URL=postgresql://edf_lab:edf_lab_dev@localhost:5432/edf_lab PORT=3100 node index.js`
    3. POST user, GET users, POST duplicate email → expect 409

    If postgres not running yet, document smoke commands in 12-02-SUMMARY for 12-03 UAT. Minimum: verify module loads with node -e "require('./db-pg')" after node --check.

    Do NOT modify index.test.js for Postgres (Phase 13).
  </action>
  <verify>
    <automated>node --check api/db-pg.js && node --check api/db.js</automated>
  </verify>
  <acceptance_criteria>
    - Syntax checks pass
    - If postgres reachable: POST creates user with numeric id in JSON
    - Duplicate email returns 409 when postgres running
  </acceptance_criteria>
  <done>Postgres path validated or deferred to Plan 12-03 E2E</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → PostgreSQL | DATABASE_URL contains credentials; lab-only fictional values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Spoofing | DATABASE_URL credentials | accept | Fictional dev credentials in compose; not production |
| T-12-03 | Injection | SQL queries | mitigate | Parameterized queries only ($1, $2) — PGSQL-02 |
</threat_model>

<verification>
- grep SERIAL in schema.pg.sql
- npm test passes (SQLite)
- Optional: curl CRUD against DATABASE_URL instance
</verification>

<success_criteria>
- PGSQL-01, PGSQL-02, PGSQL-03, PGSQL-04 satisfied in code
- Same JSON contract from route handlers
</success_criteria>

<output>
After completion, create `.planning/phases/12-postgresql-persistence-layer/12-02-SUMMARY.md`
</output>
