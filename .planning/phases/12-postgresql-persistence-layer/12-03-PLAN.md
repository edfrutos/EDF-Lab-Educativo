---
phase: 12-postgresql-persistence-layer
plan: 03
type: execute
wave: 2
depends_on:
  - 12-02
files_modified:
  - docker-compose.yml
autonomous: true
requirements:
  - PGCOMPOSE-01
  - PGCOMPOSE-02
  - PGCOMPOSE-03
must_haves:
  truths:
    - "D-08: Postgres uses named volume postgres_data; API keeps bind mount ./api/data for users.json"
    - "D-09: Compose uses fictional lab credentials POSTGRES_USER=edf_lab, POSTGRES_PASSWORD=edf_lab_dev, POSTGRES_DB=edf_lab"
    - "D-10: API DATABASE_URL=postgresql://edf_lab:edf_lab_dev@edf-lab-postgres:5432/edf_lab with depends_on edf-lab-postgres"
    - "D-11: Postgres published on host 5432:5432 for psql learning"
    - "D-12: Postgres healthcheck pg_isready; API depends_on with condition service_healthy"
    - "D-18: No dashboard, ORM, auth, SQLite removal, or Phase 14 docs in this plan"
    - "docker compose config lists edf-lab-postgres, edf-lab-api, edf-lab-dashboard"
    - "npm run compose:up starts full stack; CRUD from dashboard works against Postgres"
    - "User created via dashboard survives docker compose down && up"
  artifacts:
    - path: docker-compose.yml
      provides: "Three-service stack with Postgres"
      contains: "edf-lab-postgres"
  key_links:
    - from: docker-compose.yml
      to: edf-lab-postgres
      via: "DATABASE_URL on edf-lab-api"
      pattern: "DATABASE_URL"
    - from: edf-lab-api
      to: postgres_data
      via: "depends_on service_healthy"
      pattern: "service_healthy"
---

<objective>
Add PostgreSQL service to Compose, wire DATABASE_URL on API, and verify end-to-end CRUD persistence.

Purpose: PGCOMPOSE-01, PGCOMPOSE-02, PGCOMPOSE-03 — full stack with Postgres.
Output: Updated `docker-compose.yml`; E2E verification documented in SUMMARY
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-postgresql-persistence-layer/12-CONTEXT.md
@.planning/phases/12-postgresql-persistence-layer/12-RESEARCH.md
@docker-compose.yml
@api/db-pg.js
@dashboard/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add edf-lab-postgres service and wire API</name>
  <read_first>docker-compose.yml, 12-CONTEXT.md (D-07 through D-14), 12-RESEARCH.md (Finding 6)</read_first>
  <files>docker-compose.yml</files>
  <action>
    Extend `docker-compose.yml`:

    **New service `edf-lab-postgres`:**
    - `image: postgres:16-alpine`
    - `environment`: POSTGRES_USER=edf_lab, POSTGRES_PASSWORD=edf_lab_dev, POSTGRES_DB=edf_lab
    - `volumes`: postgres_data:/var/lib/postgresql/data
    - `ports`: "5432:5432"
    - `healthcheck`: pg_isready -U edf_lab -d edf_lab (interval 5s, timeout 5s, retries 5)

    **Update `edf-lab-api`:**
    - `environment`: DATABASE_URL=postgresql://edf_lab:edf_lab_dev@edf-lab-postgres:5432/edf_lab
    - `depends_on`: edf-lab-postgres with condition service_healthy
    - Keep existing bind mount `./api/data:/usr/src/app/data` and ports 3100:3100

    **Top-level `volumes`:**
    ```yaml
    volumes:
      postgres_data:
    ```

    Add Spanish learner comments explaining:
    - Named volume vs bind mount contrast (Postgres vs SQLite file)
    - DATABASE_URL selects Postgres inside the API container
    - Fictional lab credentials only

    Do NOT change dashboard service except if depends_on order needs postgres (dashboard only depends on api — unchanged).
  </action>
  <verify>
    <automated>docker compose config >/dev/null && grep -q "edf-lab-postgres" docker-compose.yml && grep -q "postgres_data" docker-compose.yml && grep -q "DATABASE_URL" docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - docker compose config validates without errors
    - Three services defined: edf-lab-postgres, edf-lab-api, edf-lab-dashboard
    - API DATABASE_URL points to edf-lab-postgres hostname
    - Named volume postgres_data declared
    - Healthcheck present on postgres service
  </acceptance_criteria>
  <done>Compose stack includes Postgres with persistence volume</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end verification — Compose + dashboard CRUD</name>
  <read_first>12-RESEARCH.md (Verification Commands), 12-CONTEXT.md (D-17), dashboard/app.js</read_first>
  <files>docker-compose.yml</files>
  <action>
    Run full verification sequence:

    1. Stop conflicting services: host API on 3100, host postgres on 5432 if needed
    2. `docker compose down` (no -v first time to preserve volume test)
    3. `npm run compose:up` or `docker compose up --build -d`
    4. Wait for healthy: `docker compose ps` shows postgres healthy
    5. `curl -sf http://localhost:3100/health`
    6. `curl -sf http://localhost:3100/users` — expect `[]` or empty array on fresh DB
    7. POST user via curl with unique email
    8. Open http://localhost:5173 — create/edit/delete user via dashboard
    9. `docker compose down && docker compose up -d` — user persists (named volume)
    10. Verify host `npm start` without DATABASE_URL still uses SQLite (separate terminal, port 3000 default or PORT=3101)

    Record any real errors in NOTEBOOK.md only if they occur (PGDOCS-05 is Phase 14; optional note here if blocker found).

    Confirm API logs show `[db] Using PostgreSQL` or equivalent when under compose.
  </action>
  <verify>
    <automated>curl -sf http://localhost:3100/health | grep -q healthy</automated>
  </verify>
  <acceptance_criteria>
    - compose:up brings up 3 containers
    - GET /users returns JSON array
    - POST creates user; GET shows new user
    - User survives down/up cycle (Postgres named volume)
    - Dashboard CRUD returns same JSON shape as before
    - Host npm start without DATABASE_URL still works (PGSQL-05 regression check)
  </acceptance_criteria>
  <done>Phase 12 success criteria 1–5 verified manually</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → Postgres 5432 | Published for psql learning; lab credentials |
| Compose network | API ↔ Postgres internal DNS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information disclosure | POSTGRES_PASSWORD in compose | accept | Fictional dev password; documented as lab-only |
| T-12-05 | Denial of service | Postgres port exposed | accept | Local dev; not internet-facing |
</threat_model>

<verification>
- docker compose config
- curl health + CRUD
- dashboard manual CRUD
- compose down/up persistence
</verification>

<success_criteria>
- PGCOMPOSE-01, PGCOMPOSE-02, PGCOMPOSE-03 satisfied
- ROADMAP Phase 12 success criteria 1–5 TRUE
</success_criteria>

<output>
After completion, create `.planning/phases/12-postgresql-persistence-layer/12-03-SUMMARY.md`
</output>
