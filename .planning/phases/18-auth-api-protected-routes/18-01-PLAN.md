---
phase: 18-auth-api-protected-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/schema.sql
  - api/schema.pg.sql
  - api/seed.js
  - api/db-sqlite.js
  - api/db-pg.js
  - api/package.json
  - api/package-lock.json
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "accounts table exists in SQLite and PostgreSQL schemas"
    - "initDb seeds one admin when accounts is empty"
    - "findAccountByEmail works on both backends"
    - "bcrypt, jsonwebtoken, cookie-parser installed"
    - "users CRUD unchanged; no index.js route changes in this plan"
  artifacts:
    - path: api/schema.sql
      provides: "SQLite accounts DDL"
      contains: "CREATE TABLE IF NOT EXISTS accounts"
    - path: api/seed.js
      provides: "Admin seed helper"
      contains: "seedAdminIfEmptyAccounts"
  key_links:
    - from: api/db-sqlite.js
      to: api/seed.js
      via: "initDb calls seedAdminIfEmptyAccounts"
      pattern: "seedAdminIfEmpty"
---

<objective>
Add `accounts` persistence (dual schema), admin seed on empty DB, and account lookup helpers. Install auth dependencies.

Purpose: AUTH-01, AUTH-02 — data layer before HTTP auth (Plan 18-02).
Output: Schemas, seed, `findAccountByEmail` / `countAccounts`, updated `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-auth-api-protected-routes/18-CONTEXT.md
@.planning/phases/18-auth-api-protected-routes/18-RESEARCH.md
@.planning/phases/18-auth-api-protected-routes/18-PATTERNS.md
@api/schema.sql
@api/schema.pg.sql
@api/seed.js
@api/db-sqlite.js
@api/db-pg.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add accounts table to both schemas</name>
  <read_first>18-CONTEXT.md (D-16), api/schema.sql, api/schema.pg.sql</read_first>
  <files>api/schema.sql, api/schema.pg.sql</files>
  <action>
    Append to **both** schema files (keep `users` table unchanged):

    **SQLite (`schema.sql`):**
    ```sql
    CREATE TABLE IF NOT EXISTS accounts (
      id            INTEGER PRIMARY KEY AUTOINCREMENT,
      email         TEXT NOT NULL UNIQUE,
      password_hash TEXT NOT NULL
    );
    ```

    **PostgreSQL (`schema.pg.sql`):**
    ```sql
    CREATE TABLE IF NOT EXISTS accounts (
      id            SERIAL PRIMARY KEY,
      email         TEXT NOT NULL UNIQUE,
      password_hash TEXT NOT NULL
    );
    ```

    Add one-line SQL comments in Spanish matching existing file style.
  </action>
  <verify>
    <automated>grep -q 'accounts' api/schema.sql api/schema.pg.sql</automated>
  </verify>
  <acceptance_criteria>
    - Both files define accounts with email UNIQUE and password_hash NOT NULL
    - users table DDL untouched
  </acceptance_criteria>
  <done>Dual accounts schema ready</done>
</task>

<task type="auto">
  <name>Task 2: Admin seed and account queries</name>
  <read_first>api/seed.js, api/db-sqlite.js initDb, api/db-pg.js initDb, 18-CONTEXT.md (D-01, D-02, D-04)</read_first>
  <files>api/seed.js, api/db-sqlite.js, api/db-pg.js</files>
  <action>
    1. In `seed.js`, add `seedAdminIfEmptyAccounts({ countAccounts, insertAccount })`:
       - Read `ADMIN_EMAIL` (default `admin@lab.local`) and `ADMIN_PASSWORD` (default `changeme`)
       - If `countAccounts() === 0`, bcrypt.hash password with **cost 10**, insert row
       - Log one line: `[seed] Cuenta operador creada` (no password in log)

    2. In `db-sqlite.js`:
       - `countAccounts()` — `SELECT COUNT(*) FROM accounts`
       - `findAccountByEmail(email)` — return `{ id, email, password_hash }` or null
       - `insertAccount(email, passwordHash)` — INSERT
       - Call `seedAdminIfEmptyAccounts` at end of `initDb` (after user seed), unless `options.skipSeed`

    3. Mirror the same three functions + seed call in `db-pg.js` using `$1` placeholders.

    4. Export `findAccountByEmail` from both modules (add to module.exports). Do **not** add to `db.js` router yet unless needed — `auth.js` in plan 18-02 will require the active backend.

    **Do not** modify `api/index.js` in this plan.
  </action>
  <verify>
    <automated>cd api && node -e "process.env.DB_FILE='data/_auth-smoke.db'; const s=require('./db-sqlite'); s.initDb().then(async()=>{const a=await s.findAccountByEmail('admin@lab.local'); console.log(!!a); process.exit(a?0:1)})"</automated>
  </verify>
  <acceptance_criteria>
    - Fresh SQLite DB has one admin account after initDb
    - findAccountByEmail returns row with password_hash
    - Second initDb does not duplicate admin (UNIQUE email)
  </acceptance_criteria>
  <done>Account persistence and seed wired</done>
</task>

<task type="auto">
  <name>Task 3: Install auth dependencies</name>
  <read_first>api/package.json, 18-RESEARCH.md</read_first>
  <files>api/package.json, api/package-lock.json</files>
  <action>
    Run in `api/`:
    ```bash
    npm install bcrypt jsonwebtoken cookie-parser
    ```
    Do not add cross-env or other deps.
  </action>
  <verify>
    <automated>cd api && node -e "require('bcrypt'); require('jsonwebtoken'); require('cookie-parser'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json lists bcrypt, jsonwebtoken, cookie-parser under dependencies
    - node require succeeds
  </acceptance_criteria>
  <done>Auth npm packages installed</done>
</task>

</tasks>

<verification>
## Plan 18-01 verification

- [ ] `node --check` on modified db files and seed.js
- [ ] Existing `npm run test:sqlite` still passes (no auth middleware yet)
- [ ] Manual: delete test db, initDb, query accounts count = 1
</verification>
