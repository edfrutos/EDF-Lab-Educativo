---
phase: 18-auth-api-protected-routes
plan: 03
type: execute
wave: 3
depends_on:
  - 18-02
files_modified:
  - api/package.json
  - api/index.test.js
  - api/index.pg.test.js
  - api/openapi.yaml
  - api/README.md
autonomous: true
requirements:
  - AUTH-08
  - AUTH-09
must_haves:
  truths:
    - "npm test sets AUTH_DISABLED=1; existing 32 CRUD tests pass"
    - "Dedicated auth tests use real login + cookie"
    - "Auth tests exist in both SQLite and Postgres suites"
    - "OpenAPI documents /auth/* and secured /users"
  artifacts:
    - path: api/openapi.yaml
      provides: "Auth contract"
      contains: "/auth/login"
    - path: api/index.test.js
      provides: "SQLite auth tests"
      contains: "Autenticación"
  key_links:
    - from: api/package.json
      to: api/index.test.js
      via: "AUTH_DISABLED=1 in test script"
      pattern: "AUTH_DISABLED"
---

<objective>
Restore automated test confidence: AUTH_DISABLED for CRUD suites, new auth integration tests on SQLite and Postgres, OpenAPI update, brief api/README note.

Purpose: AUTH-08, AUTH-09.
Output: Passing `npm test`, updated OpenAPI, README pointer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-auth-api-protected-routes/18-CONTEXT.md
@.planning/phases/18-auth-api-protected-routes/18-RESEARCH.md
@api/index.test.js
@api/index.pg.test.js
@api/openapi.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: AUTH_DISABLED for existing tests</name>
  <read_first>api/index.test.js header, api/index.pg.test.js header, api/package.json scripts</read_first>
  <files>api/index.test.js, api/index.pg.test.js, api/package.json</files>
  <action>
    1. At top of **both** test files (after DB_FILE / DATABASE_URL setup, **before** `require('./index.js')`):
       `process.env.AUTH_DISABLED = '1';`

    2. Update `package.json` test script to prefix both commands:
       ```json
       "test": "AUTH_DISABLED=1 node --test index.test.js --test-force-exit && AUTH_DISABLED=1 node --test index.pg.test.js --test-force-exit"
       ```
       Same for `test:sqlite` and `test:pg` if present.

    3. Add comment in test file header explaining AUTH_DISABLED is **solo tests**.

    Run `npm test` — all existing describes must pass.
  </action>
  <verify>
    <automated>cd api && npm test</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0
    - 32 existing CRUD tests still pass (16+16)
  </acceptance_criteria>
  <done>CRUD tests unblocked</done>
</task>

<task type="auto">
  <name>Task 2: Auth integration tests (both backends)</name>
  <read_first>18-CONTEXT.md (D-06, D-07), supertest cookie patterns in 18-RESEARCH.md</read_first>
  <files>api/index.test.js, api/index.pg.test.js</files>
  <action>
    Add `describe('Autenticación API', () => { ... })` to **both** test files at end of file.

    In this describe only:
    - `before` hook: `delete process.env.AUTH_DISABLED;`
    - `after` hook: `process.env.AUTH_DISABLED = '1';`
    - Use admin@lab.local / changeme (seed defaults)

    **Cases (minimum):**
    1. `GET /users` without cookie → 401
    2. `POST /auth/login` valid → 200 + set-cookie header contains `edf_session`
    3. `GET /users` with cookie from login → 200 + array
    4. `POST /auth/login` wrong password → 403 + error message
    5. `POST /auth/logout` with cookie → 200; then `GET /users` → 401
    6. `GET /health` without cookie → 200 (public)
    7. Invalid cookie `Cookie: edf_session=invalid` → 401 on GET /users

    Helper to extract cookie from `set-cookie` for supertest `.set('Cookie', ...)`.

    PG suite: ensure `beforeEach` still resets DB; auth tests run after initDb with fresh admin.
  </action>
  <verify>
    <automated>cd api && npm test</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 including new auth describes
    - Both files contain Autenticación describe
  </acceptance_criteria>
  <done>Auth test coverage complete</done>
</task>

<task type="auto">
  <name>Task 3: OpenAPI and README</name>
  <read_first>api/openapi.yaml structure, 18-CONTEXT.md (D-18)</read_first>
  <files>api/openapi.yaml, api/README.md</files>
  <action>
    Update `openapi.yaml`:
    - Paths `/auth/login` (POST), `/auth/logout` (POST) with request/response schemas
    - `components.securitySchemes.cookieAuth` — apiKey in cookie, name `edf_session`
    - Apply `security: [{ cookieAuth: [] }]` on all `/users` operations
    - Document 401 on protected routes, 403 on login failure

    Update `api/README.md` with short section **Autenticación (fase 18)**:
    - How to copy `.env.example` → `.env`
    - curl login example with `-c` / `-b` cookies
    - Note: dashboard login UI comes in phase 19
    - `AUTH_DISABLED` test-only warning
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const y=fs.readFileSync('api/openapi.yaml','utf8'); process.exit(/\\/auth\\/login/.test(y)&&/cookieAuth/.test(y)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - openapi.yaml includes auth paths and cookieAuth scheme
    - README mentions .env.example and curl login
  </acceptance_criteria>
  <done>Contract and docs updated</done>
</task>

</tasks>

<verification>
## Phase 18 complete when

- [ ] `cd api && npm test` — all tests green
- [ ] `node --check api/index.js api/auth.js`
- [ ] Manual curl login + CRUD per 18-02 verification
- [ ] Requirements AUTH-01..AUTH-09 satisfied (dashboard AUTH-10+ deferred to phase 19)
</verification>
