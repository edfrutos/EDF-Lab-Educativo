---
phase: 16-vue-dashboard-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard-vue/package.json
  - dashboard-vue/package-lock.json
  - dashboard-vue/vite.config.js
  - dashboard-vue/index.html
  - dashboard-vue/.env.example
  - dashboard-vue/.gitignore
  - dashboard-vue/src/main.js
  - dashboard-vue/src/index.css
  - dashboard-vue/src/api.js
  - dashboard-vue/src/App.vue
  - dashboard-vue/README.md
autonomous: true
requirements:
  - FRWK-04
  - FRWK-08
must_haves:
  truths:
    - "D-01: dashboard-vue/ exists with Vite + Vue 3 + Tailwind"
    - "D-05: <script setup> in App.vue"
    - "D-12: src/api.js same logic as dashboard-react"
    - "D-15: Dev server on port 5175 strictPort"
    - "D-16: .env.example documents VITE_API_BASE_URL"
    - "No changes to api/, dashboard/, or dashboard-react/"
  artifacts:
    - path: dashboard-vue/package.json
      provides: "Vue app package"
      contains: "vue"
    - path: dashboard-vue/src/api.js
      provides: "HTTP client"
      contains: "fetchJson"
  key_links:
    - from: dashboard-vue/src/api.js
      to: dashboard-react/src/api.js
      via: "copy parity"
      pattern: "fetchJson"
---

<objective>
Scaffold `dashboard-vue/` with Vite, Vue 3 (`<script setup>`), Tailwind v4, `src/api.js`, and `App.vue` shell that loads health, API info, and users on mount.

Purpose: FRWK-04, FRWK-08 (partial — full README in 16-03).
Output: Runnable dev server on :5175 with initial data load (read-only UI acceptable before 16-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-vue-dashboard-parity/16-CONTEXT.md
@.planning/phases/16-vue-dashboard-parity/16-RESEARCH.md
@.planning/phases/16-vue-dashboard-parity/16-PATTERNS.md
@dashboard-react/vite.config.js
@dashboard-react/src/api.js
@dashboard-react/src/App.jsx
@dashboard/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vite + Vue 3 + Tailwind scaffold (FRWK-04)</name>
  <read_first>16-RESEARCH.md, 16-CONTEXT.md D-01 D-15</read_first>
  <files>dashboard-vue/*</files>
  <action>
    Create `dashboard-vue/` at repo root (sibling to `dashboard-react/`).

    - Vite Vue template (Vue 3)
    - `@vitejs/plugin-vue` + `@tailwindcss/vite` in `vite.config.js`
    - `server.port: 5175`, `strictPort: true`
    - `src/index.css`: `@import "tailwindcss";`
    - `index.html` lang="es", title referencing Vue dashboard lab
    - `.gitignore` for node_modules, dist, .env
    - `.env.example`: `VITE_API_BASE_URL=http://localhost:3100`
    - `dashboard-vue/README.md` stub: install, dev, API on :3100, port 5175

    Do NOT add Vue Router, Pinia, or axios.
  </action>
  <verify>
    <automated>test -f dashboard-vue/package.json && test -f dashboard-vue/vite.config.js && grep -q "5175" dashboard-vue/vite.config.js</automated>
  </verify>
  <acceptance_criteria>
    - `cd dashboard-vue && npm run build` succeeds
    - Dev server configured for port 5175
  </acceptance_criteria>
  <done>FRWK-04 scaffold complete</done>
</task>

<task type="auto">
  <name>Task 2: Implement src/api.js (FRWK-08)</name>
  <read_first>dashboard-react/src/api.js</read_first>
  <files>dashboard-vue/src/api.js</files>
  <action>
    Copy/adapt `dashboard-react/src/api.js`:

    - Export `API_BASE_URL` from `import.meta.env.VITE_API_BASE_URL` with fallback `http://localhost:3100`
    - Export `fetchJson` with `err.status` on failure and JSON `{ error }` parsing
    - Spanish error tone matching React
  </action>
  <verify>
    <automated>cd dashboard-vue && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - api.js exports API_BASE_URL and fetchJson
    - No axios
  </acceptance_criteria>
  <done>HTTP layer ready</done>
</task>

<task type="auto">
  <name>Task 3: App.vue initial load shell (FRWK-05 partial)</name>
  <read_first>dashboard-react/src/App.jsx loadDashboardData</read_first>
  <files>dashboard-vue/src/App.vue</files>
  <action>
    Implement `App.vue` with `<script setup>` and explicit `ref()` (D-08):

    - `onMounted` → `loadDashboardData()` with `Promise.all` on `/health`, `/`, `/users`
    - Loading disables reload / shows checking connection
    - On success: inline template sections for connection, health, API info, users (components in 16-02)
    - On failure: error panel + cleared placeholders (D-18)
    - Show `API_BASE_URL` in toolbar
    - Spanish copy from `dashboard/index.html`; optional one line noting Vue variant (D-03)

    CRUD may be stubs; full parity in 16-02.
  </action>
  <verify>
    <automated>cd dashboard-vue && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Build passes; triple fetch on load implemented
  </acceptance_criteria>
  <done>FRWK-05 load path implemented</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | VITE_* env | accept | Lab-only; .env.example |
</threat_model>

<verification>
- `cd dashboard-vue && npm run build`
</verification>

<success_criteria>
- FRWK-04 and FRWK-08 scaffold satisfied
- FRWK-05 load logic present (CRUD in 16-02)
</success_criteria>

<output>
After completion, create `.planning/phases/16-vue-dashboard-parity/16-01-SUMMARY.md`
</output>
