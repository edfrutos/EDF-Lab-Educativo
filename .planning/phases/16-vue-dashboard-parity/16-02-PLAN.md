---
phase: 16-vue-dashboard-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - dashboard-vue/src/App.vue
  - dashboard-vue/src/components/ConnectionStatus.vue
  - dashboard-vue/src/components/HealthCard.vue
  - dashboard-vue/src/components/ApiInfoCard.vue
  - dashboard-vue/src/components/UsersTable.vue
  - dashboard-vue/src/components/UserForm.vue
autonomous: true
requirements:
  - FRWK-05
must_haves:
  truths:
    - "D-07: Five .vue components mirror React names"
    - "D-08-D-09: ref() state in App.vue, props/emits to children"
    - "D-13: Reload after successful mutations"
    - "D-14: Edit/Cancel flow matches vanilla/React"
    - "D-17: 409 dual feedback global + inline email"
    - "FRWK-05: Same HTTP methods and JSON bodies as vanilla/React"
  artifacts:
    - path: dashboard-vue/src/components/UserForm.vue
      provides: "Create/edit form"
      contains: "submit"
    - path: dashboard-vue/src/components/UsersTable.vue
      provides: "Row actions"
      contains: "Eliminar"
  key_links:
    - from: dashboard-vue/src/App.vue
      to: dashboard-react/src/App.jsx
      via: "behavioral parity"
      pattern: "loadDashboardData"
---

<objective>
Extract SFC components and implement full CRUD parity with React/vanilla, including dual 409 feedback.

Purpose: FRWK-05 complete.
Output: Feature-complete Vue dashboard (except UAT/README polish in 16-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-vue-dashboard-parity/16-CONTEXT.md
@.planning/phases/16-vue-dashboard-parity/16-PATTERNS.md
@dashboard-react/src/App.jsx
@dashboard-react/src/components/*
@dashboard/app.js
@dashboard/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Vue SFC components (D-07, D-09)</name>
  <read_first>dashboard-react/src/components/, 16-PATTERNS.md</read_first>
  <files>dashboard-vue/src/components/*.vue, dashboard-vue/src/App.vue</files>
  <action>
    Create under `src/components/` with `<script setup>` + `defineProps` / `defineEmits`:

    - `ConnectionStatus.vue` — `isOnline`, `connectionText`
    - `HealthCard.vue` — status, timestamp
    - `ApiInfoCard.vue` — message, version, endpoints list
    - `UsersTable.vue` — users array, `isFormBusy`; emit `edit`, `delete`
    - `UserForm.vue` — props for name, email, mode, feedback, emailFieldError, busy; emit `submit`, `cancel`, `update:name`, `update:email`

    Refactor `App.vue` to own all `ref()` state and pass props / handle emits.
    Tailwind layout matches `dashboard-react/` (D-02).
    Spanish strings from `dashboard/index.html` (D-03).
  </action>
  <verify>
    <automated>cd dashboard-vue && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Five component files exist
    - App.vue orchestrates via props/emits
  </acceptance_criteria>
  <done>Component architecture complete</done>
</task>

<task type="auto">
  <name>Task 2: CRUD + edit mode (FRWK-05)</name>
  <read_first>dashboard-react/src/App.jsx, dashboard/app.js</read_first>
  <files>dashboard-vue/src/App.vue, dashboard-vue/src/components/*.vue</files>
  <action>
    Implement in App.vue:

    - POST `/users`, PUT `/users/:id`, DELETE `/users/:id` via `fetchJson`
    - `editingUserId` ref; Edit fills form; Cancel resets
    - Success strings (D-20): POST/PUT/DELETE messages matching React
    - `confirm()` before DELETE (D-19)
    - After success: `resetUserForm` + `loadDashboardData()` (D-13)
    - Empty users: "No hay usuarios disponibles."

    Use `fetchJson` from `src/api.js` only.
  </action>
  <verify>
    <automated>cd dashboard-vue && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All three mutation types work against running API
  </acceptance_criteria>
  <done>CRUD complete</done>
</task>

<task type="auto">
  <name>Task 3: Error handling — 409 dual feedback (D-17)</name>
  <read_first>dashboard-react/src/App.jsx handleMutationError</read_first>
  <files>dashboard-vue/src/App.vue, dashboard-vue/src/components/UserForm.vue</files>
  <action>
    On mutation catch:

    - Global mutation feedback with `getMutationErrorMessage` style message
    - When `error.status === 409`, set inline email field error (red under input)
    - Clear inline error on successful submit or `resetUserForm`

    Initial load failure: `loadError` panel + offline connection state (D-18).
  </action>
  <verify>
    <automated>cd dashboard-vue && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Code branches on status 409 for inline email hint
  </acceptance_criteria>
  <done>D-17 error UX complete</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | Spoofing | API URL env | accept | Lab localhost default |
</threat_model>

<verification>
Manual after execute:
- Create, edit, delete user on :5175
- Duplicate email → 409 with both feedbacks
- Stop API → offline panel
</verification>

<success_criteria>
- FRWK-05 satisfied in code
</success_criteria>

<output>
After completion, create `.planning/phases/16-vue-dashboard-parity/16-02-SUMMARY.md`
</output>
