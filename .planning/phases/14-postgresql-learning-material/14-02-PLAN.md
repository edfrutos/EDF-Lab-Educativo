---
phase: 14-postgresql-learning-material
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - missions/12-postgres-compose-crud.md
  - missions/11-arrancar-con-compose.md
  - docs/00-indice.md
  - README.md
  - api/README.md
  - NOTEBOOK.md
autonomous: true
requirements:
  - PGDOCS-02
  - PGDOCS-04
  - PGDOCS-05
must_haves:
  truths:
    - "D-06: Mission 12 covers compose up, CRUD, restart, Postgres persistence via psql"
    - "D-07: Mission 11 notes Mission 12 for Postgres stack"
    - "D-04: Index, README, api/README list Postgres path and doc 15 / Mission 12"
    - "D-08: NOTEBOOK has Postgres integration errors section"
    - "D-04: docs/00-indice.md, README.md, api/README.md list doc 15 and Mission 12"
    - "D-09: No dashboard or API code changes"
    - "D-10: No production deployment content"
  artifacts:
    - path: missions/12-postgres-compose-crud.md
      provides: "Guided Postgres Compose mission"
      contains: "compose:up"
    - path: docs/00-indice.md
      provides: "Index entry for doc 15 and Mission 12"
      contains: "15-postgresql"
    - path: NOTEBOOK.md
      provides: "Postgres error catalog"
      contains: "PostgreSQL"
  key_links:
    - from: missions/12-postgres-compose-crud.md
      to: docs/15-postgresql.md
      via: "Lectura complementaria"
      pattern: "15-postgresql"
---

<objective>
Create Mission 12, update index/README/api README, Mission 11 pointer, and NOTEBOOK Postgres errors.

Purpose: PGDOCS-02, PGDOCS-04, PGDOCS-05 — close v1.3 learning material.
Output: Mission 12, updated navigation docs, NOTEBOOK section
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-postgresql-learning-material/14-CONTEXT.md
@.planning/phases/14-postgresql-learning-material/14-RESEARCH.md
@missions/11-arrancar-con-compose.md
@docs/15-postgresql.md
@NOTEBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create missions/12-postgres-compose-crud.md (PGDOCS-02)</name>
  <read_first>missions/11-arrancar-con-compose.md, docs/15-postgresql.md, docker-compose.yml, 14-RESEARCH.md</read_first>
  <files>missions/12-postgres-compose-crud.md</files>
  <action>
    Create `missions/12-postgres-compose-crud.md` with lab template: Objetivo, Pasos, Resultado esperado, Reto extra.

    **Objetivo:** Arrancar stack Compose con Postgres, CRUD desde dashboard, verificar datos con `psql`, reiniciar stack y confirmar persistencia en volumen Postgres.

    **Pasos (minimum):**
    1. `npm run compose:down`; liberar 3100/5173/5432 si hace falta
    2. `npm run compose:up` — esperar postgres healthy + API + dashboard
    3. Abrir http://localhost:5173 — API conectada
    4. Crear usuario (dashboard o curl POST)
    5. Verificar con psql:
       ```bash
       psql postgresql://edf_lab:edf_lab_dev@localhost:5432/edf_lab \
         -c "SELECT id, name, email FROM users WHERE email='...';"
       ```
    6. `npm run compose:down && npm run compose:up`
    7. `curl -s http://localhost:3100/users` — usuario sigue
    8. Opcional: `docker compose ps` — tres servicios

    **Resultado esperado:** Usuario del paso 4 sobrevive paso 6; API log muestra `[db] Using PostgreSQL`.

    **Reto extra:** `npm run test:db:prepare` + `cd api && npm run test:pg` con Postgres en marcha.

    **Lectura complementaria:** link `docs/15-postgresql.md`
  </action>
  <verify>
    <automated>test -f missions/12-postgres-compose-crud.md && grep -q "compose:up" missions/12-postgres-compose-crud.md && grep -q "psql" missions/12-postgres-compose-crud.md && grep -q "15-postgresql" missions/12-postgres-compose-crud.md</automated>
  </verify>
  <acceptance_criteria>
    - Mission 12 has objetivo, pasos, resultado, reto
    - Covers CRUD, restart, Postgres persistence (not sqlite3 as primary proof)
  </acceptance_criteria>
  <done>PGDOCS-02 mission ready</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/00-indice.md and README files (PGDOCS-04)</name>
  <read_first>docs/00-indice.md, README.md, api/README.md, docs/15-postgresql.md</read_first>
  <files>docs/00-indice.md, README.md, api/README.md</files>
  <action>
    **docs/00-indice.md:**
    - Add to reading order after item 14: `15-postgresql.md` (v1.3, optional/advanced)
    - Add document entry #15 with description
    - Under Misiones: add `missions/12-postgres-compose-crud.md` (avanzada, Postgres + Compose)
    - Keep Mission 11 listed; cross-note optional

    **README.md:**
    - Extend «Opcional: Docker Compose» section: stack now includes **PostgreSQL**; link `docs/15-postgresql.md` and Mission 12
    - Mention `docker compose up` uses Postgres for API data; host `npm start` still SQLite
    - Add `npm run test:db:prepare` if not present in test context

    **api/README.md:**
    - Add link to `docs/15-postgresql.md` near database / Postgres mentions
    - Brief: Compose sets DATABASE_URL; host dev without env uses SQLite
  </action>
  <verify>
    <automated>grep -q "15-postgresql" docs/00-indice.md && grep -q "12-postgres" docs/00-indice.md && grep -q "15-postgresql\|PostgreSQL" README.md && grep -q "15-postgresql" api/README.md</automated>
  </verify>
  <acceptance_criteria>
    - PGDOCS-04: index, README, api/README reference Postgres path
    - Mission 12 discoverable from index
  </acceptance_criteria>
  <done>Navigation docs updated</done>
</task>

<task type="auto">
  <name>Task 3: Update Mission 11 + NOTEBOOK (PGDOCS-05, D-07)</name>
  <read_first>missions/11-arrancar-con-compose.md, NOTEBOOK.md, 14-RESEARCH.md NOTEBOOK candidates</read_first>
  <files>missions/11-arrancar-con-compose.md, NOTEBOOK.md</files>
  <action>
    **missions/11-arrancar-con-compose.md** — add short note after Objetivo or at end:
    > **v1.3:** El stack Compose incluye PostgreSQL. Esta misión verifica SQLite en el bind mount. Para persistencia en Postgres y `psql`, usa [Misión 12](12-postgres-compose-crud.md).

    **NOTEBOOK.md** — add section `## 2026-06-01 · PostgreSQL v1.3 — errores de integración` with at least 3 entries:
    1. **Connection refused** — Postgres no arrancado; `docker compose up -d edf-lab-postgres`
    2. **Tests PG fallan** — falta `npm run test:db:prepare` o Postgres down
    3. **DATABASE_URL en shell** — rompe tests SQLite; `delete` / terminal limpio (link index.test.js guard)
    4. Optional: **puerto 5432 ocupado** — Homebrew postgres vs Compose

    Each entry: síntoma, causa, solución, aprendizaje (match NOTEBOOK style).
  </action>
  <verify>
    <automated>grep -q "12-postgres" missions/11-arrancar-con-compose.md && grep -q "PostgreSQL v1.3" NOTEBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - PGDOCS-05: NOTEBOOK documents real Postgres integration issues
    - Mission 11 points to Mission 12
  </acceptance_criteria>
  <done>PGDOCS-05 and cross-mission pointers complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs/missions only | No code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Information disclosure | Credentials in mission | accept | Same lab credentials as compose |
</threat_model>

<verification>
- All grep checks pass
- Optional: run Mission 12 steps manually if Docker available
</verification>

<success_criteria>
- PGDOCS-02, PGDOCS-04, PGDOCS-05 satisfied
- ROADMAP Phase 14 success criteria 1–5 addressable
</success_criteria>

<output>
After completion, create `.planning/phases/14-postgresql-learning-material/14-02-SUMMARY.md`
</output>
