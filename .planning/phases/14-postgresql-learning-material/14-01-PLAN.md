---
phase: 14-postgresql-learning-material
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/15-postgresql.md
  - docs/14-docker-compose.md
  - docs/13-sqlite.md
  - docs/09-glosario.md
autonomous: true
requirements:
  - PGDOCS-01
  - PGDOCS-03
must_haves:
  truths:
    - "D-01: docs/15-postgresql.md explains connection string, schema, queries with executable examples"
    - "D-02: docs/14-docker-compose.md documents edf-lab-postgres and DATABASE_URL"
    - "D-03: docs/13-sqlite.md links prominently to doc 15 from Hacia PostgreSQL"
    - "D-05: Glossary includes PostgreSQL and DATABASE_URL entries"
    - "D-04: Partial PGDOCS-04 — glossary only; index/README in plan 14-02"
    - "D-09: No code changes in api/ or dashboard/"
    - "D-10: No production Postgres docs"
  artifacts:
    - path: docs/15-postgresql.md
      provides: "Primary PostgreSQL learning doc"
      contains: "DATABASE_URL"
    - path: docs/14-docker-compose.md
      provides: "Three-service Compose stack"
      contains: "edf-lab-postgres"
  key_links:
    - from: docs/15-postgresql.md
      to: api/schema.pg.sql
      via: "schema excerpt"
      pattern: "SERIAL PRIMARY KEY"
---

<objective>
Create `docs/15-postgresql.md`, update Compose doc for Postgres service, strengthen SQLite→Postgres pointer, extend glossary.

Purpose: PGDOCS-01, PGDOCS-03 (partial PGDOCS-04 in plan 14-02).
Output: doc 15, updated docs 14, 13, 09
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-postgresql-learning-material/14-CONTEXT.md
@.planning/phases/14-postgresql-learning-material/14-RESEARCH.md
@docs/13-sqlite.md
@docs/14-docker-compose.md
@docker-compose.yml
@api/db-pg.js
@api/seed.js
@api/schema.pg.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/15-postgresql.md (PGDOCS-01)</name>
  <read_first>docs/13-sqlite.md, api/schema.pg.sql, api/db-pg.js, api/seed.js, docker-compose.yml, 14-RESEARCH.md</read_first>
  <files>docs/15-postgresql.md</files>
  <action>
    Create `docs/15-postgresql.md` in Spanish, tone matching doc 13.

    Required sections:
    1. **Intro** — Postgres cliente-servidor; activado con `DATABASE_URL`; SQLite sigue siendo default sin env
    2. **Cadena de conexión** — ejemplo Compose URL from docker-compose.yml; variables POSTGRES_* (ficticias de lab)
    3. **Esquema** — cite `api/schema.pg.sql` (SERIAL, UNIQUE email); contrast with `schema.sql`
    4. **Código en la API** — `db.js` router, `db-pg.js` Pool, `seed.js` populate + setval (brief, no full file dump)
    5. **Inspección con psql** — executable blocks:
       ```bash
       psql postgresql://edf_lab:edf_lab_dev@localhost:5432/edf_lab -c "SELECT id, name, email FROM users;"
       ```
    6. **Arranque con Compose** — `npm run compose:up`; three services; link doc 14
    7. **Persistencia** — named volume `postgres_data`; down/up survives data (contrast bind mount SQLite file)
    8. **Tests** — `npm run test:db:prepare`, `npm test` (32 tests), link doc 13 Hacia PostgreSQL
    9. **Enlaces** — Mission 12, doc 13, doc 14, api/README

    Use only real service names and credentials from docker-compose.yml.
  </action>
  <verify>
    <automated>test -f docs/15-postgresql.md && grep -q "DATABASE_URL" docs/15-postgresql.md && grep -q "schema.pg.sql" docs/15-postgresql.md && grep -q "psql" docs/15-postgresql.md</automated>
  </verify>
  <acceptance_criteria>
    - doc 15 exists with at least 4 executable command blocks
    - Explains connection, schema, and queries for this lab
    - No ORM or production cloud content
  </acceptance_criteria>
  <done>PGDOCS-01 primary doc complete</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/14-docker-compose.md for Postgres (D-02)</name>
  <read_first>docker-compose.yml (full), docs/14-docker-compose.md, 14-CONTEXT.md D-02</read_first>
  <files>docs/14-docker-compose.md</files>
  <action>
    Update `docs/14-docker-compose.md` to reflect **v1.3 three-service stack**:

    1. **Architecture ASCII** — add `edf-lab-postgres` between API and data store; show DATABASE_URL; keep dashboard flow
    2. **Comparison table** — row for Postgres service vs 2-service v1.2
    3. **YAML walkthrough** — include `edf-lab-postgres`, `postgres_data` volume, API `environment: DATABASE_URL`, `depends_on` + healthcheck (snippets from live docker-compose.yml)
    4. **Volúmenes section** — dual: bind mount `./api/data` (users.json) AND named volume `postgres_data` for Postgres data
    5. **Persistence examples** — add psql verification path alongside optional sqlite3 note for users.json only
    6. **Executable flow** — note `[db] Using PostgreSQL` in API logs when using compose

    Link to `docs/15-postgresql.md` for Postgres deep dive.
    Do not remove Mission 11 reference — add note that Mission 12 covers Postgres path.
  </action>
  <verify>
    <automated>grep -q "edf-lab-postgres" docs/14-docker-compose.md && grep -q "postgres_data" docs/14-docker-compose.md && grep -q "DATABASE_URL" docs/14-docker-compose.md</automated>
  </verify>
  <acceptance_criteria>
    - Doc 14 describes 3 services accurately
    - No claim that Compose runtime uses users.db as primary store
    - Links to doc 15
  </acceptance_criteria>
  <done>Compose doc aligned with v1.3 stack</done>
</task>

<task type="auto">
  <name>Task 3: PGDOCS-03 pointer in docs/13-sqlite.md + glossary</name>
  <read_first>docs/13-sqlite.md (Hacia PostgreSQL section), docs/09-glosario.md</read_first>
  <files>docs/13-sqlite.md, docs/09-glosario.md</files>
  <action>
    In `docs/13-sqlite.md` section «Hacia PostgreSQL»:
    - Add prominent callout at top: **Capítulo completo:** [`15-postgresql.md`](./15-postgresql.md)
    - Replace teaser line about Fase 14 with link to doc 15 and Mission 12

    In `docs/09-glosario.md`, add or update entries (Spanish):
    - **PostgreSQL** — servidor relacional cliente-servidor
    - **DATABASE_URL** — variable que selecciona Postgres en este lab
    - **Volumen nombrado (Docker)** — persistencia Postgres en Compose (`postgres_data`)

    Cross-link doc 15 from glossary entries.
  </action>
  <verify>
    <automated>grep -q "15-postgresql" docs/13-sqlite.md && grep -q "PostgreSQL" docs/09-glosario.md</automated>
  </verify>
  <acceptance_criteria>
    - PGDOCS-03: clear evolution path SQLite doc → doc 15
    - Glossary has Postgres-related terms
  </acceptance_criteria>
  <done>Cross-links and glossary updated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs only | No runtime change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information disclosure | Lab credentials in docs | accept | Fictional credentials already in compose |
</threat_model>

<verification>
- grep checks on doc 15 and doc 14
- Manual read: links resolve
</verification>

<success_criteria>
- PGDOCS-01 and PGDOCS-03 satisfied
- Learner can read Postgres concepts with executable examples
</success_criteria>

<output>
After completion, create `.planning/phases/14-postgresql-learning-material/14-01-SUMMARY.md`
</output>
