---
phase: 30-crud-e2e-vanilla
plan: 02
type: execute
wave: 2
depends_on:
  - "30-01"
files_modified:
  - e2e/playwright.config.js
  - docs/10-tests.md
autonomous: true
requirements:
  - QA-ADV-01
user_setup: []
must_haves:
  truths:
    - "npm run test:e2e ejecuta smoke auth (3) + CRUD vanilla (1) sin fallos"
    - "Proyecto vanilla de Playwright incluye crud.vanilla.spec.js"
    - "docs/10-tests.md documenta el flujo CRUD E2E y el helper crud-flow"
  artifacts:
    - path: e2e/playwright.config.js
      provides: "testMatch vanilla incluye auth-smoke y crud"
      contains: "crud"
    - path: docs/10-tests.md
      provides: "Sección CRUD E2E vanilla"
      contains: "crud-flow"
  key_links:
    - from: e2e/playwright.config.js
      to: e2e/tests/crud.vanilla.spec.js
      via: "projects.vanilla.testMatch regex"
      pattern: "crud\\.vanilla"
    - from: docs/10-tests.md
      to: e2e/helpers/crud-flow.js
      via: "referencia al helper y comando filtrado"
      pattern: "crud-flow"
---

<objective>
Cablear el spec CRUD en la configuración Playwright, documentar el flujo en docs/10-tests.md y verificar que la suite E2E completa pasa localmente.

Purpose: Integrar QA-ADV-01 en el comando estándar `npm run test:e2e` y dejar traza didáctica para el alumno.
Output: Config actualizado, docs con borrador CRUD, 4 tests verdes.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-crud-e2e-vanilla/30-RESEARCH.md
@.planning/phases/30-crud-e2e-vanilla/30-01-SUMMARY.md
@e2e/playwright.config.js
@docs/10-tests.md

<interfaces>
Config actual — proyecto vanilla:

```javascript
{
  name: 'vanilla',
  testMatch: /auth-smoke\.vanilla\.spec\.js/,
  use: { baseURL: 'http://localhost:5173' }
}
```

Target testMatch:

```javascript
testMatch: /(auth-smoke|crud)\.vanilla\.spec\.js/,
```

Scripts raíz (`package.json`): `test:e2e`, `test:e2e:ui`, `test:e2e:headed`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ampliar testMatch del proyecto vanilla</name>
  <read_first>
    e2e/playwright.config.js
    e2e/tests/crud.vanilla.spec.js
  </read_first>
  <files>e2e/playwright.config.js</files>
  <action>
En `e2e/playwright.config.js`, proyecto `vanilla`, cambiar `testMatch` de:

`/auth-smoke\.vanilla\.spec\.js/`

a:

`/(auth-smoke|crud)\.vanilla\.spec\.js/`

**No modificar** proyectos `react` ni `vue` (CRUD multi-dashboard es fase 31).

**No cambiar** `webServer`, `apiEnv`, `workers` ni `retries` — quad server ya funciona desde v2.0.

Añadir comentario breve encima del testMatch vanilla explicando que incluye smoke auth + CRUD (español, una línea).
  </action>
  <verify>
    <automated>node --check e2e/playwright.config.js</automated>
  </verify>
  <acceptance_criteria>
    - Regex incluye auth-smoke.vanilla y crud.vanilla
    - React/Vue testMatch sin cambios
    - Sintaxis del config válida
  </acceptance_criteria>
  <done>Playwright descubre ambos specs vanilla al ejecutar proyecto vanilla</done>
</task>

<task type="auto">
  <name>Task 2: Sección CRUD en docs/10-tests.md</name>
  <read_first>
    docs/10-tests.md (sección Smoke E2E existente)
    e2e/helpers/crud-flow.js
    .planning/phases/30-crud-e2e-vanilla/30-RESEARCH.md
  </read_first>
  <files>docs/10-tests.md</files>
  <action>
Añadir subsección **«CRUD E2E (vanilla)»** dentro de «Smoke E2E (Playwright)» en `docs/10-tests.md` (después del bloque de ejecución, antes de «Auth en tests API vs E2E» o en punto lógico equivalente).

Contenido mínimo (español, didáctico):

1. **Qué prueba:** login UI → crear usuario con email único → editar nombre/email → eliminar con confirmación.
2. **Helper:** `e2e/helpers/crud-flow.js` — `runCrudFlow`, `buildCrudTestUser`; reutilizable en fase 31 para React/Vue.
3. **Spec:** `e2e/tests/crud.vanilla.spec.js`.
4. **Ejecutar solo CRUD:**
   ```bash
   npx playwright test crud.vanilla --config=e2e/playwright.config.js
   ```
5. **Notas:** email único por run (`@lab.local`); manejo de `confirm()` en delete; selectores `#login-email` vs `#user-email-input` (enlace mental a NOTEBOOK).
6. Actualizar tabla «Scripts útiles» si dice solo «Smoke auth» — indicar «Smoke auth + CRUD vanilla (4 tests)».

No expandir a Postgres E2E ni multi-browser (fases 32–33 / DOCS-01 completo).
  </action>
  <verify>
    <automated>grep -q "crud-flow" docs/10-tests.md && grep -q "CRUD E2E" docs/10-tests.md</automated>
  </verify>
  <acceptance_criteria>
    - Sección CRUD E2E presente en docs/10-tests.md
    - Referencia explícita a crud-flow.js y crud.vanilla.spec.js
    - Comando para ejecutar solo spec CRUD documentado
  </acceptance_criteria>
  <done>Documentación didáctica mínima del flujo CRUD publicada</done>
</task>

<task type="auto">
  <name>Task 3: Verificar suite E2E completa</name>
  <read_first>
    package.json (script test:e2e)
    e2e/playwright.config.js
  </read_first>
  <files></files>
  <action>
Desde la raíz del repo, ejecutar la suite E2E completa:

```bash
npm run test:e2e
```

**Precondiciones:** Chromium instalado (`npm run playwright:install` si falla). Cerrar procesos en :3100/:5173/:5174/:5175 si `reuseExistingServer` no puede reutilizar.

**Resultado esperado:** 4 tests passed — 1 CRUD vanilla + 1 auth vanilla + 1 auth react + 1 auth vue.

Si falla por estado BD: borrar `api/data/e2e.users.db` y reintentar una vez.

Si falla por puertos: documentar en SUMMARY (no bloquear commit si entorno sandbox sin red/browser — en ese caso ejecutar con permisos y reportar output real).

No modificar código de producción para hacer pasar tests; arreglar solo E2E si selector incorrecto.
  </action>
  <verify>
    <automated>npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - 4/4 tests Playwright pasan
    - CRUD spec completa create→edit→delete
    - Sin regresión en los 3 smoke auth
  </acceptance_criteria>
  <done>QA-ADV-01 verificado de extremo a extremo vía npm run test:e2e</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/local → npm run test:e2e | Comando público del lab; debe ser determinista |
| docs → alumno | Instrucciones no deben filtrar secretos reales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | D | Suite E2E 4 tests | mitigate | workers:1 en CI ya configurado; emails únicos en CRUD |
| T-30-05 | I | docs/10-tests.md | accept | Solo credenciales E2E de ejemplo ya documentadas |
</threat_model>

<verification>
- `grep crud-flow docs/10-tests.md` encuentra referencia
- `npm run test:e2e` → 4 passed
- `npx playwright test crud.vanilla --config=e2e/playwright.config.js` → 1 passed
</verification>

<success_criteria>
- Config vanilla incluye spec CRUD
- Docs actualizados con borrador CRUD
- Suite completa verde localmente
</success_criteria>

<output>
After completion, create `.planning/phases/30-crud-e2e-vanilla/30-02-SUMMARY.md`
</output>
