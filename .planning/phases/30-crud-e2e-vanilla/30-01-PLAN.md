---
phase: 30-crud-e2e-vanilla
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/helpers/crud-flow.js
  - e2e/tests/crud.vanilla.spec.js
autonomous: true
requirements:
  - QA-ADV-01
user_setup: []
must_haves:
  truths:
    - "Tras login UI, el spec crea un usuario con email único y lo ve en la tabla"
    - "El spec edita nombre y email y verifica el cambio en la UI"
    - "El spec elimina el usuario y la fila desaparece de #users-table-body"
    - "El helper crud-flow exporta runCrudFlow y buildCrudTestUser reutilizables"
  artifacts:
    - path: e2e/helpers/crud-flow.js
      provides: "Flujo CRUD login→create→edit→delete con email único"
      exports: ["runCrudFlow", "buildCrudTestUser"]
    - path: e2e/tests/crud.vanilla.spec.js
      provides: "Spec Playwright vanilla que ejecuta el helper"
      contains: "runCrudFlow"
  key_links:
    - from: e2e/tests/crud.vanilla.spec.js
      to: e2e/helpers/crud-flow.js
      via: "require('../helpers/crud-flow.js')"
      pattern: "runCrudFlow"
    - from: e2e/helpers/crud-flow.js
      to: dashboard form/table
      via: "locators #user-name-input, #users-table-body, data-action buttons"
      pattern: "#user-email-input|data-action=\"delete\""
    - from: e2e/helpers/crud-flow.js
      to: confirm dialog
      via: "page.once('dialog', accept) antes de Eliminar"
      pattern: "dialog\\.accept"
---

<objective>
Implementar el helper `crud-flow.js` y el spec `crud.vanilla.spec.js` que ejecutan un ciclo CRUD completo en el dashboard vanilla tras login UI, con datos únicos por ejecución.

Purpose: Cumplir el núcleo de QA-ADV-01 y sentar el contrato reutilizable para React/Vue en fase 31.
Output: Helper documentado + spec vanilla listo para cablear en playwright.config (plan 02).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-crud-e2e-vanilla/30-RESEARCH.md
@e2e/helpers/auth-smoke-flow.js
@e2e/tests/auth-smoke.vanilla.spec.js
@dashboard/app.js

<interfaces>
Patrón a espejar desde `auth-smoke-flow.js`:

```javascript
async function runAuthSmokeFlow(page, { email, password }) { ... }
module.exports = { runAuthSmokeFlow };
```

Selectores CRUD en vanilla (`dashboard/app.js`):
- Form: `#user-name-input`, `#user-email-input`, `#user-submit-button` («Crear usuario» / «Guardar cambios»)
- Tabla: `#users-table-body` — filas con botones `data-action="edit"|"delete"`, texto «Editar»/«Eliminar»
- Delete: `confirm('¿Seguro que quieres eliminar este usuario?')` — requiere `page.once('dialog', d => d.accept())`

Anti-patrón NOTEBOOK: no usar `getByLabel('Email')` sin acotar — usar `#login-email` y `#user-email-input`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper crud-flow con datos únicos y flujo completo</name>
  <read_first>
    e2e/helpers/auth-smoke-flow.js
    .planning/phases/30-crud-e2e-vanilla/30-RESEARCH.md
    dashboard/app.js (handleUserFormSubmit, handleUsersTableClick, deleteUser)
    NOTEBOOK.md — entrada getByLabel('Email')
  </read_first>
  <files>e2e/helpers/crud-flow.js</files>
  <action>
Crear `e2e/helpers/crud-flow.js` en `'use strict'` siguiendo el estilo de `auth-smoke-flow.js`.

**Exportar dos funciones:**

1. `buildCrudTestUser()` — genera objeto con `name`, `email`, `editedName`, `editedEmail` usando sufijo único (`Date.now()` + fragmento aleatorio). Dominio `@lab.local` coherente con seeds E2E.

2. `runCrudFlow(page, { operatorEmail, operatorPassword, user })` — si `user` omitido, llamar `buildCrudTestUser()`.

**Pasos internos (en orden):**

- **Login:** `#login-email`, `#login-password`, botón «Entrar»; assert `Cerrar sesión` visible y tabla visible (reutilizar lógica de auth-smoke sin logout).
- **Create:** fill `#user-name-input` / `#user-email-input`; click `#user-submit-button`; `expect` fila en `#users-table-body` con `user.email` y `user.name`.
- **Edit:** localizar fila `page.locator('#users-table-body tr').filter({ hasText: user.email })`; click botón «Editar» (`data-action="edit"`); assert `#user-submit-button` tiene texto «Guardar cambios»; limpiar y rellenar con `editedName` / `editedEmail`; submit; assert fila con email editado visible y email original ausente en tbody.
- **Delete:** `page.once('dialog', dialog => dialog.accept())` **antes** del click; click «Eliminar» en fila del email editado; assert fila con `editedEmail` no visible (`toHaveCount(0)` o `not.toBeVisible()`).

**Documentación en cabecera JSDoc (español):** propósito, parámetros, email único, manejo de `confirm()`, ejemplo de import para fase 31.

No añadir dependencias npm. No modificar dashboard ni API.
  </action>
  <verify>
    <automated>node --check e2e/helpers/crud-flow.js</automated>
  </verify>
  <acceptance_criteria>
    - Archivo exporta `runCrudFlow` y `buildCrudTestUser`
    - Usa selectores por ID para emails (no getByLabel global)
    - Maneja diálogo confirm antes de delete
    - JSDoc describe reutilización y email único
  </acceptance_criteria>
  <done>Helper sintácticamente válido con flujo CRUD completo documentado y listo para importar desde specs</done>
</task>

<task type="auto">
  <name>Task 2: Spec crud.vanilla.spec.js</name>
  <read_first>
    e2e/tests/auth-smoke.vanilla.spec.js
    e2e/helpers/crud-flow.js (creado en task 1)
  </read_first>
  <files>e2e/tests/crud.vanilla.spec.js</files>
  <action>
Crear `e2e/tests/crud.vanilla.spec.js` paralelo al smoke auth:

```javascript
const { test } = require('@playwright/test');
const { runCrudFlow } = require('../helpers/crud-flow.js');

const operatorEmail = process.env.E2E_OPERATOR_EMAIL || 'admin@lab.local';
const operatorPassword = process.env.E2E_OPERATOR_PASSWORD || 'changeme';

test.describe('CRUD — dashboard vanilla', () => {
  test('login → crear → editar → eliminar usuario único', async ({ page }) => {
    await page.goto('/');
    await runCrudFlow(page, { operatorEmail, operatorPassword });
  });
});
```

`test.describe` y título del test en español, alineados con labels UI.

No incluir logout al final (CRUD flow termina tras delete; smoke auth cubre logout por separado).

Para verificación local en este plan (antes de plan 02 config): ejecutar spec directamente con CLI Playwright apuntando al archivo — el proyecto vanilla del config aún no incluye este spec hasta plan 02.
  </action>
  <verify>
    <automated>node --check e2e/tests/crud.vanilla.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - Spec importa runCrudFlow desde ../helpers/crud-flow.js
    - Usa credenciales E2E_OPERATOR_* con mismos defaults que smoke auth
    - Un solo test cubre create → edit → delete
    - describe/test en español
  </acceptance_criteria>
  <done>Spec vanilla creado; sintaxis válida; listo para testMatch en plan 02</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright → dashboard :5173 | Input automatizado en formularios; no confiar en UI para seguridad |
| Playwright → API :3100 | Mutaciones CRUD con cookie de sesión operador |
| confirm() nativo | Diálogo del navegador; test debe accept explícitamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | I | runCrudFlow delete | mitigate | `page.once('dialog', accept)` antes de click Eliminar |
| T-30-02 | D | e2e.users.db | accept | BD aislada E2E; emails únicos evitan colisiones |
| T-30-03 | S | Credenciales E2E | accept | Defaults locales solo; CI usa secrets si configurados |
</threat_model>

<verification>
- `node --check` pasa en helper y spec
- Tras plan 02: `npx playwright test crud.vanilla --config=e2e/playwright.config.js` pasa con quad webServer
</verification>

<success_criteria>
- Helper y spec existen con contrato exportado documentado
- Flujo cubre login + create + edit + delete con email único
- Selectores evitan colisión Email documentada en NOTEBOOK
</success_criteria>

<output>
After completion, create `.planning/phases/30-crud-e2e-vanilla/30-01-SUMMARY.md`
</output>
