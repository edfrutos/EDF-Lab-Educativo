---
phase: 10-sqlite-volume-scripts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
autonomous: true
requirements:
  - VOL-01
must_haves:
  truths:
    - "Learner finds bind mount ./api/data:/usr/src/app/data in docker-compose.yml"
    - "Users created via Compose survive docker compose down && up"
    - "Cold start on empty api/data still migrates from users.json"
  artifacts:
    - path: docker-compose.yml
      provides: "SQLite bind mount on API service"
      contains: "api/data"
  key_links:
    - from: docker-compose.yml
      to: api/data/users.db
      via: "bind mount on edf-lab-api"
      pattern: "./api/data"
---

<objective>
Add bind mount persistence for SQLite on the API Compose service.

Purpose: VOL-01 — users.db survives container restarts.
Output: Updated `docker-compose.yml` with documented volume on `edf-lab-api`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-compose-stack-foundation/09-CONTEXT.md
@.planning/phases/10-sqlite-volume-scripts/10-RESEARCH.md
@docker-compose.yml
@api/db.js
@api/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bind mount to edf-lab-api in docker-compose.yml</name>
  <read_first>10-RESEARCH.md (Finding 2), docker-compose.yml, api/db.js DB_FILE_PATH default, 09-CONTEXT.md D-08 superseded</read_first>
  <files>docker-compose.yml</files>
  <action>
    Under `edf-lab-api` service, add:

    ```yaml
    volumes:
      - ./api/data:/usr/src/app/data
    ```

    Add YAML comments (Spanish, learner-facing) explaining:
    - Host folder `api/data` is shared with the container
    - `users.db` written inside the container appears on the host
    - Dashboard service does NOT need a volume (static files only)

    Do NOT add named volumes or mount on dashboard.
    Do NOT change DB_FILE env unless needed — default path matches mount target.
  </action>
  <verify>
    <automated>grep -q "./api/data:/usr/src/app/data" docker-compose.yml && docker compose config >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Bind mount path is exactly ./api/data:/usr/src/app/data
    - docker compose config validates
    - Comments explain persistence for learners
  </acceptance_criteria>
  <done>Compose file documents and mounts SQLite data directory</done>
</task>

<task type="auto">
  <name>Task 2: Verify persistence behavior manually</name>
  <read_first>10-RESEARCH.md (Risks), api/data/users.json</read_first>
  <files>docker-compose.yml</files>
  <action>
    Run verification sequence (document commands in plan summary, not NOTEBOOK unless errors):

    1. `docker compose down` (no -v)
    2. `docker compose up --build -d`
    3. POST a unique user via curl
    4. `docker compose down && docker compose up -d`
    5. GET /users — unique user still present
    6. Optional cold start: rm api/data/users.db on host, compose up — expect migration log

    If permission errors on bind mount, fix with documented chown note in summary (do not change Dockerfile unless required).
  </action>
  <verify>
    <automated>curl -sf http://localhost:3100/health | grep -q healthy</automated>
  </verify>
  <acceptance_criteria>
    - User survives down/up cycle
    - users.db file exists on host at api/data/users.db after writes
    - Fresh delete of users.db triggers migration on next start
  </acceptance_criteria>
  <done>Persistence verified; notes captured for 10-02 UAT</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host api/data → container | Bind mount exposes host DB file to container |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | Host users.db writable | accept | Local dev lab; fictional data only |
</threat_model>

<verification>
- grep bind mount in docker-compose.yml
- curl persistence test across down/up
</verification>

<success_criteria>
- VOL-01 satisfied: SQLite persists across compose restarts
- ROADMAP criteria 1 and 5 observable
</success_criteria>

<output>
After completion, create `.planning/phases/10-sqlite-volume-scripts/10-01-SUMMARY.md`
</output>
