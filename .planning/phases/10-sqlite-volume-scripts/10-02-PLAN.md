---
phase: 10-sqlite-volume-scripts
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - package.json
  - README.md
  - docs/12-docker.md
  - .planning/phases/10-sqlite-volume-scripts/10-UAT.md
autonomous: false
requirements:
  - VOL-02
  - VOL-03
must_haves:
  truths:
    - "Learner runs npm run compose:up and npm run compose:down from repo root"
    - "docs/12-docker.md contrasts Mission 09 ephemeral docker run vs Compose bind mount"
    - "10-UAT.md confirms persistence + scripts + doc pointers"
  artifacts:
    - path: package.json
      provides: "Root compose helper scripts"
      contains: "compose:up"
    - path: docs/12-docker.md
      provides: "Ephemeral vs volume contrast"
      contains: "Compose"
  key_links:
    - from: README.md
      to: package.json
      via: "npm run compose:up"
      pattern: "compose:up"
---

<objective>
Add root compose helper scripts, VOL-02 documentation contrast, and Phase 10 UAT artifact.

Purpose: VOL-02 + VOL-03 + verification gate.
Output: `package.json`, updated README + docs/12-docker.md, `10-UAT.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-sqlite-volume-scripts/10-RESEARCH.md
@.planning/phases/09-compose-stack-foundation/09-UAT.md
@README.md
@docs/12-docker.md
@missions/09-arrancar-con-docker.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root package.json with compose scripts</name>
  <read_first>10-RESEARCH.md (Finding 4), 09-CONTEXT.md D-14, api/package.json script style</read_first>
  <files>package.json</files>
  <action>
    Create root `package.json` (private lab meta-package, not published):

    - `name`: `edf-lab-educativo`
    - `private`: true
    - `description`: short Spanish/English lab description
    - `scripts`:
      - `compose:up`: `docker compose up --build`
      - `compose:down`: `docker compose down`
      - `compose:logs`: `docker compose logs -f`

    No dependencies. No workspaces. Keep minimal for beginners (VOL-03).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts['compose:up']) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - package.json exists at repo root
    - compose:up, compose:down, compose:logs scripts present
    - private true, no unnecessary deps
  </acceptance_criteria>
  <done>npm run compose:up invocable from root</done>
</task>

<task type="auto">
  <name>Task 2: Update README and docs/12-docker.md (VOL-02)</name>
  <read_first>docs/12-docker.md ephemeral section, README optional Compose section, 10-RESEARCH.md Finding 5</read_first>
  <files>README.md, docs/12-docker.md</files>
  <action>
    **README.md** — In optional Compose subsection, add:
    - `npm run compose:up` / `npm run compose:down` as preferred wrappers
    - One line: SQLite persists via bind mount on `api/data` (see docs/12-docker.md)

    **docs/12-docker.md** — Add section `## Compose con persistencia SQLite` (after ephemeral section):
    - Table contrasting three modes:
      1. `npm start` host — persists to api/data/users.db
      2. `npm run docker:start` single container — ephemeral (Mission 09)
      3. `docker compose up` / `npm run compose:up` — persists via bind mount `./api/data`
    - Show bind mount YAML snippet from docker-compose.yml
    - Link to Mission 09 for single-container lesson
    - Note: full Compose doc coming in Phase 11 (doc 14) — one sentence only

    Fix outdated line claiming users.json not in image (it IS included since Phase 9 for migration) — minimal factual fix only, not full DOCS-03 rewrite.
    Update ephemeral table row for host persistence to mention SQLite users.db not users.json as runtime store.
  </action>
  <verify>
    <automated>grep -q "compose:up" README.md && grep -q "Compose con persistencia" docs/12-docker.md && grep -q "bind mount\|api/data" docs/12-docker.md</automated>
  </verify>
  <acceptance_criteria>
    - VOL-02 contrast documented in docs/12-docker.md
    - README references npm scripts
    - Mission 09 ephemeral path still distinguished from Compose volume path
  </acceptance_criteria>
  <done>Learner can read why Compose persists but single-container docker run does not</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Phase 10 UAT</name>
  <read_first>10-01-SUMMARY.md (if exists), docker-compose.yml volumes, package.json</read_first>
  <files>.planning/phases/10-sqlite-volume-scripts/10-UAT.md</files>
  <action>
    Create and complete `10-UAT.md` with tests:

    1. **Persistence** — create user, compose down/up, user remains (VOL-01)
    2. **Bind mount visible** — learner can grep docker-compose.yml for ./api/data (ROADMAP criterion 2)
    3. **Helper scripts** — npm run compose:down && npm run compose:up works (VOL-03)
    4. **Doc contrast** — docs/12-docker.md section readable (VOL-02)
    5. **Cold start** — rm api/data/users.db, compose up, migration log appears

    Mark status complete when all pass. Reference Phase 9 UAT Test 4 inversion (ephemeral → persistent).
  </action>
  <verify>
    <automated>test -f .planning/phases/10-sqlite-volume-scripts/10-UAT.md && grep -q "status: complete" .planning/phases/10-sqlite-volume-scripts/10-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 10-UAT.md 5/5 pass documented
    - VOL-02 and VOL-03 verified
  </acceptance_criteria>
  <done>Phase 10 UAT complete</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Information disclosure | compose:logs exposes API logs | accept | Local lab; logs contain no secrets |
</threat_model>

<verification>
- npm run compose:up/down from root
- docs/12-docker.md contains contrast section
- 10-UAT.md complete
</verification>

<success_criteria>
- VOL-02, VOL-03 complete
- ROADMAP Phase 10 success criteria 2–4 covered
</success_criteria>

<output>
After completion, create `.planning/phases/10-sqlite-volume-scripts/10-02-SUMMARY.md`
</output>
