---
phase: 41-oauth-dashboard-integration-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "41-01"
files_modified:
  - docs/17-autenticacion.md
  - missions/14-auth-vanilla-login-crud.md
  - missions/15-framework-auth-login-crud.md
  - NOTEBOOK.md
autonomous: true
requirements:
  - AUTH-ADV-01
user_setup: []
must_haves:
  truths:
    - "La documentación diferencia de forma clara login clásico vs OAuth mock (D-05)"
    - "Las misiones auth reproducen el recorrido OAuth mock sin romper el recorrido previo (D-01, D-05)"
    - "Las fricciones reales de integración OAuth quedan registradas en NOTEBOOK (D-06)"
  artifacts:
    - path: docs/17-autenticacion.md
      provides: "Guía operativa para flujo clásico y flujo OAuth mock"
      contains: "oauth"
    - path: missions/14-auth-vanilla-login-crud.md
      provides: "Práctica vanilla con camino OAuth mock visible"
      contains: "OAuth"
    - path: NOTEBOOK.md
      provides: "Trazabilidad de fricción real de fase 41"
      contains: "fase 41"
  key_links:
    - from: docs/17-autenticacion.md
      to: missions/14-auth-vanilla-login-crud.md
      via: "pasos equivalentes entre teoría y práctica"
      pattern: "oauth"
    - from: docs/17-autenticacion.md
      to: missions/15-framework-auth-login-crud.md
      via: "alineación didáctica de frameworks con contrato OAuth mock"
      pattern: "oauth"
---

<objective>
Cerrar la fase 41 con coherencia didáctica: documentación, misiones y aprendizaje real alineados a la integración OAuth mock del dashboard.

Purpose: transformar la integración técnica en un recorrido enseñable y verificable para operador/alumno.
Output: docs y misiones actualizadas + evidencia NOTEBOOK de fricción real de OAuth UI.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-oauth-dashboard-integration-foundation/41-CONTEXT.md
@.planning/phases/41-oauth-dashboard-integration-foundation/41-RESEARCH.md
@.planning/phases/41-oauth-dashboard-integration-foundation/41-01-SUMMARY.md
@docs/17-autenticacion.md
@missions/14-auth-vanilla-login-crud.md
@missions/15-framework-auth-login-crud.md
@NOTEBOOK.md
@docs/10-tests.md

<interfaces>
Bloque mínimo de narrativa esperada:

```md
### Login clásico vs OAuth mock
- cuándo usar cada ruta
- pasos del handoff OAuth mock desde dashboard
- errores frecuentes: state/cookies/callback local
```

Evidencia de validación (per D-07):
- checks API existentes continúan verdes
- verificación guiada de flujo UI OAuth + clásico en misión
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alinear docs auth con la integración OAuth dashboard (D-05)</name>
  <read_first>
    docs/17-autenticacion.md
    .planning/phases/41-oauth-dashboard-integration-foundation/41-CONTEXT.md
    .planning/phases/41-oauth-dashboard-integration-foundation/41-01-SUMMARY.md
  </read_first>
  <files>docs/17-autenticacion.md</files>
  <action>
Actualizar la guía de autenticación para reflejar explícitamente el nuevo entry point OAuth mock en dashboard, distinguiendo su uso frente al login clásico y manteniendo intacto el contrato backend de fase 40 (per D-02, D-05). Incluir sección de continuidad de flujo (login clásico, refresh/password y OAuth mock) sin introducir capacidades fuera de alcance (D-08, D-09, D-10).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const d=fs.readFileSync('docs/17-autenticacion.md','utf8').toLowerCase(); if(!(d.includes('oauth')&&d.includes('login clásico'))) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - docs auth distinguen rutas clásico vs OAuth mock con criterios de uso
    - no se documentan proveedores reales ni account linking
    - continuidad con contrato actual queda explícita
  </acceptance_criteria>
  <done>Documentación auth sincronizada con integración OAuth de fase 41</done>
</task>

<task type="auto">
  <name>Task 2: Sincronizar misiones y registrar fricción real en NOTEBOOK (D-06, D-07)</name>
  <read_first>
    missions/14-auth-vanilla-login-crud.md
    missions/15-framework-auth-login-crud.md
    NOTEBOOK.md
    docs/10-tests.md
  </read_first>
  <files>missions/14-auth-vanilla-login-crud.md, missions/15-framework-auth-login-crud.md, NOTEBOOK.md</files>
  <action>
Actualizar ambas misiones auth para incluir verificación del camino OAuth mock y su comparación con login clásico, manteniendo el foco educativo del CRUD protegido sin cambiar su comportamiento (per D-03, D-05). Registrar en `NOTEBOOK.md` al menos una fricción real observada durante integración/validación OAuth dashboard (por ejemplo state, callback local o cookies) con causa y resolución (per D-06). Incluir checks ejecutables referenciados en misión para continuidad auth (per D-07).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const m14=fs.readFileSync('missions/14-auth-vanilla-login-crud.md','utf8').toLowerCase(); const m15=fs.readFileSync('missions/15-framework-auth-login-crud.md','utf8').toLowerCase(); const nb=fs.readFileSync('NOTEBOOK.md','utf8').toLowerCase(); if(!(m14.includes('oauth')&&m15.includes('oauth')&&nb.includes('fase 41'))) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - misión vanilla y frameworks incluyen pasos OAuth mock verificables
    - misiones preservan recorrido clásico y CRUD protegido
    - NOTEBOOK contiene fricción real atribuible a integración OAuth de fase 41
  </acceptance_criteria>
  <done>Ruta didáctica OAuth de fase 41 cerrada y trazada en misiones + notebook</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| código real -> documentación | riesgo de drift entre comportamiento auth y narrativa educativa |
| ejecución de misión -> evidencia notebook | riesgo de registrar hipótesis en vez de fricción real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04 | I | `docs/17-autenticacion.md` | mitigate | Validar texto contra contrato OAuth existente y resumen de plan 41-01 |
| T-41-05 | R | `NOTEBOOK.md` | mitigate | Registrar incidente real con síntoma, causa y solución reproducible |
| T-41-06 | T | Misiones auth | mitigate | Añadir pasos exactos OAuth mock sin alterar flujo clásico/CRUD |
</threat_model>

<verification>
- `node -e "..."` check de docs auth (OAuth + login clásico)
- `node -e "..."` check de misiones + NOTEBOOK (fase 41)
- `cd api && npm run test:sqlite` para continuidad mínima de contrato auth (referenciado en misión)
</verification>

<success_criteria>
- Documentación y misiones explican y validan claramente login clásico vs OAuth mock.
- NOTEBOOK captura fricción real de integración OAuth dashboard.
- La fase 41 queda didácticamente cerrada sin ampliar capacidades fuera de su frontera.
</success_criteria>

<output>
After completion, create `.planning/phases/41-oauth-dashboard-integration-foundation/41-02-SUMMARY.md`
</output>
