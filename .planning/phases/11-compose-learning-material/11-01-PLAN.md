---
phase: 11-compose-learning-material
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/14-docker-compose.md
  - docs/12-docker.md
  - docs/00-indice.md
  - docs/09-glosario.md
  - README.md
  - api/README.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-03
  - DOCS-04
must_haves:
  truths:
    - "docs/14-docker-compose.md explains services, networks, volumes with this repo's docker-compose.yml"
    - "docs/14-docker-compose.md includes executable compose:up, CRUD, restart, persistence examples"
    - "docs/12-docker.md has no placeholder for future doc 14; links to doc 14 and Mission 11"
    - "docs/12-docker.md SQLite narrative consistent (not JSON-only as runtime store)"
    - "docs/00-indice.md lists doc 14 and links to Mission 11"
    - "README.md links doc 14 and mission 11; no deferred Compose doc sentence"
  artifacts:
    - path: docs/14-docker-compose.md
      provides: "Primary Docker Compose learning doc"
      contains: "edf-lab-api"
    - path: docs/12-docker.md
      provides: "Single-container + Compose entry aligned with SQLite"
      contains: "14-docker-compose"
  key_links:
    - from: docs/14-docker-compose.md
      to: docker-compose.yml
      via: "YAML walkthrough"
      pattern: "edf-lab-dashboard"
    - from: README.md
      to: docs/14-docker-compose.md
      via: "Compose documentation link"
      pattern: "14-docker-compose"
---

<objective>
Create the primary Docker Compose learning doc and align doc 12, index, README, and api/README.

Purpose: DOCS-01, DOCS-03, DOCS-04.
Output: `docs/14-docker-compose.md`, updated doc 12, index, README, api/README, glossary
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-compose-learning-material/11-CONTEXT.md
@.planning/phases/11-compose-learning-material/11-RESEARCH.md
@.planning/phases/11-compose-learning-material/11-PATTERNS.md
@docker-compose.yml
@docs/12-docker.md
@docs/13-sqlite.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/14-docker-compose.md (DOCS-01)</name>
  <read_first>11-RESEARCH.md (Finding 2), docker-compose.yml, dashboard/nginx.conf, 09-CONTEXT.md D-04/D-03</read_first>
  <files>docs/14-docker-compose.md</files>
  <action>
    Create `docs/14-docker-compose.md` in Spanish, matching tone of docs/13-sqlite.md.

    Required sections:
    1. **Intro** — Compose orquesta varios contenedores; contraste con Misión 09 (`docker run` único)
    2. **Arquitectura** — ASCII diagram: navegador → localhost:5173 (nginx) → fetch → localhost:3100 (API)
    3. **Servicios** — Walk through `edf-lab-api` and `edf-lab-dashboard`: build, ports, depends_on
    4. **Redes** — Red Compose por defecto; por qué el navegador usa localhost (API_BASE_URL en app.js)
    5. **Volúmenes** — Bind mount `./api/data`; solo API; enlace a docs/13-sqlite.md
    6. **Flujo ejecutable** — bash blocks:
       - `npm run compose:up` / `compose:down` / `compose:logs`
       - `curl http://localhost:3100/health`
       - POST user + GET /users
       - `compose:down && compose:up` — persistencia
       - `sqlite3 api/data/users.db "SELECT ..."` en el host
    7. **Tres modos de arranque** — Link to doc 12 table (do not duplicate full table)
    8. **Reto avanzado** — nginx reverse proxy `/api` (conceptual, no implementar)
    9. **Enlaces** — Mission 11, doc 12, Mission 09, README

    Copy YAML snippets from live docker-compose.yml. No invented service names.
  </action>
  <verify>
    <automated>test -f docs/14-docker-compose.md && grep -q "edf-lab-api" docs/14-docker-compose.md && grep -q "edf-lab-dashboard" docs/14-docker-compose.md && grep -q "compose:up" docs/14-docker-compose.md && grep -q "bind mount\|api/data" docs/14-docker-compose.md</automated>
  </verify>
  <acceptance_criteria>
    - DOCS-01: services, networks, volumes explained with repo-specific examples
    - Executable commands match root package.json scripts
    - Advanced reto documented as docs-only
  </acceptance_criteria>
  <done>Primary Compose doc exists and is self-contained for learners</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/12-docker.md (DOCS-03)</name>
  <read_first>docs/12-docker.md current content, 11-RESEARCH.md Finding 3</read_first>
  <files>docs/12-docker.md</files>
  <action>
    Surgical DOCS-03 alignment:
    - Remove placeholder sentence "La documentación completa de Compose… fase posterior" (line ~119)
    - Replace with links to `docs/14-docker-compose.md` and `missions/11-arrancar-con-compose.md`
    - In Resumen section, add Mission 11 for Compose learning path alongside Mission 09
    - Verify ephemeral section correctly describes SQLite inside container (not JSON runtime)
    - Ensure three-mode table references SQLite users.db (already mostly done in Phase 10)

    Do NOT remove the Compose con persistencia section — it stays as quick reference; doc 14 is the deep dive.
  </action>
  <verify>
    <automated>grep -q "14-docker-compose" docs/12-docker.md && ! grep -q "fase posterior" docs/12-docker.md && grep -q "users.db" docs/12-docker.md</automated>
  </verify>
  <acceptance_criteria>
    - No deferred-doc placeholder remains
    - SQLite (not JSON-only) as runtime store is clear
    - Cross-links to doc 14 and Mission 11
  </acceptance_criteria>
  <done>Doc 12 aligned with v1.2 SQLite + Compose story</done>
</task>

<task type="auto">
  <name>Task 3: Update index, README, api/README, glossary (DOCS-04)</name>
  <read_first>docs/00-indice.md, README.md optional Compose section, api/README.md arranque section, docs/09-glosario.md structure</read_first>
  <files>docs/00-indice.md, README.md, api/README.md, docs/09-glosario.md</files>
  <action>
    **docs/00-indice.md**
    - Add doc 14 to reading order (after doc 12, advanced/optional)
    - Add mission 11 to Misiones avanzadas section
    - Document list entry for doc 14 with one-line description

    **README.md**
    - Replace "documentación completa de Compose llegará en una fase posterior" with links to doc 14 and mission 11
    - Keep host-dev-primary messaging

    **api/README.md**
    - Add "Opcional: Docker Compose" subsection after Arranque:
      - `npm run compose:up` from repo root
      - SQLite persists via bind mount; link doc 14

    **docs/09-glosario.md**
    - Add block or entries: Docker Compose, servicio (Compose), bind mount, volumen (bind mount vs efímero)
    - Match existing glossary format (term + short definition + example if present)
  </action>
  <verify>
    <automated>grep -q "14-docker-compose" docs/00-indice.md && grep -q "11-arrancar-con-compose" docs/00-indice.md && grep -q "14-docker-compose" README.md && grep -q "compose:up" api/README.md && grep -qi "compose\|bind mount" docs/09-glosario.md</automated>
  </verify>
  <acceptance_criteria>
    - DOCS-04: index, README, api/README updated
    - Learner can discover doc 14 and mission 11 from index and README
  </acceptance_criteria>
  <done>Discovery paths for Compose material complete</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information disclosure | Doc examples use localhost | accept | Local lab only |
</threat_model>

<verification>
- grep doc 14 exists with service names
- grep doc 12 links to doc 14, no placeholder
- grep index + README + api/README
</verification>

<success_criteria>
- DOCS-01, DOCS-03, DOCS-04 satisfied
- ROADMAP criteria 1, 3, 4 covered
</success_criteria>

<output>
After completion, create `.planning/phases/11-compose-learning-material/11-01-SUMMARY.md`
</output>
