---
phase: 11-compose-learning-material
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - missions/11-arrancar-con-compose.md
  - missions/09-arrancar-con-docker.md
  - NOTEBOOK.md
  - .planning/phases/11-compose-learning-material/11-UAT.md
autonomous: false
requirements:
  - DOCS-02
  - DOCS-05
must_haves:
  truths:
    - "Mission 11 walks compose up, CRUD, restart, and SQLite persistence verification"
    - "Mission 09 includes v1.2 note pointing to Mission 11 for persistent stack"
    - "NOTEBOOK documents Docker daemon, EADDRINUSE with Compose, port 5173 conflict"
    - "11-UAT.md confirms doc links, mission structure, and runtime checks"
  artifacts:
    - path: missions/11-arrancar-con-compose.md
      provides: "Guided Compose hands-on mission"
      contains: "compose:up"
    - path: NOTEBOOK.md
      provides: "Compose integration errors"
      contains: "Docker daemon"
  key_links:
    - from: missions/11-arrancar-con-compose.md
      to: docs/14-docker-compose.md
      via: "Lectura complementaria"
      pattern: "14-docker-compose"
    - from: NOTEBOOK.md
      to: missions/11-arrancar-con-compose.md
      via: "error context"
      pattern: "11-arrancar"
---

<objective>
Create Mission 11, update Mission 09 pointer, record Compose errors in NOTEBOOK, and complete Phase 11 UAT.

Purpose: DOCS-02, DOCS-05 + verification gate to close v1.2 milestone.
Output: `missions/11-arrancar-con-compose.md`, NOTEBOOK entries, `11-UAT.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-compose-learning-material/11-CONTEXT.md
@.planning/phases/11-compose-learning-material/11-RESEARCH.md
@missions/10-inspeccionar-sqlite.md
@missions/09-arrancar-con-docker.md
@missions/06-restart-y-persistencia.md
@.planning/phases/10-sqlite-volume-scripts/10-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create missions/11-arrancar-con-compose.md (DOCS-02)</name>
  <read_first>11-RESEARCH.md Finding 4, missions/10 structure, 10-UAT persistence steps</read_first>
  <files>missions/11-arrancar-con-compose.md</files>
  <action>
    Create mission following lab template (Objetivo, Pasos, Resultado esperado, Reto extra).

    **Objetivo:** Arrancar el stack Compose (API + dashboard), verificar CRUD end-to-end, reiniciar el stack y confirmar que SQLite persiste en el host via bind mount.

    **Pasos (minimum):**
    1. Parar servidores host y contenedores que usen 3100/5173 (`docker compose down`, `docker stop edf-lab-api` si aplica)
    2. Desde raíz del repo: `npm run compose:up`
    3. Abrir http://localhost:5173 — dashboard conectado
    4. Crear usuario (formulario dashboard o curl POST)
    5. Verificar con `sqlite3 api/data/users.db "SELECT id, name, email FROM users WHERE email='...';"` en el host
    6. `npm run compose:down && npm run compose:up` (sin `-v`)
    7. `curl -s http://localhost:3100/users` — usuario sigue presente
    8. Opcional: `npm run compose:logs` para ver logs API

    **Resultado esperado:** Usuario creado en paso 4 sobrevive al ciclo down/up del paso 6. Contraste explícito con Misión 09 (efímero).

    **Reto extra:** Intentar `npm run compose:up` mientras `PORT=3100 npm start` corre en otra terminal — observar EADDRINUSE; consultar NOTEBOOK.

    **Lectura complementaria:** link to docs/14-docker-compose.md
  </action>
  <verify>
    <automated>test -f missions/11-arrancar-con-compose.md && grep -q "compose:up" missions/11-arrancar-con-compose.md && grep -q "compose:down" missions/11-arrancar-con-compose.md && grep -q "sqlite3" missions/11-arrancar-con-compose.md && grep -q "14-docker-compose" missions/11-arrancar-con-compose.md</automated>
  </verify>
  <acceptance_criteria>
    - DOCS-02: mission covers up, CRUD, restart, persistence
    - Structure matches other missions (objetivo/pasos/resultado/reto)
  </acceptance_criteria>
  <done>Mission 11 ready for learners</done>
</task>

<task type="auto">
  <name>Task 2: Update Mission 09 + NOTEBOOK (DOCS-05)</name>
  <read_first>missions/06 v1.1 note pattern, NOTEBOOK EADDRINUSE section, 11-RESEARCH.md Finding 5</read_first>
  <files>missions/09-arrancar-con-docker.md, NOTEBOOK.md</files>
  <action>
    **missions/09-arrancar-con-docker.md**
    - Add note at top (after title or before Objetivo):
      > **Nota v1.2:** Esta misión usa un contenedor **efímero** (datos no persisten). Para el stack Compose con SQLite persistente, sigue **[Misión 11: arrancar con Compose](./11-arrancar-con-compose.md)**.

    **NOTEBOOK.md**
    - Add dated section `2026-05-31 · Docker Compose v1.2` with entries:

    1. **Docker daemon no disponible**
       - Síntoma: `Cannot connect to the Docker daemon`
       - Causa: Docker Desktop no arrancado
       - Solución: abrir Docker Desktop; `docker info` debe responder
       - Aprendizaje: Compose depende del daemon igual que `docker run`

    2. **EADDRINUSE con Compose** — EXTEND existing 3100 section (do not duplicate):
       - Add Compose stop: `npm run compose:down` or `docker compose down`
       - Mention both edf-lab-api single container and compose stack

    3. **EADDRINUSE puerto 5173**
       - Síntoma: dashboard container fails to bind 5173
       - Causa: `python3 -m http.server 5173` still running
       - Solución: stop host server or use different port (advanced)

    4. **Confusión efímero vs persistente** (optional short entry)
       - Mission 09 loses data; Mission 11 persists — link doc 12 table

    Link missions/11 where relevant.
  </action>
  <verify>
    <automated>grep -q "Misión 11" missions/09-arrancar-con-docker.md && grep -q "Docker daemon" NOTEBOOK.md && grep -q "compose:down\|compose down" NOTEBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - DOCS-05: real Compose errors documented
    - Mission 09 cross-links to Mission 11
  </acceptance_criteria>
  <done>NOTEBOOK and mission cross-links complete</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Phase 11 UAT</name>
  <read_first>11-01-SUMMARY.md (if exists), docs/14-docker-compose.md, missions/11, ROADMAP Phase 11 success criteria</read_first>
  <files>.planning/phases/11-compose-learning-material/11-UAT.md</files>
  <action>
    Create and complete `11-UAT.md` with tests:

    1. **Doc 14** — readable; explains services, network, volume with repo YAML (DOCS-01)
    2. **Mission 11** — steps executable; persistence test documented (DOCS-02)
    3. **Doc 12 alignment** — links to doc 14; no placeholder (DOCS-03)
    4. **Index/README** — doc 14 and mission 11 discoverable (DOCS-04)
    5. **NOTEBOOK** — Compose errors present (DOCS-05)
    6. **Runtime (optional if Docker available)** — npm run compose:up, create user, down/up, user persists

    Mark `status: complete` when static checks pass; note runtime test pass/fail separately if Docker unavailable.

    Create `11-VERIFICATION.md` with `status: passed` when all DOCS requirements traceable.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-compose-learning-material/11-UAT.md && grep -q "status: complete" .planning/phases/11-compose-learning-material/11-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 11-UAT.md 5/5 static + runtime noted
    - 11-VERIFICATION.md status passed
    - All DOCS-01..05 traceable
  </acceptance_criteria>
  <done>Phase 11 UAT complete; v1.2 milestone ready to close</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Denial of service | UAT port conflicts | accept | NOTEBOOK documents mitigation |
</threat_model>

<verification>
- mission 11 file + NOTEBOOK grep
- 11-UAT.md complete
- ROADMAP Phase 11 all 5 success criteria mapped
</verification>

<success_criteria>
- DOCS-02, DOCS-05 complete
- ROADMAP criteria 2 and 5 covered
- v1.2 milestone documentation complete after 11-VERIFICATION
</success_criteria>

<output>
After completion, create `.planning/phases/11-compose-learning-material/11-02-SUMMARY.md`
</output>
