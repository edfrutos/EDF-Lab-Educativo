---
phase: 43-tls-local-proxy-trust
plan: 02
type: execute
wave: 2
depends_on:
  - "43-01"
files_modified:
  - api/index.js
  - docker-compose.prod.yml
  - scripts/smoke-prod-proxy.sh
  - docs/18-production-deploy.md
autonomous: true
requirements:
  - PROD-03
  - PROD-05
user_setup: []
must_haves:
  truths:
    - "API habilita trust proxy solo con TRUST_PROXY=1 en prod (D-08, D-10)"
    - "Smoke demuestra login HTTPS + GET /api/users con sesión (D-03)"
    - "Set-Cookie edf_session incluye Secure en respuesta login prod (D-03)"
  artifacts:
    - path: api/index.js
      provides: "trust proxy condicional"
      contains: "trust proxy"
    - path: scripts/smoke-prod-proxy.sh
      provides: "Login + users autenticado vía proxy HTTPS"
      contains: "auth/login"
    - path: docker-compose.prod.yml
      provides: "TRUST_PROXY=1 en API prod"
      contains: "TRUST_PROXY"
  key_links:
    - from: proxy/nginx.conf
      to: api/index.js
      via: "X-Forwarded-Proto https"
      pattern: "X-Forwarded-Proto"
    - from: scripts/smoke-prod-proxy.sh
      to: POST /api/auth/login
      via: "curl cookie jar -k"
      pattern: "edf_session"
---

<objective>
Cerrar el bucle auth/TLS: Express confía en nginx edge y el smoke automatizado prueba login con cookie `Secure` y lectura CRUD por HTTPS.

Purpose: cumplir PROD-03 (login Secure E2E) y PROD-05 (trust proxy).
Output: smoke ampliado + `trust proxy` gated + doc mínima de verificación.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-tls-local-proxy-trust/43-CONTEXT.md
@.planning/phases/43-tls-local-proxy-trust/43-RESEARCH.md
@.planning/phases/43-tls-local-proxy-trust/43-01-SUMMARY.md
@api/index.js
@api/auth.js
@proxy/nginx.conf
@scripts/smoke-prod-proxy.sh

<interfaces>
Trust proxy (antes de cors):

```javascript
if (process.env.TRUST_PROXY === '1') {
  app.set('trust proxy', 1);
}
```

Prod override:

```yaml
services:
  edf-lab-api:
    environment:
      TRUST_PROXY: "1"
```

Smoke login (defaults operador laboratorio):

```bash
EMAIL="${SMOKE_OPERATOR_EMAIL:-admin@lab.local}"
PASSWORD="${SMOKE_OPERATOR_PASSWORD:-changeme}"
curl -kfsSI -c "$JAR" -b "$JAR" -X POST "$BASE_URL/api/auth/login" \
  -H "Content-Type: application/json" \
  -d "{\"email\":\"$EMAIL\",\"password\":\"$PASSWORD\"}"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: trust proxy condicional (D-08, D-09, D-10)</name>
  <read_first>
    api/index.js
    proxy/nginx.conf
    .planning/phases/43-tls-local-proxy-trust/43-CONTEXT.md
  </read_first>
  <files>api/index.js, docker-compose.prod.yml</files>
  <action>
En **`api/index.js`**, inmediatamente después de `const app = express()`, añadir: si `process.env.TRUST_PROXY === '1'`, entonces `app.set('trust proxy', 1)`. Debe ejecutarse **antes** de `cors()` y rutas. En **`docker-compose.prod.yml`**, añadir `TRUST_PROXY: "1"` al bloque `environment` de `edf-lab-api` (junto a `NODE_ENV: production` de 43-01). **No** parsear `X-Forwarded-For` ni cambiar rate limiting. Host dev y `compose:up` sin override no definen `TRUST_PROXY`.
  </action>
  <verify>
    <automated>rg "trust proxy" api/index.js && rg "TRUST_PROXY" docker-compose.prod.yml && cd api && npm run test:sqlite</automated>
  </verify>
  <acceptance_criteria>
    - trust proxy solo activo con TRUST_PROXY=1
    - suite SQLite sigue verde
    - nginx X-Forwarded-Proto ya presente (sin cambios proxy en esta tarea salvo verificación)
  </acceptance_criteria>
  <done>API lista para operar detrás de nginx en prod</done>
</task>

<task type="auto">
  <name>Task 2: Smoke login Secure + GET /users (D-03, D-04, D-05)</name>
  <read_first>
    scripts/smoke-prod-proxy.sh
    api/auth.js
    .planning/phases/43-tls-local-proxy-trust/43-RESEARCH.md
  </read_first>
  <files>scripts/smoke-prod-proxy.sh</files>
  <action>
Extender **`scripts/smoke-prod-proxy.sh`** tras checks existentes (health, dashboard, OAuth start): (1) `POST $BASE_URL/api/auth/login` con JSON email/password — defaults `admin@lab.local` / `changeme`, override vía `SMOKE_OPERATOR_EMAIL` / `SMOKE_OPERATOR_PASSWORD`; (2) usar cookie jar temporal (`mktemp` + trap cleanup); (3) verificar cabecera `Set-Cookie` contiene `edf_session` y **`Secure`** (grep case-insensitive); (4) `GET $BASE_URL/api/users` con jar → 200 y JSON array con al menos un usuario. Mantener `curl -k`. **No** añadir job CI ni Playwright prod. OAuth Secure en smoke es nice-to-have — no bloquear si complejidad crece.
  </action>
  <verify>
    <automated>test -x scripts/smoke-prod-proxy.sh && rg "auth/login" scripts/smoke-prod-proxy.sh && rg "api/users" scripts/smoke-prod-proxy.sh</automated>
  </verify>
  <acceptance_criteria>
    - script falla si login o /users autenticado fallan
    - script verifica flag Secure en Set-Cookie
    - prerequisito compose:prod documentado en cabecera del script
  </acceptance_criteria>
  <done>Smoke prod prueba sesión Secure end-to-end</done>
</task>

<task type="auto">
  <name>Task 3: Doc trust proxy + checklist manual navegador (D-04, D-06)</name>
  <read_first>
    docs/18-production-deploy.md
    .planning/phases/42-compose-prod-profile-reverse-proxy/42-02-SUMMARY.md
  </read_first>
  <files>docs/18-production-deploy.md</files>
  <action>
En **`docs/18-production-deploy.md`**, añadir subsección **"Trust proxy y cookies Secure"**: explicar `TRUST_PROXY=1` + `X-Forwarded-Proto` de nginx; por qué `Secure` requiere HTTPS terminado en proxy; una línea de checklist manual (aceptar advertencia cert en `https://localhost`, login en dashboard, CRUD visible). Referenciar `./scripts/smoke-prod-proxy.sh` como prueba automatizada. No duplicar certbot ni índice completo (fase 45).
  </action>
  <verify>
    <automated>rg "trust proxy" docs/18-production-deploy.md && rg "smoke-prod-proxy" docs/18-production-deploy.md</automated>
  </verify>
  <acceptance_criteria>
    - doc explica trust proxy y Secure en prod
    - smoke script referenciado como verificación reproducible
  </acceptance_criteria>
  <done>Historia TLS/trust documentada para operador</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03 | S | trust proxy en dev | mitigate | Gate TRUST_PROXY=1 solo prod override |
| T-43-04 | I | curl -k en smoke | mitigate | Solo laboratorio; doc marca explícito |
| T-43-05 | D | Credenciales default en smoke | mitigate | Defaults de .env.example; override por env |
</threat_model>

<verification>
- `cd api && npm run test:sqlite`
- `test -x scripts/smoke-prod-proxy.sh`
- Manual (operador con Docker):
  ```bash
  cp api/.env.example api/.env  # JWT_SECRET + DATABASE_URL
  ./scripts/generate-dev-tls.sh
  npm run compose:prod
  ./scripts/smoke-prod-proxy.sh
  ```
- Crear `43-VERIFICATION.md` al cerrar fase
</verification>

<success_criteria>
- Criterios ROADMAP fase 43: login HTTPS, cookie Secure, API no asume HTTPS directo.
- Smoke PASS con stack prod levantado.
</success_criteria>

<output>
After completion, create `.planning/phases/43-tls-local-proxy-trust/43-02-SUMMARY.md` and `43-VERIFICATION.md`
</output>
