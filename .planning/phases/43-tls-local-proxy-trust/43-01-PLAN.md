---
phase: 43-tls-local-proxy-trust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.prod.yml
  - api/.env.example
  - docs/18-production-deploy.md
autonomous: true
requirements:
  - PROD-03
user_setup:
  - "cp api/.env.example api/.env y definir JWT_SECRET + DATABASE_URL antes de compose:prod"
must_haves:
  truths:
    - "edf-lab-api corre con NODE_ENV=production solo en compose:prod (D-01, D-02)"
    - "Host dev (PORT=3100 + :5173) no activa production mode (D-01)"
    - "Operador tiene checklist claro: .env + certs + compose:prod (D-06)"
  artifacts:
    - path: docker-compose.prod.yml
      provides: "Override prod con NODE_ENV=production en API"
      contains: "NODE_ENV"
    - path: api/.env.example
      provides: "Comentarios prerequisitos compose:prod"
      contains: "compose:prod"
    - path: docs/18-production-deploy.md
      provides: "Sección prod runtime y advertencia cert autofirmado"
      contains: "NODE_ENV=production"
  key_links:
    - from: docker-compose.prod.yml
      to: api/index.js
      via: "NODE_ENV=production → fail-fast JWT_SECRET"
      pattern: "NODE_ENV"
    - from: api/auth.js
      to: edf_session cookie
      via: "secure: NODE_ENV === 'production'"
      pattern: "secure"
---

<objective>
Activar modo producción en el contenedor API del perfil `prod` sin tocar el flujo dev en host ni rehacer TLS/nginx de fase 42.

Purpose: habilitar cookies `Secure` y fail-fast de secretos en el stack `compose:prod` (PROD-03 parte runtime).
Output: `docker-compose.prod.yml` con `NODE_ENV=production`; prerequisitos documentados.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-tls-local-proxy-trust/43-CONTEXT.md
@.planning/phases/43-tls-local-proxy-trust/43-RESEARCH.md
@.planning/phases/42-compose-prod-profile-reverse-proxy/42-VERIFICATION.md
@docker-compose.prod.yml
@docker-compose.yml
@api/.env.example
@docs/18-production-deploy.md

<interfaces>
Prod override (añadir a servicio edf-lab-api):

```yaml
services:
  edf-lab-api:
    environment:
      NODE_ENV: production
```

Fail-fast existente (no reimplementar):

```javascript
// api/index.js — validateProductionEnv() antes de listen
// api/auth.js — getCookieOptions().secure cuando NODE_ENV === 'production'
```

Arranque operador:

```bash
cp api/.env.example api/.env   # JWT_SECRET obligatorio + DATABASE_URL Compose
./scripts/generate-dev-tls.sh
npm run compose:prod
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NODE_ENV=production en override prod (D-01, D-02)</name>
  <read_first>
    docker-compose.prod.yml
    docker-compose.yml
    .planning/phases/43-tls-local-proxy-trust/43-CONTEXT.md
  </read_first>
  <files>docker-compose.prod.yml</files>
  <action>
En **`docker-compose.prod.yml`**, bajo **`edf-lab-api`**, añadir **`environment: NODE_ENV: production`**. No añadir secretos al YAML — `JWT_SECRET` sigue en `api/.env` vía `env_file` del compose base. **No** modificar `docker-compose.yml` base ni el flujo `compose:up` sin profile. **No** cambiar certs, nginx ni proxy (fase 42).
  </action>
  <verify>
    <automated>grep -q 'NODE_ENV' docker-compose.prod.yml && ! grep -q 'NODE_ENV' docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - compose:prod inyecta NODE_ENV=production solo en API
    - compose:up sin override no define NODE_ENV=production
    - sin JWT_SECRET en api/.env el contenedor API debe fallar al arrancar (fail-fast existente)
  </acceptance_criteria>
  <done>API prod corre en modo production vía override</done>
</task>

<task type="auto">
  <name>Task 2: Prerequisitos .env.example + doc prod runtime (D-02, D-06)</name>
  <read_first>
    api/.env.example
    docs/18-production-deploy.md
    scripts/generate-dev-tls.sh
  </read_first>
  <files>api/.env.example, docs/18-production-deploy.md</files>
  <action>
En **`api/.env.example`**, añadir comentario breve bajo sección Arranque: `compose:prod` exige `JWT_SECRET` en `.env` (fail-fast) y `DATABASE_URL` apuntando a `edf-lab-postgres` para Postgres en stack. En **`docs/18-production-deploy.md`**: (1) sección **"Modo production en compose:prod"** explicando que `NODE_ENV=production` solo aplica con override prod; (2) actualizar checklist de arranque con orden `.env` → `generate-dev-tls.sh` → `compose:prod`; (3) aclarar advertencia navegador por cert autofirmado y `curl -k` (D-06). No adelantar certbot (fase 44) ni bulk docs fase 45.
  </action>
  <verify>
    <automated>rg "NODE_ENV=production" docs/18-production-deploy.md && rg "compose:prod" api/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - .env.example menciona prerequisitos compose:prod
    - doc 18 distingue dev host HTTP vs prod NODE_ENV
    - advertencia TLS autofirmado visible para el operador
  </acceptance_criteria>
  <done>Operador sabe preparar .env y entender modo production</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | E | NODE_ENV=production sin JWT_SECRET | mitigate | Fail-fast existente en index.js/auth.js |
| T-43-02 | I | Doc sugiere subir .env | mitigate | Reiterar gitignore; solo .env.example en git |
</threat_model>

<verification>
- `grep NODE_ENV docker-compose.prod.yml`
- `cd api && npm run test:sqlite` (regresión — sin NODE_ENV=production en tests)
- Manual: con `api/.env` válido, `npm run compose:prod` y comprobar logs API sin error fatal
</verification>

<success_criteria>
- API en compose:prod arranca con NODE_ENV=production cuando JWT_SECRET está definido.
- Dev host sin override no emite cookies Secure.
</success_criteria>

<output>
After completion, create `.planning/phases/43-tls-local-proxy-trust/43-01-SUMMARY.md`
</output>
