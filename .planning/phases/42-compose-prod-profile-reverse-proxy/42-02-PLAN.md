---
phase: 42-compose-prod-profile-reverse-proxy
plan: 02
type: execute
wave: 2
depends_on:
  - "42-01"
files_modified:
  - scripts/smoke-prod-proxy.sh
  - README.md
  - docs/18-production-deploy.md
autonomous: true
requirements:
  - PROD-02
  - PROD-04
  - PROD-06
user_setup: []
must_haves:
  truths:
    - "Smoke automatizado confirma /api/health vía proxy prod (PROD-02)"
    - "Flujo dev host :3100+:5173 verificado sin regresión (PROD-04)"
    - "OAuth/login alcanzables vía /api/auth/* en prod smoke (D-13, PROD-06)"
  artifacts:
    - path: scripts/smoke-prod-proxy.sh
      provides: "Verificación curl prod same-origin"
      contains: "/api/health"
    - path: README.md
      provides: "Comando compose:prod y modo dual"
      contains: "compose:prod"
  key_links:
    - from: scripts/smoke-prod-proxy.sh
      to: https://localhost/api/health
      via: "curl -k prod proxy"
      pattern: "api/health"
---

<objective>
Cerrar fase 42 con verificación dual (prod proxy + dev host) y documentación mínima del modo prod, sin adelantar TLS/Secure de fase 43 ni bulk docs de fase 45.

Purpose: evidenciar PROD-02/04/06 con checks reproducibles.
Output: script smoke prod + README/doc hook + verificación de fase.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-compose-prod-profile-reverse-proxy/42-CONTEXT.md
@.planning/phases/42-compose-prod-profile-reverse-proxy/42-01-SUMMARY.md
@scripts/smoke-prod-proxy.sh
@README.md

<interfaces>
Smoke prod mínimo:

```bash
curl -kfsS https://localhost/api/health
curl -kfsS -o /dev/null -w "%{http_code}" https://localhost/
```

Dev regression (documented, no compose prod):

```bash
cd api && PORT=3100 npm start &
cd dashboard && python3 -m http.server 5173 &
curl -fsS http://localhost:3100/health
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script smoke prod proxy (D-13, PROD-02, PROD-06)</name>
  <read_first>
    proxy/nginx.conf
    scripts/generate-dev-tls.sh
    .planning/phases/42-compose-prod-profile-reverse-proxy/42-CONTEXT.md
  </read_first>
  <files>scripts/smoke-prod-proxy.sh</files>
  <action>
Crear **`scripts/smoke-prod-proxy.sh`** ejecutable que asuma stack prod en marcha (`compose:prod`) y verifique: `GET https://localhost/api/health` (200 + JSON status), `GET https://localhost/` (dashboard HTML), y opcionalmente `GET https://localhost/api/auth/oauth/start?provider=mock` (200 con authUrl bajo /api). Usar `curl -k` para cert autofirmado. Salir con código distinto de 0 si falla. **No** exigir cookie Secure ni login E2E completo (D-15 → fase 43).
  </action>
  <verify>
    <automated>test -x scripts/smoke-prod-proxy.sh</automated>
  </verify>
  <acceptance_criteria>
    - script verifica health vía /api detrás del proxy
    - script documenta prerequisito compose:prod
    - no bloquea en verificación TLS/Secure cookies
  </acceptance_criteria>
  <done>Smoke prod reproducible vía script</done>
</task>

<task type="auto">
  <name>Task 2: Documentar modo dual + verificación dev (PROD-04)</name>
  <read_first>
    README.md
    docs/18-production-deploy.md
    CLAUDE.md
  </read_first>
  <files>README.md, docs/18-production-deploy.md</files>
  <action>
Añadir sección breve en **README.md**: modo dev (dos terminales :3100/:5173) vs **`npm run compose:prod`** (un origen HTTPS, API en `/api`). En **docs/18-production-deploy.md**, actualizar snippet de arquitectura para reflejar `edf-lab-proxy` real (sustituir diagrama solo educativo si aplica). Documentar que certs autofirmados requieren `-k` en curl o aceptar advertencia en navegador. Confirmar que misiones que usan `docker compose up` sin profile siguen válidas.
  </action>
  <verify>
    <automated>rg "compose:prod" README.md && node --check dashboard/app.js && cd api && npm run test:sqlite</automated>
  </verify>
  <acceptance_criteria>
    - README menciona compose:prod y modo dual
    - docs/18 referencia proxy prod implementado
    - suite SQLite sigue verde
  </acceptance_criteria>
  <done>Modo dual documentado; regresión API verde</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04 | D | Smoke asume stack up | mitigate | Script falla claro si proxy no responde |
| T-42-05 | I | curl -k en docs | mitigate | Marcar como solo laboratorio; LE en fase 44 |
</threat_model>

<verification>
- `test -x scripts/smoke-prod-proxy.sh`
- `cd api && npm run test:sqlite`
- `node --check dashboard/app.js`
- Manual: `npm run compose:prod` luego `./scripts/smoke-prod-proxy.sh`
</verification>

<success_criteria>
- Criterios ROADMAP fase 42: proxy prod, same-origin /api, dev host intacto.
- Script smoke prod pasa con stack levantado.
</success_criteria>

<output>
After completion, create `.planning/phases/42-compose-prod-profile-reverse-proxy/42-02-SUMMARY.md`
</output>
