---
phase: 42-compose-prod-profile-reverse-proxy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - docker-compose.prod.yml
  - proxy/Dockerfile
  - proxy/nginx.conf
  - deploy/certs/.gitkeep
  - scripts/generate-dev-tls.sh
  - dashboard/Dockerfile
  - package.json
  - .gitignore
autonomous: true
requirements:
  - PROD-02
  - PROD-04
  - PROD-06
user_setup: []
must_haves:
  truths:
    - "Existe servicio edf-lab-proxy con profile prod y único puerto :443 en host (D-01, D-03, D-04)"
    - "nginx enruta /api/* a Express con strip de prefijo sin cambiar rutas en api/ (D-05, D-06)"
    - "Imagen dashboard prod usa API_BASE_URL=/api; host dev sigue localhost:3100 (D-09, D-10, D-11)"
  artifacts:
    - path: proxy/nginx.conf
      provides: "Edge TLS + /api strip + static upstream"
      contains: "proxy_pass"
    - path: docker-compose.prod.yml
      provides: "Override prod: sin puertos 3100/5173 en host"
      contains: "profiles"
    - path: dashboard/Dockerfile
      provides: "Build-arg para API_BASE_URL prod"
      contains: "DASHBOARD_API_BASE_URL"
  key_links:
    - from: proxy/nginx.conf
      to: edf-lab-api:3100
      via: "location /api/ proxy_pass strip"
      pattern: "/api/"
    - from: dashboard/app.js
      to: /api
      via: "API_BASE_URL en imagen prod"
      pattern: "API_BASE_URL"
    - from: edf-lab-proxy
      to: edf-lab-dashboard:5173
      via: "location / static upstream"
      pattern: "proxy_pass"
---

<objective>
Implementar el perfil Compose prod con nginx edge (`edf-lab-proxy`), enrutado `/api` con strip, TLS autofirmado mínimo en :443, y dashboard prod con base `/api`, sin romper el flujo dev en host.

Purpose: entregar same-origin prod (PROD-02, PROD-06) preservando dev host (PROD-04).
Output: stack `compose:prod` levantable con un solo origen HTTPS.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-compose-prod-profile-reverse-proxy/42-CONTEXT.md
@.planning/phases/42-compose-prod-profile-reverse-proxy/42-RESEARCH.md
@docker-compose.yml
@dashboard/Dockerfile
@dashboard/app.js
@docs/18-production-deploy.md

<interfaces>
nginx strip pattern (no Express changes):

```nginx
location /api/ {
    proxy_pass http://edf-lab-api:3100/;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto https;
}
```

Dashboard prod build:

```dockerfile
ARG DASHBOARD_API_BASE_URL=http://localhost:3100
# bake into app.js at build time for prod target only
```

Canonical command:

```bash
npm run compose:prod
# wraps: docker compose -f docker-compose.yml -f docker-compose.prod.yml --profile prod up --build
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Servicio edf-lab-proxy + TLS autofirmado (D-01, D-04)</name>
  <read_first>
    docker-compose.yml
    docs/18-production-deploy.md
    .planning/phases/42-compose-prod-profile-reverse-proxy/42-CONTEXT.md
  </read_first>
  <files>proxy/Dockerfile, proxy/nginx.conf, scripts/generate-dev-tls.sh, deploy/certs/.gitignore, docker-compose.yml</files>
  <action>
Crear `proxy/` con Dockerfile (nginx:alpine) y `nginx.conf` que escuche **443 ssl**, termine TLS con certs montados o copiados, sirva `/` vía upstream `edf-lab-dashboard:5173`, y proxee `/api/` a `edf-lab-api:3100/` con strip (D-05). Añadir `scripts/generate-dev-tls.sh` que genere cert/key en `deploy/certs/` (gitignored, con `.gitkeep` o README stub). Registrar servicio **`edf-lab-proxy`** en `docker-compose.yml` con **`profiles: [prod]`**, puerto **`443:443`**, dependencias api+dashboard+postgres. No publicar :3100/:5173 en este servicio.
  </action>
  <verify>
    <automated>test -f proxy/nginx.conf && test -f scripts/generate-dev-tls.sh</automated>
  </verify>
  <acceptance_criteria>
    - existe edf-lab-proxy con profile prod
    - nginx.conf incluye location /api/ y location /
    - script TLS autofirmado documentado y ejecutable
  </acceptance_criteria>
  <done>Edge proxy definido con TLS mínimo runnable en :443</done>
</task>

<task type="auto">
  <name>Task 2: Compose prod override + dashboard API_BASE_URL=/api (D-02, D-03, D-09–D-12)</name>
  <read_first>
    dashboard/Dockerfile
    dashboard/app.js
    package.json
    docker-compose.yml
  </read_first>
  <files>docker-compose.prod.yml, dashboard/Dockerfile, package.json, .gitignore</files>
  <action>
Crear **`docker-compose.prod.yml`** que, con profile prod, **elimine** `ports` de `edf-lab-api` y `edf-lab-dashboard` (`ports: []` o equivalente Compose v2). Añadir **`npm run compose:prod`** en `package.json` que ejecute `docker compose -f docker-compose.yml -f docker-compose.prod.yml --profile prod up --build`. Extender **`dashboard/Dockerfile`** con build-arg **`DASHBOARD_API_BASE_URL`** (default `http://localhost:3100` para builds actuales; prod build pasa `/api`). En compose prod, build dashboard con `DASHBOARD_API_BASE_URL=/api`. **No modificar** el valor por defecto en `dashboard/app.js` fuente usado por `python3 -m http.server` (D-10). Asegurar `#api-base-url` muestra el valor baked (D-12).
  </action>
  <verify>
    <automated>node --check dashboard/app.js && grep -q compose:prod package.json</automated>
  </verify>
  <acceptance_criteria>
    - compose:prod documentado en package.json
    - prod override quita puertos host de api/dashboard
    - imagen dashboard prod bakea /api sin romper dev host
  </acceptance_criteria>
  <done>Modo dual wired: compose prod same-origin /api; dev host intacto</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet/host → edf-lab-proxy | único punto expuesto en prod |
| proxy → internal API | strip /api; headers X-Forwarded-Proto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | I | TLS autofirmado | mitigate | Documentar advertencia navegador; fase 43 endurece confianza |
| T-42-02 | E | Path traversal en proxy | mitigate | location /api/ acotado; sin proxy abierto a red interna arbitraria |
| T-42-03 | D | Regresión compose dev | mitigate | docker-compose.yml base sin cambiar contrato missions 11/12 |
</threat_model>

<verification>
- `test -f proxy/nginx.conf`
- `node --check dashboard/app.js`
- `cd api && npm run test:sqlite` (sin regresión API)
- Manual: `npm run compose:prod` + `curl -k https://localhost/api/health`
</verification>

<success_criteria>
- `docker compose --profile prod` (vía compose:prod) levanta proxy en :443.
- `/api/health` responde a través del proxy.
- Dashboard prod usa `/api` como base URL.
</success_criteria>

<output>
After completion, create `.planning/phases/42-compose-prod-profile-reverse-proxy/42-01-SUMMARY.md`
</output>
