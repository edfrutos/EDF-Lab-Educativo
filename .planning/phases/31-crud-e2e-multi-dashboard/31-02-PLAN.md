---
phase: 31-crud-e2e-multi-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - "31-01"
files_modified:
  - e2e/playwright.config.js
  - docs/10-tests.md
autonomous: true
requirements:
  - QA-ADV-02
user_setup: []
must_haves:
  truths:
    - "npm run test:e2e ejecuta 6 tests (3 smoke auth + 3 CRUD) sin fallos"
    - "Proyectos react y vue incluyen crud.*.spec.js en testMatch"
    - "docs/10-tests.md documenta CRUD en los tres dashboards"
  artifacts:
    - path: e2e/playwright.config.js
      provides: testMatch react/vue con crud
      contains: crud
    - path: docs/10-tests.md
      provides: Sección CRUD multi-dashboard
      contains: crud.react
  key_links:
    - from: playwright.config.js
      to: crud.react.spec.js
      via: projects.react.testMatch
      pattern: crud\\.react
---

<objective>
Integrar specs CRUD React/Vue en Playwright, actualizar documentación y verificar suite de 6 tests.

Purpose: Cerrar QA-ADV-02 de extremo a extremo.
Output: `npm run test:e2e` → 6 passed.
</objective>

<context>
@.planning/phases/31-crud-e2e-multi-dashboard/31-RESEARCH.md
@.planning/phases/31-crud-e2e-multi-dashboard/31-01-SUMMARY.md
@e2e/playwright.config.js
@docs/10-tests.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: testMatch react y vue</name>
  <read_first>
    e2e/playwright.config.js
    e2e/tests/crud.react.spec.js
    e2e/tests/crud.vue.spec.js
  </read_first>
  <files>e2e/playwright.config.js</files>
  <action>
Proyecto `react`: cambiar testMatch a `/(auth-smoke|crud)\.react\.spec\.js/`
Proyecto `vue`: cambiar testMatch a `/(auth-smoke|crud)\.vue\.spec\.js/`

No modificar vanilla ni webServer. Comentario breve en español encima de cada testMatch ampliado.
  </action>
  <verify>
    <automated>node --check e2e/playwright.config.js && grep -q 'crud).*react' e2e/playwright.config.js</automated>
  </verify>
  <acceptance_criteria>
    - Regex incluye auth-smoke y crud para react y vue
    - Vanilla sin cambios
  </acceptance_criteria>
  <done>Playwright descubre 6 specs en total</done>
</task>

<task type="auto">
  <name>Task 2: Documentar CRUD multi-dashboard</name>
  <read_first>
    docs/10-tests.md (sección CRUD E2E vanilla)
    .planning/phases/31-crud-e2e-multi-dashboard/31-RESEARCH.md
  </read_first>
  <files>docs/10-tests.md</files>
  <action>
Ampliar sección «CRUD E2E (vanilla)» → renombrar o subtítulo **«CRUD E2E (tres dashboards)»**:

1. Listar specs: `crud.vanilla.spec.js`, `crud.react.spec.js`, `crud.vue.spec.js`
2. Nota: mismos ids en vanilla/React/Vue (`#user-name-input`, etc.)
3. Comandos filtrados:
   ```bash
   npx playwright test crud.react --config=e2e/playwright.config.js
   npx playwright test crud.vue --config=e2e/playwright.config.js
   ```
4. Tabla Scripts útiles: `npm run test:e2e` → «6 tests (3 smoke + 3 CRUD)»
5. Tabla Auth E2E: actualizar fila smoke si aún dice 4 tests

No documentar Postgres/multi-browser (fases 32–33).
  </action>
  <verify>
    <automated>grep -q "crud.react" docs/10-tests.md && grep -q "6 tests" docs/10-tests.md</automated>
  </verify>
  <acceptance_criteria>
    - Referencias a los tres specs CRUD
    - Conteo 6 tests documentado
  </acceptance_criteria>
  <done>Docs alineados con suite ampliada</done>
</task>

<task type="auto">
  <name>Task 3: Verificar suite E2E (6 tests)</name>
  <read_first>
    package.json
    e2e/playwright.config.js
  </read_first>
  <files></files>
  <action>
Desde raíz:

```bash
npm run test:e2e
```

Esperado: **6 passed** — vanilla (auth+crud), react (auth+crud), vue (auth+crud).

Si falla por BD: borrar `api/data/e2e.users.db` y reintentar.
Si falla selector: revisar ids en framework components (plan 01), no debilitar tests.

Opcional smoke: cada spec CRUD aislado con filtro playwright.
  </action>
  <verify>
    <automated>npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - 6/6 tests Playwright pasan
    - Sin regresión smoke auth
    - CRUD react y vue completan create→edit→delete
  </acceptance_criteria>
  <done>QA-ADV-02 verificado vía npm run test:e2e</done>
</task>

</tasks>

<verification>
- npm run test:e2e → 6 passed
- grep crud.react en docs
</verification>

<success_criteria>
- Config y docs actualizados
- Suite completa verde
</success_criteria>

<output>
After completion, create `.planning/phases/31-crud-e2e-multi-dashboard/31-02-SUMMARY.md`
</output>
