---
phase: 03-api-tests-and-quality-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - api/package.json
  - api/README.md
  - NOTEBOOK.md
autonomous: true
requirements:
  - TEST-01
  - TEST-05
  - QUAL-01
  - QUAL-02
  - QUAL-05

must_haves:
  truths:
    - "npm test desde api/ ejecuta node --test, no el placeholder echo de error"
    - "npm run dev desde api/ arranca nodemon"
    - "package.json refleja nombre e identidad del lab educativo"
    - "api/README.md tiene sección ## Comprobaciones y tests con los 4 comandos explicados"
    - "NOTEBOOK.md registra el fix de parseUserId como error real resuelto"
  artifacts:
    - path: "api/package.json"
      provides: "Metadata educativa + scripts test y dev"
      contains: "edf-lab-api"
    - path: "api/README.md"
      provides: "Sección de comprobaciones y tests (QUAL-05)"
      contains: "Comprobaciones y tests"
    - path: "NOTEBOOK.md"
      provides: "Registro del fix parseUserId como aprendizaje documentado"
      contains: "parseUserId"
  key_links:
    - from: "api/package.json scripts.test"
      to: "node --test index.test.js --test-force-exit"
      via: "npm test"
      pattern: "node --test index.test.js --test-force-exit"
    - from: "api/package.json scripts.dev"
      to: "nodemon index.js"
      via: "npm run dev"
      pattern: "nodemon index.js"
---

<objective>
Actualizar api/package.json (metadata + scripts), completar la sección de comandos en api/README.md, y documentar el fix de parseUserId en NOTEBOOK.md.

Purpose: Cierra QUAL-01 (metadata), QUAL-02 (script dev), QUAL-05 (documentación de comandos) y TEST-05 (cómo correr e interpretar los tests). Este plan puede ejecutarse en paralelo con el Plan 02 ya que no toca los mismos archivos. No tiene dependencia directa del resultado de los tests — solo necesita que api/index.js esté preparado (Plan 01).

Output: package.json con identidad educativa y scripts completos; api/README.md con sección de comandos explicados; NOTEBOOK.md con el fix de parseUserId como aprendizaje documentado (D-06).
</objective>

<execution_context>
@/Users/edefrutos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edefrutos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-api-tests-and-quality-fixes/03-CONTEXT.md
@.planning/phases/03-api-tests-and-quality-fixes/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Estado actual de los archivos a modificar — extraído directamente del código fuente -->

api/package.json estado actual (a reemplazar completamente):
```json
{
  "name": "test-project",
  "version": "1.0.0",
  "description": "A simple test project",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^4.18.2",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  },
  "keywords": ["test", "demo", "nodejs"],
  "author": "Test Author",
  "license": "MIT"
}
```

api/README.md sección existente "## Validaciones recomendadas" (líneas 261-275):
```markdown
## Validaciones recomendadas

Comprobar sintaxis:
```bash
node --check index.js
```

Revisar vulnerabilidades de nivel alto:
```bash
npm audit --audit-level=high
```
```
La nueva sección "## Comprobaciones y tests" REEMPLAZA "## Validaciones recomendadas" (misma ubicación, mismo propósito, más completa).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Actualizar api/package.json con metadata educativa y scripts completos</name>
  <files>api/package.json</files>
  <read_first>
    - api/package.json (leer estado actual completo antes de sobreescribir)
  </read_first>
  <action>
Sobreescribir api/package.json con el siguiente contenido exacto.

IMPORTANTE: El campo `devDependencies` debe incluir TANTO nodemon como supertest. Si el Plan 02 ya ejecutó `npm install --save-dev supertest`, package.json ya tendrá supertest — en ese caso conservar la versión exacta que npm instaló en lugar de `^7.2.2`. Si el Plan 02 aún no ha corrido, usar `^7.2.2` como placeholder (npm install lo actualizará).

```json
{
  "name": "edf-lab-api",
  "version": "1.0.0",
  "description": "API REST educativa para EDF Lab — enseña Express, JSON y CRUD a principiantes",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev":   "nodemon index.js",
    "test":  "node --test index.test.js --test-force-exit"
  },
  "keywords": ["educativo", "express", "rest-api", "crud", "node"],
  "author": "edefrutos",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^4.18.2",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "supertest": "^7.2.2"
  }
}
```

Campos que cambian respecto al estado actual (per D-08, D-09):
- `name`: "test-project" → "edf-lab-api"
- `description`: "A simple test project" → "API REST educativa para EDF Lab — enseña Express, JSON y CRUD a principiantes"
- `scripts.test`: placeholder echo → "node --test index.test.js --test-force-exit"
- `scripts.dev`: (no existía) → "nodemon index.js"
- `keywords`: ["test","demo","nodejs"] → ["educativo","express","rest-api","crud","node"]
- `author`: "Test Author" → "edefrutos"
- `devDependencies`: añadir supertest (si Plan 02 ya instaló, conservar versión exacta de package-lock.json)

NO cambiar las versions de dependencies existentes (cors, express, lodash, nodemon).
  </action>
  <verify>
    <automated>node -e "const p=require('/Users/edefrutos/Desktop/EDF-Lab-Educativo/api/package.json'); console.log(p.name==='edf-lab-api'?'NAME_OK':'NAME_FAIL', p.scripts.dev==='nodemon index.js'?'DEV_OK':'DEV_FAIL', p.scripts.test.includes('node --test')?'TEST_OK':'TEST_FAIL', p.author==='edefrutos'?'AUTHOR_OK':'AUTHOR_FAIL')"</automated>
  </verify>
  <done>
    - api/package.json contiene `"name": "edf-lab-api"`
    - api/package.json contiene `"dev": "nodemon index.js"` en scripts
    - api/package.json contiene `"test": "node --test index.test.js --test-force-exit"` en scripts
    - api/package.json contiene `"author": "edefrutos"`
    - api/package.json contiene `"supertest"` en devDependencies
    - node --check no aplica a JSON, pero `node -e "require('./api/package.json')"` no lanza error
  </done>
</task>

<task type="auto">
  <name>Task 2: Actualizar api/README.md y documentar fix parseUserId en NOTEBOOK.md</name>
  <files>api/README.md, NOTEBOOK.md</files>
  <read_first>
    - api/README.md (leer entero para encontrar la sección "## Validaciones recomendadas" a reemplazar, y el final del archivo)
    - NOTEBOOK.md (leer entero para añadir al final sin romper el formato existente)
  </read_first>
  <action>
**Cambio A — api/README.md:**

Localizar la sección "## Validaciones recomendadas" (aproximadamente líneas 261-275) y REEMPLAZARLA por la sección expandida "## Comprobaciones y tests" (per D-10).

La sección existente empieza con `## Validaciones recomendadas` y termina antes de `## Relacion con el dashboard`. Reemplazar ese bloque completo por:

```markdown
## Comprobaciones y tests

Estos comandos comprueban la calidad del código. Ejecútalos desde `api/`.

**Sintaxis** — detecta errores de parseo JavaScript sin ejecutar el servidor:

```bash
node --check index.js
```

**Seguridad** — revisa vulnerabilidades conocidas en las dependencias:

```bash
npm audit --audit-level=high
```

**Tests automáticos** — ejecuta la suite de tests sobre todos los endpoints:

```bash
npm test
```

Resultado esperado: todos los tests en verde. El proceso termina solo (sin Ctrl+C).

**Desarrollo con recarga automática** — arranca el servidor y lo reinicia al guardar cambios:

```bash
npm run dev
```

Útil durante el desarrollo: evita tener que parar y volver a arrancar manualmente.
```

NO tocar la sección "## Relacion con el dashboard" ni ninguna otra sección del README.

---

**Cambio B — NOTEBOOK.md:**

Añadir al final del archivo (sin modificar el contenido existente) la siguiente entrada:

```markdown

---

## Fase 3 — Fix: parseUserId rechazaba mal los IDs con prefijo numérico

**Fecha:** 2026-05-28
**Archivo:** `api/index.js` función `parseUserId()`

### Error encontrado

`Number.parseInt('1abc', 10)` devuelve `1` — acepta el prefijo numérico y descarta el resto.
Esto hacía que `GET /users/1abc` no devolviera 400 sino que buscaba el usuario con id=1.

### Comportamiento incorrecto

```bash
# Antes del fix:
curl http://localhost:3100/users/1abc
# → 200 { id: 1, name: 'John Doe', ... }  ← debería ser 400
```

### Fix aplicado

```javascript
// ANTES (bug):
function parseUserId(value) {
  const id = Number.parseInt(value, 10);  // '1abc' → 1
  return Number.isInteger(id) ? id : null;
}

// DESPUÉS (fix):
function parseUserId(value) {
  // Number.parseInt('1abc', 10) devuelve 1 — acepta prefijo numérico.
  // Number('1abc') devuelve NaN — rechaza cualquier carácter no numérico.
  const id = Number(value);
  return Number.isInteger(id) && id > 0 ? id : null;
}
```

### Aprendizaje

`Number.parseInt` está diseñado para parsear texto con unidades (`parseInt('10px')` → 10). Para validar que un string ES un entero puro, `Number()` es más estricto: convierte el string completo o devuelve `NaN`.

La condición `id > 0` también rechaza `0` como ID válido, lo que es correcto porque los IDs empiezan en 1.

### Tests que documentan el fix

```bash
# Desde api/
npm test
# Los tres casos están en la suite "Validación de IDs":
# ✓ GET /users/1abc responde 400
# ✓ GET /users/0 responde 400
# ✓ GET /users/abc responde 400
```
```
  </action>
  <verify>
    <automated>grep "Comprobaciones y tests" /Users/edefrutos/Desktop/EDF-Lab-Educativo/api/README.md &amp;&amp; grep "npm test" /Users/edefrutos/Desktop/EDF-Lab-Educativo/api/README.md &amp;&amp; grep "npm run dev" /Users/edefrutos/Desktop/EDF-Lab-Educativo/api/README.md &amp;&amp; grep "parseUserId" /Users/edefrutos/Desktop/EDF-Lab-Educativo/NOTEBOOK.md &amp;&amp; echo "DOCS_OK"</automated>
  </verify>
  <done>
    - api/README.md contiene `## Comprobaciones y tests` (reemplazó `## Validaciones recomendadas`)
    - api/README.md contiene los 4 comandos: `node --check`, `npm audit`, `npm test`, `npm run dev`
    - Cada comando tiene una explicación en prosa de qué hace (no solo el comando en bloque bash)
    - api/README.md conserva la sección `## Relacion con el dashboard` sin cambios
    - NOTEBOOK.md contiene una entrada sobre `parseUserId` con el fix documentado
    - NOTEBOOK.md contiene el bloque de código con ANTES/DESPUÉS del fix
    - NOTEBOOK.md contiene el apartado "Aprendizaje" explicando la diferencia Number.parseInt vs Number
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json name/scripts | Solo afecta metadatos npm e identidad del paquete. Sin superficie de ataque. |
| NOTEBOOK.md / README.md | Documentación estática. Sin ejecución de código. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | scripts.test en package.json | accept | El script usa `--test-force-exit` para evitar cuelgues. El comando no tiene parámetros de entrada de usuario. Riesgo nulo en contexto educativo local. |
| T-03-08 | Information Disclosure | NOTEBOOK.md registra rutas de archivo | accept | Solo rutas relativas de proyecto local. Sin credenciales ni secretos. Apropiado para un lab educativo. |
</threat_model>

<verification>
Verificaciones finales de este plan:

```bash
# Metadata package.json correcta
node -e "const p=require('./api/package.json'); console.log(p.name, p.author)"
# Esperado: edf-lab-api edefrutos

# Script test correcto (no es el placeholder)
node -e "const p=require('./api/package.json'); console.log(p.scripts.test)"
# Esperado: node --test index.test.js --test-force-exit

# Script dev presente
node -e "const p=require('./api/package.json'); console.log(p.scripts.dev)"
# Esperado: nodemon index.js

# Sección de comandos en README
grep "## Comprobaciones y tests" api/README.md
grep "npm run dev" api/README.md

# Fix documentado en NOTEBOOK
grep "parseUserId" NOTEBOOK.md
grep "Number.parseInt" NOTEBOOK.md
```
</verification>

<success_criteria>
- `node -e "require('./api/package.json').name"` devuelve "edf-lab-api"
- `node -e "require('./api/package.json').scripts.test"` devuelve "node --test index.test.js --test-force-exit"
- `node -e "require('./api/package.json').scripts.dev"` devuelve "nodemon index.js"
- `grep "Comprobaciones y tests" api/README.md` encuentra la sección
- `grep "npm run dev" api/README.md` encuentra el comando con su explicación
- `grep "parseUserId" NOTEBOOK.md` encuentra la entrada del fix
- `grep "Validaciones recomendadas" api/README.md` NO encuentra nada (fue reemplazada)
</success_criteria>

<output>
Tras completar este plan, crear `.planning/phases/03-api-tests-and-quality-fixes/03-03-SUMMARY.md` siguiendo la plantilla en @/Users/edefrutos/.claude/get-shit-done/templates/summary.md
</output>
