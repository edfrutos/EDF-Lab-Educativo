---
phase: 03-api-tests-and-quality-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - api/index.test.js
  - api/data/users.test.json
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04

must_haves:
  truths:
    - "npm test desde api/ ejecuta la suite y termina el proceso solo"
    - "Tests cubren GET /health y GET /users (TEST-02)"
    - "Tests cubren POST /users, PUT /users/:id, DELETE /users/:id (TEST-03)"
    - "Tests cubren los 3 casos de ID inválido: 1abc, 0, abc (TEST-04)"
    - "Tests cubren validación de payload: name vacío y email vacío"
    - "Cada test parte de un fixture limpio — los tests no se contaminan entre sí"
    - "users.json real nunca se modifica durante los tests"
  artifacts:
    - path: "api/index.test.js"
      provides: "Suite completa de tests HTTP con node:test + supertest"
      min_lines: 100
      exports: []
    - path: "api/data/users.test.json"
      provides: "Fixture de tests: 2 usuarios conocidos, nextId=3"
      contains: "nextId"
  key_links:
    - from: "api/index.test.js"
      to: "api/index.js"
      via: "require('./index.js') con process.env.DATA_FILE seteado antes"
      pattern: "process\\.env\\.DATA_FILE.*TEST_FILE"
    - from: "beforeEach"
      to: "api/data/users.test.json"
      via: "writeFile con TEST_SEED antes de cada test"
      pattern: "writeFile.*TEST_FILE.*TEST_SEED"
---

<objective>
Instalar supertest y escribir la suite completa de tests HTTP en api/index.test.js.

Purpose: Es la entrega principal de TEST-01/02/03/04. Con index.js ya preparado (Plan 01), este plan instala la única dependencia nueva (supertest), crea el fixture de tests, y escribe todos los casos de test usando node:test + Arrange-Act-Assert. El alumno podrá ejecutar `npm test` y ver todos los endpoints verificados en verde.

Output: api/index.test.js con suite completa; api/data/users.test.json como fixture; supertest en devDependencies.
</objective>

<execution_context>
@/Users/edefrutos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edefrutos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-api-tests-and-quality-fixes/03-CONTEXT.md
@.planning/phases/03-api-tests-and-quality-fixes/03-RESEARCH.md
@.planning/phases/03-api-tests-and-quality-fixes/03-PATTERNS.md
@.planning/phases/03-api-tests-and-quality-fixes/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos de api/index.js que los tests deben respetar — extraídos del código fuente -->

Respuestas HTTP exactas (strings en español — NO parafrasear en los assert):
```javascript
// GET /health — 200:
{ status: 'healthy', timestamp: '<ISO string>' }

// GET /users — 200:
// Array ordenado alfabéticamente por name (_.sortBy)
// Con fixture TEST_SEED: [{ id:2, name:'Jane Smith', ... }, { id:1, name:'John Doe', ... }]

// POST /users — 201 (éxito):
{ id: <number>, name: '<trimmed>', email: '<trimmed>' }

// POST /users — 400 (name vacío):
{ error: 'El campo "name" es obligatorio y debe ser texto.' }

// POST /users — 400 (email vacío):
{ error: 'El campo "email" es obligatorio y debe ser texto.' }

// PUT /users/:id — 200 (éxito):
{ id: <number>, name: '<trimmed>', email: '<trimmed>' }

// DELETE /users/:id — 200 (éxito):
{ message: 'Usuario eliminado correctamente.', user: { id, name, email } }

// GET|PUT|DELETE /users/:id — 400 (ID inválido):
{ error: 'El parámetro ":id" debe ser un número entero.' }

// GET|PUT|DELETE /users/:id — 404 (no encontrado):
{ error: 'Usuario no encontrado.' }
```

Fixture TEST_SEED (semilla controlada — NO copiar users.json que tiene 3 usuarios por Fase 2):
```json
{
  "users": [
    { "id": 1, "name": "John Doe",   "email": "john@example.com" },
    { "id": 2, "name": "Jane Smith", "email": "jane@example.com" }
  ],
  "nextId": 3
}
```

Orden crítico de imports en index.test.js:
```javascript
// 1. process.env.DATA_FILE ANTES del require de index.js (Node cachea módulos)
process.env.DATA_FILE = TEST_FILE;
// 2. Luego el require
const app = require('./index.js');
// 3. supertest después
const request = require('supertest');
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Instalar supertest y crear api/data/users.test.json</name>
  <files>api/package.json, api/data/users.test.json</files>
  <read_first>
    - api/package.json (verificar devDependencies actuales antes de instalar)
    - api/data/users.json (confirmar schema del fixture)
  </read_first>
  <action>
**Paso 1 — Instalar supertest como devDependency:**
```bash
cd /Users/edefrutos/Desktop/EDF-Lab-Educativo/api
npm install --save-dev supertest
```
Esto añade `"supertest": "^7.2.2"` a devDependencies en package.json y actualiza package-lock.json.

**Paso 2 — Crear api/data/users.test.json con fixture controlado:**
Crear el archivo con exactamente este contenido (2 usuarios conocidos, nextId=3):
```json
{
  "users": [
    { "id": 1, "name": "John Doe",   "email": "john@example.com" },
    { "id": 2, "name": "Jane Smith", "email": "jane@example.com" }
  ],
  "nextId": 3
}
```
Este archivo sirve como seed de referencia en git. beforeEach lo sobreescribirá con writeFile antes de cada test, así que su contenido en disco es siempre el canónico.

NO modificar api/data/users.json.
  </action>
  <verify>
    <automated>node -e "require('/Users/edefrutos/Desktop/EDF-Lab-Educativo/api/node_modules/supertest')" &amp;&amp; echo "SUPERTEST_OK" &amp;&amp; grep '"supertest"' /Users/edefrutos/Desktop/EDF-Lab-Educativo/api/package.json &amp;&amp; cat /Users/edefrutos/Desktop/EDF-Lab-Educativo/api/data/users.test.json | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.log(d.users.length===2 &amp;&amp; d.nextId===3 ? 'FIXTURE_OK' : 'FAIL')"</automated>
  </verify>
  <done>
    - api/package.json contiene `"supertest"` en devDependencies
    - api/node_modules/supertest/ existe
    - api/data/users.test.json existe con 2 usuarios y nextId=3
    - api/data/users.json NO ha sido modificado
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Escribir api/index.test.js — suite completa node:test + supertest</name>
  <files>api/index.test.js</files>
  <read_first>
    - api/index.js (leer entero — contratos de respuesta, mensajes de error exactos en español)
    - api/data/users.test.json (confirmar estructura del fixture)
    - .planning/phases/03-api-tests-and-quality-fixes/03-PATTERNS.md (patrones de test AAA y mensajes exactos)
  </read_first>
  <behavior>
    Suite completa con estos casos (todos deben PASAR con npm test):

    GET /health:
    - responde 200 con status 'healthy' y campo timestamp de tipo string

    GET /users:
    - responde 200 con array de 2 usuarios ordenados por nombre (Jane Smith antes que John Doe)

    POST /users:
    - payload válido { name: 'Nueva Persona', email: 'nueva@example.com' } → 201 + objeto con id numérico
    - payload con name='' → 400 + { error: 'El campo "name" es obligatorio y debe ser texto.' }
    - payload con email='' → 400 + { error: 'El campo "email" es obligatorio y debe ser texto.' }

    PUT /users/:id:
    - actualizar id=1 con { name: 'John Updated', email: 'john.new@example.com' } → 200 + objeto actualizado
    - id inexistente (99) → 404 + { error: 'Usuario no encontrado.' }

    DELETE /users/:id:
    - eliminar id=1 → 200 + { message: 'Usuario eliminado correctamente.', user: { id:1, ... } }
    - id inexistente (99) → 404 + { error: 'Usuario no encontrado.' }

    Validación de IDs (TEST-04, decisiones D-06/D-07):
    - GET /users/1abc → 400 + { error: 'El parámetro ":id" debe ser un número entero.' }
    - GET /users/0 → 400
    - GET /users/abc → 400
  </behavior>
  <action>
Crear api/index.test.js con el siguiente contenido completo:

```javascript
'use strict';

// ─── Setup ───────────────────────────────────────────────────────────────────
// CRÍTICO: DATA_FILE debe asignarse ANTES del require de index.js.
// Node.js cachea módulos en el primer require — si index.js se importa antes
// de setear la variable, DATA_FILE_PATH quedará con el valor por defecto.

const { describe, it, beforeEach, afterEach } = require('node:test');
const assert = require('node:assert/strict');
const { writeFile, unlink } = require('fs/promises');
const path = require('path');

const TEST_FILE = path.join(__dirname, 'data', 'users.test.json');
process.env.DATA_FILE = TEST_FILE;

const app = require('./index.js');
const request = require('supertest');

// Semilla controlada: 2 usuarios conocidos, nextId=3.
// NO se usa copyFile(users.json) porque ese archivo tiene 3 usuarios (estado Fase 2).
const TEST_SEED = {
  users: [
    { id: 1, name: 'John Doe',   email: 'john@example.com' },
    { id: 2, name: 'Jane Smith', email: 'jane@example.com' }
  ],
  nextId: 3
};

beforeEach(async () => {
  // Arrange: restaurar fixture limpio antes de cada test
  await writeFile(TEST_FILE, JSON.stringify(TEST_SEED, null, 2), 'utf8');
});

afterEach(async () => {
  // Cleanup: eliminar fixture tras cada test
  await unlink(TEST_FILE).catch(() => {});
});

// ── GET /health ───────────────────────────────────────────────────────────────

describe('GET /health', () => {
  it('responde 200 con status healthy y timestamp', async () => {
    // Arrange — fixture listo por beforeEach
    // Act
    const res = await request(app).get('/health');
    // Assert
    assert.equal(res.status, 200);
    assert.equal(res.body.status, 'healthy');
    assert.ok(typeof res.body.timestamp === 'string', 'timestamp debe ser string');
  });
});

// ── GET /users ────────────────────────────────────────────────────────────────

describe('GET /users', () => {
  it('responde 200 con array de usuarios ordenados por nombre', async () => {
    // Arrange — fixture con John Doe (id=1) y Jane Smith (id=2)
    // Act
    const res = await request(app).get('/users');
    // Assert
    assert.equal(res.status, 200);
    assert.ok(Array.isArray(res.body), 'la respuesta debe ser un array');
    assert.equal(res.body.length, 2);
    // getSortedUsers() aplica _.sortBy(users, 'name') — Jane precede a John
    assert.equal(res.body[0].name, 'Jane Smith');
    assert.equal(res.body[1].name, 'John Doe');
  });
});

// ── POST /users ───────────────────────────────────────────────────────────────

describe('POST /users', () => {
  it('crea un usuario válido y responde 201 con el objeto creado', async () => {
    // Arrange
    const payload = { name: 'Nueva Persona', email: 'nueva@example.com' };
    // Act
    const res = await request(app).post('/users').send(payload);
    // Assert
    assert.equal(res.status, 201);
    assert.equal(res.body.name, payload.name);
    assert.equal(res.body.email, payload.email);
    assert.ok(typeof res.body.id === 'number', 'id debe ser número');
  });

  it('responde 400 si name está vacío', async () => {
    // Arrange
    const payload = { name: '', email: 'x@example.com' };
    // Act
    const res = await request(app).post('/users').send(payload);
    // Assert
    assert.equal(res.status, 400);
    assert.equal(res.body.error, 'El campo "name" es obligatorio y debe ser texto.');
  });

  it('responde 400 si email está vacío', async () => {
    // Arrange
    const payload = { name: 'Alguien', email: '' };
    // Act
    const res = await request(app).post('/users').send(payload);
    // Assert
    assert.equal(res.status, 400);
    assert.equal(res.body.error, 'El campo "email" es obligatorio y debe ser texto.');
  });
});

// ── PUT /users/:id ────────────────────────────────────────────────────────────

describe('PUT /users/:id', () => {
  it('actualiza un usuario existente y responde 200 con el objeto actualizado', async () => {
    // Arrange
    const payload = { name: 'John Updated', email: 'john.new@example.com' };
    // Act
    const res = await request(app).put('/users/1').send(payload);
    // Assert
    assert.equal(res.status, 200);
    assert.equal(res.body.id, 1);
    assert.equal(res.body.name, payload.name);
    assert.equal(res.body.email, payload.email);
  });

  it('responde 404 si el usuario no existe', async () => {
    // Arrange
    const payload = { name: 'Alguien', email: 'x@example.com' };
    // Act
    const res = await request(app).put('/users/99').send(payload);
    // Assert
    assert.equal(res.status, 404);
    assert.equal(res.body.error, 'Usuario no encontrado.');
  });
});

// ── DELETE /users/:id ─────────────────────────────────────────────────────────

describe('DELETE /users/:id', () => {
  it('elimina un usuario existente y responde 200 con mensaje y datos del usuario', async () => {
    // Arrange — fixture tiene usuario con id=1 (John Doe)
    // Act
    const res = await request(app).delete('/users/1');
    // Assert
    assert.equal(res.status, 200);
    assert.equal(res.body.message, 'Usuario eliminado correctamente.');
    assert.equal(res.body.user.id, 1);
    assert.equal(res.body.user.name, 'John Doe');
  });

  it('responde 404 si el usuario no existe', async () => {
    // Arrange
    // Act
    const res = await request(app).delete('/users/99');
    // Assert
    assert.equal(res.status, 404);
    assert.equal(res.body.error, 'Usuario no encontrado.');
  });
});

// ── Validación de IDs (TEST-04) ───────────────────────────────────────────────
// Cubre los tres tipos de ID inválido: string con prefijo numérico, cero, y string puro.

describe('Validación de IDs', () => {
  it('GET /users/1abc responde 400 (string con prefijo numérico)', async () => {
    // Arrange — (sin fixture especial; el handler rechaza antes de buscar en datos)
    // Act
    const res = await request(app).get('/users/1abc');
    // Assert
    assert.equal(res.status, 400);
    assert.equal(res.body.error, 'El parámetro ":id" debe ser un número entero.');
  });

  it('GET /users/0 responde 400 (cero no es ID válido)', async () => {
    // Act
    const res = await request(app).get('/users/0');
    // Assert
    assert.equal(res.status, 400);
    assert.equal(res.body.error, 'El parámetro ":id" debe ser un número entero.');
  });

  it('GET /users/abc responde 400 (string no numérico)', async () => {
    // Act
    const res = await request(app).get('/users/abc');
    // Assert
    assert.equal(res.status, 400);
    assert.equal(res.body.error, 'El parámetro ":id" debe ser un número entero.');
  });
});
```

El archivo debe crearse completo exactamente como se muestra. No añadir ni omitir bloques.
  </action>
  <verify>
    <automated>cd /Users/edefrutos/Desktop/EDF-Lab-Educativo/api &amp;&amp; node --check index.test.js &amp;&amp; echo "SYNTAX_OK" &amp;&amp; grep -c "describe(" index.test.js &amp;&amp; grep "process.env.DATA_FILE = TEST_FILE" index.test.js &amp;&amp; echo "STRUCTURE_OK"</automated>
  </verify>
  <done>
    - api/index.test.js existe y pasa node --check
    - Contiene 6 bloques describe (health, users, POST, PUT, DELETE, Validación IDs)
    - process.env.DATA_FILE se asigna antes del require('./index.js')
    - beforeEach usa writeFile con TEST_SEED
    - afterEach usa unlink con catch vacío
    - Todos los assert usan strings exactos en español para mensajes de error
    - grep -c "it(" api/index.test.js devuelve al menos 11
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TEST_SEED → writeFile → DATA_FILE_PATH | Fixture controlado en código — no hay entrada de usuario en este flujo |
| request(app).METHOD() → Express handlers | supertest llama a los handlers directamente; los handlers validan input (cubierto en Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | api/data/users.json durante tests | mitigate | DATA_FILE env var apunta a users.test.json; beforeEach/afterEach aíslan el fixture. Plan 01 preparó esta arquitectura. |
| T-03-05 | Denial of Service | proceso npm test colgado | mitigate | Script usa `--test-force-exit` (definido en Plan 03). supertest puede dejar conexiones TCP abiertas sin esta flag. |
| T-03-06 | Information Disclosure | users.json real contaminado por tests de mutación | mitigate | process.env.DATA_FILE seteado antes del require garantiza que DATA_FILE_PATH nunca apunta a users.json durante tests. |
</threat_model>

<verification>
Tras completar ambas tareas, ejecutar la suite completa:

```bash
cd /Users/edefrutos/Desktop/EDF-Lab-Educativo/api
DATA_FILE=data/users.test.json node --test index.test.js --test-force-exit
```

Resultado esperado:
- Todos los tests en verde (✓)
- "pass 11" o más en el resumen final
- El proceso termina solo (no es necesario Ctrl+C)
- users.json real no modificado: `cat api/data/users.json` sigue con 3 usuarios

Verificación de aislamiento:
```bash
# El archivo de test no debe existir entre ejecuciones (afterEach lo elimina)
ls api/data/users.test.json
# Esperado: No such file or directory
```
</verification>

<success_criteria>
- `cd api && DATA_FILE=data/users.test.json node --test index.test.js --test-force-exit` pasa todos los tests
- "fail 0" en el resumen de la suite
- api/data/users.json tiene el mismo contenido antes y después de ejecutar los tests
- api/index.test.js contiene `process.env.DATA_FILE = TEST_FILE` antes del require de index.js
- api/index.test.js contiene todos los strings de error exactos en español
</success_criteria>

<output>
Tras completar este plan, crear `.planning/phases/03-api-tests-and-quality-fixes/03-02-SUMMARY.md` siguiendo la plantilla en @/Users/edefrutos/.claude/get-shit-done/templates/summary.md
</output>
