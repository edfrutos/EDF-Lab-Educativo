---
phase: 23-vue-dashboard-auth
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - dashboard-vue/src/App.vue
  - dashboard-vue/README.md
autonomous: true
requirements:
  - FRWK-AUTH-05
  - FRWK-AUTH-06
must_haves:
  truths:
    - "D-08: Full shell hidden until isAuthenticated"
    - "D-09: isBootstrapping shows Comprobando sesión"
    - "D-07: bootstrapAuth on onMounted health then users probe"
    - "D-14: Logout in toolbar beside Recargar datos"
    - "D-16 D-17: 401 returns to gate with Spanish message"
    - "D-11: v-if template blocks for bootstrap gate dashboard"
    - "Cookie visible on :3100 requests after login on :5175"
  artifacts:
    - path: dashboard-vue/src/App.vue
      provides: "auth bootstrap and v-if gating"
      contains: "isAuthenticated"
    - path: dashboard-vue/README.md
      provides: "Vue login documentation"
      contains: "emit"
  key_links:
    - from: dashboard-vue/src/App.vue
      to: dashboard-vue/src/components/LoginGate.vue
      via: "@login handler"
      pattern: "LoginGate"
    - from: dashboard-vue/src/App.vue
      to: api logout
      via: "handleLogout"
      pattern: "logout()"
---

<objective>
Wire auth bootstrap, v-if gating, logout, 401 UX in App.vue; update README (FRWK-AUTH-05, FRWK-AUTH-06).

Purpose: Vue auth parity with React Phase 22 on port 5175.
Output: `dashboard-vue/src/App.vue`, `dashboard-vue/README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-vue-dashboard-auth/23-CONTEXT.md
@.planning/phases/23-vue-dashboard-auth/23-RESEARCH.md
@dashboard-react/src/App.jsx
@dashboard-vue/src/App.vue
@dashboard-vue/src/components/LoginGate.vue
@dashboard-vue/src/api.js
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Dashboard flash before auth | Medium | isBootstrapping + v-if gate |
| 401 as connection error | Medium | Branch error.status === 401 before loadError |
| Stale session in UI | Medium | clearDashboardData on logout/401 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: App.vue — refs, bootstrapAuth, v-if template</name>
  <read_first>23-CONTEXT.md (D-04, D-07, D-08, D-11), dashboard-react/src/App.jsx</read_first>
  <files>dashboard-vue/src/App.vue</files>
  <action>
    1. Add refs: `isAuthenticated` (false), `isBootstrapping` (true), `loginError` (''), `isLoginSubmitting` (false).
    2. Import `LoginGate`, `login`, `logout` from api.js.
    3. Implement `bootstrapAuth` (mirror React App.jsx):
       - health check → on fail: clearDashboardData, loadError, isAuthenticated false
       - users probe → 200: isAuthenticated true, loadDashboardData()
       - 401 on probe: isAuthenticated false, loginError '' (first visit)
    4. Replace `onMounted(loadDashboardData)` with `onMounted(bootstrapAuth)`.
    5. Restructure `<template>` with three blocks:
       - `v-if="isBootstrapping"` — «Comprobando sesión…»
       - `v-else-if="!isAuthenticated"` — LoginGate `:error="loginError"` `:is-submitting="isLoginSubmitting"` `@login="handleLogin"` + optional rose loadError section
       - `v-else` — existing dashboard sections (hero, toolbar, CRUD, pedagogy)
    6. `handleLogin(email, password)`: call login(); on success isAuthenticated true, loadDashboardData(); 403 → loginError inline.
    7. Add `AUTH_FALLBACK` constant and `getAuthMessage(error)` helper like React App.jsx.
  </action>
  <verify>
    <automated>grep -q 'isAuthenticated' dashboard-vue/src/App.vue && grep -q 'bootstrapAuth' dashboard-vue/src/App.vue && grep -q 'LoginGate' dashboard-vue/src/App.vue && grep -q 'v-else-if' dashboard-vue/src/App.vue</automated>
  </verify>
  <acceptance_criteria>
    - onMounted calls bootstrapAuth
    - v-if blocks gate unauthenticated users
    - @login wired to handleLogin
  </acceptance_criteria>
  <done>Vue auth bootstrap and gating</done>
</task>

<task type="auto">
  <name>Task 2: App.vue — 401, logout, toolbar</name>
  <read_first>23-CONTEXT.md (D-14–D-19), dashboard-react/src/App.jsx</read_first>
  <files>dashboard-vue/src/App.vue</files>
  <action>
    1. `returnToLoginGate(message)` helper: clearDashboardData, isAuthenticated false, loadError '', loginError message.
    2. `loadDashboardData` catch: if error.status === 401 → returnToLoginGate(getAuthMessage(error)); return.
    3. `handleMutationError`: if 401 → resetUserForm + returnToLoginGate; else existing 409 logic.
    4. `handleLogout`: await logout(); resetUserForm(); returnToLoginGate('') with loginError cleared.
    5. Toolbar: add «Cerrar sesión» button before «Recargar datos» (muted border style like React).
    6. Recargar datos still calls loadDashboardData only.
  </action>
  <verify>
    <automated>grep -q 'Cerrar sesión' dashboard-vue/src/App.vue && grep -q 'handleLogout' dashboard-vue/src/App.vue && grep -q '401' dashboard-vue/src/App.vue</automated>
  </verify>
  <acceptance_criteria>
    - Logout in toolbar
    - 401 flips to gate
    - loadError only for connectivity
  </acceptance_criteria>
  <done>401 and logout complete</done>
</task>

<task type="auto">
  <name>Task 3: README — Vue login and emit pattern</name>
  <read_first>dashboard-vue/README.md, dashboard-react/README.md, 23-CONTEXT.md D-05</read_first>
  <files>dashboard-vue/README.md</files>
  <action>
    Update README:
    1. Replace «sin pantalla de login» / AUTH_DISABLED-primary with: Vue dashboard **includes login** on :5175.
    2. Document httpOnly cookie + `credentials: 'include'` in `src/api.js`.
    3. Note **LoginGate.vue** emits `login` to App (contrast with React callback — didactic bullet).
    4. Lab credentials admin@lab.local / changeme.
    5. AUTH_DISABLED test-only mention.
    6. Link `docs/17-autenticacion.md`.
    7. Pointer to phase 23 UAT checklist (Task 4).
  </action>
  <verify>
    <automated>grep -q 'login\|Iniciar sesión\|credenciales' dashboard-vue/README.md && ! grep -q 'sin pantalla de login' dashboard-vue/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README documents Vue auth and emit pattern
  </acceptance_criteria>
  <done>README updated</done>
</task>

<task type="checkpoint" human="true">
  <name>Task 4: Manual UAT — Vue auth on :5175</name>
  <read_first>23-RESEARCH.md Manual UAT</read_first>
  <action>
    API on 3100 without AUTH_DISABLED; Vue on 5175:
    1. First visit — login gate only
    2. Wrong password — inline error
    3. Login admin@lab.local / changeme — dashboard loads
    4. Network tab: Cookie on :3100/users
    5. Cerrar sesión — gate returns
    6. Delete cookie, Recargar — gate with 401 message
    7. Optional: POST after cookie delete → gate
  </action>
  <acceptance_criteria>
    - FRWK-AUTH-05 and FRWK-AUTH-06 observable in browser
  </acceptance_criteria>
  <done>Phase 23 auth verified</done>
</task>

</tasks>

<verification>
- Grep checks
- curl Origin :5175 (optional automated)
- Manual UAT Task 4
</verification>

<success_criteria>
- Vue auth parity with React Phase 22 on :5175
- FRWK-AUTH-05 and FRWK-AUTH-06 satisfied
</success_criteria>
