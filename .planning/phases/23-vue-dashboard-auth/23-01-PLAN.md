---
phase: 23-vue-dashboard-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard-vue/src/api.js
  - dashboard-vue/src/components/LoginGate.vue
autonomous: true
requirements:
  - FRWK-AUTH-05
  - FRWK-AUTH-06
must_haves:
  truths:
    - "D-06: login() and logout() exported from api.js using fetchJson with credentials"
    - "D-00e: fetchJson retains credentials include on all requests"
    - "D-03: LoginGate.vue provides email/password form with emit login"
    - "D-12 D-13: Tailwind card with lab credentials hint on port 5175"
    - "D-15: LoginGate shows inline error prop; not global loadError"
  artifacts:
    - path: dashboard-vue/src/api.js
      provides: "login and logout helpers"
      contains: "export async function login"
    - path: dashboard-vue/src/components/LoginGate.vue
      provides: "login form SFC"
      contains: "Iniciar sesión"
  key_links:
    - from: dashboard-vue/src/api.js
      to: api POST /auth/login
      via: "login()"
      pattern: "/auth/login"
    - from: dashboard-vue/src/components/LoginGate.vue
      to: dashboard-vue/src/App.vue
      via: "emit login"
      pattern: "defineEmits"
---

<objective>
Add `login()`/`logout()` to Vue `api.js` and create `LoginGate.vue` with emit-based form (FRWK-AUTH-05 partial, FRWK-AUTH-06).

Purpose: HTTP + UI building blocks; App.vue wiring in plan 23-02.
Output: `dashboard-vue/src/api.js`, `dashboard-vue/src/components/LoginGate.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-vue-dashboard-auth/23-CONTEXT.md
@.planning/phases/23-vue-dashboard-auth/23-RESEARCH.md
@.planning/phases/23-vue-dashboard-auth/23-PATTERNS.md
@.planning/phases/22-react-dashboard-auth/22-01-PLAN.md
@dashboard-react/src/api.js
@dashboard-react/src/components/LoginGate.jsx
@dashboard-vue/src/api.js
@api/auth.js
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| XSS steals session | High | httpOnly cookie only; no localStorage |
| Credentials omitted | High | login/logout via fetchJson with credentials include |
| Password logged | Medium | No console.log of credentials |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: api.js — login() and logout()</name>
  <read_first>23-CONTEXT.md (D-06), dashboard-react/src/api.js, dashboard-vue/src/api.js</read_first>
  <files>dashboard-vue/src/api.js</files>
  <action>
    1. Confirm `fetchJson` has `credentials: 'include'` — keep as-is.
    2. Add exports mirroring React `api.js`:
       ```javascript
       export async function login(email, password) {
         return fetchJson('/auth/login', {
           method: 'POST',
           headers: { 'Content-Type': 'application/json' },
           body: JSON.stringify({ email, password }),
         });
       }

       export async function logout() {
         return fetchJson('/auth/logout', { method: 'POST' });
       }
       ```
    3. No new dependencies.
  </action>
  <verify>
    <automated>grep -q "export async function login" dashboard-vue/src/api.js && grep -q "export async function logout" dashboard-vue/src/api.js && grep -q "credentials: 'include'" dashboard-vue/src/api.js</automated>
  </verify>
  <acceptance_criteria>
    - login and logout exported
    - Both use fetchJson
  </acceptance_criteria>
  <done>Vue API auth helpers ready</done>
</task>

<task type="auto">
  <name>Task 2: LoginGate.vue — card form, emit login, hint</name>
  <read_first>23-CONTEXT.md (D-03, D-05, D-12–D-13), dashboard-react/src/components/LoginGate.jsx</read_first>
  <files>dashboard-vue/src/components/LoginGate.vue</files>
  <action>
    Create `LoginGate.vue` with `<script setup>`:

    Props: `error` (String, default ''), `isSubmitting` (Boolean, default false)
    Emits: **`login`** with payload `(email, password)` via `defineEmits(['login'])`

    Local `ref()` for email and password inputs.

    Template (Spanish, Tailwind — copy classes from LoginGate.jsx):
    - Card `max-w-md mx-auto rounded-xl border border-slate-200 bg-white p-6 shadow-sm`
    - Badge: «Variante Vue (Vite) — puerto 5175»
    - `h2` «Iniciar sesión»
    - Form `@submit.prevent` → `emit('login', email.trim(), password)`
    - Email + password inputs (required)
    - Inline error `p` with `role="alert"` `aria-live="polite"` when `error` non-empty
    - Submit «Entrar» disabled when `isSubmitting`
    - Hint: «Credenciales del lab: admin@lab.local / changeme»

    Do not call api.js from LoginGate — parent handles auth (D-05).
  </action>
  <verify>
    <automated>test -f dashboard-vue/src/components/LoginGate.vue && grep -q 'Iniciar sesión' dashboard-vue/src/components/LoginGate.vue && grep -q "defineEmits" dashboard-vue/src/components/LoginGate.vue && grep -q 'admin@lab.local' dashboard-vue/src/components/LoginGate.vue</automated>
  </verify>
  <acceptance_criteria>
    - LoginGate.vue exists with emit login
    - Lab hint visible
    - Tailwind card matches React gate
  </acceptance_criteria>
  <done>LoginGate.vue ready for App.vue</done>
</task>

</tasks>

<verification>
- Grep checks on api.js and LoginGate.vue
</verification>

<success_criteria>
- FRWK-AUTH-06 at HTTP layer
- LoginGate shell for FRWK-AUTH-05 (gating in 23-02)
</success_criteria>
