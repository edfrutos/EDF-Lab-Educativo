---
phase: 01-dashboard-crud-flow
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - docs/04-dashboard-fetch.md
  - missions/05-mejorar-dashboard.md
  - NOTEBOOK.md
autonomous: true
requirements:
  - DASH-05
must_haves:
  truths:
    - "Dashboard documentation explains browser-to-API mutation flow."
    - "Mission includes objective, steps, expected result, and extra challenge for CRUD."
    - "NOTEBOOK.md records the stale path fix or CRUD implementation lesson."
    - "D-08 and D-09 are reflected in docs as method/endpoint feedback examples."
  artifacts:
    - "docs/04-dashboard-fetch.md contains POST, PUT, and DELETE examples."
    - "missions/05-mejorar-dashboard.md teaches create/edit/delete from the dashboard."
    - "NOTEBOOK.md contains a 2026-05-26 entry for Phase 1 CRUD learning."
  key_links:
    - "Docs refer to the same UI copy and endpoints implemented in dashboard/app.js."
---

<objective>
Update learning material so Phase 1 is teachable, not only functional.

Purpose: The lab's rules require every learner-facing concept to have an executable example and every relevant real error to be documented.
Output: Updated dashboard fetch documentation, CRUD mission, and notebook entry.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-dashboard-crud-flow/01-CONTEXT.md
@.planning/phases/01-dashboard-crud-flow/01-UI-SPEC.md
@.planning/phases/01-dashboard-crud-flow/01-RESEARCH.md
@.planning/phases/01-dashboard-crud-flow/01-01-SUMMARY.md
@.planning/phases/01-dashboard-crud-flow/01-02-SUMMARY.md
@docs/04-dashboard-fetch.md
@missions/05-mejorar-dashboard.md
@NOTEBOOK.md
</context>

<threat_model>
Risk level: low.

Threats considered:
- Documentation must not instruct learners to run destructive commands outside the project.
- Examples should use local endpoints only and should not include secrets.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Explain non-GET fetch in dashboard docs</name>
  <files>docs/04-dashboard-fetch.md</files>
  <read_first>docs/04-dashboard-fetch.md, dashboard/app.js, .planning/phases/01-dashboard-crud-flow/01-UI-SPEC.md</read_first>
  <action>Expand `docs/04-dashboard-fetch.md` to explain that `fetch()` can send methods beyond GET. Add concise examples for `POST /users`, `PUT /users/:id`, and `DELETE /users/:id` using `method`, `headers: { 'Content-Type': 'application/json' }`, and `body: JSON.stringify(...)` where applicable. Include the feedback examples `POST /users -> usuario creado`, `PUT /users/:id -> usuario actualizado`, and `DELETE /users/:id -> usuario eliminado`.</action>
  <verify>rg -n "POST /users|PUT /users/:id|DELETE /users/:id|Content-Type|JSON.stringify|usuario creado" docs/04-dashboard-fetch.md</verify>
  <acceptance_criteria>
    - `docs/04-dashboard-fetch.md` contains `POST /users`.
    - `docs/04-dashboard-fetch.md` contains `PUT /users/:id`.
    - `docs/04-dashboard-fetch.md` contains `DELETE /users/:id`.
    - `docs/04-dashboard-fetch.md` contains `JSON.stringify`.
    - `docs/04-dashboard-fetch.md` contains `POST /users -> usuario creado`.
  </acceptance_criteria>
  <done>Dashboard fetch docs explain non-GET mutation requests.</done>
</task>

<task type="auto">
  <name>Task 2: Turn dashboard mission into a CRUD mission</name>
  <files>missions/05-mejorar-dashboard.md</files>
  <read_first>missions/05-mejorar-dashboard.md, docs/04-dashboard-fetch.md, .planning/PROJECT.md</read_first>
  <action>Update `missions/05-mejorar-dashboard.md` so it teaches the implemented CRUD dashboard flow. Preserve the required sections `Objetivo`, `Pasos`, `Resultado esperado`, and `Reto extra`. Include steps to create a user, edit that user, delete that user, and observe method/endpoint feedback. Keep rules: no frameworks, keep HTML/CSS/JS separated, document relevant learning in `NOTEBOOK.md`.</action>
  <verify>rg -n "Objetivo|Pasos|Resultado esperado|Reto extra|Crear usuario|Guardar cambios|Eliminar|POST /users|PUT /users/:id|DELETE /users/:id" missions/05-mejorar-dashboard.md</verify>
  <acceptance_criteria>
    - `missions/05-mejorar-dashboard.md` contains `## Objetivo`.
    - `missions/05-mejorar-dashboard.md` contains `## Pasos`.
    - `missions/05-mejorar-dashboard.md` contains `## Resultado esperado`.
    - `missions/05-mejorar-dashboard.md` contains `## Reto extra`.
    - `missions/05-mejorar-dashboard.md` contains `POST /users`, `PUT /users/:id`, and `DELETE /users/:id`.
  </acceptance_criteria>
  <done>Mission 05 guides learners through dashboard CRUD.</done>
</task>

<task type="auto">
  <name>Task 3: Record Phase 1 learning in NOTEBOOK</name>
  <files>NOTEBOOK.md</files>
  <read_first>NOTEBOOK.md, .planning/phases/01-dashboard-crud-flow/01-CONTEXT.md</read_first>
  <action>Add a dated `2026-05-26` notebook entry for Phase 1 Dashboard CRUD. Mention the stale dashboard help path fix if it was changed, explain why the shared form pattern was chosen, record that native `confirm()` was used for delete to avoid extra UI complexity, and note that method/endpoint feedback makes the HTTP flow visible to beginners.</action>
  <verify>rg -n "2026-05-26|Dashboard CRUD|confirm\\(\\)|POST /users|PUT /users/:id|DELETE /users/:id|formulario" NOTEBOOK.md</verify>
  <acceptance_criteria>
    - `NOTEBOOK.md` contains a `2026-05-26` entry for Dashboard CRUD.
    - `NOTEBOOK.md` mentions the shared form pattern.
    - `NOTEBOOK.md` mentions native `confirm()`.
    - `NOTEBOOK.md` mentions method/endpoint feedback for POST, PUT, or DELETE.
  </acceptance_criteria>
  <done>Notebook captures the relevant Phase 1 learning decisions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "POST /users|PUT /users/:id|DELETE /users/:id" docs/04-dashboard-fetch.md missions/05-mejorar-dashboard.md NOTEBOOK.md`
- [ ] Confirm `missions/05-mejorar-dashboard.md` has objective, steps, expected result, and extra challenge.
- [ ] Confirm docs mention method/endpoint feedback shown by the dashboard.
</verification>

<success_criteria>
- Non-GET fetch is explained with executable examples.
- Mission 05 teaches the CRUD dashboard flow.
- Notebook records the real decision and learning context.
- Documentation matches implemented UI behavior and endpoint copy.
</success_criteria>

<output>
After completion, create `.planning/phases/01-dashboard-crud-flow/01-03-SUMMARY.md`
</output>
