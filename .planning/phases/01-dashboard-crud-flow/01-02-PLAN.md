---
phase: 01-dashboard-crud-flow
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - dashboard/app.js
  - dashboard/styles.css
autonomous: true
requirements:
  - DASH-02
  - DASH-03
  - DASH-04
must_haves:
  truths:
    - "D-03 covered: create and edit use the same form."
    - "D-04 covered: clicking Edit loads row data into the shared form."
    - "D-05 covered: edit mode visibly says which user is being edited."
    - "D-06 covered: delete uses native confirm() before DELETE /users/:id."
    - "D-07 covered: no custom modal or inline confirmation component is introduced."
    - "D-08 and D-09 covered: edit/delete success feedback shows method and endpoint."
  artifacts:
    - "dashboard/app.js contains edit and delete row action handling."
    - "dashboard/styles.css contains row action and edit-mode styling."
  key_links:
    - "Rendered table action buttons map to `handleUsersTableClick` or equivalent delegated handler."
    - "PUT /users/:id and DELETE /users/:id success paths refresh GET /users table data."
---

<objective>
Complete the dashboard CRUD interaction loop by adding edit mode, cancel edit, row actions, native delete confirmation, PUT/DELETE requests, and mutation error feedback.

Purpose: Learners can now perform the full CRUD set from the browser and observe the HTTP method/endpoint used for each mutation.
Output: Updated dashboard behavior and styles for edit/delete interactions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-dashboard-crud-flow/01-CONTEXT.md
@.planning/phases/01-dashboard-crud-flow/01-UI-SPEC.md
@.planning/phases/01-dashboard-crud-flow/01-RESEARCH.md
@.planning/phases/01-dashboard-crud-flow/01-PATTERNS.md
@.planning/phases/01-dashboard-crud-flow/01-01-SUMMARY.md
@dashboard/app.js
@dashboard/styles.css
@api/index.js
</context>

<threat_model>
Risk level: low.

Threats considered:
- Delete is destructive. Native `confirm()` must run before the request is sent.
- User-controlled name/email continue to be rendered with `textContent`.
- API error messages should be displayed as text, never injected as HTML.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add row action rendering and delegated click handling</name>
  <files>dashboard/app.js</files>
  <read_first>dashboard/app.js, dashboard/index.html, .planning/phases/01-dashboard-crud-flow/01-UI-SPEC.md, .planning/phases/01-dashboard-crud-flow/01-PATTERNS.md</read_first>
  <action>Update `renderUsers(users)` so each row appends an actions cell containing `Editar` and `Eliminar` buttons with data attributes for user id and action. Store enough user data for edit mode by keeping the latest users array in a module-level variable such as `currentUsers = []` when rendering. Add a delegated click listener on `elements.usersTableBody` that routes `data-action="edit"` to edit mode and `data-action="delete"` to delete flow.</action>
  <verify>node --check dashboard/app.js</verify>
  <acceptance_criteria>
    - `dashboard/app.js` contains `Editar`.
    - `dashboard/app.js` contains `Eliminar`.
    - `dashboard/app.js` contains `data-action`.
    - `dashboard/app.js` contains a table body click listener or equivalent delegated action handler.
    - `node --check dashboard/app.js` exits 0.
  </acceptance_criteria>
  <done>Users table exposes edit/delete actions wired to JavaScript handlers.</done>
</task>

<task type="auto">
  <name>Task 2: Implement shared form edit mode with PUT /users/:id</name>
  <files>dashboard/app.js</files>
  <read_first>dashboard/app.js, api/index.js, .planning/phases/01-dashboard-crud-flow/01-CONTEXT.md, .planning/phases/01-dashboard-crud-flow/01-UI-SPEC.md</read_first>
  <action>Add `editingUserId` state initialized to `null`. Add `startEditingUser(user)` that fills `user-name-input` and `user-email-input`, sets `editingUserId`, changes submit button text to `Guardar cambios`, shows mode message `Editando usuario {id}` or `Editando usuario: {name}`, and reveals `Cancelar edición`. Add `resetUserForm()` that clears fields, sets `editingUserId = null`, restores submit text `Crear usuario`, clears edit mode message, and hides or de-emphasizes cancel. Update submit handling so edit mode sends `PUT /users/${editingUserId}` with JSON body and success feedback `PUT /users/:id -> usuario actualizado` before refreshing users.</action>
  <verify>node --check dashboard/app.js</verify>
  <acceptance_criteria>
    - `dashboard/app.js` contains `editingUserId`.
    - `dashboard/app.js` contains `Guardar cambios`.
    - `dashboard/app.js` contains `Cancelar edición`.
    - `dashboard/app.js` contains `method: 'PUT'`.
    - `dashboard/app.js` contains `PUT /users/:id -> usuario actualizado`.
    - `node --check dashboard/app.js` exits 0.
  </acceptance_criteria>
  <done>Edit mode uses the shared form and updates users via PUT.</done>
</task>

<task type="auto">
  <name>Task 3: Implement native-confirm delete flow and mutation errors</name>
  <files>dashboard/app.js, dashboard/styles.css</files>
  <read_first>dashboard/app.js, dashboard/styles.css, api/index.js, .planning/phases/01-dashboard-crud-flow/01-UI-SPEC.md</read_first>
  <action>Add delete handling that calls `confirm('¿Seguro que quieres eliminar este usuario?')` before sending `DELETE /users/${userId}`. On success, show feedback `DELETE /users/:id -> usuario eliminado` and refresh users. On mutation failure for POST, PUT, or DELETE, show a readable feedback message using the UI-SPEC error copy `No se ha podido completar la operación. Revisa la API y vuelve a intentarlo.` and include HTTP status from the thrown error when available. Add CSS for row actions, quiet edit button, destructive delete button, edit mode message, and success/error feedback variants.</action>
  <verify>node --check dashboard/app.js</verify>
  <acceptance_criteria>
    - `dashboard/app.js` contains `confirm(`.
    - `dashboard/app.js` contains `method: 'DELETE'`.
    - `dashboard/app.js` contains `DELETE /users/:id -> usuario eliminado`.
    - `dashboard/app.js` contains `No se ha podido completar la operación`.
    - `dashboard/styles.css` contains row action styling and destructive delete styling.
    - `node --check dashboard/app.js` exits 0.
  </acceptance_criteria>
  <done>Edit/delete interactions and mutation error states are implemented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check dashboard/app.js`
- [ ] `rg -n "Editar|Eliminar|confirm\\(|PUT /users/:id|DELETE /users/:id|Guardar cambios" dashboard`
- [ ] With API and dashboard running, edit a user and verify the row updates after refresh.
- [ ] With API and dashboard running, delete a user, accept native confirmation, and verify the row disappears.
</verification>

<success_criteria>
- Edit loads the selected user into the shared form.
- Edit mode is visibly announced.
- Cancel edit returns to create mode.
- Delete asks for native confirmation before the API call.
- Edit and delete success feedback shows method and endpoint.
- Mutation failures show a readable Spanish error.
</success_criteria>

<output>
After completion, create `.planning/phases/01-dashboard-crud-flow/01-02-SUMMARY.md`
</output>
