---
phase: 01-dashboard-crud-flow
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/index.html
  - dashboard/app.js
  - dashboard/styles.css
autonomous: true
requirements:
  - DASH-01
  - DASH-04
  - QUAL-04
user_setup: []
must_haves:
  truths:
    - "D-01 covered: CRUD form appears above the users table."
    - "D-02 covered: learner can submit the form, API request runs, and users table refreshes."
    - "D-08 covered: successful create flow shows POST /users feedback."
    - "D-10 covered: create feedback is concise and does not require raw JSON display."
    - "Stale dashboard error help path points to /Users/edefrutos/Desktop/EDF-Lab-Educativo/api."
  artifacts:
    - "dashboard/index.html contains user form markup with visible labels for name and email."
    - "dashboard/app.js contains create submit handling for POST /users."
    - "dashboard/styles.css contains CRUD form and feedback styling."
  key_links:
    - "dashboard/index.html form IDs are referenced from dashboard/app.js elements object."
    - "POST /users success path refreshes GET /users table data."
---

<objective>
Add the first vertical CRUD slice: a visible create-user form above the users table, stale path correction, POST request handling, table refresh, and concise HTTP feedback.

Purpose: This gives learners the first browser-visible mutation path before edit/delete complexity is added.
Output: Updated dashboard markup, JavaScript, and CSS for create mode.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-dashboard-crud-flow/01-CONTEXT.md
@.planning/phases/01-dashboard-crud-flow/01-UI-SPEC.md
@.planning/phases/01-dashboard-crud-flow/01-RESEARCH.md
@.planning/phases/01-dashboard-crud-flow/01-PATTERNS.md
@dashboard/index.html
@dashboard/app.js
@dashboard/styles.css
@api/index.js
</context>

<threat_model>
Risk level: low.

Threats considered:
- Untrusted user input is rendered in the dashboard table. Use `textContent`, not `innerHTML`, for user-controlled name/email values.
- Form submission sends JSON to the local API. Keep content type explicit as `application/json`.
- Do not add new dependencies or external scripts.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add create form markup above users table</name>
  <files>dashboard/index.html</files>
  <read_first>dashboard/index.html, .planning/phases/01-dashboard-crud-flow/01-UI-SPEC.md, .planning/phases/01-dashboard-crud-flow/01-CONTEXT.md</read_first>
  <action>Add a `form` with id `user-form` above the existing `table-wrapper` inside the users section. Include heading text `Gestionar usuarios`, helper text `Crea un usuario nuevo o edita uno existente desde el mismo formulario.`, visible labels `Nombre` and `Email`, inputs with ids `user-name-input` and `user-email-input`, primary submit button id `user-submit-button` with text `Crear usuario`, hidden or initially non-prominent cancel button id `cancel-edit-button` with text `Cancelar edición`, mode message id `form-mode-message`, and feedback region id `mutation-feedback` with `aria-live="polite"`. Add an `Acciones` table header after Email. Correct the stale error help command path to `/Users/edefrutos/Desktop/EDF-Lab-Educativo/api`.</action>
  <verify>rg -n "user-form|user-name-input|user-email-input|mutation-feedback|Acciones|EDF-Lab-Educativo/api" dashboard/index.html</verify>
  <acceptance_criteria>
    - `dashboard/index.html` contains `id="user-form"`.
    - `dashboard/index.html` contains `id="mutation-feedback"` and `aria-live="polite"`.
    - `dashboard/index.html` contains `Acciones` in the users table header.
    - `dashboard/index.html` contains `/Users/edefrutos/Desktop/EDF-Lab-Educativo/api`.
    - `dashboard/index.html` no longer contains `/Users/edefrutos/Desktop/express-api-demo/api`.
  </acceptance_criteria>
  <done>Create form markup and stale path correction are present.</done>
</task>

<task type="auto">
  <name>Task 2: Implement POST /users create flow</name>
  <files>dashboard/app.js</files>
  <read_first>dashboard/app.js, dashboard/index.html, api/index.js, .planning/phases/01-dashboard-crud-flow/01-CONTEXT.md, .planning/phases/01-dashboard-crud-flow/01-PATTERNS.md</read_first>
  <action>Extend the `elements` object with the new form, input, submit, cancel, mode message, and mutation feedback DOM nodes. Change `fetchJson(path)` to `fetchJson(path, options = {})`, preserving current GET behavior and `response.ok` error handling. Add a form submit listener that prevents default, reads trimmed `name` and `email`, sends `POST /users` with `Content-Type: application/json` and body `{ name, email }`, then shows feedback text `POST /users -> usuario creado`, resets the form, refreshes `loadDashboardData()`, and uses `textContent` for all user-controlled values. Add submit loading state so the submit button is disabled while the mutation is running.</action>
  <verify>node --check dashboard/app.js</verify>
  <acceptance_criteria>
    - `dashboard/app.js` contains `fetchJson(path, options = {})`.
    - `dashboard/app.js` contains `method: 'POST'`.
    - `dashboard/app.js` contains `POST /users -> usuario creado`.
    - `dashboard/app.js` attaches a submit listener to `elements.userForm`.
    - `node --check dashboard/app.js` exits 0.
  </acceptance_criteria>
  <done>Create form submits to POST /users and refreshes the users table.</done>
</task>

<task type="auto">
  <name>Task 3: Style CRUD form and feedback states</name>
  <files>dashboard/styles.css</files>
  <read_first>dashboard/styles.css, dashboard/index.html, .planning/phases/01-dashboard-crud-flow/01-UI-SPEC.md</read_first>
  <action>Add CSS for the CRUD form using the existing palette and spacing scale: classes for the form block, form grid, field groups, visible labels, inputs, secondary/cancel button, feedback region, success/error feedback variants, and compact row-action placeholders if needed. Keep the form visually connected to the users table without adding a nested `.card`. Add mobile rules under the existing `@media (max-width: 820px)` block so fields and actions stack vertically and button text remains readable.</action>
  <verify>rg -n "user-form|form-grid|mutation-feedback|secondary|row-actions" dashboard/styles.css</verify>
  <acceptance_criteria>
    - `dashboard/styles.css` contains selectors for `.user-form` or equivalent form block class.
    - `dashboard/styles.css` contains selectors for `.mutation-feedback`.
    - `dashboard/styles.css` contains responsive styling under `@media (max-width: 820px)` for the form or form actions.
    - CSS does not introduce `background-clip: text`.
    - CSS does not add a nested `.card .card` pattern.
  </acceptance_criteria>
  <done>CRUD create UI follows the approved UI-SPEC styling and responsive behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check dashboard/app.js`
- [ ] `rg -n "user-form|mutation-feedback|POST /users -> usuario creado|Acciones" dashboard`
- [ ] Start API with `PORT=3100 npm start` from `api/`, serve dashboard on `5173`, create a user, and verify the table refreshes.
</verification>

<success_criteria>
- Create form appears above the users table.
- Stale dashboard help path is corrected.
- Creating a user from the dashboard calls `POST /users`.
- Successful create shows `POST /users -> usuario creado`.
- The table refreshes after creation.
- No new dependencies are added.
</success_criteria>

<output>
After completion, create `.planning/phases/01-dashboard-crud-flow/01-01-SUMMARY.md`
</output>
