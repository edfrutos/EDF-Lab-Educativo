---
phase: 24-ci-rate-limiting
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - .github/workflows/ci.yml
  - docs/10-tests.md
  - README.md
  - api/README.md
  - .github/copilot-instructions.md
autonomous: true
requirements:
  - CI-01
  - CI-02
must_haves:
  truths:
    - "D-05: ci.yml triggers on push and PR to main"
    - "D-06: CI runs npm ci and npm run test:sqlite in api/"
    - "D-07: docs/10-tests.md explains optional Postgres CI job"
    - "README mentions CI workflow"
    - "copilot-instructions no longer says npm test is placeholder"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "GitHub Actions SQLite CI"
      contains: "test:sqlite"
    - path: docs/10-tests.md
      provides: "CI documentation"
      contains: "GitHub Actions"
  key_links:
    - from: .github/workflows/ci.yml
      to: api/package.json
      via: "npm run test:sqlite"
      pattern: "test:sqlite"
    - from: docs/10-tests.md
      to: .github/workflows/ci.yml
      via: "link or path reference"
      pattern: "workflows/ci"
---

<objective>
Add GitHub Actions CI for SQLite tests and document optional Postgres job (CI-01, CI-02).

Purpose: Automate regression checks on every push to main; teach CI without requiring Postgres in default pipeline.
Output: `.github/workflows/ci.yml`, `docs/10-tests.md`, `README.md`, `api/README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-ci-rate-limiting/24-CONTEXT.md
@.planning/phases/24-ci-rate-limiting/24-RESEARCH.md
@docs/10-tests.md
@README.md
@api/README.md
@.github/copilot-instructions.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Broken main branch | Medium | CI fails on test regression |
| CI exposes secrets | High | No env secrets in workflow; AUTH_DISABLED via script |
| Learner confusion on PG | Low | Document optional job separately from default |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/ci.yml</name>
  <read_first>24-RESEARCH.md GitHub Actions section</read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    Create workflow:
    - `name: CI`
    - `on: push` and `pull_request` to `branches: [main]`
    - Job `test-sqlite`: `ubuntu-latest`
    - `defaults.run.working-directory: api`
    - Steps: checkout@v4, setup-node@v4 (node 20, cache npm, cache-dependency-path: api/package-lock.json)
    - `npm ci`
    - `npm run test:sqlite`
    No secrets, no Postgres service in default job.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q "test:sqlite" .github/workflows/ci.yml && grep -q "main" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - Workflow file exists and runs test:sqlite on main
  </acceptance_criteria>
  <done>CI-01 workflow created</done>
</task>

<task type="auto">
  <name>Task 2: docs/10-tests.md — CI section + optional Postgres</name>
  <read_first>24-CONTEXT.md D-07, docs/10-tests.md</read_first>
  <files>docs/10-tests.md</files>
  <action>
    Add section «CI en GitHub Actions» after scripts table:
    1. Explain default workflow runs `test:sqlite` (24 tests) on push/PR to `main`.
    2. Link to `.github/workflows/ci.yml`.
    3. Subsection «Job Postgres opcional (avanzado)»: prerequisites (`test:db:prepare`, Postgres service), example YAML snippet with `services: postgres` and `npm run test:pg` — marked as copy-paste extension, not in repo by default.
    4. Note CI uses SQLite only so contributors without Docker still get green checks.
  </action>
  <verify>
    <automated>grep -q "GitHub Actions" docs/10-tests.md && grep -q "Postgres opcional\|Postgres optional\|job Postgres" docs/10-tests.md</automated>
  </verify>
  <acceptance_criteria>
    - CI documented with optional Postgres path
  </acceptance_criteria>
  <done>CI-02 documentation complete</done>
</task>

<task type="auto">
  <name>Task 3: README and api/README pointers</name>
  <read_first>README.md tests section, api/README.md scripts table</read_first>
  <files>README.md, api/README.md, .github/copilot-instructions.md</files>
  <action>
    1. **README.md**: In tests/verification area, add line that push to `main` runs GitHub Actions (`test:sqlite`); link `docs/10-tests.md#ci-en-github-actions` or section anchor.
    2. **api/README.md**: Add row for rate limit env vars; mention 24 tests in test:sqlite; one line on CI.
    3. **.github/copilot-instructions.md**: Replace obsolete «npm test is placeholder» with pointer to `npm run test:sqlite` and CI workflow.
    Do not add NOTEBOOK entries (Phase 25 or executor discretion unless friction found).
  </action>
  <verify>
    <automated>grep -q "GitHub Actions\|CI" README.md && grep -q "LOGIN_RATE_LIMIT" api/README.md && ! grep -q "placeholder" .github/copilot-instructions.md</automated>
  </verify>
  <acceptance_criteria>
    - Root README mentions CI
    - api/README documents rate limit vars
    - copilot-instructions accurate
  </acceptance_criteria>
  <done>Cross-doc pointers complete</done>
</task>

<task type="checkpoint" human="true">
  <name>Task 4: Verify CI workflow (optional)</name>
  <read_first>24-RESEARCH.md</read_first>
  <action>
    After push to remote (or `gh workflow run` if available):
    1. Confirm GitHub Actions job appears on push to main
    2. Job runs `npm run test:sqlite` and passes (24 tests)
    Local substitute: `cd api && npm run test:sqlite` already green from 24-01.
  </action>
  <acceptance_criteria>
    - CI job definition valid; local test:sqlite passes
  </acceptance_criteria>
  <done>CI ready for first push</done>
</task>

</tasks>

<verification>
- Workflow YAML grep checks
- `npm run test:sqlite` — 24/24 (from 24-01)
- docs grep for GitHub Actions + Postgres optional
</verification>

<success_criteria>
- CI-01: workflow on main runs SQLite suite
- CI-02: Postgres optional job documented
- Learners can find CI docs from README
</success_criteria>
