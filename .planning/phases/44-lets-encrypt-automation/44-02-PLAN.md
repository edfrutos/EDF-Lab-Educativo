---
phase: 44-lets-encrypt-automation
plan: 02
type: execute
wave: 2
depends_on:
  - "44-01"
files_modified:
  - docs/18-production-deploy.md
  - api/.env.example
  - README.md
autonomous: true
requirements:
  - PROD-01
user_setup: []
must_haves:
  truths:
    - "Doc 18 describe flujo VPS dominio → certbot → nginx → health HTTPS (D-12 parcial)"
    - "Checklist sin credenciales reales en repo (D-09)"
    - "CORS prod con dominio real documentado"
  artifacts:
    - path: docs/18-production-deploy.md
      provides: "Sección Let's Encrypt VPS"
      contains: "letsencrypt"
    - path: README.md
      provides: "Hook compose:prod:le"
      contains: "letsencrypt"
  key_links:
    - from: docs/18-production-deploy.md
      to: scripts/letsencrypt/obtain-cert.sh
      via: "checklist VPS"
      pattern: "obtain-cert"
---

<objective>
Documentar el despliegue LE de punta a punta para el operador VPS y cerrar la guía prod sin adelantar misión/índice de fase 45.

Purpose: completar PROD-01 (flujo descrito + checklist).
Output: docs/18 ampliado, README hook, comentario CORS en .env.example.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-lets-encrypt-automation/44-CONTEXT.md
@.planning/phases/44-lets-encrypt-automation/44-RESEARCH.md
@.planning/phases/44-lets-encrypt-automation/44-01-SUMMARY.md
@docs/18-production-deploy.md
@NOTEBOOK.md

<interfaces>
Checklist VPS (doc):

```bash
# DNS A → VPS
cp deploy/letsencrypt/.env.example deploy/letsencrypt/.env
./scripts/letsencrypt/obtain-cert.sh
./scripts/letsencrypt/render-nginx-le.sh
npm run compose:prod:le
curl https://lab.ejemplo.com/api/health
```

CORS prod dominio (api/.env comentario):

```env
# CORS_ORIGINS=https://lab.ejemplo.com
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sección Let's Encrypt en doc 18 (D-12)</name>
  <read_first>
    docs/18-production-deploy.md
    .planning/phases/44-lets-encrypt-automation/44-CONTEXT.md
    scripts/letsencrypt/
  </read_first>
  <files>docs/18-production-deploy.md</files>
  <action>
Añadir sección **«Let's Encrypt en VPS (fase 44)»** en **`docs/18-production-deploy.md`**: prerequisitos (dominio, puertos 80/443, certbot en host), contraste con certs autofirmados locales, pasos obtain → render nginx → `compose:prod:le` → `curl` sin `-k`, renovación con `renew-cert.sh` y cron ejemplo. Actualizar **«Qué no cubre este lab»** — quitar «Automatización Let's Encrypt» (ya cubierta); mantener K8s/PaaS fuera de scope. Enlazar scripts y `deploy/letsencrypt/.env.example`. No duplicar NOTEBOOK ni misión (fase 45).
  </action>
  <verify>
    <automated>rg "letsencrypt" docs/18-production-deploy.md && rg "obtain-cert" docs/18-production-deploy.md</automated>
  </verify>
  <acceptance_criteria>
    - flujo punta a punta documentado
    - staging LE mencionado
    - sin credenciales reales
  </acceptance_criteria>
  <done>Operador VPS tiene guía LE</done>
</task>

<task type="auto">
  <name>Task 2: README + CORS dominio prod (D-09, D-10)</name>
  <read_first>
    README.md
    api/.env.example
    package.json
  </read_first>
  <files>README.md, api/.env.example</files>
  <action>
En **README.md**, párrafo breve bajo sección prod: TLS local (`generate-dev-tls.sh` + `compose:prod`) vs **LE en VPS** (`compose:prod:le` + scripts `letsencrypt/`). En **`api/.env.example`**, comentario bajo CORS: en VPS con dominio real, añadir origen `https://<tu-dominio>` a `CORS_ORIGINS` si accedes dashboards en host dev contra API remota (caso avanzado); en prod same-origin `/api` no requiere CORS extra. Sin valores reales ni asignaciones que activen git-secrets.
  </action>
  <verify>
    <automated>rg "compose:prod:le" README.md && cd api && npm run test:sqlite</automated>
  </verify>
  <acceptance_criteria>
    - README distingue local vs VPS LE
    - tests SQLite verdes
  </acceptance_criteria>
  <done>Descubribilidad LE desde README</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-04 | I | Docs con emails reales | mitigate | Solo placeholders lab.ejemplo.com |
| T-44-05 | D | Doc implica LE en localhost | mitigate | Encabezado explícito «solo VPS con dominio» |
</threat_model>

<verification>
- `rg letsencrypt docs/18-production-deploy.md`
- `cd api && npm run test:sqlite`
- Crear `44-VERIFICATION.md` al cerrar fase
</verification>

<success_criteria>
- Criterios ROADMAP fase 44: scripts documentados, flujo dominio→certbot→nginx→health, sin credenciales en repo.
</success_criteria>

<output>
After completion, create `.planning/phases/44-lets-encrypt-automation/44-02-SUMMARY.md` and `44-VERIFICATION.md`
</output>
