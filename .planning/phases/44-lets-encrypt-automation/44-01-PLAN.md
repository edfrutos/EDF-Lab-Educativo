---
phase: 44-lets-encrypt-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/letsencrypt/.env.example
  - deploy/letsencrypt/.gitignore
  - scripts/letsencrypt/obtain-cert.sh
  - scripts/letsencrypt/renew-cert.sh
  - scripts/letsencrypt/render-nginx-le.sh
  - proxy/nginx.letsencrypt.conf.template
  - docker-compose.letsencrypt.yml
  - package.json
autonomous: true
requirements:
  - PROD-01
user_setup:
  - "VPS con dominio DNS apuntando al servidor; certbot instalado en host (documentar apt/brew)"
must_haves:
  truths:
    - "Operador puede emitir cert LE inicial con script documentado (D-06)"
    - "Operador puede renovar cert y recargar nginx con script (D-07)"
    - "Certs LE no se commitean; solo plantillas en repo (D-08, D-09)"
  artifacts:
    - path: scripts/letsencrypt/obtain-cert.sh
      provides: "Emisión inicial certbot"
      contains: "certbot"
    - path: scripts/letsencrypt/renew-cert.sh
      provides: "Renovación + reload proxy"
      contains: "renew"
    - path: deploy/letsencrypt/.env.example
      provides: "Plantilla dominio y email"
      contains: "CERT_DOMAIN"
    - path: docker-compose.letsencrypt.yml
      provides: "Override montaje LE en proxy"
      contains: "letsencrypt"
  key_links:
    - from: docker-compose.letsencrypt.yml
      to: edf-lab-proxy
      via: "volume /etc/letsencrypt"
      pattern: "letsencrypt"
    - from: proxy/nginx.letsencrypt.conf.template
      to: fullchain.pem
      via: "ssl_certificate"
      pattern: "ssl_certificate"
---

<objective>
Entregar scripts certbot y wiring Compose/nginx para montar certificados Let's Encrypt en el proxy prod, sin alterar el flujo local autofirmado.

Purpose: cumplir PROD-01 (emisión + renovación ejecutables).
Output: scripts `letsencrypt/`, plantillas y override compose opcional.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-lets-encrypt-automation/44-CONTEXT.md
@.planning/phases/44-lets-encrypt-automation/44-RESEARCH.md
@scripts/generate-dev-tls.sh
@proxy/nginx.conf
@docker-compose.yml

<interfaces>
deploy/letsencrypt/.env.example:

```env
CERT_DOMAIN=lab.ejemplo.com
CERTBOT_EMAIL=operador@ejemplo.com
CERTBOT_STAGING=1
```

Compose merge (VPS):

```bash
docker compose -f docker-compose.yml -f docker-compose.prod.yml \
  -f docker-compose.letsencrypt.yml --profile prod up -d
```

nginx LE template (fragmento):

```nginx
server_name ${CERT_DOMAIN};
ssl_certificate     /etc/letsencrypt/live/${CERT_DOMAIN}/fullchain.pem;
ssl_certificate_key /etc/letsencrypt/live/${CERT_DOMAIN}/privkey.pem;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plantillas deploy/letsencrypt (D-03, D-08)</name>
  <read_first>
    deploy/certs/.gitignore
    .planning/phases/44-lets-encrypt-automation/44-CONTEXT.md
  </read_first>
  <files>deploy/letsencrypt/.env.example, deploy/letsencrypt/.gitignore</files>
  <action>
Crear **`deploy/letsencrypt/`** con **`.env.example`** (`CERT_DOMAIN`, `CERTBOT_EMAIL`, `CERTBOT_STAGING`) y **`.gitignore`** que ignore `.env` y cualquier export local. Comentarios en español: dominio debe resolver al VPS; usar staging primero. **No** incluir emails ni dominios reales del operador.
  </action>
  <verify>
    <automated>test -f deploy/letsencrypt/.env.example && grep -q CERT_DOMAIN deploy/letsencrypt/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - plantilla trackeada sin secretos
    - .env local gitignored
  </acceptance_criteria>
  <done>Config LE externalizada fuera del repo</done>
</task>

<task type="auto">
  <name>Task 2: Scripts obtain + renew + render nginx (D-06, D-07, D-05)</name>
  <read_first>
    scripts/generate-dev-tls.sh
    proxy/nginx.conf
    .planning/phases/44-lets-encrypt-automation/44-RESEARCH.md
  </read_first>
  <files>scripts/letsencrypt/obtain-cert.sh, scripts/letsencrypt/renew-cert.sh, scripts/letsencrypt/render-nginx-le.sh, proxy/nginx.letsencrypt.conf.template</files>
  <action>
Crear **`scripts/letsencrypt/obtain-cert.sh`** (ejecutable): carga `deploy/letsencrypt/.env`, comprueba `certbot` en PATH, usa **certonly --standalone** en :80 (documentar que el proxy debe estar parado) o flag documentado; respeta `CERTBOT_STAGING`. Crear **`renew-cert.sh`**: `certbot renew` con deploy-hook que recargue nginx en `edf-lab-proxy` vía `docker compose` (mismos `-f` que prod+le). Crear **`render-nginx-le.sh`**: `envsubst` sobre **`proxy/nginx.letsencrypt.conf.template`** (copiar estructura location /api/ y / de `nginx.conf`, cambiar `server_name` y rutas LE). Salida: `proxy/nginx.le.conf` gitignored o montada por override. Cabeceras de scripts: prerequisitos DNS, puertos 80/443, no ejecutar en CI.
  </action>
  <verify>
    <automated>test -x scripts/letsencrypt/obtain-cert.sh && test -x scripts/letsencrypt/renew-cert.sh && test -f proxy/nginx.letsencrypt.conf.template</automated>
  </verify>
  <acceptance_criteria>
    - tres scripts ejecutables
    - plantilla nginx LE incluye /api/ strip igual que prod actual
    - staging documentado
  </acceptance_criteria>
  <done>Tooling certbot listo para VPS</done>
</task>

<task type="auto">
  <name>Task 3: Override Compose LE + npm script (D-04, D-10)</name>
  <read_first>
    docker-compose.yml
    docker-compose.prod.yml
    package.json
  </read_first>
  <files>docker-compose.letsencrypt.yml, package.json, .gitignore</files>
  <action>
Crear **`docker-compose.letsencrypt.yml`**: en `edf-lab-proxy`, montar `/etc/letsencrypt:/etc/letsencrypt:ro` (o subpath live), usar **`proxy/nginx.le.conf`** generado (volume o build arg documentado). Añadir en **`package.json`**: `compose:prod:le` que mergee los tres compose files con profile prod. Asegurar **`.gitignore`** para `proxy/nginx.le.conf` y `deploy/letsencrypt/.env`. **No** cambiar comportamiento de `compose:prod` sin override LE.
  </action>
  <verify>
    <automated>grep compose:prod:le package.json && test -f docker-compose.letsencrypt.yml</automated>
  </verify>
  <acceptance_criteria>
    - override LE es opt-in
    - compose:prod local sin LE sigue igual
  </acceptance_criteria>
  <done>Stack prod puede consumir certs LE en VPS</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | I | Privkey en repo | mitigate | gitignore paths; solo live mount read-only |
| T-44-02 | D | obtain con proxy up | mitigate | Script verifica puerto 80 o documenta down |
| T-44-03 | I | Email real en .env.example | mitigate | Placeholders ficticios |
</threat_model>

<verification>
- `test -x scripts/letsencrypt/obtain-cert.sh`
- `test -f docker-compose.letsencrypt.yml`
- `cd api && npm run test:sqlite` (regresión)
</verification>

<success_criteria>
- Scripts obtain/renew existen y son ejecutables con prerequisitos documentados en cabecera.
- Override LE no rompe compose:prod local.
</success_criteria>

<output>
After completion, create `.planning/phases/44-lets-encrypt-automation/44-01-SUMMARY.md`
</output>
