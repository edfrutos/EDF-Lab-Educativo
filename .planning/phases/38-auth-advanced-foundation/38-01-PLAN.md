---
phase: 38-auth-advanced-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/auth.js
  - api/index.js
  - api/index.test.js
  - api/index.pg.test.js
  - api/test-auth-helpers.js
autonomous: true
requirements:
  - AUTH-ADV-03
user_setup:
  - "Postgres opcional para validar `npm run test:pg`"
must_haves:
  truths:
    - "Existe endpoint autenticado para cambio de contraseña del operador"
    - "El endpoint exige contraseña actual válida"
    - "Login con nueva contraseña funciona y login con anterior falla"
  artifacts:
    - path: api/index.js
      provides: "Ruta auth de cambio de contraseña"
      contains: "/auth/password"
    - path: api/test-auth-helpers.js
      provides: "Casos de tests auth para password change"
      contains: "password"
  key_links:
    - from: api/index.js
      to: api/auth.js
      via: "handler de password change"
      pattern: "auth"
---

<objective>
Implementar la base técnica de AUTH-ADV-03: endpoint de cambio de contraseña del operador con validaciones, manteniendo contrato actual de sesión/cookies.

Purpose: añadir capacidad auth avanzada incremental sin romper login/logout ni flujo CRUD protegido.
Output: endpoint funcional + cobertura de tests API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-auth-advanced-foundation/38-CONTEXT.md
@.planning/phases/38-auth-advanced-foundation/38-RESEARCH.md
@api/index.js
@api/auth.js
@api/test-auth-helpers.js
@api/index.test.js
@api/index.pg.test.js

<interfaces>
Contrato sugerido:

```http
PATCH /auth/password
Cookie: edf_session=...
Content-Type: application/json

{
  "currentPassword": "...",
  "newPassword": "..."
}
```

Respuestas:
- `200` cambio exitoso
- `400` payload inválido
- `401` sin sesión
- `403` contraseña actual inválida
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Endpoint de cambio de contraseña</name>
  <read_first>
    api/auth.js
    api/index.js
    docs/17-autenticacion.md
  </read_first>
  <files>api/auth.js, api/index.js</files>
  <action>
Añadir handler y ruta para cambio de contraseña del operador autenticado.

Reglas mínimas:
- requerir sesión válida (`requireAuth` o equivalente)
- validar `currentPassword` y `newPassword` no vacíos
- verificar contraseña actual con bcrypt
- persistir hash nuevo y responder éxito

No alterar comportamiento de `/auth/login` y `/auth/logout`.
  </action>
  <verify>
    <automated>cd api && node --check index.js && node --check auth.js</automated>
  </verify>
  <acceptance_criteria>
    - Ruta `/auth/password` existe
    - Exige sesión y contraseña actual
    - No rompe endpoints auth existentes
  </acceptance_criteria>
  <done>Base API de password change implementada</done>
</task>

<task type="auto">
  <name>Task 2: Cobertura de tests SQLite y Postgres</name>
  <read_first>
    api/test-auth-helpers.js
    api/index.test.js
    api/index.pg.test.js
  </read_first>
  <files>api/test-auth-helpers.js, api/index.test.js, api/index.pg.test.js</files>
  <action>
Extender tests de autenticación para cubrir:
- rechazo sin sesión
- rechazo con contraseña actual incorrecta
- éxito al cambiar contraseña
- login con nueva contraseña OK y antigua KO
  </action>
  <verify>
    <automated>cd api && npm run test:sqlite</automated>
  </verify>
  <acceptance_criteria>
    - Nuevos tests auth pasan en SQLite
    - tests existentes de auth/login/logout siguen verdes
  </acceptance_criteria>
  <done>Password change cubierto por tests API</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session cookie auth | El endpoint debe respetar requireAuth |
| Credential update | Cambio de hash sin exponer password en logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | E | Password change sin sesión | mitigate | Reusar guardia auth actual |
| T-38-02 | T | Cambio sin verificar password actual | mitigate | Validar currentPassword con bcrypt.compare |
</threat_model>

<verification>
- `cd api && node --check index.js auth.js`
- `cd api && npm run test:sqlite`
- (opcional) `cd api && npm run test:pg`
</verification>

<success_criteria>
- AUTH-ADV-03 implementado a nivel API con tests
- contrato auth existente permanece estable
</success_criteria>

<output>
After completion, create `.planning/phases/38-auth-advanced-foundation/38-01-SUMMARY.md`
</output>
