---
phase: 09-compose-stack-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - docker-compose.yml
  - api/.dockerignore
autonomous: true
requirements:
  - COMPOSE-01
  - COMPOSE-03
  - COMPOSE-05
must_haves:
  truths:
    - "Learner runs docker compose up from project root and both edf-lab-api and edf-lab-dashboard start"
    - "API container logs Migrados N usuarios on cold start when users.json is in the image"
    - "README still documents npm start + python3 -m http.server as primary dev path"
  artifacts:
    - path: docker-compose.yml
      provides: "Two-service orchestration"
      contains: "edf-lab-api"
    - path: api/.dockerignore
      provides: "Allows users.json in API image"
      min_lines: 35
  key_links:
    - from: docker-compose.yml
      to: api/Dockerfile
      via: "build context ./api"
      pattern: "build:.*api"
    - from: docker-compose.yml
      to: dashboard/Dockerfile
      via: "build context ./dashboard"
      pattern: "build:.*dashboard"
---

<objective>
Wire the Compose stack at repo root and fix API image seed data for container migration.

Purpose: Single-command multi-container startup (COMPOSE-01) reusing both Dockerfiles (COMPOSE-03).
Output: `docker-compose.yml`, updated `api/.dockerignore`, minimal README Compose pointer (COMPOSE-05)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-compose-stack-foundation/09-CONTEXT.md
@.planning/phases/09-compose-stack-foundation/09-RESEARCH.md
@api/Dockerfile
@api/.dockerignore
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update api/.dockerignore for users.json seed</name>
  <read_first>09-CONTEXT.md (D-09), api/.dockerignore lines 38-40, api/db.js populateIfEmpty()</read_first>
  <files>api/.dockerignore</files>
  <action>
    Edit `api/.dockerignore` per D-09:

    - REMOVE the line `data/users.json` so seed JSON is copied into the API image
    - KEEP `data/users.db` and `data/*.db` excluded (ephemeral DB inside container — D-08)
    - Add a one-line comment above the data exclusions explaining: seed JSON included for migration; runtime DB excluded

    Do not remove other ignore rules.
  </action>
  <verify>
    <automated>! grep -q "^data/users.json$" api/.dockerignore && grep -q "data/\*\.db" api/.dockerignore</automated>
  </verify>
  <acceptance_criteria>
    - data/users.json is NOT listed in .dockerignore
    - data/*.db or data/users.db remains excluded
    - Comment documents why seed is included
  </acceptance_criteria>
  <done>.dockerignore allows users.json into Docker build context</done>
</task>

<task type="auto">
  <name>Task 2: Create docker-compose.yml at project root</name>
  <read_first>09-CONTEXT.md (D-04, D-08, D-11, D-12, D-13), 09-RESEARCH.md (Finding 1), api/Dockerfile, dashboard/Dockerfile</read_first>
  <files>docker-compose.yml</files>
  <action>
    Create `docker-compose.yml` at repo root (D-11).

    Services (D-12):
    - `edf-lab-api`:
      - `build: ./api`
      - `ports: ["3100:3100"]`
      - No volumes (D-08)
    - `edf-lab-dashboard`:
      - `build: ./dashboard`
      - `ports: ["5173:5173"]`
      - `depends_on: [edf-lab-api]` (D-13)
      - No volumes

    Use Compose file format version compatible with Docker Compose v2 (`docker compose` CLI).
    Add brief YAML comments identifying each service for learners.

    Do NOT add healthcheck, networks override, or volume mounts (Phase 10).
    Do NOT add root package.json scripts (VOL-03 deferred to Phase 10).
  </action>
  <verify>
    <automated>grep -q "edf-lab-api" docker-compose.yml && grep -q "edf-lab-dashboard" docker-compose.yml && grep -q "3100:3100" docker-compose.yml && grep -q "5173:5173" docker-compose.yml && ! grep -qi "volumes:" docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml exists at repo root
    - Both services defined with correct build contexts and ports
    - depends_on links dashboard to API
    - No volumes section
  </acceptance_criteria>
  <done>`docker compose config` validates without errors</done>
</task>

<task type="auto">
  <name>Task 3: Add minimal Compose pointer to README (COMPOSE-05)</name>
  <read_first>09-CONTEXT.md (D-14, COMPOSE-05), README.md Puesta en marcha section</read_first>
  <files>README.md</files>
  <action>
    Add a short subsection **after** the existing Terminal 1/2 quick start (do NOT replace it):

    Title: `### Opcional: Docker Compose (avanzado)`

    Content (3-5 lines max):
    - From repo root: `docker compose up --build`
    - Dashboard: http://localhost:5173 — API: http://localhost:3100
    - Note: primary learning path remains `npm start` + `python3 -m http.server`; full Compose docs in Phase 11

    Mark as optional/advanced consistent with docs/12-docker.md tone.
  </action>
  <verify>
    <automated>grep -q "docker compose up" README.md && grep -q "npm start" README.md && grep -q "5173" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README still shows npm start + python http.server as first/quick start
    - Compose command documented as optional
    - Ports 5173 and 3100 mentioned
  </acceptance_criteria>
  <done>COMPOSE-05 satisfied with minimal README addition</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → published ports | 3100/5173 exposed to localhost |
| API container | Same Express app as host; no new auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | Published ports | accept | Local dev lab; bind to localhost via default compose port mapping |
| T-09-04 | Information disclosure | users.json in image | accept | Fictional seed data only; same as git-tracked seed |
</threat_model>

<verification>
- `docker compose config` exits 0 from repo root
- `docker compose up --build -d` starts both services
- `curl http://localhost:3100/health` returns healthy JSON
- API logs contain "Migrados" on first cold start
</verification>

<success_criteria>
- COMPOSE-01, COMPOSE-03, COMPOSE-05 requirements addressable after this plan
- Stack starts with single compose command from root
- users.json available in API container for migration
</success_criteria>

<output>
After completion, create `.planning/phases/09-compose-stack-foundation/09-02-SUMMARY.md`
</output>
