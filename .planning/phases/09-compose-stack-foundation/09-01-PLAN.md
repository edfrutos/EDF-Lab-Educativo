---
phase: 09-compose-stack-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/Dockerfile
  - dashboard/nginx.conf
autonomous: true
requirements:
  - COMPOSE-02
  - COMPOSE-03
must_haves:
  truths:
    - "Learner can open dashboard/Dockerfile and see nginx:alpine serving static files"
    - "dashboard/nginx.conf listens on port 5173 inside the container"
    - "Dashboard container serves index.html, app.js, and styles.css without a Node build step"
  artifacts:
    - path: dashboard/Dockerfile
      provides: "Dashboard container image definition"
      contains: "nginx:alpine"
    - path: dashboard/nginx.conf
      provides: "nginx static server on port 5173"
      contains: "listen 5173"
  key_links:
    - from: dashboard/Dockerfile
      to: dashboard/nginx.conf
      via: "COPY nginx.conf"
      pattern: "COPY nginx.conf"
---

<objective>
Create the dashboard container: nginx Dockerfile and config serving static files on port 5173.

Purpose: Dashboard service artifact before wiring docker-compose.yml (Plan 09-02).
Output: `dashboard/Dockerfile`, `dashboard/nginx.conf`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-compose-stack-foundation/09-CONTEXT.md
@.planning/phases/09-compose-stack-foundation/09-RESEARCH.md
@.planning/phases/09-compose-stack-foundation/09-PATTERNS.md
@dashboard/index.html
@dashboard/app.js
@dashboard/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard/nginx.conf</name>
  <read_first>09-CONTEXT.md (D-05, D-06, D-07), 09-RESEARCH.md (Finding 2), dashboard/index.html</read_first>
  <files>dashboard/nginx.conf</files>
  <action>
    Create `dashboard/nginx.conf` as a complete nginx config (not a snippet).

    Requirements (D-06, D-07):
    - Top-level `events { worker_connections 1024; }` block
    - `http` block with `include /etc/nginx/mime.types;` and `default_type application/octet-stream;`
    - `server` block with `listen 5173;` and `server_name localhost;`
    - `root /usr/share/nginx/html;` and `index index.html;`
    - `location / { try_files $uri $uri/ /index.html; }` for SPA-style fallback (single-page dashboard)

    Do NOT add reverse proxy to API (D-03, D-04 out of scope).
  </action>
  <verify>
    <automated>grep -q "listen 5173" dashboard/nginx.conf && grep -q "try_files" dashboard/nginx.conf</automated>
  </verify>
  <acceptance_criteria>
    - nginx.conf exists as standalone config file
    - Server listens on port 5173 (not default 80)
    - Static root is /usr/share/nginx/html
    - No proxy_pass or /api location blocks
  </acceptance_criteria>
  <done>nginx.conf validates with grep; ready for Dockerfile COPY</done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard/Dockerfile</name>
  <read_first>09-CONTEXT.md (D-05), api/Dockerfile (layering reference), dashboard/nginx.conf</read_first>
  <files>dashboard/Dockerfile</files>
  <action>
    Create `dashboard/Dockerfile` using `FROM nginx:alpine` (D-05).

    Steps:
    1. `COPY nginx.conf /etc/nginx/nginx.conf` — replace default config entirely
    2. `COPY index.html app.js styles.css /usr/share/nginx/html/`
    3. `EXPOSE 5173`

    Do NOT install Node.js, npm, or build tools. Do NOT modify app.js API_BASE_URL.
  </action>
  <verify>
    <automated>test -f dashboard/Dockerfile && grep -q "nginx:alpine" dashboard/Dockerfile && grep -q "EXPOSE 5173" dashboard/Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile uses nginx:alpine base image
    - Copies exactly three static assets plus nginx.conf
    - Exposes port 5173
    - No API proxy configuration
  </acceptance_criteria>
  <done>docker build -t edf-lab-dashboard-test ./dashboard succeeds (manual or executor verify)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host browser → nginx static | Read-only static files; no server-side execution |
| nginx container | Serves public dashboard assets only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information disclosure | Static files in image | accept | Dashboard is public lab UI; no secrets in static files |
| T-09-02 | Denial of service | nginx default | accept | Local lab traffic only; no rate limiting required |
</threat_model>

<verification>
- `grep listen 5173 dashboard/nginx.conf` passes
- `docker build -t edf-lab-dashboard-test ./dashboard` succeeds
- Optional smoke: `docker run --rm -p 5173:5173 edf-lab-dashboard-test` serves index.html on localhost:5173
</verification>

<success_criteria>
- dashboard/Dockerfile and dashboard/nginx.conf satisfy D-05, D-06, D-07
- Dashboard image builds and serves static files on port 5173
</success_criteria>

<output>
After completion, create `.planning/phases/09-compose-stack-foundation/09-01-SUMMARY.md`
</output>
