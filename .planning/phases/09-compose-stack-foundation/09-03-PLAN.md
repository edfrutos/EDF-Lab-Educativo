---
phase: 09-compose-stack-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - .planning/phases/09-compose-stack-foundation/09-UAT.md
autonomous: false
requirements:
  - COMPOSE-04
must_haves:
  truths:
    - "Learner opens http://localhost:5173 with compose stack running and sees API connected"
    - "Learner creates, edits, and deletes a user from dashboard against containerized API"
    - "After docker compose down && up --build, migration/seed runs again (ephemeral data — D-10)"
    - "D-02: No CORS changes in api/index.js — existing cors() allows dashboard on :5173"
  artifacts:
    - path: .planning/phases/09-compose-stack-foundation/09-UAT.md
      provides: "Human verification checklist for Phase 9"
      contains: "status:"
  key_links:
    - from: dashboard/app.js
      to: http://localhost:3100
      via: "fetch() from browser on host"
      pattern: "API_BASE_URL"
---

<objective>
Verify end-to-end CRUD and ephemeral restart behavior for the Compose stack; capture UAT results.

Purpose: Close COMPOSE-04 and D-10 verification gate before Phase 10 volumes.
Output: `09-UAT.md` with pass/fail results, any NOTEBOOK-worthy errors documented separately in execution
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-compose-stack-foundation/09-CONTEXT.md
@.planning/phases/09-compose-stack-foundation/09-RESEARCH.md
@docker-compose.yml
@dashboard/app.js
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Compose stack smoke test</name>
  <read_first>09-CONTEXT.md (D-01, D-04, D-08, D-10), docker-compose.yml, README.md optional compose section</read_first>
  <files>.planning/phases/09-compose-stack-foundation/09-UAT.md</files>
  <action>
    Human verification steps:

    1. Stop any host API on :3100 and dashboard on :5173 (`lsof -i :3100`, `:5173`)
    2. From repo root: `docker compose up --build`
    3. Confirm API log shows `Migrados 2 usuarios desde users.json` (or seed message)
    4. Open http://localhost:5173 — dashboard shows "API conectada" / health OK
    5. `curl http://localhost:3100/users` returns John and Jane

    Create `09-UAT.md` with frontmatter `status: testing` and Test 1 marked pending.
    Record results after user confirms or executor completes automated curl checks.
  </action>
  <verify>
    <automated>curl -sf http://localhost:3100/health | grep -q healthy</automated>
  </verify>
  <acceptance_criteria>
    - Both containers running
    - GET /health returns status healthy
    - GET /users returns array with at least 2 users
    - Dashboard loads without CORS errors in browser console
  </acceptance_criteria>
  <done>Test 1 in 09-UAT.md marked pass with notes</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Dashboard CRUD via Compose</name>
  <read_first>dashboard/app.js CRUD handlers, 09-CONTEXT.md COMPOSE-04</read_first>
  <files>.planning/phases/09-compose-stack-foundation/09-UAT.md</files>
  <action>
    From http://localhost:5173 with compose stack running:

    1. Create a new user — verify success feedback in Spanish
    2. Edit that user — verify update feedback
    3. Delete that user — verify confirmation and removal from table
    4. Attempt duplicate email (john@example.com) — verify HTTP 409 error message in UI

    Update 09-UAT.md Test 2 and Test 3 entries.
  </action>
  <verify>
    <automated>curl -sf -X POST http://localhost:3100/users -H "Content-Type: application/json" -d '{"name":"Compose Test","email":"compose-test@example.com"}' | grep -q compose-test</automated>
  </verify>
  <acceptance_criteria>
    - POST returns 201 and user appears in GET /users
    - PUT updates user
    - DELETE removes user
    - Duplicate POST returns 409
  </acceptance_criteria>
  <done>Tests 2-3 in 09-UAT.md marked pass</done>
</task>

<task type="auto">
  <name>Task 3: Ephemeral restart verification (D-10)</name>
  <read_first>09-CONTEXT.md (D-08, D-10), 09-RESEARCH.md (Finding 5)</read_first>
  <files>.planning/phases/09-compose-stack-foundation/09-UAT.md</files>
  <action>
    Verify ephemeral behavior:

    1. Note a user created during UAT (or create one)
    2. `docker compose down`
    3. `docker compose up --build -d`
    4. Confirm API logs migration/seed again
    5. Confirm previously created user is NOT present (only seed users) — proves no volume yet

    Document in 09-UAT.md Test 4: "Ephemeral restart — data reset expected in Phase 9".
    Set 09-UAT.md summary: all tests pass, status complete when verified.
  </action>
  <verify>
    <automated>docker compose ps --format json 2>/dev/null | grep -q edf-lab-api || docker compose ps 2>/dev/null | grep -q edf-lab-api</automated>
  </verify>
  <acceptance_criteria>
    - After down/up cycle, only seed users present (unless volume added — must NOT be present in Phase 9)
    - Migration log appears on second cold start
    - 09-UAT.md documents ephemeral lesson explicitly
  </acceptance_criteria>
  <done>Test 4 pass; 09-UAT.md status complete with 4/4 pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT operator | Human verifies browser behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Repudiation | UAT results | mitigate | Document pass/fail in 09-UAT.md with command notes |
</threat_model>

<verification>
- 09-UAT.md exists with 4 tests and final status complete
- All COMPOSE-04 success criteria observable
- Ephemeral restart documented (D-10)
</verification>

<success_criteria>
- Full CRUD works through compose stack
- UAT artifact captures verification for Phase 9 closeout
- Ephemeral data behavior confirmed before Phase 10 volumes
</success_criteria>

<output>
After completion, create `.planning/phases/09-compose-stack-foundation/09-03-SUMMARY.md`
</output>
