---
phase: 25-framework-auth-learning-material
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/16-frameworks.md
  - missions/15-framework-auth-login-crud.md
  - missions/13-frameworks-network-tab.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
must_haves:
  truths:
    - "D-01: doc 16 compares auth across vanilla, React, Vue with code pointers"
    - "D-02 D-03: Mission 15 walks login CRUD logout cookie inspection"
    - "D-04: Mission 15 mentions React callback vs Vue emit"
    - "D-05: Mission 13 updated — auth available; AUTH_DISABLED test-only"
    - "No stale sin login en React/Vue in doc 16"
  artifacts:
    - path: docs/16-frameworks.md
      provides: "three-frontend auth comparison"
      contains: "LoginGate"
    - path: missions/15-framework-auth-login-crud.md
      provides: "framework auth mission"
      contains: "edf_session"
    - path: missions/13-frameworks-network-tab.md
      provides: "updated framework mission"
      contains: "v1.6"
  key_links:
    - from: missions/15-framework-auth-login-crud.md
      to: docs/16-frameworks.md
      via: "prerequisite"
      pattern: "16-frameworks"
    - from: docs/16-frameworks.md
      to: dashboard-react/src/App.jsx
      via: "code pointer"
      pattern: "bootstrapAuth"
---

<objective>
Update frameworks doc with v1.6 auth comparison and create Mission 15; patch Mission 13 (DOCS-01, DOCS-02).

Purpose: Learners can read how auth works in three frontends then practice on React or Vue.
Output: `docs/16-frameworks.md`, `missions/15-framework-auth-login-crud.md`, updated `missions/13-frameworks-network-tab.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-framework-auth-learning-material/25-CONTEXT.md
@.planning/phases/25-framework-auth-learning-material/25-RESEARCH.md
@.planning/phases/25-framework-auth-learning-material/25-PATTERNS.md
@docs/16-frameworks.md
@missions/14-auth-vanilla-login-crud.md
@missions/13-frameworks-network-tab.md
@dashboard-react/src/App.jsx
@dashboard-vue/src/App.vue
@dashboard/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/16-frameworks.md — auth comparison (DOCS-01)</name>
  <read_first>25-CONTEXT.md D-01, docs/16-frameworks.md L11-28 and L296-305, dashboard-* auth files</read_first>
  <files>docs/16-frameworks.md</files>
  <action>
    1. **Replace** section «Autenticación y los tres paneles» (L11–27) with v1.6 content:
       - Table: Vanilla / React / Vue — login UI, port, child→parent pattern (`elements` vs `onLogin` vs `emit('login')`), bootstrap (`bootstrapAuth` / `onMounted`).
       - Shared behavior: `credentials: 'include'`, cookie `edf_session`, gate until session, logout toolbar, 401 → gate with Spanish message.
       - Code pointers to `dashboard/app.js`, `dashboard-react/src/App.jsx` + `LoginGate.jsx`, `dashboard-vue/src/App.vue` + `LoginGate.vue`.
       - Link [`17-autenticacion.md`](./17-autenticacion.md) and new Mission 15.
       - Note: `AUTH_DISABLED=1` solo tests API automatizados — no camino didáctico con login.

    2. **Update** «Qué no incluimos» (L296+): **remove** bullet «Pantalla de login en React/Vue». Optionally add «Rate limiting global» or keep list focused on state routers/axios.

    3. **Update** «Siguiente paso» to include Mission 15 after Mission 13/14.

    Spanish, same tone as rest of doc. No new file — expand in place.
  </action>
  <verify>
    <automated>grep -q "LoginGate" docs/16-frameworks.md && grep -qE "emit|onLogin" docs/16-frameworks.md && ! grep -q "No \| React.*5174.*No" docs/16-frameworks.md && grep -q "15-framework-auth" docs/16-frameworks.md</automated>
  </verify>
  <acceptance_criteria>
    - Three-frontends auth comparison with implementation pointers
    - Stale v1.4 no-login guidance removed
  </acceptance_criteria>
  <done>DOCS-01 complete</done>
</task>

<task type="auto">
  <name>Task 2: missions/15-framework-auth-login-crud.md (DOCS-02)</name>
  <read_first>25-RESEARCH.md mission_15_outline, missions/14-auth-vanilla-login-crud.md</read_first>
  <files>missions/15-framework-auth-login-crud.md</files>
  <action>
    Create Mission 15 following Mission 14 structure (Spanish):

    - **Objetivo:** login → CRUD → logout on React **or** Vue; DevTools cookie inspection.
    - **Requisitos previos:** 01–03, doc 17, doc 16 auth section; Mission 14 recommended.
    - **Importante:** no `AUTH_DISABLED=1`.
    - **Pasos:**
      1. API `PORT=3100 npm start` with auth on
      2. Choose React :5174 OR Vue :5175 (`npm run dev`)
      3. Login gate only on first visit
      4. Login `admin@lab.local` / `changeme`
      5. CRUD smoke + Network status
      6. Cookie `edf_session` in Request Headers
      7. Cerrar sesión → gate returns
    - **Resultado esperado:** explain operador vs users; `credentials: 'include'`; if Vue chosen, contrast `emit` vs React `onLogin`.
    - **Reto extra:** delete cookie → Recargar → Spanish 401 under form (not connection error box).
    - **Reto extra 2 (opcional):** 10+ wrong passwords → observe 429 (rate limit — link doc 10-tests / api README).
    - **Enlaces:** doc 16, 17, 06-debugging, framework README, Mission 14.
  </action>
  <verify>
    <automated>test -f missions/15-framework-auth-login-crud.md && grep -q "edf_session" missions/15-framework-auth-login-crud.md && grep -q "5174\|5175" missions/15-framework-auth-login-crud.md && grep -q "AUTH_DISABLED" missions/15-framework-auth-login-crud.md</automated>
  </verify>
  <acceptance_criteria>
    - Mission 15 complete with DevTools steps and framework choice
  </acceptance_criteria>
  <done>DOCS-02 complete</done>
</task>

<task type="auto">
  <name>Task 3: Patch missions/13-frameworks-network-tab.md</name>
  <read_first>25-CONTEXT.md D-05, missions/13-frameworks-network-tab.md</read_first>
  <files>missions/13-frameworks-network-tab.md</files>
  <action>
    1. Add short **«Actualización v1.6»** note after Objetivo: React y Vue incluyen login; para auth completo usa Mission 15; esta misión puede seguir con `AUTH_DISABLED=1` si solo quieres inspeccionar CRUD sin gate.

    2. Replace step 1 instruction «descomenta AUTH_DISABLED» with:
       - **Opción A (rápida, sin login):** `AUTH_DISABLED=1` en `api/.env` — solo desarrollo.
       - **Opción B (recomendada v1.6):** auth activa + Mission [`15-framework-auth-login-crud.md`](./15-framework-auth-login-crud.md).

    3. Update expected results if auth on: first load may show 401 on `/users` until login — mention if Option B.

    Keep mission usable for beginners who want Network tab only.
  </action>
  <verify>
    <automated>grep -q "v1.6\|15-framework-auth" missions/13-frameworks-network-tab.md</automated>
  </verify>
  <acceptance_criteria>
    - Mission 13 no longer implies frameworks lack login
  </acceptance_criteria>
  <done>Mission 13 aligned with v1.6</done>
</task>

</tasks>

<verification>
- Grep checks on doc 16 and missions
- Read-through: no contradictory AUTH_DISABLED-as-primary for frameworks
</verification>

<success_criteria>
- DOCS-01 and DOCS-02 satisfied
- Learning path: doc 16 → Mission 15
</success_criteria>
