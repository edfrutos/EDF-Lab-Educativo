---
phase: 20-secrets-deploy-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - docker-compose.yml
  - docs/18-production-deploy.md
  - docs/14-docker-compose.md
  - README.md
autonomous: true
requirements:
  - DEPLOY-02
  - DEPLOY-04
must_haves:
  truths:
    - "edf-lab-api uses env_file ./api/.env"
    - "DATABASE_URL not hardcoded in compose environment for API"
    - "docs/18-production-deploy.md explains secrets, env_file, nginx TLS termination"
    - "README Compose section requires api/.env before compose:up"
    - ".env remains gitignored"
  artifacts:
    - path: docs/18-production-deploy.md
      provides: "Production deploy and TLS guide"
    - path: docker-compose.yml
      provides: "env_file on API service"
  key_links:
    - from: docker-compose.yml
      to: api/.env
      via: "env_file"
      pattern: "env_file"
---

<objective>
Wire Compose to `api/.env` and publish deploy documentation with nginx TLS termination pattern.

Purpose: DEPLOY-02, DEPLOY-04 — secrets outside YAML; learner understands HTTPS proxy.
Output: Updated compose, new doc 18, cross-links in README and doc 14.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-secrets-deploy-hardening/20-CONTEXT.md
@.planning/phases/20-secrets-deploy-hardening/20-RESEARCH.md
@docker-compose.yml
@docs/14-docker-compose.md
@docs/17-autenticacion.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose env_file for API</name>
  <read_first>docker-compose.yml, api/.env.example, 20-CONTEXT.md (D-05, D-06)</read_first>
  <files>docker-compose.yml</files>
  <action>
    Update `edf-lab-api` service:

    1. Add `env_file: - ./api/.env`
    2. Remove inline `DATABASE_URL` from `environment:` (must come from `.env`)
    3. Optional: set `environment: NODE_ENV: production` only if pedagogically useful — otherwise leave NODE_ENV in `.env` only
    4. Add YAML comment: crear `api/.env` desde `.env.example` antes de `compose up`

    Update `api/.env.example` commented DATABASE_URL line to show Compose value:
    `DATABASE_URL=postgresql://edf_lab:edf_lab_dev@edf-lab-postgres:5432/edf_lab`

    Do not change postgres service credentials (lab fictional — keep comment).

    Verify root `.gitignore` still ignores `.env` and `api/.env` patterns.
  </action>
  <verify>
    <automated>grep -q 'env_file' docker-compose.yml && grep -q './api/.env' docker-compose.yml && ! grep -q 'DATABASE_URL: postgresql' docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml edf-lab-api has env_file ./api/.env
    - No DATABASE_URL in environment block for edf-lab-api
    - api/.env.example shows compose DATABASE_URL hostname edf-lab-postgres
  </acceptance_criteria>
  <done>Compose env_file wired</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/18-production-deploy.md</name>
  <read_first>20-CONTEXT.md (D-08), docs/14-docker-compose.md, docs/17-autenticacion.md, .planning/research/ARCHITECTURE.md</read_first>
  <files>docs/18-production-deploy.md</files>
  <action>
    Create `docs/18-production-deploy.md` (Spanish, educational tone):

    Sections:
    1. **Objetivo** — secrets + TLS at proxy, not in images
    2. **Secretos con `.env`** — copy example, variables table (JWT_SECRET, ADMIN_*, DATABASE_URL, CORS_ORIGINS), gitignore, fail-fast production
    3. **Compose y `env_file`** — prerequisite `api/.env`, example DATABASE_URL for internal hostname, `npm run compose:up`, never commit `.env`
    4. **TLS en nginx (terminación)** — ASCII diagram: Internet → nginx:443 TLS → api:3100, dashboard:5173; Node serves HTTP inside network; `Secure` cookies need HTTPS
    5. **Snippet nginx educativo** — minimal `server { listen 443 ssl; proxy_pass http://edf-lab-api:3100; }` in fenced block (not a full deployable config)
    6. **Qué no cubre este lab** — Let's Encrypt automation, K8s secrets (link REQUIREMENTS deferred)

    Executable commands: cp .env.example, compose up, curl health.
  </action>
  <verify>
    <automated>test -f docs/18-production-deploy.md && grep -q nginx docs/18-production-deploy.md && grep -q env_file docs/18-production-deploy.md</automated>
  </verify>
  <acceptance_criteria>
    - Doc exists with TLS termination explanation
    - Doc mentions env_file and JWT_SECRET
    - Includes at least one executable bash block
  </acceptance_criteria>
  <done>Deploy doc published</done>
</task>

<task type="auto">
  <name>Task 3: Cross-links README and doc 14</name>
  <read_first>README.md (Compose section), docs/14-docker-compose.md (intro)</read_first>
  <files>README.md, docs/14-docker-compose.md</files>
  <action>
    README — in Docker Compose optional section add:
    - Before `npm run compose:up`: `cp api/.env.example api/.env` and set `JWT_SECRET` + uncomment `DATABASE_URL` for compose
    - Link to `docs/18-production-deploy.md`

    docs/14-docker-compose.md — add short pointer after intro:
    - Secrets and production deploy: see `docs/18-production-deploy.md`
    - API reads `api/.env` via `env_file` (v1.5)

    Do not update `docs/00-indice.md` (Phase 21 DOCS-03 scope) unless user explicitly wants — CONTEXT defers index to 21; skip 00-indice per phase boundary.
  </action>
  <verify>
    <automated>grep -q '18-production-deploy' README.md && grep -q '18-production-deploy' docs/14-docker-compose.md</automated>
  </verify>
  <acceptance_criteria>
    - README links to doc 18 and mentions api/.env prerequisite
    - doc 14 links to doc 18
  </acceptance_criteria>
  <done>Cross-links added</done>
</task>

</tasks>

<verification>
## Plan Verification

1. `cp api/.env.example api/.env` — set JWT_SECRET and DATABASE_URL for compose
2. `npm run compose:up` — API healthy, login works
3. `grep env_file docker-compose.yml` — present on edf-lab-api
4. Read `docs/18-production-deploy.md` — TLS section present
</verification>
