---
phase: 20-secrets-deploy-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/.env.example
  - api/auth.js
  - api/index.js
  - api/README.md
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-03
must_haves:
  truths:
    - "api/.env.example documents JWT_SECRET, ADMIN_*, CORS_ORIGINS, DATABASE_URL, DB_FILE, AUTH_DISABLED, NODE_ENV"
    - "NODE_ENV=production without JWT_SECRET exits before listen with Spanish fatal message"
    - "Development without JWT_SECRET still warns and uses dev default"
    - "getJwtSecret does not silently use dev default in production"
  artifacts:
    - path: api/.env.example
      provides: "Documented secrets template"
    - path: api/index.js
      provides: "validateProductionEnv before startServer"
  key_links:
    - from: api/index.js
      to: api/auth.js
      via: "production guard before initDb/listen"
      pattern: "NODE_ENV"
---

<objective>
Harden secrets documentation and production boot: complete `.env.example`, fail-fast without `JWT_SECRET` in production, align `auth.js`.

Purpose: DEPLOY-01, DEPLOY-03 — learner can copy `.env` and understand production vs dev.
Output: Updated env example, startup guard, auth.js production behavior, api/README section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-secrets-deploy-hardening/20-CONTEXT.md
@.planning/phases/20-secrets-deploy-hardening/20-RESEARCH.md
@api/.env.example
@api/auth.js
@api/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand api/.env.example</name>
  <read_first>20-CONTEXT.md (D-01), api/.env.example, api/auth.js</read_first>
  <files>api/.env.example</files>
  <action>
    Reorganize `api/.env.example` with Spanish section headers:

    1. **Arranque** — `# NODE_ENV=production` (comment: en producción exige JWT_SECRET)
    2. **Autenticación** — placeholder JWT (comentario: cadena larga aleatoria, nunca en git), `JWT_EXPIRES_IN`, `ADMIN_EMAIL`, `ADMIN_PASSWORD`
    3. **CORS** — `CORS_ORIGINS` (keep current default ports)
    4. **Base de datos** — commented `DATABASE_URL` for Compose, commented `DB_FILE` for tests
    5. **Solo tests** — commented `AUTH_DISABLED=1` with never-in-production warning

    Do not put real secret values in the example file.
  </action>
  <verify>
    <automated>grep -q JWT_SECRET api/.env.example && grep -q NODE_ENV api/.env.example && grep -q DATABASE_URL api/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - File contains section comments for auth, CORS, database, tests
    - JWT_SECRET line has no real secret value
    - NODE_ENV documented as production trigger
  </acceptance_criteria>
  <done>env.example complete</done>
</task>

<task type="auto">
  <name>Task 2: Production fail-fast in index.js and auth.js</name>
  <read_first>api/index.js (startServer), api/auth.js (getJwtSecret), 20-CONTEXT.md (D-02, D-03, D-04)</read_first>
  <files>api/index.js, api/auth.js</files>
  <action>
  In `api/index.js`, add `validateProductionEnv()`:

  - If `process.env.NODE_ENV === 'production'` and `!process.env.JWT_SECRET?.trim()`, log `[fatal] JWT_SECRET es obligatorio cuando NODE_ENV=production. Copia api/.env.example a api/.env y define una clave larga.` then `process.exit(1)`.
  - Call `validateProductionEnv()` at start of `startServer()` before `initDb()`.

  In `api/auth.js`, update `getJwtSecret()`:

  - If `NODE_ENV === 'production'` and no JWT_SECRET, call `process.exit(1)` with same message (defense in depth) OR rethrow after validate — avoid returning dev default in production.
  - Non-production: keep existing warn + `'dev-only-change-in-production'`.

  Do not change route behavior or AUTH_DISABLED logic.
  </action>
  <verify>
    <automated>cd api && NODE_ENV=production node -e "require('./index.js')" 2>&1; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - NODE_ENV=production without JWT_SECRET exits non-zero
    - node --check passes on index.js and auth.js
    - Development start without JWT_SECRET still works with warning
  </acceptance_criteria>
  <done>Production fail-fast active</done>
</task>

<task type="auto">
  <name>Task 3: Document secrets in api/README.md</name>
  <read_first>api/README.md (Autenticación section), 20-CONTEXT.md</read_first>
  <files>api/README.md</files>
  <action>
    Add subsection **Producción y secretos** under or after Autenticación:

    - Copy `cp .env.example .env`
    - Set `JWT_SECRET` to a long random string
    - `NODE_ENV=production` requires JWT_SECRET (fail-fast)
    - Never commit `.env` or set `AUTH_DISABLED=1` in production
    - Example: `NODE_ENV=production` con secreto JWT en entorno + `PORT=3100 npm start`

    Link forward: Compose uses same `api/.env` via `env_file` (Plan 20-02).
  </action>
  <verify>
    <automated>grep -q "NODE_ENV=production" api/README.md && grep -q "fail-fast\|obligatorio" api/README.md</automated>
  </verify>
  <acceptance_criteria>
    - api/README.md mentions production JWT_SECRET requirement
    - Mentions .env.example copy step
  </acceptance_criteria>
  <done>API README updated</done>
</task>

</tasks>

<verification>
## Plan Verification

1. `cd api && NODE_ENV=production node index.js` → exits with fatal message (no JWT_SECRET)
2. `cd api && NODE_ENV=production` con `JWT_SECRET` en entorno (≥32 chars) + `PORT=3100 node index.js` → starts
3. `npm run test:sqlite` still passes (uses AUTH_DISABLED, not production mode)
</verification>
