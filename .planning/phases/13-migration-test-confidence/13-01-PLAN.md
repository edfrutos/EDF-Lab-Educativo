---
phase: 13-migration-test-confidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/seed.js
  - api/db-sqlite.js
  - api/db-pg.js
autonomous: true
requirements:
  - PGMIG-01
must_haves:
  truths:
    - "D-01: api/seed.js shared by db-sqlite.js and db-pg.js"
    - "D-02: Postgres seed inserts explicit ids then setval on users id sequence"
    - "D-03: Console messages match SQLite (Migrados N usuarios, corrupt JSON warn)"
    - "D-04: populateIfEmpty no-op when COUNT(*) > 0 on Postgres"
    - "D-16: No dashboard or SQLite removal"
    - "Empty edf_lab_test or fresh Postgres shows John id=1 and Jane id=2 after initDb"
  artifacts:
    - path: api/seed.js
      provides: "Shared seed resolution and populateIfEmpty"
      contains: "resolveSeedUsers"
    - path: api/db-pg.js
      provides: "Postgres initDb calls seed when not skipSeed"
      contains: "populateIfEmpty"
  key_links:
    - from: api/db-pg.js
      to: api/seed.js
      via: "initDb after schema"
      pattern: "populateIfEmptyPg"
---

<objective>
Extract shared seed logic to `api/seed.js` and wire both SQLite and PostgreSQL adapters so empty Postgres gets the same users as SQLite.

Purpose: PGMIG-01 — parity with v1.1 JSON migration path.
Output: `api/seed.js`, refactored `db-sqlite.js`, updated `db-pg.js`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-migration-test-confidence/13-CONTEXT.md
@.planning/phases/13-migration-test-confidence/13-RESEARCH.md
@.planning/phases/13-migration-test-confidence/13-PATTERNS.md
@api/db-sqlite.js
@api/db-pg.js
@api/data/users.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create api/seed.js with shared seed logic</name>
  <read_first>api/db-sqlite.js (DEFAULT_SEED, tryParseUsersJson, populateIfEmpty, insertUsers), 13-RESEARCH.md (Pattern 1, code examples)</read_first>
  <files>api/seed.js</files>
  <action>
    Create `api/seed.js` exporting:

    1. `DEFAULT_SEED` — same two users as today (id 1 John, id 2 Jane)
    2. `USERS_JSON_PATH` — `path.join(__dirname, 'data', 'users.json')`
    3. `tryParseUsersJson()` — move from db-sqlite unchanged behavior
    4. `resolveSeedUsers()` — returns `{ users }` after same decision tree:
       - corrupt JSON → warn `[warn] data/users.json corrupto — restaurando semilla` → DEFAULT_SEED
       - empty array → DEFAULT_SEED
       - valid users → log `Migrados ${n} usuarios desde users.json` → fromJson
    5. `populateIfEmptySqlite(getDb)` — COUNT; if > 0 return; resolveSeedUsers; INSERT (id, name, email) with prepared statement
    6. `populateIfEmptyPg(pool)` — COUNT via `SELECT COUNT(*)::int`; if > 0 return; resolveSeedUsers; INSERT with $1,$2,$3 for each row; then:
       ```sql
       SELECT setval(
         pg_get_serial_sequence('users', 'id'),
         (SELECT COALESCE(MAX(id), 1) FROM users)
       );
       ```

    Do NOT change console message text (D-03).
  </action>
  <verify>
    <automated>node --check api/seed.js</automated>
  </verify>
  <acceptance_criteria>
    - api/seed.js exists and passes node --check
    - Exports populateIfEmptySqlite and populateIfEmptyPg
    - resolveSeedUsers logs identical strings to current db-sqlite behavior
  </acceptance_criteria>
  <done>Shared seed module ready for both adapters</done>
</task>

<task type="auto">
  <name>Task 2: Refactor db-sqlite.js to use seed.js</name>
  <read_first>api/db-sqlite.js, api/seed.js</read_first>
  <files>api/db-sqlite.js</files>
  <action>
    Remove duplicated `DEFAULT_SEED`, `tryParseUsersJson`, `insertUsers`, and inline `populateIfEmpty` from `db-sqlite.js`.

    In `initDb`, after `db.exec(schema)`:
    ```javascript
    if (!options.skipSeed) {
      const { populateIfEmptySqlite } = require('./seed');
      populateIfEmptySqlite(getDb);
    }
    ```

  Keep all CRUD and DuplicateEmailError unchanged. Run `cd api && npm test` — 16/16 SQLite tests must pass.
  </action>
  <verify>
    <automated>cd api && npm test</automated>
  </verify>
  <acceptance_criteria>
    - db-sqlite.js requires ./seed.js for populate only
    - npm test exits 0 with 16 passing tests
    - No duplicate seed logic left in db-sqlite.js
  </acceptance_criteria>
  <done>SQLite path uses shared seed; tests green</done>
</task>

<task type="auto">
  <name>Task 3: Wire seed into db-pg.js initDb</name>
  <read_first>api/db-pg.js, api/seed.js, 13-CONTEXT.md (D-04)</read_first>
  <files>api/db-pg.js</files>
  <action>
    After `await pool.query(schema)` in `initDb`, add:

    ```javascript
    if (!options.skipSeed) {
      const { populateIfEmptyPg } = require('./seed');
      await populateIfEmptyPg(pool);
    }
    ```

    Keep `console.log('[db] Using PostgreSQL')` after schema+seed.

    Manual verify (document in SUMMARY if Postgres not running in executor env):
    1. `docker compose up -d edf-lab-postgres`
    2. Connect to empty DB `edf_lab_test` or truncate `edf_lab` dev DB only if safe for local lab
    3. `DATABASE_URL=postgresql://edf_lab:edf_lab_dev@localhost:5432/edf_lab_test PORT=3101 node index.js`
    4. `curl -s http://localhost:3101/users` — expect 2 users, ids 1 and 2, names John/Jane order by name → Jane first in GET /users
  </action>
  <verify>
    <automated>node --check api/db-pg.js && cd api && npm test</automated>
  </verify>
  <acceptance_criteria>
    - db-pg.js calls populateIfEmptyPg when skipSeed is false
    - initDb with skipSeed true still skips seed (for tests later)
    - SQLite npm test still 16/16
  </acceptance_criteria>
  <done>Postgres empty DB seeds John and Jane with correct ids</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed.js reads users.json | Host file only; same trust as before |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | Explicit id INSERT | accept | Lab seed only; setval restores SERIAL |
</threat_model>

<verification>
- npm test 16/16 SQLite
- Optional: curl GET /users against DATABASE_URL empty DB shows 2 users
</verification>

<success_criteria>
- PGMIG-01 satisfied: empty Postgres + initDb → seed users visible
- ROADMAP criterion 1 observable
</success_criteria>

<output>
After completion, create `.planning/phases/13-migration-test-confidence/13-01-SUMMARY.md`
</output>
