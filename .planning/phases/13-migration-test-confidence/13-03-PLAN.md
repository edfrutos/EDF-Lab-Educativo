---
phase: 13-migration-test-confidence
plan: 03
type: execute
wave: 2
depends_on:
  - 13-02
files_modified:
  - docs/13-sqlite.md
  - api/README.md
autonomous: true
requirements:
  - PGMIG-02
must_haves:
  truths:
    - "D-13: docs/13-sqlite.md ends with Hacia PostgreSQL section — table + executable commands"
    - "D-14: No Mission 12, no docs/15-postgresql.md content, no Compose walkthrough mission"
    - "D-15: docs/00-indice.md not modified"
    - "D-17: No NOTEBOOK bulk pass unless blocker-only"
    - "D-16: No dashboard changes"
    - "Learner can run psql, test:db:prepare, npm test from documented commands"
  artifacts:
    - path: docs/13-sqlite.md
      provides: "SQLite vs PostgreSQL lab comparison"
      contains: "Hacia PostgreSQL"
  key_links:
    - from: docs/13-sqlite.md
      to: api/package.json
      via: "documented npm test and test:db:prepare"
      pattern: "test:db:prepare"
---

<objective>
Document SQLite vs PostgreSQL trade-offs and executable test/seed commands at the end of `docs/13-sqlite.md`.

Purpose: PGMIG-02 — comparison without Phase 14 full tutorial.
Output: Updated `docs/13-sqlite.md`; optional brief `api/README.md` test prerequisite
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-migration-test-confidence/13-CONTEXT.md
@.planning/phases/13-migration-test-confidence/13-RESEARCH.md
@docs/13-sqlite.md
@api/README.md
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add «Hacia PostgreSQL» section to docs/13-sqlite.md</name>
  <read_first>docs/13-sqlite.md (full), 13-CONTEXT.md (D-13, D-14, D-15), docker-compose.yml, api/package.json scripts</read_first>
  <files>docs/13-sqlite.md</files>
  <action>
    Replace or extend the final paragraph (lines ~218–220) with a new section **`## Hacia PostgreSQL`** before «Resumen» OR integrate into Resumen with clear subsection — prefer **new section before Resumen** plus update Resumen bullet pointing to it.

    Section must include:

    1. **Comparison table** (markdown) with rows such as:
       - Almacén: archivo `.db` vs servidor Postgres
       - Compose: bind mount `./api/data` vs volumen nombrado `postgres_data`
       - Cuándo usar cada uno en el lab: `npm start` sin env → SQLite; `compose:up` → Postgres
       - Tests: `users.test.db` vs `edf_lab_test`

    2. **Executable commands** (copy-paste bash blocks):
       - Arrancar solo Postgres: `docker compose up -d edf-lab-postgres`
       - Crear BD de tests: `npm run test:db:prepare` (desde raíz del repo)
       - Inspeccionar con psql: `psql postgresql://edf_lab:edf_lab_dev@localhost:5432/edf_lab`
       - Suite completa: `cd api && npm test` (16 SQLite + 16 Postgres; Postgres debe estar en :5432)
       - Solo Postgres: `npm run test:pg --prefix api`
       - Variable opcional: `TEST_DATABASE_URL` para otra URL de test

    3. **Prerequisite note (D-08):** si Postgres no está en marcha, la segunda mitad de `npm test` falla — no es skip silencioso.

    4. Pointer: «Guía completa Compose + Postgres → Fase 14 (doc 15 y Misión 12)» — one line only (D-14).

    Do NOT edit `docs/00-indice.md`. Do NOT create `docs/15-postgresql.md`.
  </action>
  <verify>
    <automated>grep -q "Hacia PostgreSQL" docs/13-sqlite.md && grep -q "test:db:prepare" docs/13-sqlite.md && grep -q "edf_lab_test" docs/13-sqlite.md</automated>
  </verify>
  <acceptance_criteria>
    - Section heading ## Hacia PostgreSQL exists
    - Table compares SQLite file vs Postgres server
    - At least 4 executable command blocks present
    - docs/00-indice.md unchanged
  </acceptance_criteria>
  <done>PGMIG-02 documentation complete in doc 13</done>
</task>

<task type="auto">
  <name>Task 2: Update api/README.md test prerequisites</name>
  <read_first>api/README.md (test section if any), docs/13-sqlite.md new section</read_first>
  <files>api/README.md</files>
  <action>
    Add or extend a **Tests** subsection in `api/README.md` (Spanish, learner-facing):

    - `npm test` — 16 tests SQLite + 16 Postgres (encadenados)
    - `npm run test:sqlite` — solo SQLite
    - `npm run test:pg` — solo Postgres (requiere Postgres + `npm run test:db:prepare` primero)
    - Link to `docs/13-sqlite.md` section «Hacia PostgreSQL»

    Keep under 15 lines; do not duplicate full comparison table.
  </action>
  <verify>
    <automated>grep -q "test:db:prepare" api/README.md && grep -q "index.pg.test" api/README.md</automated>
  </verify>
  <acceptance_criteria>
    - api/README.md mentions dual test run and test:db:prepare
    - Points to docs/13-sqlite.md for Postgres details
  </acceptance_criteria>
  <done>API README reflects Phase 13 test matrix</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation only | No runtime behavior change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Information disclosure | Lab credentials in docs | accept | Same fictional credentials as compose; already public in repo |
</threat_model>

<verification>
- grep Hacia PostgreSQL docs/13-sqlite.md
- grep test:db:prepare api/README.md
</verification>

<success_criteria>
- PGMIG-02 satisfied
- ROADMAP criterion 2 TRUE
</success_criteria>

<output>
After completion, create `.planning/phases/13-migration-test-confidence/13-03-SUMMARY.md`
</output>
