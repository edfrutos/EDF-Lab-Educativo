---
phase: 36-ci-visual-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - package.json
autonomous: true
requirements:
  - QA-VIS-04
  - QA-CI-06
user_setup: []
must_haves:
  truths:
    - "Existe job CI `visual-regression` dedicado y aislado de jobs funcionales"
    - "Existe script `test:visual:ci` para la ejecución canónica en CI"
    - "El job visual usa Chromium y ejecuta `npm run test:visual:ci`"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Job visual-regression"
      contains: "visual-regression"
    - path: package.json
      provides: "Script test:visual:ci"
      contains: "test:visual:ci"
  key_links:
    - from: .github/workflows/ci.yml
      to: package.json
      via: "npm run test:visual:ci"
      pattern: "test:visual:ci"
---

<objective>
Crear la base CI de regresión visual con un job dedicado `visual-regression` y un script `test:visual:ci`, sin tocar contratos de jobs E2E/API existentes.

Purpose: Cumplir la primera mitad de QA-VIS-04 y QA-CI-06 con cableado reproducible en GitHub Actions.
Output: workflow CI actualizado + script CI visual en package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-ci-visual-regression/36-CONTEXT.md
@.planning/phases/36-ci-visual-regression/36-RESEARCH.md
@.github/workflows/ci.yml
@package.json
@e2e/playwright.config.js

<interfaces>
Job CI esperado:

```yaml
visual-regression:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
    - run: npm ci
    - run: npm ci
      working-directory: api
    - run: npm ci
      working-directory: dashboard-react
    - run: npm ci
      working-directory: dashboard-vue
    - run: npx playwright install --with-deps chromium
    - run: npm run test:visual:ci
      env:
        CI: true
```

Script:

```json
"test:visual:ci": "playwright test --config=e2e/playwright.config.js --project=vanilla-chromium-visual --project=react-chromium-visual --project=vue-chromium-visual"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir script test:visual:ci</name>
  <read_first>
    package.json
  </read_first>
  <files>package.json</files>
  <action>
Añadir `scripts.test:visual:ci` en la raíz con los tres proyectos visual Chromium.

No modificar `test:e2e`, `test:e2e:ci`, `test:e2e:pg` ni `test:visual`.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const s=p.scripts['test:visual:ci']||''; if(!s.includes('vanilla-chromium-visual')||!s.includes('react-chromium-visual')||!s.includes('vue-chromium-visual')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Existe `test:visual:ci` en scripts
    - El script incluye los 3 proyectos visual
    - Scripts funcionales previos permanecen intactos
  </acceptance_criteria>
  <done>Contrato de comando CI visual definido</done>
</task>

<task type="auto">
  <name>Task 2: Añadir job visual-regression a CI</name>
  <read_first>
    .github/workflows/ci.yml
    package.json
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
Agregar job `visual-regression` en `.github/workflows/ci.yml` siguiendo patrón de instalación de `e2e-smoke`:

- checkout + setup-node con cache
- `npm ci` en raíz, `api`, `dashboard-react`, `dashboard-vue`
- `npx playwright install --with-deps chromium`
- `npm run test:visual:ci` con `CI=true`

No alterar pasos ni comandos de jobs existentes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const y=fs.readFileSync('.github/workflows/ci.yml','utf8'); if(!y.includes('visual-regression:')||!y.includes('npm run test:visual:ci')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Job `visual-regression` existe
    - Ejecuta `npm run test:visual:ci`
    - Instala Chromium con deps en el job visual
    - No modifica contratos de jobs funcionales
  </acceptance_criteria>
  <done>CI tiene gate visual dedicado y aislado</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visual job vs functional jobs | Nuevo job no altera matrices ni scripts funcionales |
| CI browser install | Chromium visual aislado de firefox/webkit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | D | CI duration increase | accept | Mantener Chromium-only en visual job |
| T-36-02 | T | Accidental e2e contract drift | mitigate | No tocar `test:e2e*`; checks explícitos |
</threat_model>

<verification>
- `node -e` check para script `test:visual:ci`
- `node -e` check para presencia de `visual-regression` en workflow
- (Opcional local) `npm run test:visual:ci`
</verification>

<success_criteria>
- `visual-regression` queda cableado en CI
- `test:visual:ci` existe y ejecuta 3 proyectos visuales
- Jobs funcionales conservan su contrato
</success_criteria>

<output>
After completion, create `.planning/phases/36-ci-visual-regression/36-01-SUMMARY.md`
</output>
