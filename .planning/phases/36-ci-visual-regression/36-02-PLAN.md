---
phase: 36-ci-visual-regression
plan: 02
type: execute
wave: 2
depends_on:
  - "36-01"
files_modified:
  - .github/workflows/ci.yml
  - docs/10-tests.md
autonomous: true
requirements:
  - QA-VIS-04
  - QA-CI-06
user_setup: []
must_haves:
  truths:
    - "Fallo visual en CI deja artefactos revisables (diff/report)"
    - "docs/10-tests.md documenta flujo baseline update para PR"
    - "test:e2e, test:e2e:ci y test:e2e:pg siguen sin cambios de contrato"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Upload de artefactos Playwright para visual-regression"
      contains: "actions/upload-artifact"
    - path: docs/10-tests.md
      provides: "Flujo CI visual y baseline update"
      contains: "test:visual:ci"
  key_links:
    - from: docs/10-tests.md
      to: .github/workflows/ci.yml
      via: "visual-regression"
      pattern: "visual-regression"
---

<objective>
Completar la puerta CI visual con diagnósticos revisables en PR y documentación operativa para actualizar baselines sin romper el gate funcional existente.

Purpose: Cerrar QA-VIS-04 y QA-CI-06 con observabilidad de fallos y flujo didáctico de mantenimiento de snapshots.
Output: artefactos CI de diff/report + docs de baseline update + verificación de no regresión contractual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-ci-visual-regression/36-CONTEXT.md
@.planning/phases/36-ci-visual-regression/36-01-SUMMARY.md
@.github/workflows/ci.yml
@docs/10-tests.md
@package.json

<interfaces>
Artefactos esperados en CI:

```yaml
- name: Upload visual artifacts
  if: failure()
  uses: actions/upload-artifact@v4
  with:
    name: visual-regression-artifacts
    path: |
      test-results
      playwright-report
```

Flujo docs esperado:
- Ejecutar `npm run test:visual -- --update-snapshots` localmente
- Revisar cambios PNG por dashboard
- Commit intencional de baselines + referencia al PR
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publicar artefactos visuales en fallo</name>
  <read_first>
    .github/workflows/ci.yml
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
En el job `visual-regression`, añadir paso(s) de upload de artefactos Playwright cuando falle la suite visual.

Priorizar al menos:
- `test-results`
- `playwright-report`

Mantener upload condicionado para no inflar almacenamiento innecesariamente (por ejemplo `if: failure()`).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const y=fs.readFileSync('.github/workflows/ci.yml','utf8'); if(!y.includes('actions/upload-artifact@v4')||!y.includes('visual-regression-artifacts')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Job visual sube artefactos en fallo
    - Diffs/report quedan accesibles para revisión de PR
  </acceptance_criteria>
  <done>Fallo visual en CI es diagnosticable sin reproduccion manual inmediata</done>
</task>

<task type="auto">
  <name>Task 2: Documentar baseline update en flujo PR</name>
  <read_first>
    docs/10-tests.md
    .planning/phases/36-ci-visual-regression/36-CONTEXT.md
  </read_first>
  <files>docs/10-tests.md</files>
  <action>
Ampliar sección visual en `docs/10-tests.md` con bloque CI:

- Qué hace `test:visual:ci`
- Dónde revisar artefactos cuando falla `visual-regression`
- Flujo recomendado para actualización de baselines en cambios intencionales:
  1) `npm run test:visual -- --update-snapshots`
  2) revisión PNG y diff textual
  3) commit de baselines + cambios UI

Mantener consistencia didáctica y aclarar que visual CI complementa (no reemplaza) E2E funcional.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const d=fs.readFileSync('docs/10-tests.md','utf8'); if(!d.includes('test:visual:ci')||!d.includes('--update-snapshots')||!d.includes('visual-regression')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Docs incluyen flujo PR baseline update
    - Docs enlazan ejecución CI visual con artefactos de fallo
  </acceptance_criteria>
  <done>Operador puede resolver fallos visuales de PR con guía clara</done>
</task>

<task type="auto">
  <name>Task 3: Verificación de no regresión contractual</name>
  <read_first>
    package.json
    .github/workflows/ci.yml
  </read_first>
  <files>.github/workflows/ci.yml, package.json</files>
  <action>
Verificar explícitamente que `test:e2e`, `test:e2e:ci`, `test:e2e:pg` conservan su contrato y que el job visual es aditivo.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json').scripts; if(!p['test:e2e']||!p['test:e2e:ci']||!p['test:e2e:pg']) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - No hay drift en scripts funcionales
    - QA-CI-06 cubierto con evidencia explícita
  </acceptance_criteria>
  <done>Fase visual CI se integra sin romper gates funcionales</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Artifact upload | Diagnóstico visual sin exponer secretos |
| Documentation workflow | Evita baseline churn accidental |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03 | R | PR mismatch root cause unknown | mitigate | Subir artefactos de test-results/report en fallo |
| T-36-04 | I | Baseline drift por updates no intencionales | mitigate | Documentar checklist de baseline update y review |
</threat_model>

<verification>
- check de presencia de upload-artifact en CI
- check de docs (`test:visual:ci`, `--update-snapshots`, `visual-regression`)
- check de scripts funcionales intactos
</verification>

<success_criteria>
- CI visual deja evidencia revisable cuando falla
- docs cubren flujo PR de baseline update
- contratos E2E/API funcionales siguen intactos
</success_criteria>

<output>
After completion, create `.planning/phases/36-ci-visual-regression/36-02-SUMMARY.md`
</output>
