---
phase: 34-fundaci-n-visual-vanilla
plan: 02
type: execute
wave: 2
depends_on:
  - "34-01"
files_modified:
  - e2e/playwright.config.js
  - package.json
  - docs/10-tests.md
  - e2e/__snapshots__/visual.vanilla.spec.js/dashboard-post-login-chromium-darwin.png
autonomous: true
requirements:
  - QA-VIS-01
  - QA-VIS-03
user_setup: []
must_haves:
  truths:
    - "npm run test:visual ejecuta visual.vanilla en Chromium y pasa localmente"
    - "Proyecto vanilla-chromium-visual aislado de test:e2e funcional"
    - "docs/10-tests.md documenta threshold, masks y --update-snapshots"
    - "Baseline dashboard-post-login commiteada bajo e2e/__snapshots__/"
  artifacts:
    - path: e2e/playwright.config.js
      provides: "Proyecto vanilla-chromium-visual"
      contains: "vanilla-chromium-visual"
    - path: package.json
      provides: "Script test:visual"
      contains: "test:visual"
    - path: docs/10-tests.md
      provides: "Sección borrador regresión visual vanilla"
      contains: "maxDiffPixelRatio"
  key_links:
    - from: package.json
      to: e2e/playwright.config.js
      via: "playwright test --project=vanilla-chromium-visual"
      pattern: "test:visual"
    - from: e2e/playwright.config.js
      to: e2e/tests/visual.vanilla.spec.js
      via: "testMatch visual.vanilla"
      pattern: "visual\\.vanilla"
---

<objective>
Cablear el proyecto visual en Playwright, añadir `npm run test:visual`, documentar política anti-flake (QA-VIS-03) y verificar la suite visual vanilla con baseline commiteada.

Purpose: Cerrar fase 34 con comando local reproducible y traza didáctica.
Output: Config + script + docs borrador + 1 test visual verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-fundaci-n-visual-vanilla/34-01-SUMMARY.md
@.planning/phases/34-fundaci-n-visual-vanilla/34-CONTEXT.md
@e2e/playwright.config.js
@docs/10-tests.md
@package.json

<interfaces>
Añadir proyecto (no reemplazar vanilla-chromium):

```javascript
{
  name: 'vanilla-chromium-visual',
  testMatch: /visual\.vanilla\.spec\.js/,
  use: {
    ...devices['Desktop Chrome'],
    baseURL: 'http://localhost:5173',
    viewport: { width: 1280, height: 720 }
  }
}
```

Script raíz:

```json
"test:visual": "playwright test --config=e2e/playwright.config.js --project=vanilla-chromium-visual"
```

Mantener `test:e2e` y `test:e2e:ci` sin incluir proyecto visual (D-13, QA-CI-06 preparación).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proyecto vanilla-chromium-visual</name>
  <read_first>
    e2e/playwright.config.js
    e2e/tests/visual.vanilla.spec.js
    .planning/phases/34-fundaci-n-visual-vanilla/34-CONTEXT.md
  </read_first>
  <files>e2e/playwright.config.js</files>
  <action>
En `e2e/playwright.config.js`, añadir proyecto `vanilla-chromium-visual` al array `projects` (después de proyectos CI o al final, antes de `vanilla-webkit` si existe):

- `name: 'vanilla-chromium-visual'`
- `testMatch: /visual\.vanilla\.spec\.js/`
- `use: { ...devices['Desktop Chrome'], baseURL: 'http://localhost:5173', viewport: { width: 1280, height: 720 } }`

**No modificar** `testMatch` de `vanilla-chromium`, `react-chromium`, etc.

Confirmar `snapshotPathTemplate` de plan 34-01 sigue presente.

Comentario en español: suite visual separada de E2E funcional.
  </action>
  <verify>
    <automated>node --check e2e/playwright.config.js</automated>
  </verify>
  <acceptance_criteria>
    - Proyecto vanilla-chromium-visual existe con testMatch visual.vanilla
    - Viewport 1280×720 en use del proyecto visual
    - Proyectos E2E existentes intactos
  </acceptance_criteria>
  <done>Playwright puede ejecutar solo specs visual vanilla</done>
</task>

<task type="auto">
  <name>Task 2: Script npm run test:visual</name>
  <read_first>
    package.json
    e2e/playwright.config.js
  </read_first>
  <files>package.json</files>
  <action>
En `package.json` raíz, añadir script:

```json
"test:visual": "playwright test --config=e2e/playwright.config.js --project=vanilla-chromium-visual"
```

No cambiar `test:e2e`, `test:e2e:ci`, ni `test:e2e:pg`.

Opcional en comentario de docs (no script separado aún): actualizar baselines con `npm run test:visual -- --update-snapshots`.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts['test:visual']) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contiene clave scripts.test:visual
    - Comando referencia --project=vanilla-chromium-visual
  </acceptance_criteria>
  <done>Comando local documentado y ejecutable</done>
</task>

<task type="auto">
  <name>Task 3: Docs borrador + baseline + verificación</name>
  <read_first>
    docs/10-tests.md
    e2e/helpers/visual-flow.js
    .planning/phases/34-fundaci-n-visual-vanilla/34-CONTEXT.md
  </read_first>
  <files>docs/10-tests.md</files>
  <action>
**docs/10-tests.md** — Añadir sección `## Regresión visual (vanilla, borrador fase 34)` con:

- Comando: `npm run test:visual`
- Qué captura: `#dashboard-panel` post-login, snapshot `dashboard-post-login.png`
- Masks: `#health-timestamp`, `#users-table-body`
- Threshold: `maxDiffPixelRatio: 0.01`
- Actualizar baseline: `npm run test:visual -- --update-snapshots` (solo cambios UI intencionales)
- Relación con E2E funcional: visual no sustituye CRUD/smoke
- Tabla scripts: añadir fila `test:visual`

**Baseline:** Ejecutar `npm run test:visual -- --update-snapshots` si no existe PNG bajo `e2e/__snapshots__/visual.vanilla.spec.js/`. Commitear baseline generada.

**Verificación:** `npm run test:visual` debe pasar **1 passed**. `npm run test:e2e` debe seguir pasando **6 passed** (regresión — no romper E2E funcional).

Si `test:e2e` no puede ejecutarse en entorno sandbox, documentar en SUMMARY el resultado local esperado.
  </action>
  <verify>
    <automated>npm run test:visual</automated>
  </verify>
  <acceptance_criteria>
    - docs/10-tests.md contiene sección regresión visual con maxDiffPixelRatio y --update-snapshots
    - Baseline PNG existe bajo e2e/__snapshots__/
    - npm run test:visual exit 0
  </acceptance_criteria>
  <done>Fase 34 verificada localmente con 1 test visual verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test:visual vs test:e2e | Proyectos separados — CI E2E no ejecuta visual hasta fase 36 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | D | test:e2e regression | mitigate | Verificar 6 passed tras añadir proyecto visual |
| T-34-04 | I | Accidental baseline update | accept | Documentar --update-snapshots solo en cambios UI intencionales |
</threat_model>

<verification>
- `npm run test:visual` → 1 passed
- `npm run test:e2e` → 6 passed (si entorno lo permite)
- `node --check` en archivos tocados
</verification>

<success_criteria>
- QA-VIS-01 y QA-VIS-03 cubiertos en vanilla
- Comando test:visual operativo
- Política anti-flake documentada
</success_criteria>

<output>
After completion, create `.planning/phases/34-fundaci-n-visual-vanilla/34-02-SUMMARY.md`
</output>
