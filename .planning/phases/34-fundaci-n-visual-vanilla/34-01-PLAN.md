---
phase: 34-fundaci-n-visual-vanilla
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/helpers/visual-flow.js
  - e2e/tests/visual.vanilla.spec.js
  - e2e/playwright.config.js
autonomous: true
requirements:
  - QA-VIS-01
user_setup: []
must_haves:
  truths:
    - "Tras login UI, prepareVisualState deja #dashboard-panel visible y estable"
    - "visual.vanilla.spec.js captura snapshot dashboard-post-login con masks en zonas dinámicas"
    - "snapshotPathTemplate apunta a e2e/__snapshots__/{testFilePath}/"
    - "Helper visual-flow exporta prepareVisualState y getVisualScreenshotOptions reutilizables"
  artifacts:
    - path: e2e/helpers/visual-flow.js
      provides: "Login + estabilización + opciones mask/threshold"
      exports: ["prepareVisualState", "getVisualScreenshotOptions"]
    - path: e2e/tests/visual.vanilla.spec.js
      provides: "Spec visual vanilla con toHaveScreenshot"
      contains: "dashboard-post-login"
    - path: e2e/__snapshots__/visual.vanilla.spec.js/dashboard-post-login-chromium-darwin.png
      provides: "Baseline PNG commiteada (o generada con --update-snapshots en verify)"
      contains: ""
  key_links:
    - from: e2e/tests/visual.vanilla.spec.js
      to: e2e/helpers/visual-flow.js
      via: "require('../helpers/visual-flow.js')"
      pattern: "prepareVisualState"
    - from: e2e/helpers/visual-flow.js
      to: dashboard/index.html
      via: "#login-email, #dashboard-panel, #health-timestamp, #users-table-body"
      pattern: "#dashboard-panel"
    - from: e2e/playwright.config.js
      to: e2e/__snapshots__
      via: "snapshotPathTemplate"
      pattern: "__snapshots__"
---

<objective>
Implementar `visual-flow.js`, el spec `visual.vanilla.spec.js` y la plantilla de rutas de baseline en Playwright para el primer snapshot estable del dashboard vanilla post-login.

Purpose: Núcleo de QA-VIS-01 y contrato reutilizable en fase 35 (React/Vue).
Output: Helper + spec + snapshotPathTemplate; baseline generable con `--update-snapshots`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-fundaci-n-visual-vanilla/34-CONTEXT.md
@.planning/phases/34-fundaci-n-visual-vanilla/34-RESEARCH.md
@e2e/helpers/auth-smoke-flow.js
@e2e/tests/auth-smoke.vanilla.spec.js
@dashboard/index.html

<interfaces>
Patrón helper (espejar auth-smoke-flow / crud-flow):

```javascript
async function prepareVisualState(page, { email, password }) { ... }
function getVisualScreenshotOptions(page) { return { maxDiffPixelRatio: 0.01, mask: [...] }; }
module.exports = { prepareVisualState, getVisualScreenshotOptions };
```

DOM targets (CONTEXT D-09, D-05, D-06):
- Panel: `#dashboard-panel` (debe estar visible tras login)
- Masks: `#health-timestamp`, `#users-table-body`
- Login: `#login-email`, `#login-password`, botón «Entrar»
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper visual-flow.js</name>
  <read_first>
    e2e/helpers/auth-smoke-flow.js
    .planning/phases/34-fundaci-n-visual-vanilla/34-CONTEXT.md
    dashboard/index.html
  </read_first>
  <files>e2e/helpers/visual-flow.js</files>
  <action>
Crear `e2e/helpers/visual-flow.js` en `'use strict'` con `expect` de `@playwright/test`.

**`prepareVisualState(page, { email, password })`:**
1. Si no está en `/`, `page.goto('/')`.
2. Login: fill `#login-email`, `#login-password`, click «Entrar» (mismos selectores que auth-smoke).
3. Assert: `#dashboard-panel` visible (`not.toHaveAttribute('hidden')` o `toBeVisible()`), `#login-gate` oculto, botón «Cerrar sesión» visible.
4. Esperar estabilidad: `expect(page.getByText('John Doe')).toBeVisible()` (seed E2E).
5. Desactivar animaciones: `page.addStyleTag` con regla que anule `transition` y `animation` en `*` (D-07).
6. Fijar viewport 1280×720 si no está ya aplicado (D-10).

**`getVisualScreenshotOptions(page)`:**
- Retornar objeto para `toHaveScreenshot`:
  - `maxDiffPixelRatio: 0.01` (D-08)
  - `mask: [page.locator('#health-timestamp'), page.locator('#users-table-body')]`
  - `animations: 'disabled'` si la versión de Playwright lo soporta en la API usada

JSDoc en español: propósito, masks, reutilización fase 35, no incluye logout.

No modificar dashboard ni API.
  </action>
  <verify>
    <automated>node --check e2e/helpers/visual-flow.js</automated>
  </verify>
  <acceptance_criteria>
    - Exporta `prepareVisualState` y `getVisualScreenshotOptions`
    - Usa `#login-email` / `#login-password` (no getByLabel global)
    - Documenta masks y threshold en JSDoc
  </acceptance_criteria>
  <done>Helper sintácticamente válido listo para importar desde spec visual</done>
</task>

<task type="auto">
  <name>Task 2: Spec visual.vanilla.spec.js</name>
  <read_first>
    e2e/tests/auth-smoke.vanilla.spec.js
    e2e/helpers/visual-flow.js
  </read_first>
  <files>e2e/tests/visual.vanilla.spec.js</files>
  <action>
Crear `e2e/tests/visual.vanilla.spec.js`:

```javascript
const { test, expect } = require('@playwright/test');
const { prepareVisualState, getVisualScreenshotOptions } = require('../helpers/visual-flow.js');

const operatorEmail = process.env.E2E_OPERATOR_EMAIL || 'admin@lab.local';
const operatorPassword = process.env.E2E_OPERATOR_PASSWORD || 'changeme';

test.describe('Visual — dashboard vanilla', () => {
  test('dashboard post-login snapshot estable', async ({ page }) => {
    await page.goto('/');
    await prepareVisualState(page, { email: operatorEmail, password: operatorPassword });
    const panel = page.locator('#dashboard-panel');
    await expect(panel).toHaveScreenshot('dashboard-post-login.png', getVisualScreenshotOptions(page));
  });
});
```

`describe` y título en español. Un solo test (D-04).

Para generar baseline inicial (verificación manual en este plan): `npx playwright test visual.vanilla --config=e2e/playwright.config.js --update-snapshots` (proyecto visual se cablea en plan 34-02; hasta entonces usar CLI con ruta al spec).
  </action>
  <verify>
    <automated>node --check e2e/tests/visual.vanilla.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - Importa prepareVisualState y getVisualScreenshotOptions
    - toHaveScreenshot nombre `dashboard-post-login.png` sobre `#dashboard-panel`
    - Credenciales E2E_OPERATOR_* con defaults admin@lab.local / changeme
  </acceptance_criteria>
  <done>Spec visual creado con sintaxis válida</done>
</task>

<task type="auto">
  <name>Task 3: snapshotPathTemplate en playwright.config.js</name>
  <read_first>
    e2e/playwright.config.js
    .planning/phases/34-fundaci-n-visual-vanilla/34-RESEARCH.md
  </read_first>
  <files>e2e/playwright.config.js</files>
  <action>
En `defineConfig({ ... })` de `e2e/playwright.config.js`, añadir a nivel raíz (junto a `use`):

```javascript
snapshotPathTemplate: '{testDir}/__snapshots__/{testFilePath}/{arg}-{projectName}-{platform}{ext}',
```

Comentario breve en español: baselines versionadas bajo `e2e/__snapshots__/`.

**No añadir aún** el proyecto `vanilla-chromium-visual` ni `npm run test:visual` (plan 34-02).

**No modificar** testMatch de proyectos E2E existentes.
  </action>
  <verify>
    <automated>node --check e2e/playwright.config.js</automated>
  </verify>
  <acceptance_criteria>
    - snapshotPathTemplate presente en defineConfig
    - Ruta incluye `__snapshots__` y `{testFilePath}`
    - Proyectos vanilla-chromium existentes sin cambios de testMatch
  </acceptance_criteria>
  <done>Config acepta baselines en ruta predecible</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright → dashboard :5173 | Captura UI autenticada; masks evitan filtrar datos dinámicos en diff |
| Baselines en git | PNGs commiteados — revisar en PR cuando UI cambia intencionalmente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | D | Snapshot flakes | mitigate | Masks + maxDiffPixelRatio 0.01 + animations disabled |
| T-34-02 | I | Baseline drift | accept | `--update-snapshots` documentado en plan 34-02 |
</threat_model>

<verification>
- `node --check` en helper, spec y config
- Tras plan 34-02: `npm run test:visual` pasa con baseline commiteada
</verification>

<success_criteria>
- Helper y spec existen con contrato exportado
- snapshotPathTemplate configurado
- Un snapshot post-login sobre #dashboard-panel con política anti-flake
</success_criteria>

<output>
After completion, create `.planning/phases/34-fundaci-n-visual-vanilla/34-01-SUMMARY.md`
</output>
