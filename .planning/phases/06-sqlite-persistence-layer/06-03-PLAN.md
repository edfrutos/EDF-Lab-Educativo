---
phase: 06-sqlite-persistence-layer
plan: 03
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - api/index.test.js
  - api/.dockerignore
  - api/Dockerfile
  - .gitignore
autonomous: false
requirements:
  - SQLITE-01
  - SQLITE-04
  - SQLITE-05
must_haves:
  truths:
    - "npm test passes all 12 existing API tests against SQLite"
    - "Docker build succeeds and container serves CRUD from ephemeral users.db"
    - "Runtime .db files excluded from git and Docker build context"
    - "Learner can point API to custom DB via DB_FILE env var"
  artifacts:
    - path: api/index.test.js
      provides: "SQLite test harness with DB_FILE isolation"
      pattern: "process.env.DB_FILE"
    - path: api/.dockerignore
      provides: "Excludes runtime db files from image"
      contains: "data/users.db"
  key_links:
    - from: api/index.test.js
      to: api/db.js
      via: "app.initDb() in beforeEach"
      pattern: "app\\.initDb"
    - from: api/.dockerignore
      to: Docker ephemeral DB
      via: "exclude data/*.db from build context"
      pattern: "data/\\*\\.db"
---

<objective>
Update tests, Docker config, and gitignore so the SQLite-backed API is verifiable and container-ready.

Purpose: Keep npm test green after persistence switch; preserve ephemeral Docker behavior from Phase 5.
Output: Passing test suite, updated Docker exclusions, human verification gate.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-sqlite-persistence-layer/06-RESEARCH.md (Pattern 5, Docker section)
@.planning/phases/06-sqlite-persistence-layer/06-PATTERNS.md (index.test.js, .dockerignore, Dockerfile)
@api/index.test.js
@api/.dockerignore
@api/Dockerfile
@docs/12-docker.md

<interfaces>
Test env-before-require pattern (must preserve):
```javascript
process.env.DB_FILE = TEST_DB;  // BEFORE require('./index.js')
const app = require('./index.js');
// ...
beforeEach: await unlink(TEST_DB); await app.initDb();
```

Seed parity: initDb seed-on-empty → John id=1, Jane id=2; POST test expects id=3.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Docker and gitignore for SQLite runtime files</name>
  <read_first>06-PATTERNS.md (.dockerignore + Dockerfile sections), docs/12-docker.md lines 75-84</read_first>
  <files>api/.dockerignore, api/Dockerfile, .gitignore</files>
  <action>
    **api/.dockerignore** — add alongside existing `data/users.json` exclusion:
    ```
    data/users.db
    data/*.db
    ```
    Keep `data/users.json` exclusion (still seed reference, not runtime).

    **api/Dockerfile** — comment-only update (lines 15-17):
    - Change `loadUsers() crea users.json` → `initDb() crea users.db`
    - No structural Dockerfile changes; `mkdir -p data && chown` pattern stays

    **.gitignore** — add runtime SQLite files:
    ```
    api/data/*.db
    ```
    Do NOT gitignore users.json (remains seed/migration reference per MIG-03).

    Verify Docker build succeeds.
  </action>
  <verify>
    <automated>grep -q "data/users.db" api/.dockerignore && grep -q "data/\*\.db" api/.dockerignore && grep -q "initDb()" api/Dockerfile && grep -q "api/data/\*\.db" .gitignore && cd api && docker build -t edf-api-sqlite-test . 2>&1 | tail -1 | grep -qiE "successfully|exporting"</automated>
  </verify>
  <acceptance_criteria>
    - .dockerignore excludes users.db and *.db
    - Dockerfile comment references initDb/users.db
    - .gitignore excludes runtime .db files
    - Docker image builds successfully
  </acceptance_criteria>
  <done>Docker and gitignore updated; image builds</done>
</task>

<task type="auto">
  <name>Task 2: Migrate index.test.js to DB_FILE + initDb</name>
  <read_first>06-RESEARCH.md (Pattern 5), api/index.test.js lines 1-39</read_first>
  <files>api/index.test.js</files>
  <action>
    Minimal test harness swap per RESEARCH (Phase 7 owns deeper isolation):

    1. Replace `TEST_FILE` / `DATA_FILE` with:
       ```javascript
       const TEST_DB = path.join(__dirname, 'data', 'users.test.db');
       process.env.DB_FILE = TEST_DB;
       ```
       Keep CRITICAL comment about env-before-require (update variable name to DB_FILE)

    2. Remove `TEST_SEED` object and `writeFile` import (keep `unlink`)

    3. Replace beforeEach:
       ```javascript
       beforeEach(async () => {
         await unlink(TEST_DB).catch(() => {});
         await app.initDb();
       });
       ```

    4. Replace afterEach: `await unlink(TEST_DB).catch(() => {});`

    5. Update file header comments to reference SQLite instead of JSON fixture

    6. Do NOT change test assertions or describe/it structure — seed-on-empty produces same John/Jane ids

    Run full suite: `cd api && npm test`
  </action>
  <verify>
    <automated>cd api && npm test</automated>
  </verify>
  <acceptance_criteria>
    - All 12 existing tests pass
    - Tests use DB_FILE not DATA_FILE
    - beforeEach calls initDb not loadUsers
    - No JSON fixture writes in test setup
  </acceptance_criteria>
  <done>npm test exits 0 with SQLite-backed store</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end verification — host + Docker + dashboard contract</name>
  <read_first>CLAUDE.md (startup commands), docs/12-docker.md</read_first>
  <files>api/index.js, api/db.js, api/schema.sql</files>
  <action>
    Pause for human verification after automated tasks 1-2 pass. No code changes unless issues found.

    Confirm:
    - Host API persists users across restart with same DB_FILE
    - Docker container serves 2 seed users (ephemeral — no volume)
    - Dashboard CRUD works unchanged against localhost:3100
    - api/schema.sql is readable DDL artifact
  </action>
  <what-built>
    Complete Phase 6 SQLite persistence layer: schema.sql, db.js, refactored index.js, updated tests and Docker config.
  </what-built>
  <how-to-verify>
    1. **Syntax checks:**
       ```bash
       cd api && node --check index.js && node --check db.js && npm test
       ```

    2. **Host persistence (DB_FILE):**
       ```bash
       cd api
       rm -f /tmp/edf-phase6.db
       DB_FILE=/tmp/edf-phase6.db PORT=3100 node index.js
       ```
       In another terminal:
       ```bash
       curl -s http://localhost:3100/users
       curl -s -X POST http://localhost:3100/users \
         -H 'Content-Type: application/json' \
         -d '{"name":"Test User","email":"test@example.com"}'
       ```
       Stop server, restart with same DB_FILE — user must persist (3 users).
       Confirm `/tmp/edf-phase6.db` file exists.

    3. **Docker ephemeral behavior:**
       ```bash
       cd api
       docker build -t edf-api .
       docker run --rm -p 3100:3100 edf-api
       curl -s http://localhost:3100/users
       ```

    4. **Dashboard (optional):** API on 3100 + dashboard on 5173 — verify CRUD.

    5. **Schema visibility:** Open `api/schema.sql` — confirm id/name/email DDL.
  </how-to-verify>
  <verify>
    <automated>cd api && node --check index.js && node --check db.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - All automated checks pass before human review
    - Human confirms persistence, Docker ephemeral behavior, and dashboard CRUD
  </acceptance_criteria>
  <done>Human approves end-to-end Phase 6 behavior or reports issues</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → DB_FILE | Tests write to isolated .db path |
| Docker build context | Must not bake runtime data into image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering | Test DB path | mitigate | TEST_DB under api/data/; unlink before each test |
| T-06-10 | Information disclosure | Docker image | mitigate | .dockerignore excludes runtime .db files |
| T-06-11 | Elevation | Docker USER node | mitigate | Existing chown pattern; initDb mkdir before write |
| T-06-12 | Denial of service | Test file cleanup | accept | unlink in afterEach; Phase 7 hardens isolation |
</threat_model>

<verification>
- `cd api && npm test` — 12/12 pass
- `node --check index.js && node --check db.js`
- Docker build succeeds; container serves GET /users with 2 seed users
- DB_FILE env var allows custom path (verified in checkpoint)
</verification>

<success_criteria>
- All automated checks green
- Human confirms host persistence + Docker ephemeral behavior + dashboard CRUD
- Phase 6 requirements SQLITE-01 through SQLITE-05 satisfied end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/06-sqlite-persistence-layer/06-03-SUMMARY.md`
</output>
